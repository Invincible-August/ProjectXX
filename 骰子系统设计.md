# 修为骰子系统 — 设计说明

> 依据：用户定案「YAML 查表默认上下限 + 动态修正」· 开发计划 §0.7 显性设计 · M2 突破 · M3 战斗  
> 版本：v1.2 · 2026-08-07（ADM：`dice` 为高危域；M2-D01 覆盖已通；突破 dice UI → M5-D12 已消化）  
> **地位**：后续凡检定 / 先攻 / 伤害骰 / 双修掷骰等，**一律按本框架设计与 coding**；禁止再散落 `randint(1, 20)` 或裸 `random()<rate`（概率类须走统一 RNG 门面）。  
> **后台**：正式改 `dice.yaml` 走运营后台域 `dice`（须 `confirm_high_risk`）。

---

## 1. 目标

1. 骰子上下限随**大境界 / 小境界**变化：大境锁定表内上限波段，小境抬表内下限（**数值全来自 YAML，不写死公式**）。
2. **功法等级、体修、道具、气运、装备**可修正上下限（通道可开关）。
3. **突破、战斗先攻/伤害/阵法**接入区间骰；制造失败 / 遮天 / 品阶权重等保留原语义，但 RNG 走统一门面。
4. 面板可拆解（base + Δ），便于调数值。

---

## 2. 核心公式

```text
base_min, base_max = dice.yaml.realm_bounds[major_realm][stage]
Δmin = Σ enabled 通道修正（功法层、体修、道具、气运、装备…）
Δmax = Σ …
lo = clamp(base_min + Δmin, absolute_min, absolute_max)
hi = clamp(base_max + Δmax, absolute_min, absolute_max)
若 lo > hi：交换或抬 hi = lo
roll = uniform_int(lo, hi)   # 可注入 RNG
```

设计语义（占位表体现，非代码硬编码）：

| 大境界 | 示例波段 | 小境界 |
| --- | --- | --- |
| 锻体 | max≈10 | 层数抬 min（1→1-10，2→2-10…） |
| 炼气 | 11–20 | 层抬 min |
| 筑基及以后 | 每境一段 | early/middle/late/perfection 抬 min |

---

## 3. Purpose（用途）

| purpose | 用途 |
| --- | --- |
| `breakthrough` | 突破成功判定 |
| `combat_initiative` | 先攻骰 |
| `combat_damage` | 伤害骰 |
| `formation` | 阵法四象对抗骰 |
| `dual_cultivation` | 双修掷骰（M7 WD3 占位） |
| `generic` | 通用检定 |

概率类（非区间骰）：`chance` / `weighted_pick` 门面 — 制造失败、遮天、品阶权重等。

---

## 4. 配置

### 4.1 `dice.yaml`

- `realm_bounds.<major>.<stage>.{min,max}`
- `monster_default` / `fallback_bounds`（无境界上下文）
- `body_realm_bonus`（体修道附加）
- `fate_luck_tiers`
- `bonus_channels.*`
- `clamp.absolute_min/max`
- `breakthrough.use_legacy_success_rate`：用 `breakthrough.yaml` 的 `success_rate` 映射阈值
- `combat.damage_midpoint_normalizer`：伤害因子 = `roll / mid(lo,hi)`（避免换区间爆炸）

### 4.2 `techniques.yaml`

```yaml
dice_mods:
  # 按功法等级 1..N；缺省级视为 {min_bonus:0,max_bonus:0}
  - { min_bonus: 0, max_bonus: 0 }
  - { min_bonus: 1, max_bonus: 0 }
```

体修轨功法同样可读；另可叠 `body_realm_bonus`。

---

## 5. 突破判定迁移

旧：`success = random() < success_rate`  
新：掷 `purpose=breakthrough` 得 `roll∈[lo,hi]`；

```text
span = hi - lo + 1
success_count = max(1, round(success_rate * span))
threshold = hi - success_count + 1
success = roll >= threshold
```

近似保持原成功率；调数值可改表或改 `success_rate`。

---

## 6. 战斗

- 单位携带 `dice_lo` / `dice_hi`（进攻方按角色解析；怪用 `monster_default` 或怪境界表）。
- 先攻 / 伤害：`randint(dice_lo, dice_hi)`；回落 `1..board.dice_sides`。
- 伤害：`Dice/normalizer` 改为 `roll / max(1, mid)`（或配置 normalizer）。
- 阵法：攻守各用己方 bounds 掷骰。

---

## 7. 与里程碑

| 系统 | 关系 |
| --- | --- |
| M2 突破 | 已接入区间骰 |
| M3 战斗 | 已接入单位 bounds |
| M7 WD3 双修 | purpose 占位；会话 UI 延后 |
| 装备/道具时效 | `后续待完成` DICE-* |

---

## 8. 验收

- [x] YAML 查表：锻体 1 / 锻体 10 / 炼气 1 base 不同
- [x] 功法 `dice_mods` 改变 Δ
- [x] 突破走区间骰 + success_rate 映射
- [x] 战斗先攻/伤害读单位 lo-hi
- [x] 概率类走 `chance`/`weighted_pick` 门面
- [x] README / CHANGELOG / 延后项登记

---

## 9. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-06 | v1.0 框架落地 |
| 2026-08-07 | v1.1：突破 `attempt` 响应含 `dice` 块（服务端权威）；前端结果弹窗展示出目登记为 [`后续待完成.md`](./后续待完成.md) **M5-D12** |

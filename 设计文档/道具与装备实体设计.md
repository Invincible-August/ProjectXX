# 道具与装备实体设计（基础）

> **状态**：基础冻结 **v0.8.12**（2026-09-01）· **R0 穿戴栏=17 区 + 化身开关 + 符箓编成 + 傀儡最多 3 只** · 秘籍使用流 **尚未拍板**（见 §8）  
> **挂载**：[`开发计划.md`](./开发计划.md) **M8 R0**（道具系统；竖切最高优先，先于 R3 自研阵法）  
> **类层次真理源**：本文（Item 树）∪ [`统一实体类层次与重构方案.md`](./统一实体类层次与重构方案.md)（六大域 / Character / Ability）  
> **自研/穿戴玩法**：[`M8自研与内容管线设计.md`](./M8自研与内容管线设计.md)（路由与竖切不改；本文化道具法则）  
> **属性键**：[`ATTR战斗属性占位设计.md`](./ATTR战斗属性占位设计.md) **v1.3**（装备 `stats` 只写已登记键）  
> **傀儡神识**：件数无槽位硬顶、战力看神识——玩法见 [`M4双线程成长设计.md`](./M4双线程成长设计.md) **§6.4** 与 GDD §12.6；本文只定穿戴栏怎么兼容  
> **丹药**：方案 **A**（`use_effect` 白名单 + 多轨时效契约）见 §5.4；时钟结算仍 **M8-D05**  
> **目标**：给背包里的一切「物」立与 `Character` 对等的 ABC；自研产出、工坊成品、交易、穿戴走同一套法则，避免 M8 各写一套 item_type。

---

## 0. 现状与为何现在补

ARCH S1 已有薄 `app.game.item.base.Item` 与 `GenericItem` 包一层 YAML dict；`inventory.yaml` 已有 `tradable` / `bound` / `unique` / `max_stack`。  
**缺口**：M8 设计曾只锁穿戴与自研，没有把「道具是什么」写成法则层——子类文件未拆、基类契约未覆盖绑定/唯一/出售/堆叠、秘籍与配方定义混谈、灵宠蛋/傀儡的双切面未写进 M8。没有这层，自研图纸、丹方、法宝都无法标准化。

**本期只冻结基础**（基类 + 分类树 + 双切面 + 与穿戴栏/Content 的边界 + **丹药方案 A / 多轨时效契约**）。秘籍使用流、分解绑定时机 **下一轮依次讨论**（§8），不在本文一次填满。

---

## 1. 一句话与成功标准

**一句话**：`Item` 管「在不在包里、能不能叠、能不能卖」；槽位/上阵只是对背包行的 **占用指针**；`Character` 管「上场怎么算属性」。禁止一个 Python 类同时继承两者。

**成功标准（基础验收）**

1. Item ABC 成员表写死；子类清单写死（细字段可后补）。  
2. 绑定 / 唯一 / 可出售 / 堆叠 四个协议键语义写死，且与 `inventory.yaml` 对齐。  
3. 装备穿戴栏与 `equip_kind` 映射写死（§5 扩槽表）；细字段走 `extras`，不改占用管道（§5.1）。  
4. 秘籍（背包物）与配方/功法 **定义**（Content）边界写死。  
5. 傀儡 / 灵宠 / 灵宠蛋双切面方案写死。  
6. **占位不迁格**（冒险岛宠物模型推广）：穿戴/上阵不把格子腾空；背包打「已装备 / 已上阵」。  
7. **背包四页**：装备 / 丹药 / 材料 / 秘籍（§4.5）。  
8. **主体机制冻结表**（§5.1）写死：后开玩法只加枚举/字段/Ability，禁止换占用模型、换大类树；**装备指针槽**只许追加、不许改已登记槽语义；**傀儡编成板不是单指针槽**（§5.3），禁止事后改成「一格=一只」。  
9. **丹药方案 A**（§5.4）：`on_use` 只走 `use_effect` 白名单；时效用多轨时钟，禁止单一裸 `duration` 整数；新效果/新时钟只追加。  
10. 实现时：禁止业务文件 `if item_id == "xxx"`；新 `item_type` 进 `app.constants.inventory`。

---

## 2. 与 Character / Content 的边界

| 域 | ABC | 管什么 | 不管什么 |
| --- | --- | --- | --- |
| **item** | `Item` | 背包实例：堆叠、绑定、交易、使用、穿戴指针 | 开战走位、境界 |
| **actor** | `Character` | 可参战行动体：ATTR、布阵、开战种子 | 堆叠、上架 |
| **content** | `ContentEntity` | **定义**：功法模板、工坊配方、阵法蓝图 schema、丹方配方正文 | 背包格子 |

**秘籍在包里是 Item；学会之后的配方/功法进度是 Content。**  
图纸道具消耗或「学习」后，写入角色已解锁的 `RecipeDef` / `TechniqueProgress`，不要把配方定义本身当成可堆叠材料。

```text
EntityRef
├── Character          # 玩家 / 化身 / 灵宠 / 傀儡 / 怪
├── Item               # 包内物
└── ContentEntity      # 定义与进度（非格子）
```

---

## 3. 方案比选（基础层）

### 3.1 装备：深继承 vs 种类字段

| 方案 | 做法 | 优点 | 缺点 |
| --- | --- | --- | --- |
| **A（采用）** | 一个 `EquipmentItem` + `equip_kind` | 浅继承；穿戴栏用槽表扩展 | 细类行为用钩子 |
| B | `WeaponItem` / `ArmorItem` / … 各一个类 | 看起来更「面向对象」 | 行为几乎相同，类爆炸；槽位还要再映射一次 |
| C | 法宝/灵宝另起 `item_type` | 目录好分 | 与已落地穿戴栏、`item_type=equipment` 分叉 |

**定案 A**：细类是装备的 **种类**（`equip_kind`），不是新的背包大类。穿戴栏见 §5（**17 区**为权威）。

### 3.2 傀儡 / 灵宠 / 蛋：双切面

| 方案 | 做法 | 优点 | 缺点 |
| --- | --- | --- | --- |
| **A（采用）** | **一物两面、禁止双继承**：包内 `Item`，上阵 `Character`，1:1 互指 | 与已落地傀儡 `PuppetItem` ↔ `PuppetActor` 一致；交易只碰 Item 面 | 要维持互指不漂 |
| B | 一个类多重继承 Item+Character | 「一个对象」直觉强 | Python MRO 脏；开战/交易钩子缠在一起 |
| C | 上阵后毁掉背包行，只留 Actor | 实现短 | 分解/交易/轮回要对两套生命周期；也违反占位不迁格 |

**定案 A**，细则见 §6。占用规则见 §4.4。

---

## 4. Item 基类契约（必须实现）

对齐 `Character`：基类只定法则，子类给实现。机读英文，展示中文。

### 4.1 身份

| 成员 | 机读 | 说明 |
| --- | --- | --- |
| `get_item_kind()` | `item_type` | 大类：`material` / `consumable` / `equipment` / … |
| `get_def_id()` | `item_id` | 配置定义 id |
| `instance_uid` | `item_uid` | 背包行实例；**即使可堆叠，行也有 uid** |
| `label_zh` | — | 玩家可见名（配置 `name` / `label_zh`） |

### 4.2 四个基础法则（本期写死）

| 法则 | 机读键 | 默认 | 语义 |
| --- | --- | --- | --- |
| **是否绑定** | `bound` | `false` | `true`：可自用、**不可**交易/上架/机缘赠出；绑定后强制 `tradable=false` |
| **是否唯一** | `unique` | `false` | `true`：同一角色对同一 `def_id` **最多一份**（含已穿戴/已上阵绑定）；通常 `max_stack=1` |
| **是否可出售** | `tradable` | `true` | `false`：不可玩家交易、不可上架、不可当可交易机缘；**不**禁止自用/分解（分解另键，后补） |
| **堆叠数量** | `quantity` + `max_stack` | 数量实例；上限见类型表 | 「对垒」= **堆叠**。超过 `max_stack` 必须拆行。`unique` 或装备/傀儡默认 `max_stack=1` |

**推导（实现时写进基类，禁止各服务复制 if）**

- `can_trade()` = `tradable and not bound and occupancy is none`  
- `can_stack_with(other)` = 同 `def_id` + 同 `item_type` + 同 `bag_kind` + 双方未绑定冲突 + 非 unique + 未达 `max_stack` + **双方均未占用**  
- 绑定物可穿戴、可自研投入（若规则允许），但不可进入坊市。

类型默认堆叠（可被条目 `max_stack` 覆盖；与现行 `inventory.yaml` `stack_rules` 对齐）：

| `item_type` | 默认 `max_stack` |
| --- | --- |
| `material` | 999 |
| `consumable` | 99 |
| `talisman` | 20 |
| `skill_book` / `manual` | 20 |
| `pet_egg` | 20 |
| `equipment` / `puppet` / `pet` | 1 |

实例数量在 ORM `inventory_items.quantity`；法则在配置。绑定/唯一/可出售 **定义态**在 YAML，**实例态**可写在 `meta_json`（例如获得后绑定），优先级：实例覆盖定义。

### 4.3 钩子

| 成员 | 谁必须实现 | 说明 |
| --- | --- | --- |
| `get_stack_rules()` | 全部 | 返回 bound / unique / tradable / max_stack 摘要 |
| `as_grant_source()` | 装备等可授予者 | 默认 `None` |
| `on_use(ctx)` | 消耗品 / 秘籍 / 蛋 | 默认不支持 |
| `as_actor_ref()` | 傀儡 / 灵宠 | 指向 Actor 主键；默认 `None` |
| `equip_slot()` | 装备 | 映射到穿戴栏之一（双手武器可返回两个槽）；非装备 `None` |
| `get_occupancy()` | 全部 | 见 §4.4；默认未占用 |
| `get_bag_tab()` | 全部 | 见 §4.5；由 item_type 映射 |

**明确不进 Item ABC**：境界、体力、布阵坐标、聊天、宗门职位。

### 4.4 占位不迁格（仿冒险岛宠物，推广到全部可占用物）

> **对照**：经典冒险岛里，**装备**双击后会从背包格子搬走、人物栏出现；**宠物**则是「召唤后物品仍在背包」。  
> **本项目定案**：不学装备那种「搬格子」，**学宠物那套**，并用于武器/防具/法宝/傀儡/灵宠等一切占用。槽位只是指针，背包行是唯一实物。

| 占用态 `occupancy` | 中文 | 谁 |
| --- | --- | --- |
| `none` | （无标） | 闲置 |
| `equipped` | 已装备 | 穿戴栏（含双手占两格） |
| `deployed` | 已上阵 | 穿戴栏 `pet` 格 / 傀儡编成板（与布阵同源） |

**法则**

1. 穿戴/上阵 **不删除、不隐藏** 背包行；仍占一个格子与容量。  
2. 槽位表只存 `inventory_item_id`（穿戴栏已如此；傀儡/灵宠 Actor 同样互指该行）。  
3. 背包 UI 在该格打中文标：已装备 / 已上阵。  
4. 占用中禁止：交易、上架、机缘赠出、丢弃、换袋、堆叠、消耗性 `on_use`。须先卸下/撤阵。  
5. 一行最多一种占用 **态**；双手武器例外：同一背包行可同时占用 `weapon_1`+`weapon_2` 两个指针。  
6. 试炼木傀仍无背包行（ephemeral），例外写死。

HTTP 背包 DTO 增字段（旧键不变）：`occupancy`、`occupancy_label_zh`、`occupancy_slot`（可选）、`bag_tab`。

### 4.5 背包分页（方案 C · 已拍）

冒险岛是「装备 / 消耗 / 设置 / 其他」分页格子。本项目 **分页**，页名改修仙：

| `bag_tab` | 中文 | 收入的 `item_type` |
| --- | --- | --- |
| `gear` | 装备 | `equipment`、`puppet`、`pet`、`pet_egg` |
| `elixir` | 丹药 | `consumable`、`talisman` |
| `material` | 材料 | `material` |
| `manual` | 秘籍 | `manual`、`skill_book` |

- 机读用 `gear`/`elixir`，避免与 `item_type=equipment` 撞名。  
- 傀儡/灵宠进 **装备** 页：与「会出现在装备中」一致；占用后仍在本页，打「已上阵」。  
- 符箓进 **丹药** 页：都是使用/预载型，不是穿戴栏。  
- 蛋与成宠同一页，孵化后格子从蛋换成 `PetItem`。  
- 普通袋 / 轮回袋是 **袋**，不是页；每页仍可再分袋（后补 UI）。  
- 页内是固定格子还是滚动列表 → 实现 ITEM-0 时默认滚动列表 + 页签；格子容量（冒险岛 24/32）未冻结。

`Item.get_bag_tab()` 由 `item_type` 映射，禁止按 `item_id` 写死。

---

## 5. 子类树（分类冻结，字段后补）

```text
Item (ABC)
├── MaterialItem              # 材料（矿、草、符纸…）
├── ConsumableItem            # 丹药及其它消耗（use_effect）
├── EquipmentItem             # 可穿戴；equip_kind 细分
│     equip_kind:
│       weapon_1h / weapon_2h     # 单手 / 双手
│       armor_head/chest/legs/shoes/hands
│       accessory / ring
│       fabao / natal_fabao / fubao   # 福宝：装备页消耗件，耐久不可逆
│       lingbao                   # 种类，穿法宝槽，不是独立槽
├── TalismanItem              # 符箓成品（画符产出）
├── ManualItem                # 秘籍（背包物）
│     manual_kind:
│       alchemy_formula       # 丹方
│       forge_blueprint       # 装备图纸
│       puppet_blueprint      # 傀儡图纸
│       technique             # 功法秘籍
│       talisman_blueprint    # 制符图纸
│       formation_blueprint   # 阵法图纸
│       skill_book            # 既有灵宠技能书（别名，可迁入）
├── PuppetItem                # 傀儡背包面
├── PetEggItem                # 灵宠蛋（孵化前）
└── PetItem                   # 孵化后的灵宠；占用时标「已上阵」
```

**`item_type` 协议（大类，进背包枚举）**

| 机读 | 中文 | 子类 |
| --- | --- | --- |
| `material` | 材料 | `MaterialItem` |
| `consumable` | 消耗品 / 丹药 | `ConsumableItem` |
| `equipment` | 装备 | `EquipmentItem` |
| `talisman` | 符箓 | `TalismanItem` |
| `manual` | 秘籍 | `ManualItem`（新；R5 图纸类型并入此树） |
| `skill_book` | 技能书 | 过渡期保留，语义并入 `manual`+`skill_book` |
| `puppet` | 傀儡 | `PuppetItem` |
| `pet_egg` | 灵宠蛋 | `PetEggItem` |
| `pet` | 灵宠（孵化后） | `PetItem` |

既有 M8 D15 的 `forge_blueprint` 等 **不是第三套大类**：实现时映射为 `item_type=manual` + `manual_kind=…`，或短期双写别名，禁止再平行发明一套。

**穿戴栏（完整表 · 权威）**

占用模型：槽=指针，背包行不消失。旧五槽名（`weapon`/`armor`/`fabao`）**不再作为 API/ORM 权威**，仅读档时迁到下表。

| 分组 | 槽 id | 中文 |
| --- | --- | --- |
| 武器 | `weapon_1` | 主手 |
| | `weapon_2` | 副手 |
| 防具 | `armor_head` | 头 |
| | `armor_chest` | 胸 |
| | `armor_legs` | 腿 |
| | `armor_shoes` | 鞋 |
| | `armor_hands` | 手 |
| 饰品 | `accessory_1` | 项链 |
| | `accessory_2` | 饰品 |
| | `ring_1` | 戒指一 |
| | `ring_2` | 戒指二 |
| 法宝 | `fabao_1` | 法宝一 |
| | `fabao_2` | 法宝二 |
| | `natal_fabao` | 本命法宝 |
| | `fubao` | 符宝（界面名；机读 id 仍为 `fubao`，法则仍为福宝消耗件） |
| 上阵 | `pet` | 灵宠（**单指针，硬顶 1**） |
| 编成 | （无槽 id） | **傀儡编成板**（列表，见 §5.3） |

角色页是 **17 区**（15 装备格 + 灵宠 1 格 + 傀儡编成板 1 区），**不是**「17 个单指针槽」。灵宠 / 傀儡规则见 §5.3；布阵页 **只摆格子坐标**。

**`equip_kind` → 槽**

| `equip_kind` | 占用 | 规则 |
| --- | --- | --- |
| `weapon_1h` | `weapon_1` **或** `weapon_2` | 穿戴时选空槽；两格都可各一件单手 |
| `weapon_2h` | `weapon_1` **且** `weapon_2` | 同一 `inventory_item_id` 写进两格；卸任一侧=两格都卸 |
| `armor_head` / `chest` / `legs` / `shoes` / `hands` | 对应防具槽 | 1:1 |
| `accessory` | `accessory_1` 或 `_2` | 选空槽 |
| `ring` | `ring_1` 或 `_2` | 选空槽 |
| `fabao` / `lingbao` | `fabao_1` 或 `_2` | 选空槽；灵宝是种类不是独立槽 |
| `natal_fabao` | `natal_fabao` | 建议 unique；锁卸下走 `extras.soul_lock`（后开） |
| `fubao` | `fubao` | 见 §5.2：耐久不可逆 |
| （灵宠无 equip_kind） | `pet`（单指针） | `item_type=pet`；占用标「已上阵」；**硬顶 1** |
| （傀儡无 equip_kind） | **不是槽** | 见 §5.3：`puppet_loadout` 列表；占用标「已上阵」；**无件数硬顶** |

双手穿上时：若副手/主手已有单手，先卸旧件回闲置（仍在背包），再占用两格。

**旧档迁移（一次性）**

| 旧槽（已废弃） | 迁到 |
| --- | --- |
| `weapon` | `weapon_1` |
| `armor` | `armor_chest` |
| `fabao` | `fabao_1` |
| `accessory_1` / `accessory_2` | 同名保留 |

**R0 深度仍 = 方案 A**：权威为 **15 装备指针槽** + `pet` 单指针 + **傀儡编成列表** + `equip_kind`/`slot` + `stats`/`grants`。耐久 **法则**见 §5.2（键进 `extras`）；战斗里扣耐久可后开，但模式不许再改。**禁止**再以五槽作正式协议。

### 5.1 主体机制冻结 vs 可加扩展（法律级）

> 目的：后续开本命锁、耐久结算、套装时，**只加数据与钩子，不改这条管道**。违反「禁止改」= 设计回退。

| 层 | **冻结（禁止改语义）** | **开放（只许追加）** |
| --- | --- | --- |
| 实物 | 背包行是唯一实例；装备/灵宠槽存 `inventory_item_id`（双手=两槽同一 id） | 新 **装备指针槽**只能新增；禁止改名/删除已登记装备槽与 `pet`；禁止把傀儡编成板改成单指针槽 |
| 占用 | `none` / `equipped` / `deployed`；占位不迁格 | 可加占用态（如 `preloaded`）；双手是双指针不是新占用态 |
| 大类 | `item_type` + 四页 `bag_tab` | 新类型须进工厂并归入已有四页 |
| 装备种类 | 单一 `EquipmentItem` + `equip_kind` | `equip_kind` **只追加**；优先映射到上表已有槽 |
| 数值/特效 | 战斗键只进 ATTR `stats`；特效只进 `grants` | 新键走 `combat_attrs.yaml`；新特效走 Ability |
| 实例养成 | 不把耐久/祭炼写进 ABC | `meta_json` / `extras` 透传 |
| 耐久模式 | 普通装备默认可修；**福宝不可逆**（§5.2） | 可后开「战斗扣耐久 / 修理 UI」，不许把福宝改成可修 |
| HTTP | 旧 DTO 键不删；槽 id 以扩槽表为准 | 只加字段 |
| 类树 | 不为头胸腿再拆继承 | 钩子 `can_unequip()` / `occupied_slots()` |

**预留 `extras` 键（本期可不写值，未知键透传、不进战斗）**

| 键 | 预定用途 |
| --- | --- |
| `durability` / `durability_max` | 当前/上限 |
| `durability_mode` | `repairable` \| `irreversible` |
| `refine_level` | 祭炼/强化 |
| `soul_lock` / `cannot_unequip` | 本命穿上后不可卸 |
| `set_id` | 套装（M8-D02） |
| `damage_kind` | 武器伤害类型 |
| `handedness` | 可与 `equip_kind` 重复校验单手/双手 |

### 5.2 耐久与福宝（法则冻结，结算可后开）

| 对象 | `item_type` / 页 | 耐久 | 到 0 |
| --- | --- | --- | --- |
| 普通装备 | `equipment` / 装备页 | 默认 `repairable`（修理玩法后开） | 先 **失效不能用**，修理后恢复 |
| **福宝** | 仍是 `equipment` / 装备页（**不是**丹药页） | `irreversible`：只减不增、不可修理 | **破碎消失**（真正消耗）；卸下不能回满 |

- 福宝穿在 `fubao` 槽，背包仍占格，标「已装备」。  
- 禁止把福宝改成 `item_type=consumable`（否则会进丹药页，和「属于装备」冲突）。  
- `on_use` 不是福宝的主路径；消耗靠耐久归零。  
- ITEM-0 实现可先只存三个耐久键，不在战斗里扣；**不可逆**不许改成可修。

### 5.3 灵宠一格 vs 傀儡编成板（兼容冻结）

> **冲突**：角色页要「一眼看到带了谁」，但不能把傀儡做成「一格=一只」。  
> **玩法**：灵宠出场 **只能 1 只**；傀儡编成板上阵 **最多 3 只**；化身用开关上阵（不占装备格）。神识占用在装备栏结算（开关开的化身 + 灵宠槽 + 傀儡编成），**与棋盘是否落子无关**。符箓同样走编成列表，无件数上限，战斗叠层见 [`符箓叠层与异常状态设计.md`](./符箓叠层与异常状态设计.md)。

三层不要混：

| 层 | 灵宠 | 傀儡 |
| --- | --- | --- |
| 持有 | 包内可多只（unique 实例） | 包内任意多只 |
| 角色页 | **`pet` 单指针槽**，硬顶 1 | **编成板**（`puppet_loadout`）：勾选列表，**最多 3 只** |
| 棋盘落子 | `unit_kinds.pet.unique=true`；Bench 只出槽内那只 | `unique=false`；Bench = 编成板上的全部；落子仍受 `max_units` + 合法部署格（棋盘/阵法，**不是**装备槽） |
| 战斗力 | 占共享神识池（M4-D03，已有 `cost_pet`） | 上阵最多 3 只；消耗并入同一神识池（超过容量后削弱） |

**编成板不是第 17 个 `slot_id=puppet`。** v0.6 把 `puppet` 写成单指针槽，**未实现，作废**。ITEM-0 禁止登记单指针 `puppet` 槽，禁止 `equip` 第二只时返回「槽已占用」。

```text
角色页 17 区
├── 15 装备指针槽（weapon/armor/…/fubao）
├── 1  pet 指针槽          ← 硬顶 1；蛋不能进
└── 1  傀儡编成板          ← 列表 UI；内部是 N 条 occupancy=deployed
```

**权威数据**

| 对象 | 存什么 |
| --- | --- |
| 装备 / 灵宠 | `EquipmentLoadout`：槽 id → 一个 `inventory_item_id` |
| 傀儡 | 各 `PuppetItem` 的 `occupancy=deployed`（可 N 条同时 deployed）；编成板 = 这些行的投影，不另做「只能写 1 个 id」的列 |

勾进编成板 → 背包行仍在，标「已上阵」，禁交易（§4.4）。  
布阵：只能摆编成板上的傀儡；**没勾上的包内傀儡不上 Bench**。  
角色简示栏神识分子按 **编成板已上阵真傀 + 进攻预设化身/灵宠** 合计。开战衰减按棋盘实际落子。  
角色页编成板按勾选 **预览**「若全部上场」的傀儡发挥百分比（选择/取消即算，不必点上阵、也不必布阵）。

试炼木傀：仍无背包行、不进编成板（已落地例外）。

**禁止**

- 为迁就 17 格把傀儡改成「只能带 1 只」。编成硬顶是 **3 只**，不是单指针槽。  
- 用神识 **硬拒** 已勾选的 1～3 只（只许衰减）。第 4 只按件数上限拒绝。  
- 把 `max_units` 解释成「傀儡持有上限」（它是棋盘总棋子数，含本体/化身/宠）。  
- Item+Character 双继承。

### 5.4 丹药（方案 A · 白名单 + 多轨时效）

> **定案 A**：丹药是 `ConsumableItem`；双击/使用走 `on_use(ctx)` → 扣堆叠 → 按 `use_effect` **白名单**结算。禁止 `if item_id == "xxx"`。  
> **时效是多轨的**：有的持续几场战斗，有的持续几回合，有的持续现实时间。禁止把时效收成一个无单位的整数。  
> **结算分期**：契约本期冻结；**时钟推进与通道打开**仍是 [`后续待完成.md`](./后续待完成.md) **M8-D05**（IDLE-R02 / DICE-R02）。ITEM-0 只校验 schema、落地即时 kind（已有 `stamina`）；**禁止**偷开 `channels.pill_buff` / `buff_pill`。

#### 管道（冻结）

```text
背包行 ConsumableItem
    → on_use(ctx)          # 占用中拒绝（§4.4）
    → 校验 use_effect.kind 在白名单
    → 扣 quantity（到 0 删行）
    → instant：当场改状态（体力等）
    → timed：写入角色 buff 实例（M8-D05 才持久化/推进）
    → 战斗/挂机/骰子只读 AdditiveSource(source=pill_buff) 等通道
```

`use_effect` 可以是 **一个对象**（现行 YAML）或 **列表**（一颗药多个效果，例如「回体 + 一时攻」）。列表里每条独立校验。

#### `kind` 白名单（只许追加）

| `kind` | 语义 | 何时结算 |
| --- | --- | --- |
| `stamina` | 恢复体力；`amount` | **即时**（已落地） |
| `attr_mod` | 改已登记 ATTR 键（`stats`） | 有 `duration` 则时效；无则禁止（避免永久堆药，永久养成走灵宠喂养等专管线） |
| `idle_mod` | 挂机乘区（`idle_mult`） | 时效；通道 `buff_pill` |
| `dice_mod` | 骰子区间 | 时效；骰通道 `item` |
| `grant` | Ability `grants` | 时效或即时由条目声明 |
| `pet_skill_book` | 技能书；**不是丹药**，`item_type=skill_book` | 既有专管线 |
| （未知） | — | **拒绝使用、不扣数量** |

灵宠兽丹（`pet_pill_*`）**不走**角色背包 `on_use`，走灵兽园喂养 API（PET-D04）。符箓 **不走** 本管道，走预载（R4 / M8-D04）。

#### 时钟（多轨 · 只许追加）

时效 **不是** `duration: 30`。用时钟枚举；一条效果默认 **一轨**，需要「谁先到谁结束」时用 `tracks`。

| `clock` | 中文 | 单位键 | 何时扣 1 |
| --- | --- | --- | --- |
| `wall` | 现实时间 | `seconds` | 惰性读时按 UTC 流逝（同挂机，无全表扫） |
| `battle` | 场次 | `count` | **产生战报的战斗结束**时 −1（PVE/PVP/快照/道主）；中途崩溃无战报不扣 |
| `round` | 回合 | `count` | 自走棋引擎推进回合且本方仍有存活棋子时 −1；不在战斗中冻结 |

缺 `duration` = **即时**（如小体力丹）。  
`round` 药若从未开战，回合钟不走——需要「别挂太久」时再加一条 `wall` 轨（见下）。

**一轨（默认）**

```yaml
use_effect:
  kind: attr_mod
  stats: { phys_atk: 8 }
  duration:
    clock: battle
    count: 3
  stack: refresh
```

**多轨（扩展，先到先失效）**

```yaml
use_effect:
  kind: attr_mod
  stats: { phys_atk: 8 }
  duration:
    expire: any          # 默认 any：任一轨耗尽则整段效果结束
    tracks:
      - { clock: battle, count: 3 }
      - { clock: wall, seconds: 3600 }
```

| 键 | 冻结 |
| --- | --- |
| `expire` | 只承认 `any`。禁止用 `all`（「所有钟走完才结束」会让短钟失去意义） |
| `stack` | 同源（同 `item_id`+`kind`）默认 `refresh`（重开时钟，不叠层）。可追加 `reject` / `independent`，**禁止**无上限叠乘 |
| `apply_to` | 默认 `self`（使用的角色）。可后加 `avatar` / `pet`，本期不实现 |

预留时钟（登记即可，ITEM-0 / M8-D05 都不必推进）：`idle_tick`、`tribulation_hit`。新钟只追加枚举，不改管道。

#### 扩展点 vs 禁止改

| 层 | **冻结** | **开放（只许追加）** |
| --- | --- | --- |
| 管道 | `on_use` → 白名单 → 扣堆叠；禁止按 `item_id` 分支 | 新 `kind`、新 `clock` |
| 时效 | 多轨时钟 + `expire: any`；禁止裸整数 duration | `tracks` 组合；预留钟 |
| 数值 | 战斗键只进已登记 ATTR；挂机/骰子走已有通道名 | 通道打开属 M8-D05 |
| 边界 | 兽丹≠角色使用；符箓≠丹药 `on_use` | 新消耗大类须进工厂并归四页 |

#### 与 R0 / M8-D05

| 步 | 做 | 不做 |
| --- | --- | --- |
| **R0** | `ConsumableItem`；校验 `kind`/`clock`/`tracks`；即时 `stamina` 继续可用 | 不写 buff 行、不推进时钟、不打开 `pill_buff` |
| **M8-D05** | 持久化 buff 实例；推进三钟；打开战斗/挂机/骰子通道；面板剩余场次/回合/时间 | 不另写一套 `use_effect` |

---

## 6. 双切面：傀儡、灵宠、灵宠蛋

> 核心句：**包里永远有这一行道具；上阵/穿戴只改占用态。** 养成与开战仍走 Character 面（互指，不双继承）。  
> 对标冒险岛宠物：召唤后人物身边出现，背包格仍在，只是显示使用中。

### 6.1 三态

| 对象 | 背包行 | 占用标 | Actor / 开战 |
| --- | --- | --- | --- |
| **装备** | `EquipmentItem` 一直在 | 穿上 → `equipped`「已装备」 | 无 Actor；穿戴栏指针（双手可两槽同 id） |
| **傀儡成品** | `PuppetItem` 一直在 | 勾进编成板 → `deployed`「已上阵」（可 N 只同时） | `PuppetActor` 1:1；布阵只摆坐标 |
| **灵宠蛋** | `PetEggItem` | 无占用；`on_use`=孵化 | 无 Actor，不能进 `pet` 格 |
| **已孵灵宠** | `PetItem` 一直在 | 穿进 `pet` 格 → `deployed`「已上阵」 | `PetCharacter` 1:1；灵兽园=养成窗 |

试炼木傀：仅 `PuppetCharacter(ephemeral=True)`，**无**背包行（已落地例外）。

### 6.2 傀儡

```text
炼成/获得 → InventoryItem（PuppetItem）  ← 始终占格
                 ↕ 1:1  inventory_item_id
              PuppetActor
                 ↓ 勾进编成板 occupancy=deployed（可并行 N 条）
              布阵摆坐标（须已 deployed）
              PuppetCharacter → BattleUnitSeed
                 ↓ 开战
              养成面板 × 傀儡神识乘区（M4 §6.4）
```

- 交易/分解/轮回：占用中拒绝；闲置时先按 Item 法则，再级联 Actor。  
- 养成数字只经 `channels.puppet`；**神识乘区不写进养成行**，开战/快照时套。  
- 禁止把傀儡改成 `equipment` 去穿单格；禁止用单指针槽冒充编成板。

### 6.3 灵宠蛋 → 灵宠（已拍：占位不迁格）

```text
PetEggItem.on_use（孵化）
    → 消耗蛋
    → 新建 PetItem 背包行（unique）+ Pet 养成行
    → 闲置时 occupancy=none，仍在包里
    → 上阵：穿进穿戴栏 `pet` 格，occupancy=deployed
```

灵兽园 = 宠物窗口（改名、喂养、技能），打开的是这件 `PetItem` 的 Character 面，不是把宠搬出背包。

### 6.4 禁止事项

- Item 子类实现 `on_battle_enter`。  
- Character 子类实现 `get_stack_rules`。  
- 蛋不能进 `pet` 格。  
- 第二只灵宠进 `pet` 格（硬顶 1；换宠=先卸再穿）。  
- 同一 `inventory_item_id` 绑两个 Actor。  
- 穿戴/上阵时从背包列表剔除该行（违反占位不迁格）。  
- 单指针 `puppet` 槽；`equip` 因「傀儡槽已满」失败。

---

## 7. 配置 / 常量 / 存量代码

| 层 | 约定 |
| --- | --- |
| 配置 | 目录仍以 `inventory.yaml` `items` 为实例法则源；装备战斗字段在 `equipment.yaml`（`slot`/`equip_kind`/`stats`/`grants`） |
| 常量 | `ItemType`、`bag_tab`、`equip_kind`、`manual_kind`、四法则键 → `app.constants.inventory` / `equipment` |
| ORM | 继续 `inventory_items`；实例覆盖放 `meta_json`（`bound`/`unique` 等），不先加列 |
| 工厂 | `Item.from_inventory_row(row, def)` 按 `item_type` 分派子类；消灭业务里直接读裸 dict 当法则 |
| 存量 | `GenericItem` 可留作未知类型回退；正式类型必须有子类文件 |
| 后台 | 物品目录域须有 `bound`/`unique`/`tradable`/`max_stack` 的 `label_zh`/`help_zh` |

HTTP 背包 DTO 可加字段，**不改**旧键名（含 `occupancy` / `occupancy_label_zh` / `bag_tab`）。

### 7.0 格子展示 · 图标预留（接开发计划 §0.0.4）

穿戴槽、选择格、背包格统一：

| 优先级 | 规则 |
| --- | --- |
| 1 | 若配置/DTO 有可用 **物品 UI**（`icon`，缺省=定义 id；资源在 `frontend/src/assets/items/`）→ 格内以图为主 |
| 2 | **无对应资源文件** → 格内显示中文全名（单行省略）；**不**强制配短名字段 |
| 悬停 | 始终全名 + 属性/说明 |

实现：`Item.get_icon()` · API 透出 `icon` · 前端 `ItemSlotVisual` + `itemIcon.ts`。细则见 [`开发计划.md`](./开发计划.md) **§0.0.4**。

### 7.1 穿戴栏悬停（固定 format）

与功法栏同一卡片（详见 [`功法系统设计.md`](./功法系统设计.md) §5）。装备侧输入是 `stats_preview`（ATTR 键 → `{ label_zh, value }`），format 成：

```text
┌ 铁剑                         主手 ┐
│ 物理攻击                 +12      │
│ 生命                     +8       │
└───────────────────────────────────┘
```

| 项 | 规则 |
| --- | --- |
| 名称 | `item_label_zh` / 背包 `name` |
| 角标 | 部位中文（主手/头…） |
| 属性行 | 每条 `label_zh` + 已带符号的值（正数 `+N`） |
| 空槽 | 名称=部位名，说明「此部位尚未穿戴」 |
| 选择栏 | 点部位后用紧凑方格列出兼容装备；悬停同上，再点格穿上/卸下 |

禁止在选择栏把每件装备铺成整行（名称+属性+按钮）。实现：`hoverFromEquipment` / `statsFromPreview`。

### 7.2 工坊成品悬停

工坊图纸悬停走同一 `ItemHoverTip` 外壳。字段来自 `inventory.yaml` `items.*.inspect`：

| 项 | 规则 |
| --- | --- |
| 名称 | 左上，配方/物品中文名 |
| 角标 | 右上：`炼丹等级 N` 等 |
| 属性 | **一种**：金木水火土风雷暗芯片；同属性灵根额外收益本版不结算 |
| 功效 | 中文标签芯片 |
| 使用条件 | 仅境界（`realm_req_zh`，无门槛写「无」） |
| 说明 | `help_zh` |

无抗性。禁止把材料、灵石、体力写进悬停。点选后「所需材料」= 材料 + 灵石，「所需体力」另栏。工坊页只展示配方与队列，不展示背包栏。

---

## 8. 下一轮依次讨论（未冻结）

1. ~~背包分页~~：**已拍 C**（§4.5）  
2. ~~装备细类深度~~：**已拍 A**；穿戴栏 **17 区权威**（15 装备 + `pet` 1 + 傀儡编成板）+ 双手占两格 + 福宝不可逆耐久（§5 / §5.3）；**五槽已废**  
3. ~~丹药~~：**已拍 A**（§5.4）——`use_effect` 白名单 + 多轨时钟（`wall`/`battle`/`round`）；结算仍 M8-D05  
4. **秘籍使用流**：学习后销毁 vs 留在包里当「已读」  
5. **分解 / 精炼 / 绑定时机**：获得绑定 vs 穿戴绑定  
6. **套装**：仍指针 M8-D02  
7. 页内固定格子容量（冒险岛 24/32）与轮回袋如何叠在四页上  

---

## 9. 与 M8 竖切

| 步 | 关系 |
| --- | --- |
| **R0** | **当前最高优先**：基类落地 + 工厂 + 四法则读穿；不新开玩法页 |
| R1 / R2 | 已落地；穿戴/自研继续走现 API，逐步改为走 Item 工厂 |
| R3～R6 | 阵法/符/图纸必须挂在本树的 `ManualItem` / `TalismanItem` 上，禁止第三套类型 |

---

## 10. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-09-01 | **v0.8.12**：§7.0 物品格展示——有 UI 用图、无 UI 用名；预留 icon/ui_key（接开发计划 §0.0.4） |
| 2026-08-19 | **v0.8.11**：工坊成品 `inspect.element` 单一属性；无抗性；工坊页去掉背包栏 |
| 2026-08-18 | **v0.8.9**：穿戴栏悬停走装备 `stats` 固定 format；选择栏紧凑方格 |
| 2026-08-18 | **v0.8.8**：化身装备栏开关；符箓编成板（无件数上限）；神识只按装备栏，阵法只布阵 |
| 2026-08-18 | **v0.8.7**：神识池含上阵傀儡；简示栏分子按编成板+预设合计 |
| 2026-08-18 | **v0.8.5**：角色页编成板按勾选预览傀儡发挥%（开战仍按棋盘落子） |
| 2026-08-17 | **v0.8.4**：玩家可见槽名改为主手/副手、项链/饰品、符宝（id 不变；福宝法则仍挂 `fubao`） |
| 2026-08-17 | **v0.8.2**：穿戴栏 **废除五槽过渡**；API/ORM 权威改为 17 区指针槽；旧 `weapon`/`armor`/`fabao` 仅读档迁移 |
| 2026-08-17 | **v0.8.1**：竖切升为 **R0**；工厂/四法则/四页/`occupancy`/丹药白名单落地（17 区 UI 与傀儡编成板后续） |
| 2026-08-17 | **v0.8**：丹药方案 **A**——白名单 `use_effect` + 多轨时效（场次/回合/现实时间）；结算仍 M8-D05 |
| 2026-08-17 | **v0.7**：废止 v0.6 单指针 `puppet` 槽（未实现）；灵宠硬顶 1；傀儡编成板无件数硬顶，战力走 M4 §6.4 神识乘区 |
| 2026-08-17 | **v0.6**：穿戴栏 **17 槽**（+灵宠/傀儡）——其中傀儡单槽已被 v0.7 作废；福宝不可逆耐久 |
| 2026-08-17 | **v0.5**：穿戴栏扩为 15 槽（双武器/五防具/饰品戒/双法宝/本命/福宝）；双手武器双指针；R1 五槽作别名 |
| 2026-08-17 | **v0.4**：装备深度 **A**；§5.1 主体冻结 vs `extras` 扩展点（后续不加第六套机制） |
| 2026-08-17 | **v0.3**：冻结背包四页（装备/丹药/材料/秘籍）及 `item_type` 映射 |
| 2026-08-17 | **v0.2**：冻结 **占位不迁格**（冒险岛宠物模型推广到装备/傀儡/灵宠）；成宠改为 `PetItem` 留包；占用中禁交易 |
| 2026-08-17 | **v0.1**：开篇基础冻结——Item ABC、四法则、子类树、五槽映射、双切面方案 A；细类字段留 §8 |

---

*冲突时：类契约以本文 + `统一实体类层次与重构方案.md` 为准；穿戴与自研路由以 `M8自研与内容管线设计.md` 为准；ATTR 键以属性专文为准。*

# 统一实体类层次与重构方案（OOP / ABC）

> **状态**：设计冻结 **v1.0.6**（2026-08-19）· **S1-1 代码骨架已落地**（`app/game/**`）  
> **落地怎么拆**：存量整改 vs 增量规范 vs YAML/DB 兼容 → 见 [`双轨整改与配置兼容方案.md`](./双轨整改与配置兼容方案.md)（**先读该文再开工**）  
> **排期**：ARCH S1 已收口；M8 玩法中 **R0（道具 ABC）最高优先，先于 R3 阵法**  
> **依据**：Ability 三层 + 六大域 + UE 式 Character ABC · [`ATTR战斗属性占位设计.md`](./ATTR战斗属性占位设计.md) · [`道具与装备实体设计.md`](./道具与装备实体设计.md) · [`M3战斗成型设计.md`](./M3战斗成型设计.md) · [`后续待完成.md`](./后续待完成.md)（ARCH-R01/R02）  
> **目标**：用 **抽象基类定契约 + 子类实现** 管理全项目「有属性/有效果」的对象（类似 UE/Unity）；**不改变**玩家 HTTP 路径与后台 `domain_id`（除非另开迁移任务）。

### 拍板冻结（v1.0）

| 项 | 定案 |
| --- | --- |
| 傀儡 | 模型 **A**（一物一灵：PuppetItem ↔ PuppetCharacter） |
| 装备槽 | 见 [`道具与装备实体设计.md`](./道具与装备实体设计.md) §5：**17 区**（15 装备 + `pet` 1 + 傀儡编成板）；旧五槽名仅读档迁移 |
| 装备数值 | 快捷 `stats` + 特效 `grants` |
| 同 ability 多来源 | **默认不叠**（可标 `stackable`） |
| 包名 | `app/game/` |
| ORM `Character` rename | **延后 R6**（现用 `PlayerCharacter` 包装） |
| 测试配置 | 推荐 `CONTENT_STORE_MODE=yaml_authority`；仓库默认仍 `yaml_base_db_overlay` 兼容现行后台 |
---

## 0. 一句话与成功标准

**一句话**：以 `Entity` → 六大域 ABC 为项目「法则层」；玩家/灵宠/怪物等继承 `Character`；装备/丹药等继承 `Item`；特殊效果统一走 `Ability`；战斗演算只吃 `BattleUnit` 快照。

**与双轨文档分工**：本文定义 **类与能力是什么**；[`双轨整改与配置兼容方案.md`](./双轨整改与配置兼容方案.md) 定义 **已实现怎么改、未开发怎么做、测试 YAML / 正式 DB 怎么兼容**。

**成功标准（设计验收）**

1. 六大域、Character/Item 子类清单、abstractmethod 契约表写死。  
2. Ability（domain/kind/ops）与 ATTR `AdditiveSource` 咬合关系写死。  
3. 傀儡双切面（Item ∪ Character）与实例模型写死。  
4. 目标包目录 ↔ 现行 `domain/`/`services/` 映射表写死。  
5. 分期迁移（兼容 shim）与「禁止事项」写死；HTTP / admin `domain_id` 稳定。  
6. 配置读取可经 ContentStore 切换后端而不改 OOP（见双轨方案 §4）。

---

## 1. 设计原则（UE 感，但适配服务端）

| 原则 | 落地 |
| --- | --- |
| **基类定法则，子类定实现** | `abc.ABC` + `@abstractmethod`；缺实现则无法实例化 |
| **浅继承 + 组合** | Character 下修士走 `CultivatorCharacter`（后台按类渲染境界栏）；有神识池再一层 `PlayerNpcCharacter`；灵宠/傀儡走 `SenseMinionCharacter`。化身上阵神识占用用切面 `DivineSenseConsumer`，不进修士 ABC。装备/技能/背包用 Component |
| **养成对象 ≠ 棋盘对象** | `Character.on_battle_enter()` → `BattleUnit`；`simulate_battle` 不碰 ORM |
| **配置驱动内容** | 经 ContentStore 读域数据（测试 YAML / 正式可 DB）；类提供行为钩子，禁止 `if item_id == "xxx"` |
| **一套 ATTR** | 一切 combatant 共用 `CombatAttrBlock`；子类只填/裁剪 |
| **一套 Ability** | 特效唯一正文在 `abilities`；Source 只 `grants` |
| **兼容迁移** | 先立类与 shim 再迁文件；旧 `from app.domain.x` 可短期 re-export |
| **边界不变** | 玩家 API 前缀、admin `domain_id`、战报事件名优先不动 |

---

## 2. 六大域（Taxonomy）

| `domain_root` | ABC 名 | 管什么 | 现行主要来源 |
| --- | --- | --- | --- |
| `actor` | `Character` | 可参战/可养成行动体 | ATTR `entity_kind`；formation `unit_kind` |
| `item` | `Item` | 背包/装配/消耗物 | `inventory.yaml`；craft 产出 |
| `content` | `Content` | 功法/配方定义/体质/秘境定义 | `techniques` / `craft_recipes` / `constitution` |
| `law` | `LawEntity` | 大道/道主/克制 | `dao*` / `dao_lord` |
| `environment` | `Environment` | 天气/时辰/地形/阵法层 | `weather` / `calendar` / formation layers |
| `org` | `Organization` | 宗门/盟 | `sects` |

> **功法、秘境、配方定义** 归 `content`，不归 `item`。图纸道具 `blueprint` 才是 `item`。

---

## 3. 类层次（完整清单）

### 3.1 根与 Character 树

```text
Entity (可选根：uid/def_id/label)
│
├── Character (ABC)                    # 可参战；基础属性含 hp / mp / phys_atk
│   ├── CultivatorCharacter (ABC)      # 修士：境界/分配/突破；后台按此类渲染境界栏
│   │   ├── PlayerNpcCharacter (ABC)   # 玩家与 NPC：神识容量/占用
│   │   │   ├── PlayerCharacter        # unit_kind=main；可道主/轮回
│   │   │   └── NpcCharacter           # unit_kind=npc；不继承怪物
│   │   └── AvatarCharacter            # unit_kind=avatar；+ DivineSenseConsumer；禁道主/轮回
│   ├── SenseMinionCharacter (ABC)     # 灵宠/傀儡：divine_sense_cost
│   │   ├── PetCharacter               # unit_kind=pet
│   │   └── PuppetCharacter            # unit_kind=puppet（战斗面）
│   ├── MonsterCharacter               # pve_monsters（无神识池）
│   └── BossCharacter                  # 可后置；先 Monster+flags
│
├── Item (ABC)                         # 细则 [`道具与装备实体设计.md`](./道具与装备实体设计.md)
│   ├── MaterialItem
│   ├── ConsumableItem                 # 丹药等
│   ├── EquipmentItem                  # equip_kind：武器/防具/饰品/法宝/灵宝/本命
│   ├── TalismanItem
│   ├── ManualItem                     # 秘籍；manual_kind=丹方/图纸/功法…
│   ├── SkillBookItem                  # 过渡别名 → ManualItem
│   ├── BlueprintItem                  # 过渡别名 → ManualItem
│   ├── PetEggItem
│   ├── PetItem                        # 孵化后留包；上阵 occupancy=deployed
│   └── PuppetItem                     # 背包面；绑定 PuppetCharacter
│
├── Content (ABC)
│   ├── TechniqueProgress              # 角色已学功法实例门面
│   ├── RecipeDef                      # 工坊配方定义
│   ├── ConstitutionAffix
│   ├── SecretRealmDef
│   └── BuffDef
│
├── LawEntity (ABC)
│   ├── DaoDef
│   ├── DaoLordshipSeat                # 席位实例门面
│   └── DaoRestraintEdge
│
├── Environment (ABC)
│   ├── WeatherState
│   ├── ShichenSlot
│   ├── TerrainCell
│   ├── FormationLayer                 # env/weather/effect
│   └── TauntAuraDef
│
└── Organization (ABC)
    ├── Sect
    └── SectBuffSource
```

### 3.2 战斗运行时（短生命周期，可独立包）

```text
BattleUnit                             # 非养成树；由 Character.on_battle_enter 产出
BattleBoard / BattleState              # 现行 autochess 内聚演进
ModifierStack                           # ATTR + Buff 修正；脏标记
AbilityRuntime                         # 触发索引 / ICD / 预算
```

### 3.3 能力与授予（横切）

```text
AbilityDef                             # abilities.yaml 一行
AbilityInstance                        # source + ability_id + 运行时状态
GrantSource (Protocol/ABC)             # equipment / set / technique / dao / lord / sect / env
AbilityBag                             # 装配期展开 grants → instances
```

### 3.4 组件（挂在 Player/Pet 上，不进基类 abstract）

| Component | 挂载 | 职责 |
| --- | --- | --- |
| `EquipmentLoadout` | Player | 槽位、套装计数、grants |
| `InventoryBag` | Player | 或仍由 InventoryService；门面可选 |
| `TechniqueBag` | Player | 已学功法 → grants |
| `DaoBag` | Player | 本命道/运用 → grants |
| `AbilityBag` | 任意 Grant 宿主 | 统一展开 |
| `PetSkillLoadout` | Pet | 四栏技能（可后置 Ability 化） |

---

## 4. 抽象契约（必须重写）

### 4.1 `Character`（核心）

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| `get_entity_kind()` | abstractmethod | 对齐 ATTR / `combat_attrs.entity_profiles` |
| `get_piece_kind()` | abstractmethod | 对齐 formation：`main/avatar/pet/puppet/monster/npc/boss` |
| `build_base_attrs()` | abstractmethod | 无临时 Buff 的 Attr 块来源 |
| `iter_grant_sources()` | abstractmethod | 装备/天赋/模板被动等 |
| `on_battle_enter(ctx)` | abstractmethod | → `BattleUnitSeed`（可序列化） |
| `phys_atk` 等核心有效值 | abstract `@property` | 读缓存 AttrBlock；禁止副作用 |
| `can_be_deployed()` | abstractmethod | 布阵合法性钩子 |

**明确不进 Character ABC**：开秘境、交易、宗门任命、聊天——用 Mixin/`DaoCarrier`/`SectMember` 等玩家侧接口。

### 4.2 `BattleUnit`（战斗内；可为具体类 + 少量 abstract 钩子）

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| `phys_atk` / `speed` / `hp_max`… | `@property` | base + `ModifierStack`；脏缓存 |
| `try_move(to, board)` | method | 校验、扣 AP、写坐标 |
| `basic_attack(target, rng)` | method | 命中/伤害/`on_hit` |
| `on_event(event)` | method | 交给 `AbilityRuntime` |
| 直接 `self.phys_atk = x` | **禁止** | 只通过 modifier add/remove |

### 4.3 `Item`（细表见 [`道具与装备实体设计.md`](./道具与装备实体设计.md)）

| 成员 | 类型 | 说明 |
| --- | --- | --- |
| `get_item_kind()` | abstractmethod | `item_type`：material/equipment/manual/puppet… |
| `get_def_id()` | abstractmethod | 配置定义 id |
| `get_stack_rules()` | abstractmethod | **四法则**：`bound` / `unique` / `tradable` / `max_stack` |
| `as_grant_source()` | 可选 | 装备/可触发道具 |
| `on_use(ctx)` | 可选 | 消耗品 / 秘籍 / 蛋 |
| `as_actor_ref()` | 可选 | 傀儡（及可选灵宠令牌）指向 Actor |
| `equip_slot()` | 装备 | 映射到穿戴栏指针槽；非装备为空 |

**禁止**：Item 实现 `on_battle_enter`；Character 实现堆叠/交易。双切面互指，不双继承。

### 4.4 `GrantSource` / `AbilityDef`

| 成员 | 说明 |
| --- | --- |
| `source_type` / `source_id` | 去重与拆解 |
| `list_ability_ids()` | 含套装阈值展开后的列表 |
| Ability：`domain` / `kind` / `trigger?` / `ops[]` | 见 §5 |

### 4.5 `Environment` / `LawEntity` / `Organization`

最少契约：`as_side_mod()` 或 `iter_grant_sources()`（二选一或都有）。  
天气/时辰多为 **side_mod**（settle/开战注入），不是给单个角色永久 grants（除非设计如此）。

---

## 5. Ability 方案（并入本重构）

### 5.1 枚举

**domain**：`combat` | `idle` | `craft` | `dice` | `privilege` | `panel`  
**kind**：`stat_mod` | `passive_mod` | `trigger` | `active` | `aura` | `privilege`  
**trigger**：`on_equip` | `on_battle_start` | `on_hit` | `on_hurt` | `on_kill` | `on_cast_skill` | `on_skill_tag` | `on_tick` | `manual`

**ops（一期）**：`add_attr` | `mul_channel` | `cast_ability` | `modify_skill` | `apply_buff` | `grant_flag` | `unlock_slot`

### 5.2 叠层规则

- 同 `ability_id` 多来源：**默认不叠**（可标 `stackable`）  
- 套装用独立 ability_id  
- `stat_mod` → `AdditiveSource`（equipment/technique/dao…）  
- `trigger`：分实例 ICD + `max_per_battle`  
- `privilege`：**禁止**进入 `simulate_battle`

### 5.3 配置文件（新增/演进）

| 文件 | 说明 |
| --- | --- |
| `abilities.yaml` | 效果库 |
| `equipment.yaml` | 装备 def：slot/stats/grants/set_id |
| `equipment_sets.yaml` | 件数 → grants |
| 现有 `inventory.yaml` | 增 `equipment`；`puppet` 增 `actor_def_id` |
| `dao_lord.yaml` | `privileges_on_claim.grants`；布尔兼容一期 |

---

## 6. 傀儡（双切面 · 定案）

**模型 A（推荐写死）**：一物一灵  

- 炼成：`PuppetItem`（InventoryItem）+ `PuppetCharacter` 行，互指  
- 上阵：bench 用 Character 面；分解/轮回规则同时清两边  
- 试炼木傀：`ephemeral=True` 的 `PuppetCharacter`，无背包行（保留 `trial_puppet_count`）

| 面 | 类 | 职责 |
| --- | --- | --- |
| 道具 | `PuppetItem` | 堆叠上限、交易、craft 产出 |
| 行动体 | `PuppetCharacter` | ATTR、布阵、开战种子 |
| 能力 | 可选 grants | 自身被动，不是给人穿的 equippable |

---

## 7. 目标包结构 ↔ 现行文件映射

> **策略**：新建 `app/game/`（或分域包）放 OOP 门面；`domain/` 纯函数逐步迁入或由类委托；`services/` 仍编排 IO；旧路径 **re-export shim** 一到两里程碑。

```text
backend/app/
  api/ schemas/ db/ core/ config_data/ config_source/   # 保持
  services/                                            # 保持用例编排；逐步依赖 game.*

  game/                                                # 新增：类层次「项目内容层」
    entity.py                                          # Entity 可选根
    character/
      base.py                                          # Character ABC
      player.py avatar.py pet.py puppet.py
      monster.py npc.py boss.py
      components/                                      # EquipmentLoadout…
    item/
      base.py
      material.py consumable.py equipment.py
      talisman.py blueprint.py skill_book.py
      pet_egg.py puppet_item.py
    content/                                           # technique/recipe/constitution/realm
    law/                                               # dao / lord / restraint
    environment/                                       # weather/shichen/terrain/layers
    org/                                               # sect
    ability/
      def.py instance.py bag.py ops.py runtime.py
    battle/
      unit.py stack.py seed.py
      # 引擎可仍住 domain/autochess，由 BattleUnit 适配

  domain/                                              # 过渡期保留；纯规则被类调用
    autochess.py combat_ai.py terrain.py …             # 战斗纯度不变
    combat.py                                          # ATTR 组装；供 Character.build_* 调用
    …
```

### 7.1 映射表（现行 → 新类/包）

| 现行 | 新归属 |
| --- | --- |
| `db.models.character.Character` | **改名语义**：ORM=`CharacterRow`；领域=`PlayerCharacter`（包装/工厂） |
| `services/character_service.py` | 构建/持久化 `PlayerCharacter`；`build_combat_attrs` 委托 |
| `domain/combat.py` | `AttrBlock` 权威算法；被各类 `build_base_attrs` 使用 |
| `services/pet_service.py` + `domain/pet_*.py` | `PetCharacter` + 组件；规则函数可留 domain |
| `services/avatar_service.py` | `AvatarCharacter` |
| `inventory` + craft `puppet` | `PuppetItem` + `PuppetCharacter` |
| `pve_monsters.yaml` + Autochess 组装 | `MonsterCharacter` / `from_template` |
| `domain/autochess.py` | 保持纯引擎；入参改为 `BattleUnitSeed` dict（兼容现 setup） |
| `services/autochess_service.py` | 调各 Character.`on_battle_enter` 组装 setup |
| `services/formation_service.py` | bench 对 `Character.can_be_deployed` |
| `domain/dao_*.py` + services | `DaoDef` / `DaoBag` / lord grants |
| `domain/weather_rules.py` + services | `WeatherState` / side_mod |
| `domain/sect_*.py` + services | `Sect` / `SectBuffSource` |
| `techniques` / constitution | `content.*` + GrantSource |
| 无（新） | `EquipmentItem` + Ability；对接 ATTR-D02 |

### 7.2 明确不动（本方案默认）

- `api/*` 路径与响应字段名（可加字段，不改旧键）  
- admin `domain_id` 字符串与 `registry.py` 键（新 YAML 以**新 domain_id** 注册，如 `abilities` / `equipment`）  
- `simulate_battle` 纯度：仍禁止 SQLAlchemy/FastAPI  
- 灵宠回合制 `pet_duel`：可后置对齐 Ability，不强制一期重写  

---

## 8. 与 ATTR / 战斗 / 道主的接线

```text
PlayerCharacter.iter_grant_sources()
        │
        ▼
AbilityBag.expand()
        ├─ stat_mod(combat) ──► AdditiveSource(equipment/…) ──► build_combat_attrs
        ├─ idle/craft/dice   ──► bonus_channels（IDLE-R01/DICE-R01）
        ├─ privilege         ──► flags（道主开秘境等）
        └─ combat trigger/active ──► on_battle_enter 写入 BattleUnitSeed
                                              │
                                              ▼
                                    simulate_battle（只读快照）
```

| 延后项 | 本方案中的位置 |
| --- | --- |
| **ATTR-D02** | `EquipmentItem` + `stat_mod` → 打开 `channels.equipment`；傀儡 Actor 喂入 |
| **M3-D03** | `AbilityRuntime` + BattleUnit 事件；战报 `skill_cast` / `item_trigger` |
| **M6-D04** | `privilege` Ability 替换布尔占位 |
| **M8 自研** | 产出 Technique/Formation/Talisman 仍走 Content/Item + grants |

---

## 9. 分期重构计划（可执行）

### 阶段 R0 · 设计冻结（本文）

- [x] 拍板：傀儡模型 A；装备槽位；同 ability 默认不叠  
- [x] 本文升为 **v1.0**；`后续待完成` 登记 **ARCH-R01**  
- [x] 不写破坏性搬迁  

### 阶段 R1 · 骨架与 shim（**当前** · 优先于 M8）

- [x] 建 `app/game/character/base.py`、`item/`、`ability/`、`content_store/` + 契约  
- [x] `PlayerCharacter.from_orm`；`CharacterService.build_player_character`  
- [x] `PetCharacter` / `AvatarCharacter` / `MonsterCharacter` / `PuppetCharacter` 最小实现  
- [x] 旧 import 路径不删；单测 `tests/test_game_arch_abc.py`  

**验收**：无行为变化；新增单元测试绿。

### 阶段 R2 · 开战统一入口（**S1-2 · 已落地**）

- [x] `Character.on_battle_enter` → seed；`AutochessService._attacker_units` / `_monster_units` 经 assemble  
- [x] 行为等价；棋子 kind / side / UID 前缀走 `app.constants.battle`  

**验收**：相关单测绿（formation / autochess / constants）。

### 阶段 R3 · ATTR 源门面（**S1-3 · 已落地骨架**；装备喂入仍归 M8）

- [x] `TechniqueGrantSource` / `ConstitutionGrantSource`；`build_player_character` 注入  
- [ ] `EquipmentItem` / slots / 打开 equipment 通道 → **M8 / ATTR-D02**  

### 阶段 R3b · 傀儡双切面（**S1-4 · 已落地**）

- [x] `PuppetActor` ORM + `PuppetService`（炼成/懒绑定）  
- [x] 布阵 bench `ref_id=Actor.id`；开战真傀经 `PuppetCharacter`；试炼木傀 ephemeral  
- [x] `inventory.yaml` `actor_def_id` / `max_stack: 1`  

### 阶段 R4 · Ability 战斗触发（跟 M3-D03）

- [ ] `ModifierStack` + `AbilityRuntime` 进战斗热路径  
- [ ] 样本：on_hit 施法 / modify_skill / 套装 2/4  
- [ ] 战报事件对齐登记册  

### 阶段 R5 · 道主 privilege + 环境/宗门 GrantSource 化（**S1-5 投影已落地**；秘境鉴权仍后置）

- [x] `normalize_privileges_payload` 布尔 ↔ grants 双写（就任/GM/赛会更替/摘要）  
- [ ] 秘境鉴权读 `has_privilege`  
- [ ] weather/sect_buff 标 side_mod 或 grants  
- [ ] dao usage 迁 grants（可双写一期）  

### 阶段 R6 · 目录收敛（**S1-6 · 已落地最小集**；大搬家仍后置）

- [ ] 将稳定纯函数迁入 `game/*/rules.py` 或保留 `domain` 由类委托（**不阻塞**；按竖切渐进）  
- [x] 删除过期 shim：`domain.m4_constants` → 一律 `app.constants.m4`  
- [x] ORM `CharacterRow` 别名（= `Character`）；**表名/类名 rename migration 仍延后**  
- [x] 更新开发计划/README/登记册：ARCH S1 收口；下一主线 M8  

---

## 10. ORM / 服务 / 领域类命名纪律

| 层 | 命名 | 例 |
| --- | --- | --- |
| ORM | `*Row` 或保持表名类 + 注释 | `CharacterRow`（现 `Character`） |
| 领域 OOP | `PlayerCharacter` | 无 Session |
| Application | `*Service` | 持有 `session`，组装领域对象 |
| 纯函数 | `domain.*_rules` | 无状态；被类/服务调用 |
| DTO | `schemas.*` | HTTP 边界 |

禁止：领域 `PlayerCharacter` 上挂 `self.session`。

---

## 11. 目录与测试约定

```text
backend/tests/
  test_game_character_abc.py      # 契约
  test_game_ability_expand.py     # grants 展开/去重
  test_game_puppet_dual.py        # 双切面
  # 现有 test_autochess_* / test_combat_attrs 保持；经 shim 仍绿
```

---

## 12. 风险与缓解

| 风险 | 缓解 |
| --- | --- |
| 大搬家导致长期红灯 | R1～R2 只加类不挪文件；shim |
| Character ABC 膨胀 | 硬上限：核心契约 ≤15；玩家专属走 Mixin |
| 战斗性能回退 | BattleUnit 热路径保持扁平；property 必须缓存+脏标记 |
| 与 M8 抢主线 | R3 与 ATTR-D02 绑在一起做；R4 等 M3-D03 |
| 后台配置爆炸 | abilities/equipment 新 domain_id；旧 items 渐进 |

---

## 13. 待拍板清单（升 v1.0 前）

1. ~~装备槽：一期五槽？~~ **已废**：权威为道具专文 §5 **17 区**；旧 `weapon/armor/fabao` 仅读档迁移。  
2. 装备基础属性：快捷 `stats` 字段 vs 强制 Ability？建议 **stats 快捷 + grants 特效**。  
3. 同 ability 多来源默认不叠 —— 是否确认？  
4. 傀儡模型 A（一物一灵）—— 是否确认？  
5. 新包名：`app/game/` 是否接受（或 `app/entities/`）？  
6. ORM `Character` 是否在 R6 rename 为 `CharacterRow`？  

---

## 14. 文档与排期挂钩

| 文档 | 动作 |
| --- | --- |
| 本文 | 类 / Ability **是什么**（OOP 真理源） |
| [`双轨整改与配置兼容方案.md`](./双轨整改与配置兼容方案.md) | **怎么落地**：步骤1存量 / 步骤2增量 / YAML↔DB |
| `ATTR战斗属性占位设计.md` | ATTR-D02 实现时引用 Character/Equipment |
| [`道具与装备实体设计.md`](./道具与装备实体设计.md) | **Item ABC / 四法则 / 双切面**（M8 **R0**） |
| `后续待完成.md` | **ARCH-R01**（OOP）+ **ARCH-R02**（双轨/ContentStore） |
| `开发计划.md` | M8 注记可并行 S1-1/1-2 |
| `README.md` | 后端架构节增加 OOP + ContentStore 指针 |

---

## 15. 修订记录

| 日期 | 版本 | 说明 |
| --- | --- | --- |
| 2026-08-19 | **v1.0.6** | 插入 `CultivatorCharacter`；化身改为修士叶 + `DivineSenseConsumer`；灵宠/傀儡仍 `SenseMinionCharacter` |
| 2026-08-18 | **v1.0.5** | Character 基础属性含法力；插入 `PlayerNpcCharacter`（神识）与 `SenseMinionCharacter`（消耗神识）；Npc 不再继承 Monster |
| 2026-08-14 | v0.1 | 首版：六大域 + Character/Item ABC + Ability + 傀儡双切面 + 分期映射全项目现行代码 |
| 2026-08-14 | v0.1.1 | 回链 [`双轨整改与配置兼容方案.md`](./双轨整改与配置兼容方案.md)；配置原则改为 ContentStore |
| 2026-08-14 | **v1.0** | 拍板冻结；**R1 落地** `app/game/**` + ContentStore + 单测；排期改为 ARCH 优先于 M8 |
| 2026-08-14 | **v1.0.1** | **S1-4/S1-5 落地**：`PuppetActor` + privilege 投影；R3b/R5 部分勾选；下一主线可开 M8 |
| 2026-08-17 | **v1.0.3** | Item 树与契约改指针 [`道具与装备实体设计.md`](./道具与装备实体设计.md)；法宝不再单列 `FabaoItem` |
| 2026-08-17 | **v1.0.4** | 装备槽改为 17 区（`pet` 单指针 + 傀儡编成板）；指针道具专文 v0.7 |
| 2026-08-14 | **v1.0.2** | **S1-6**：删 `m4_constants` shim；`CharacterRow` 别名；R6 最小集勾选 |

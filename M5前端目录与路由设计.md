# M5 前端目录与路由设计

| 项 | 内容 |
| --- | --- |
| **针对里程碑** | **M5（环境与轮回外环）** |
| **设计日期** | 2026-08-10（**v1.5**：§0.0.2 渡劫/探索面板中文 label；依据 `M5环境与轮回外环设计.md` v1.5） |
| **依据** | `M5环境与轮回外环设计.md`；[`骰子系统设计.md`](./骰子系统设计.md)；`M4前端目录与路由设计.md`；`M3`/`M2`/`M1`/`M0` 前端目录设计；`开发计划.md` §7 |
| **原则** | **再拆两条玩法路由**（`/tribulation` `/reincarnation`）；**全局顶栏**展示六时/天气（非独立 path）；`/hall` 仍为养成枢纽；M4 三路由与 `/formation` `/battle` 保留并接环境提示；目录骨架继承 M0～M4；**检定/掷骰展示权威来自服务端**（修为区间骰 + 概率门面，禁止前端自掷成败） |

---

## 1. 设计目标

约定 M5 在 `frontend/src` 下的文件增量、**新路由表**、顶栏与大厅枢纽信息架构，以及与后端世界时钟 / 天气 / 渡劫 / 待引渡 / 轮回接口的对接落点。

M5 可玩范围：顶栏择时、环境修正提示、**渡劫准备格 + 雷劫双维批次结算**、待引渡三条路径、主动轮回祭坛预览、**新生选角 + 轮回商店（固定/随机/刷新）**、轮回后状态与 story flag 只读展示、背包普通袋/轮回袋分栏。  
范围外：道友/宗门真引渡 UI、完整剧情播放器、六时 WS 推送、劫云真实邻区对他玩家特效（→ M9）、气运/魔性养成页、完整带宠传承数值面板；以及 M3/M4 延后打磨项（见 [`后续待完成.md`](./后续待完成.md) **§0.1**）。本文描述 M5 主路径路由与组件（含收口后强化 UI）。

### 1.1 与修为骰子系统（前端纪律）

凡突破结果、遮天成败、护主触发、战斗先攻/伤害出目，**只展示服务端返回的 `dice` / `roll` / `threshold` / `lo`/`hi` 或战报事件字段**；客户端禁止用本地 `Math.random` 判定成功。区间含义与配置见 [`骰子系统设计.md`](./骰子系统设计.md)。大厅 `IdleEnvPanel` / 突破弹窗若展示检定摘要，字段与 `POST /breakthrough/attempt` 响应中的 `dice` 块对齐。

---

## 2. 路由策略（相对 M4：**再增两条玩法 path + 全局环境壳**）

| path | name | 文件 | M5 变化 |
| --- | --- | --- | --- |
| `/login` | `login` | `views/LoginView.vue` | 无 |
| `/register` | `register` | `views/RegisterView.vue` | 无 |
| `/create-character` | `create-character` | `views/CreateCharacterView.vue` | 无 |
| `/hall` | `hall` | `views/HallView.vue` | **枢纽扩展**：环境摘要 + 渡劫/轮回入口 |
| `/formation` | `formation` | `views/FormationView.vue` | 渡劫时 Bench 化身灰置文案兑现 |
| `/battle` | `battle` | `views/BattleView.vue` | 开战前展示锁定环境预览；状态禁战提示 |
| `/avatar` | `avatar` | `views/AvatarView.vue` | 渡劫提示「不可上阵，挂机仍可用」真态 |
| `/workshop` | `workshop` | `views/WorkshopView.vue` | 开工天气提示；效率文案接环境 |
| `/pets` | `pets` | `views/PetsView.vue` | 轮回带宠说明占位（闸关时隐藏） |
| `/tribulation` | `tribulation` | `views/TribulationView.vue` | **新**：渡劫会话 / 波次 / 战报 |
| `/reincarnation` | `reincarnation` | `views/ReincarnationView.vue` | **新**：待引渡 / 祭坛预览 / 流水 |
| `/` | — | 重定向 | 逻辑不变；**例外**：`awaiting_ferry` 时可优先进 `/reincarnation` |
| `/test` | `test` | `test/apps.vue` | 可选；非验收 |

### 2.1 守卫逻辑（继承并扩展）

| 规则 | 说明 |
| --- | --- |
| 需登录 | 既有玩法页 + `/tribulation` `/reincarnation` 均 `requiresAuth` |
| 无角色 | → `/create-character` |
| 有角色访创角 | → `/hall`（若待引渡见下） |
| 已登录访登录/注册 | → `homePathAfterAuth()` |
| `redirect` 白名单 | `resolveSafeRedirect` **追加** `/tribulation`、`/reincarnation` |
| 待引渡强引导 | `status===awaiting_ferry` 时访问 `/battle` `/formation` `/workshop` 等「积极玩法」→ 重定向 `/reincarnation`（允许 `/hall` 只读摘要） |
| 新生强引导 | `status===reincarnating` 时访问大厅与积极玩法 → `/reincarnation?mode=newborn`（确认新生前不可进厅） |
| 渡劫引导 | `status===tribulation` 且无会话页时，大厅门闸高亮「前往渡劫」；**不**全局禁 `/hall` |
| 境界/状态硬拦 | **不做**「未达元婴禁止进 `/tribulation`」路由级硬拦；页内空态 + API 错误 |

```text
App.vue
  └── 布局壳（可选）
        ├── WorldClockBar.vue（全局：时辰 + 倒计时 + 天气）
        └── <RouterView />
              ├── /login | /register | /create-character
              ├── /hall → HallView.vue
              │     ├── CharacterPanel（status / 轮回点 / ferry 摘要）
              │     ├── Idle / Allocate / Constitution / Breakthrough（保留）
              │     ├── HallBattleGate / HallDualThreadGate（保留）
              │     ├── HallEnvGate.vue（新：渡劫/轮回/祭坛入口）
              │     ├── EnvModifierHint.vue（可选：当前行为修正一行）
              │     └── GameLogPanel.vue
              ├── /formation | /battle | /avatar | /workshop | /pets（保留+环境提示）
              ├── /tribulation → TribulationView.vue
              │     ├── TribulationStatusPanel.vue   # 双维：威力档/次数档/预估品阶
              │     ├── TribulationPrepBoard.vue     # 准备格排序
              │     ├── TribulationVeilPanel.vue      # 遮天道具确认
              │     ├── TribulationCloudBanner.vue    # 劫云/锁前天气说明
              │     ├── TribulationResolvePanel.vue   # 批次进度 / 跳过
              │     └── TribulationResultDialog.vue
              └── /reincarnation → ReincarnationView.vue
                    ├── FerryCountdownPanel.vue
                    ├── FerryActionsPanel.vue
                    ├── ReincarnationPreviewPanel.vue
                    ├── AltarConfirmPanel.vue
                    └── StoryFlagsReadonly.vue
```

### 2.2 为何现在再拆路由

| 理由 | 说明 |
| --- | --- |
| 对齐开发计划 Q3/R4 | 渡劫专属 UI、待引渡三条路径均为独立决策面，不宜塞进大厅折叠 |
| 状态机强引导 | 待引渡需全屏倒计时与不可逆确认，独立路由便于守卫 |
| 环境是横切 | 六时/天气用顶栏组件，**不**为每个修正开 path |
| 守卫复用 | 同一套 JWT + `hasCharacter`；仅追加状态分流 |

**不做**：把突破弹窗升级为唯一渡劫入口（允许 Breakthrough 跳转，但 `/tribulation` 必须可直达）；不做剧情播放路由（→ M10）。

### 2.3 可选 query（非新 path）

| 用法 | 说明 |
| --- | --- |
| `/reincarnation?mode=ferry` | 打开待引渡区（默认） |
| `/reincarnation?mode=altar` | 打开主动祭坛预览 |
| `/reincarnation?mode=logs` | 轮回流水 |
| `/reincarnation?mode=newborn` | **新生选角**（`status=reincarnating` 强制；含轮回商店） |
| `/tribulation?wave=2` | 高亮当前波（只读意图） |
| `/workshop?env=1` | 展开天气对分支效率说明 |
| `/hall?hint=env` | 滚动到环境门闸 |

### 2.4 `homePathAfterAuth` 扩展

```text
if !hasCharacter → /create-character
if status == awaiting_ferry → /reincarnation?mode=ferry
if status == reincarnating → /reincarnation?mode=newborn
if status == tribulation → /tribulation
→ /hall
if status == tribulation → /tribulation
else → /hall
```

---

## 3. `src` 目录增量

M0～M4 目录职责不变；M5 **追加文件与路由**，不推翻骨架。

```text
src/
├── api/
│   ├── world.ts                 # 新：calendar / weather / env
│   ├── tribulation.ts           # 新：me / start / wave
│   ├── ferry.ts                 # 新：me / self-rescue / enter-reincarnation
│   ├── reincarnation.ts         # 新：preview / altar / logs
│   ├── breakthrough.ts          # 扩展：needs_tribulation 分支
│   ├── battle.ts                # 扩展：展示 lock 字段
│   ├── craft.ts                 # 扩展：env 提示
│   ├── character.ts             # 扩展 apply 新摘要
│   └── gm.ts                    # 扩展时辰/天气/渡劫/待引渡
├── components/
│   ├── layout/
│   │   └── WorldClockBar.vue    # 新：全局顶栏
│   ├── hall/
│   │   ├── HallEnvGate.vue      # 新：渡劫/轮回入口卡
│   │   └── EnvModifierHint.vue  # 可选
│   ├── tribulation/
│   │   ├── TribulationStatusPanel.vue
│   │   ├── TribulationPrepBoard.vue
│   │   ├── TribulationVeilPanel.vue
│   │   ├── TribulationCloudBanner.vue
│   │   ├── TribulationResolvePanel.vue
│   │   └── TribulationResultDialog.vue
│   ├── reincarnation/
│   │   ├── FerryCountdownPanel.vue
│   │   ├── FerryActionsPanel.vue
│   │   ├── ReincarnationPreviewPanel.vue
│   │   ├── AltarConfirmPanel.vue
│   │   └── StoryFlagsReadonly.vue
│   ├── workshop/
│   │   └── CraftEnvHint.vue     # 可选：开工天气一行
│   ├── battle/
│   │   └── BattleEnvLockLine.vue # 可选：锁定环境
│   └── …（M4 avatar/pets、M3 board 等保留）
├── stores/
│   ├── world.ts                 # 新：shichen / weather / poll
│   ├── tribulation.ts           # 新
│   ├── ferry.ts                 # 新（或并入 reincarnation）
│   ├── reincarnation.ts         # 新：preview / logs
│   ├── character.ts             # 扩展 status 分流字段
│   └── craft / battle           # 扩展展示字段
├── types/
│   ├── world.ts
│   ├── tribulation.ts
│   ├── ferry.ts
│   └── reincarnation.ts
├── utils/
│   ├── safeRedirect.ts          # 白名单扩展
│   ├── shichenLabel.ts          # 时辰中文
│   ├── weatherIcon.ts           # 图标/文案映射（可用 emoji 或简单 SVG，非终局美术）
│   └── ferryCountdown.ts        # 本地倒计时格式化
├── router/
│   └── index.ts                 # 登记两路由；待引渡守卫
└── views/
    ├── HallView.vue             # 挂 EnvGate；可嵌顶栏
    ├── TribulationView.vue      # 新
    ├── ReincarnationView.vue    # 新
    ├── WorkshopView.vue         # 环境提示
    ├── BattleView.vue           # 环境锁展示
    └── …（其余保留）
```

### 3.1 命名约定（继承）

- 页面：`XxxView.vue`（M5 **新增** Tribulation / Reincarnation）
- 零件：`XxxPanel.vue` / `XxxBar.vue` / `XxxGate.vue`；按域放 `components/tribulation|reincarnation|layout|hall/`
- API 文件与后端模块同名
- 路由 path：小写英文词（`/tribulation` `/reincarnation`）

### 3.2 子目录说明

- `views/` 仍扁平（再多 2 个整页，可接受）。  
- `components/layout/` 放跨页壳（顶栏）；避免把 `WorldClockBar` 塞进 `hall/`。  
- `components/` 继续按域分子夹（与 M3/M4 一致）。

### 3.3 顶栏挂载位置

| 方案 | 说明 | M5 选择 |
| --- | --- | --- |
| A. `App.vue` 全局 | 登录页也可见 | 需按路由隐藏 |
| B. 仅玩法布局 | 登录/注册/创角不显示 | **采用**：`meta.showWorldBar: true` 的路由显示 |

---

## 4. 信息架构

### 4.1 全局顶栏（WorldClockBar）

```text
┌──────────────────────────────────────────────────────────────┐
│ 清晨 · 距下一时 0:42   天气：雷暴   提示：渡劫压力↑ 炼丹↓      │
│（可选折叠详情：对挂机/突破/工坊三条修正摘要）                  │
└──────────────────────────────────────────────────────────────┘
```

- 数据源：`GET /world/env` 或 calendar+weather；按 `VITE_WORLD_POLL_MS` 刷新。  
- **不**在前端推算权威时辰（可做倒计时动画，到点必须拉服务器校准）。  
- 待引渡时顶栏可追加红色「待引渡」角标，点击进 `/reincarnation`。

### 4.2 大厅（枢纽）布局增量

```text
┌──────────────────────────────────────────────────────────────┐
│ WorldClockBar                                                │
│ AuthSessionBar · 修仙大厅 · M5 环境与轮回                       │
│ [布阵][战斗][化身][工坊][灵兽园]  [渡劫][轮回/祭坛]              │
├────────────────────────────┬─────────────────────────────────┤
│ CharacterPanel             │ GameLogPanel                    │
│  · status 真态文案         │  · 环境切换（可选）              │
│  · 轮回点 / 周目           │  · 渡劫波次 / 轮回结算           │
│ IdlePanel + 环境修正提示   │                                 │
│ HallBattleGate             │                                 │
│ HallDualThreadGate         │                                 │
│ HallEnvGate（新）          │                                 │
│  · 渡劫中：去渡劫          │                                 │
│  · 待引渡：去引渡（强调）  │                                 │
│  · 正常：祭坛入口          │                                 │
│ Breakthrough …             │                                 │
└────────────────────────────┴─────────────────────────────────┘
```

### 4.3 渡劫页布局

分两阶段：**准备** → **开渡结算**（同一路由，按 `phase` 切换）。

**准备阶段**

```text
┌────────────────────────────────────────────────────────────┐
│ ← 回大厅 · 渡劫准备                                         │
│ TribulationStatusPanel                                      │
│  · 目标境界 · 预估突破品阶（凡品/天道…）                    │
│  · 雷劫：【天妒】×【千劫天雷】1000 击                       │
│  · 锁前天气：雷暴（结算用）· 是否已在劫云：否/是(×2警告)   │
│ TribulationCloudBanner：开渡后将覆盖半径 N 的劫云           │
├────────────────────────────┬───────────────────────────────┤
│ TribulationPrepBoard       │ 背包候选（护劫丹/法宝/普通宝） │
│  · 有限格拖拽排序          │  · 普通法宝标注「抵劫效率差」 │
│  · 格序号=消耗顺序         │ TribulationVeilPanel 遮天     │
│  · 阵法下拉                │  · 成功降档 / 失败重罚文案    │
│ [确认准备] [开渡]          │ 气运/魔性只读摘要（占位）     │
└────────────────────────────┴───────────────────────────────┘
```

**开渡结算阶段**

```text
┌────────────────────────────────────────────────────────────┐
│ 劫云中 · 结算仍按开渡前·雷暴 · 当前档：天妒 →(护主后)普通   │
│ HP 条 · 进度 240/1000 · 准备格剩余 2                       │
│ TribulationResolvePanel                                     │
│  · [下一批] [一键跳过到结束]                                │
│  · 摘要日志：耐久扣减 / 一击破 / 护主触发                   │
└────────────────────────────────────────────────────────────┘
→ TribulationResultDialog：成功进阶+品阶 / 失败惩罚 / 陨落→轮回
```

> 千劫/万劫：**禁止**逐雷按钮；只提供批次与一键跳过。
### 4.4 轮回 / 待引渡页布局

```text
┌────────────────────────────────────────────────────────────┐
│ ← 回大厅（待引渡时可弱化）· 轮回与引渡                      │
├────────────────────────────────────────────────────────────┤
│ mode=ferry：                                               │
│  FerryCountdownPanel（大倒计时）                           │
│  FerryActionsPanel                                         │
│   [自救] [道友引渡·未开放] [进入轮回]                       │
│  ReincarnationPreviewPanel（自选前必看）                   │
├────────────────────────────────────────────────────────────┤
│ mode=altar：                                               │
│  ReincarnationPreviewPanel + AltarConfirmPanel             │
│  （二次确认：重置境界 / 保留体质 / 快照作废）               │
├────────────────────────────────────────────────────────────┤
│ mode=logs / 侧栏：                                         │
│  流水列表 · StoryFlagsReadonly（已历节点 ID，无正文）      │
└────────────────────────────────────────────────────────────┘
```

### 4.5 组件职责

| 组件 | 职责 | 不负责 |
| --- | --- | --- |
| `WorldClockBar` | 展示权威环境；到点校准 | 本地发明时辰公式当权威 |
| `HallEnvGate` | 跳转渡劫/轮回 + 角标 | 结算轮回 |
| `EnvModifierHint` | 只读展示服务端提示文案 | 重算乘区 |
| `TribulationStatusPanel` | 展示双维、预估品阶、互斥说明 | 本地改档位 |
| `TribulationPrepBoard` | 拖拽排序准备格；提交 prep | 扣背包权威 |
| `TribulationVeilPanel` | 遮天确认与风险文案；展示服务端 veil 结果 | **禁止**客户端掷成功；成败只信 API |
| `TribulationCloudBanner` | 锁前天气 vs 劫云表现；×2 警告 | 改世界天气 |
| `TribulationResolvePanel` | 批次/跳过；展示护主与破宝日志 | 逐雷微操；护主触发由服务端骰门面结算 || `TribulationResultDialog` | 成败/陨落跳转 | 改境界 |
| `FerryCountdownPanel` | 本地倒计时展示；到期触发 refresh | 客户端强制轮回 |
| `FerryActionsPanel` | 自救/自选轮回；灰置未开放引渡 | 伪造道友救援成功 |
| `ReincarnationPreviewPanel` | 展示保留清单 | 改保留规则 |
| `AltarConfirmPanel` | 二次确认主动轮回 | 跳过预览直接祭坛（禁止） |
| `StoryFlagsReadonly` | 只读节点 ID 列表 | 剧情播放 |
| `TribulationView` 等 | 编排拉数、日志、脏提示 | 大段公式 |

### 4.6 关键时序

**突破遇雷劫**

```text
大厅 Breakthrough → POST /breakthrough/attempt
  → 响应含 dice{roll,threshold,lo,hi,…}（无劫时即用此判成败；需雷劫则 needs_tribulation）
  → needs_tribulation
  → POST /tribulation/start-prep → 展示映射双维
  → router.push('/tribulation')
  → 玩家排准备格 → PUT prep → commit-prep
  → （可选 veil-check：概率门面，非区间骰）→ begin
  → resolve-batch / auto-resolve
  → 成功：applyCharacter；结果弹窗「确认突破」
  → fallen：push('/reincarnation?mode=ferry')
```

无劫突破（小境界 / 锻体～元婴跨境）：同一 `attempt` 接口，前端应能展示 `dice` 出目与阈值（显性设计 §0.7），勿只显示成功/失败布尔。
**待引渡自救**

```text
FerryActions → POST /ferry/self-rescue
  → 40066：石不足/冷却
  → 成功：status=normal；可回大厅
```

**自选 / 强制轮回**

```text
先 GET/POST preview → 用户勾选确认
  → POST enter-reincarnation 或 服务端超时惰性结算
  → applyCharacter（锻体一层）
  → 提示快照已作废；StoryFlags 仍在
```

**主动祭坛**

```text
/reincarnation?mode=altar → preview → 二次确认
  → POST /reincarnation/altar
  → 40068：状态不符
```

**工坊开工**

```text
选配方 → 展示 CraftEnvHint（当前将锁定的天气）
  → POST /craft/start → 队列带 lock；进度区显示锁定天气
```

---

## 5. API / Store 对应

| 文件 | 函数（示意） |
| --- | --- |
| `api/world.ts` | `fetchCalendar` · `fetchWeather` · `fetchWorldEnv` |
| `api/tribulation.ts` | `startPrep` · `savePrep` · `commitPrep` · `veilCheck` · `begin` · `resolveBatch` · `autoResolve` · `fetchTribulation` |
| `api/ferry.ts` | `fetchFerry` · `selfRescue` · `enterReincarnation` |
| `api/reincarnation.ts` | `previewReincarnation` · `altarReincarnation` · `fetchReincarnationLogs` · `fetchNewbornOptions` · `completeNewborn` · `fetchReincarnationShop` · `buyReincarnationShopItem` |
| `api/breakthrough.ts` | 识别 `needs_tribulation` |

**`useWorldStore`**

| state / action | 说明 |
| --- | --- |
| `shichen` / `weather` / `nextShichenAt` | 服务端权威 |
| `hints` | 对当前行为提示文案 |
| `poll` / `calibrate` | 轮询与可见性唤醒校准 |

**`useTribulationStore`**

| state / action | 说明 |
| --- | --- |
| `session` | null=无 |
| `load` / `start` / `wave` | |

**`useFerryStore` / `useReincarnationStore`**

| state / action | 说明 |
| --- | --- |
| `ferry` / `preview` / `logs` | |
| `selfRescue` / `enter` / `altar` | |

**`useCharacterStore`**：`applyCharacter` 识别 `ferry`、`reincarnation_points`、`story_flags`、`tribulation` 摘要；供守卫与门闸使用。

---

## 6. 交互与文案约定

### 6.1 环境顶栏

- 时辰用中文固定六字；倒计时 `m:ss`。  
- 天气变化可短 toast（节流，避免每 5s 刷屏）。  
- 文案示例：「雷暴：渡劫更难；炼丹效率下降」。  
- `CALENDAR_ENABLED=false` 时显示「历法校准中（固定正午）」类 DEV 提示可选。

### 6.2 渡劫

- 首屏写明互斥与双维：「本次雷劫：天妒 · 千劫天雷（由预估上品映射）」。  
- 明确两行天气：「结算天气（开渡前）」≠「表现天气（劫云）」。  
- 已在劫云内：红色警告「基础伤害 ×2」。  
- 普通法宝：灰色标签「可抵劫，效率极差」。  
- 遮天：二次确认「失败可能升档/重伤」。  
- 万劫：进度用百分比 + 已结算批次数，不渲染 10000 行。  
- 护主触发：强提示「灵宝护主！威力降档 / 怜悯档伤害降至 1%」。  
- 无会话空态：「当前无需渡劫；元婴之后的进阶将引发雷劫」。

### 6.3 待引渡 / 轮回

- 倒计时归零：前端停止幻想自救成功，立刻 `refresh`；若已轮回则全屏结果。  
- 道友/宗门按钮：**可见但禁用**，tooltip「将在宗门社交阶段开放（M7）」。  
- 祭坛二次确认必须列出：境界重置、快照作废、化身解散、体质保留。  
- Story flags：标题「前世阅历（系统标记）」；说明「剧情播放器将在后续开放」。

### 6.4 与 M4 页一致性

- 化身页：渡劫真态替换原「钩子」文案。  
- 工坊：区分「制造业挂机」与「配方队列」；追加天气行。  
- 布阵：化身灰置原因显示「渡劫中」。

### 6.5 UI 基调

- 继承 M0～M4：功能优先；Element Plus；不为 M5 做终局劫云特效。  
- 至少 2～3 处轻量动效：顶栏时辰切换淡入、渡劫波次进度、待引渡倒计时脉冲。

### 6.6 GM（DEV）

- 强制时辰/天气、一键开渡、置待引渡、立即超时轮回、标记 story 节点。  
- 仅 `import.meta.env.DEV`。

---

## 7. 类型对齐（要点）

```ts
export type ShichenId =
  | 'dawn'
  | 'noon'
  | 'afternoon'
  | 'dusk'
  | 'night'
  | 'late_night'

export type WorldWeatherId =
  | 'clear'
  | 'overcast'
  | 'rain'
  | 'hurricane'
  | 'storm'
  | 'thunderstorm'

export interface WorldEnvPublic {
  shichen: ShichenId
  shichen_label: string
  next_shichen_at: string
  weather: WorldWeatherId
  weather_label: string
  weather_next_roll_at?: string
  hints: { idle?: string; breakthrough?: string; craft?: string; tribulation?: string }
}

export type CharacterStatus =
  | 'normal'
  | 'breaking_through'
  | 'tribulation'
  | 'awaiting_ferry'
  | 'reincarnating'

export interface TribulationSessionPublic {
  id: number
  phase: 'preparing' | 'committed' | 'running' | 'won' | 'failed' | 'fallen'
  target_label: string
  projected_grade?: string
  power_tier: 'mercy' | 'normal' | 'jealousy' | 'apocalypse'
  power_label: string
  count_tier: 'nine' | 'hundred' | 'thousand' | 'myriad'
  count_label: string
  strike_total: number
  strike_done: number
  locked_shichen: ShichenId
  locked_weather: WorldWeatherId
  display_weather: 'tribulation_cloud' | WorldWeatherId
  in_cloud_double: boolean
  cloud_radius: number
  hp_current: number
  hp_max: number
  prep_slots: Array<{ slot: number; item_uid?: string; item_name?: string }>
  guardian_used: boolean
  axis_hints?: { power?: string; mitigation?: string }
}

export interface FerryPublic {
  deadline_at: string
  can_self_rescue: boolean
  self_rescue_cost?: number
}

export interface ReincarnationPreview {
  path: 'altar' | 'voluntary_ferry' | 'forced'
  keep: string[]
  lose: string[]
  points_gain: number
  pet_carry_note?: string
}
```

---

## 8. 与后续里程碑

| 里程碑 | 对本设计的影响 |
| --- | --- |
| **M5** | 本文范围；按上表建齐双路由与顶栏 |
| **M6** | 可选 WS 推时辰；道主页另开；勿把 WS 塞进 M5 出口 |
| **M7** | 启用道友/宗门引渡按钮；邮件通知占位 |
| **M9** | `region_id` 非 default；劫云半径真实邻区；顶栏区域名 |
| **M10** | `StoryFlagsReadonly` 进化为播放器入口 |
| 延后项 | 一律先查 [`后续待完成.md`](./后续待完成.md) |

---

## 9. 开发进度与步骤（对照 M5）

> 本节随实现同步更新；接口以 `M5环境与轮回外环设计.md` 为准。

### 9.1 总览（对齐外环设计 §11 E1～E6）

| 步 | 内容 | 状态 | 说明 |
| --- | --- | --- | --- |
| 0 | 设计文档（本文 + 外环骨架） | **已完成** | 2026-08-06；**v1.1** 渡劫加厚 |
| **E1** | 类型 + `api/world` calendar 联调壳 + WorldClockBar 时辰 | **已完成** | 前端落地 2026-08-06 |
| **E2** | weather + 开战/工坊锁展示 | **已完成** | BattleEnvLockLine / CraftEnvHint |
| **E3** | EnvModifierHint / 工坊提示接修正文案 | **已完成** | |
| **E4** | `/tribulation` 准备格 + 双维展示 + 批次结算跳转 | **已完成** | |
| **E5** | `/reincarnation` 待引渡/祭坛/preview/新生/商店 | **已完成** | 道友引渡按钮灰置 M7；新生+商店 2026-08-06 |
| **E6** | 守卫分流 + 大厅 EnvGate + GM + 总验收 | **已完成** | 前后端联调 2026-08-06 |
| 收尾 | GameLog / smoke 对照 | **已完成** | `smoke_m5.py`；强化续测见 `test_reincarnation_boost.py` |
| 打磨 | 突破结果 dice 出目 UI | **已完成** | 2026-08-07 · 原 M5-D12 |

### 9.2 本地联调口令（实现后）

```powershell
# 后端
cd backend
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000

# 前端
cd frontend
npm run dev
```

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/world/calendar` | GET | 六时 |
| `/api/v1/world/weather` | GET | 天气 |
| `/api/v1/world/env` | GET | 聚合+提示 |
| `/api/v1/tribulation/me` | GET | 渡劫会话 |
| `/api/v1/tribulation/start-prep` | POST | 创建准备 |
| `/api/v1/tribulation/prep` | PUT | 保存准备格 |
| `/api/v1/tribulation/begin` | POST | 开渡切劫云 |
| `/api/v1/tribulation/resolve-batch` | POST | 批次结算 |
| `/api/v1/tribulation/auto-resolve` | POST | 一键结算 |
| `/api/v1/ferry/me` | GET | 待引渡 |
| `/api/v1/ferry/self-rescue` | POST | 自救 |
| `/api/v1/reincarnation/preview` | POST | 预览 |
| `/api/v1/reincarnation/altar` | POST | 主动轮回（→ reincarnating） |
| `/api/v1/reincarnation/newborn` | GET | 新生选角目录 |
| `/api/v1/reincarnation/complete-newborn` | POST | 确认新生 → normal |
| `/api/v1/reincarnation/shop` | GET | 轮回商店 |
| `/api/v1/reincarnation/shop/buy` | POST | 花费轮回点购买 |

### 9.3 下一步建议

1. 实现顺序严格 **E1→E6**；先时钟权威再渡劫 UI。  
2. 勿在 E6 前做剧情播放或道友引渡真接口。  
3. N4 物种表可并行，不阻塞顶栏与轮回主路径。  
4. 联调优先：GM 强制时辰 → 观察挂机差 → GM 开渡 → 陨落 → 自救/轮回 → 断言大厅境界。

---

## 10. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-06 | 初稿：新增 `/tribulation` `/reincarnation`；`WorldClockBar`；大厅 `HallEnvGate`；待引渡守卫；对齐 E1～E6 |
| 2026-08-06 | **v1.1**：渡劫页拆准备/结算；准备格、双维、劫云横幅、遮天、批次/跳过；类型与 API 对齐外环设计 v1.1 |
| 2026-08-06 | **前端落地**：E1～E6 组件/路由/守卫/GM/环境变量；道友引渡与 WS 仍延后 |
| 2026-08-06 | **v1.2**：对齐 [`骰子系统设计.md`](./骰子系统设计.md)——突破/遮天/护主展示纪律；`attempt` 响应 `dice` 块；禁止客户端自掷成败 |
| 2026-08-06 | **新生选角**：`mode=newborn` + 商店；`reincarnating` 强引导 |
| 2026-08-07 | **v1.3 收口核对**：E1～E6 + 收尾标已完成；商店固定/随机/刷新 + 轮回袋分栏已落地；突破 dice UI 明确 → **M5-D12**；范围描述同步强化 UI |
| 2026-08-07 | **v1.4**：突破 dice UI 落地（D12 消化）；下一会话并行包指针 → [`后续待完成.md`](./后续待完成.md) **§0.1** |
| 2026-08-10 | **v1.5**：渡劫状态/探索面板用服务端 `*_label_zh` / 中文映射；离线方向中文；对齐 §0.0.2 |

---

*接口与领域以 `M5环境与轮回外环设计.md` 为准；检定骰以 `骰子系统设计.md` 为准；本文只固化前端目录、路由增量与页面落点，并跟踪实现进度。*

# 嘲讽光环设计（M3-D06）

> **文档性质**：自走棋默认 AI 的「强制目标」扩展真相源；消化 [`后续待完成.md`](./后续待完成.md) **M3-D06**，细则对齐 [`M3自动移动攻击算法设计.md`](./M3自动移动攻击算法设计.md) §6 / §7 / §8  
> **版本**：v1.1.2 · 2026-08-10  
> **实现状态**：**Phase A（M3-D06）已落地**；显性+封装收口；**Phase B = M3-D06b → 挂 M3-D03**  
> **关联**：[`开发计划.md`](./开发计划.md) **§0.0 / §0.0.1 / §0.7** · [`M3战斗成型设计.md`](./M3战斗成型设计.md) D9 · [`后台管理系统开发计划.md`](./后台管理系统开发计划.md) · GDD [`project修仙.md`](./project修仙.md)（战斗框架，无嘲讽专节）  
> **原则**：嘲讽是 **移动揭晓类强制**（与障/渊同属「进入格后短路」）；光环形状与来源 **全进配置 / 后台**；决策层短路 + 执行层 `STOP_TAUNT`；**不必等完整技能树（M3-D03）**。

---

## 0. 落地对照（Phase A）

| 设计项 | 代码落点 |
| --- | --- |
| 光环注册表 | `config_data/taunt_auras.yaml` + Bundle 域 `taunt_auras` |
| 单位挂载 | `pve_monsters.yaml`（`taunt_guardian`）· `MonsterUnitConfig.taunt_aura_id` |
| 掩码聚合 / 归属 | `domain/taunt_aura.py` |
| 运行态 | `BattleState.taunt_target` / `taunt_aura` / `taunt_aura_mask` / `aura_owner` |
| 决策短路 | `combat_ai.decide_action` 步骤 1 |
| 执行层 | `autochess._execute_move` → `stop_reason=taunt`；嘲讽者移动重建掩码 |
| 死亡解除 | `_resolve_attack_on_unit` → `clear_taunts_pointing_to` |
| 战报 | `taunt` / `ai_retarget(reason=taunt)`；`battle_text` 中文行 |
| 渲染 | 前端 `BattleEvent` 类型含 `taunt` / `ai_retarget`；播放器 `ONE_LINE_TYPES` 同步；选怪下拉展示嘲讽摘要 |
| ADM | 域 `taunt_auras`：entries+JSON；catalog/schema/probe |
| 样本 | PVE `taunt_guardian`（护关木傀 + 后排妖法） |
| 单测 | `tests/test_taunt_aura.py`（8 例） |
| 选怪 API | `AutochessService.list_pve_monsters_public` → `GET /battle/pve/monsters.taunt_auras` |
| 配置封装 | `TauntAuraDef.to_engine_snapshot` / `to_public_summary`；`TauntAurasConfig.resolve_snapshot` / `public_summaries_for_units` |
| Phase B 钩子 | `taunt_aura.clear_taunt`（时长/驱散；登记 **M3-D06b**） |

---

## 1. 定案与边界

### 1.1 一句话目标

单位可携带 **嘲讽光环**。敌方棋子 **移动进入** 光环覆盖格时，立即被强制以该嘲讽者为唯一目标（优先级高于自由估价选目标）；嘲讽者死亡（及后续驱散/到期）后解除。玩家在战报中能看见「被谁嘲讽、因嘲讽换目标」。

### 1.2 写死边界

| 做 | 不做 / 说明 |
| --- | --- |
| 静态 YAML 光环注册表 + 单位 `taunt_aura_id` 引用 | **不**阻塞于完整技能树（M3-D03）；技能施加光环 → Phase B |
| 按侧聚合 `taunt_aura_mask`；进入格 **一次位运算** 判定 | 禁止每步遍历全场技能问「你嘲讽吗」 |
| 决策层嘲讽短路；执行层 `STOP_TAUNT` 中断本段移动并重规划 | 嘲讽 **不**改地形表 / 不重建全源距离 |
| 战报 `taunt` + `ai_retarget(reason=taunt)` + `move.stop_reason=taunt` | 不落库；仍零保留 |
| 多嘲讽重叠时 **确定性** 归属（见 §5.3） | 禁止 `set` 迭代挑主人 |
| 嘲讽者死亡 → 清除所有指向它的 `taunt_target` 并重算掩码 | Phase A **不做**驱散道具；时长到期 → Phase B |
| 配置进 ADM 域，字段中文双写（§0.0.1） | 禁止 `if uid == "wolf_king"` 写死嘲讽 |

### 1.3 与现行落地的关系

| 已落地（保持） | 本文扩展 |
| --- | --- |
| AP=2；乐观探路；障/渊揭晓换目标 | 进入光环 → 强制目标换目标 |
| `decide_action` 自由估价 + 友军软墙让路 | 步骤 1 嘲讽短路；让路规则见 §6.3 |
| `_execute_move`：`arrived/budget/blocked_*/abyss_bounce` | 增 `taunt` 停止原因 |
| 事件全集已预留 `taunt` / `ai_retarget.reason=taunt` | 引擎真正发射；渲染补中文 |
| 陷阱掩码同款 O(1) 逐格检查（算法 §12.4） | 嘲讽光环同一走子循环顺带检查 |

### 1.4 配置与后台纪律（§0.0）

| 必须 | 禁止 |
| --- | --- |
| 域 **`taunt_auras`**：形状、半径、是否含自身格、时长策略、中文名 | 引擎硬编码「邻接四格就是嘲讽」而无表 |
| 怪物/傀儡/单位 setup 只写 **`taunt_aura_id`**（外键） | 单位行内嵌一整段形状逻辑副本且无法运营改 |
| 玩家服只读 `GameConfigBundle.taunt_auras` | 把 DEV `/gm` 当运营改光环入口 |
| 校验：未知 id 开战拒绝或启动硬校验（与 formations 同级） | 静默忽略坏引用导致「看起来没嘲讽」 |

**§0.0 自检**：运营要加「切比雪夫半径 2 的嘲讽」→ 只改 `taunt_auras` 发布 + 给某怪挂 id，玩家端无发版。

### 1.5 显性设计（§0.7）

玩家必须能感知：

1. **谁在嘲讽**：单位详情 / 图鉴 / PVE 怪说明可见「嘲讽光环：{label_zh}」与范围描述（来自配置，非前端写死）；  
2. **战报过程**：详细日志出现「被 X 嘲讽，强制目标改为 X」；简易摘要可折叠但事件仍在 `events[]`；  
3. **解除原因**（若发生）：嘲讽者阵亡 → 下一次自由选目标前可见 `ai_retarget` 或等价说明（Phase A 至少死亡解除可测）。

禁止：隐性强制目标却战报仍显示「因更近改打 B」。

---

## 2. 术语

| 术语 | 含义 |
| --- | --- |
| **嘲讽光环** `taunt_aura` | 注册表中的一条形状定义（id + shape + 参数） |
| **嘲讽者** | 存活且挂载了有效 `taunt_aura_id` 的单位 |
| **被嘲讽者** | `taunt_target[slot]` 指向某嘲讽者的单位 |
| **光环掩码** `taunt_aura_mask[side]` | 该侧所有存活嘲讽者覆盖格的按位或（影响 **对方**） |
| **强制目标** `taunt_target[slot]` | 被嘲讽者决策时的唯一候选敌人槽位；`-1` = 无 |
| **进入** | 移动执行层踏入 **新格** 的瞬间（含绕行/飞跃过程中的中间格） |
| **STOP_TAUNT** | 执行层中断码：因进入光环停止本段剩余步数 |

> 「光环」是 **逻辑覆盖区**，不是地形层；不写 `terrain`、不挡 LOS、不占部署格。

---

## 3. 分相交付

| 相 | 范围 | 状态目标 |
| --- | --- | --- |
| **Phase A（= M3-D06）** | 静态表 + 单位挂载；掩码；进入触发；死亡解除；决策短路；战报；单测；ADM 最小域；选怪显性 | **已落地（2026-08-10）** |
| **Phase B（= M3-D06b）** | 技能/被动开战或触发施加光环；`duration_rounds`；驱散；来源死亡摘修正栈 | **挂账 → [`后续待完成.md`](./后续待完成.md) M3-D06b / 与 M3-D03 同迭代** |
| **不做** | 嘲讽改伤害公式、嘲讽免疫种族写死、跨场持久嘲讽 | — |

---

## 4. 配置契约

### 4.1 注册表 `taunt_auras.yaml`

```yaml
# 用途：嘲讽光环形状与展示文案（热插拔；ADM 域 taunt_auras）
# 加载：开战组 setup 前由 Bundle 读取；引擎只吃已解析的 aura 快照

schema_version: 1

auras:
  ortho_guard:                    # id：单位 taunt_aura_id 引用
    label_zh: 护卫嘲讽
    help_zh: 正交四邻进入即强制打我
    summary: 邻接四格
    shape: ortho_adjacent         # 见 §4.2
    include_self_cell: false      # 嘲讽者脚下默认不进掩码（防自我触发无意义）
    duration_rounds: null         # Phase A：null = 直至死亡/显式清除
    # Phase B 预留：
    # apply_mode: passive_static | on_cast | on_battle_start
```

### 4.2 形状枚举（写死语义，参数进表）

| `shape` | 覆盖格 | 参数 |
| --- | --- | --- |
| **`ortho_adjacent`** | 嘲讽者正交四邻 | 无 |
| **`chebyshev`** | 切比雪夫距离 ≤ `radius` 的格（可含斜角） | `radius: int ≥ 1` |
| **`offsets`** | 相对偏移列表（进攻方视角；防守镜像由引擎对 x 处理） | `cells: [{dx, dy}, ...]` |

约束：

1. 覆盖格必须在棋盘 `0..48` 内；越界偏移丢弃（不报错，便于配表）。  
2. **不含** 友军/敌军过滤：掩码是几何集合；「谁被嘲讽」只看 **敌方单位进入**。  
3. 禁止第三种隐式「全图嘲讽」；若要大范围用大 `radius` 显式配置。

### 4.3 单位挂载

开战 `setup.units[]`（及配置源）增加可选字段：

```yaml
taunt_aura_id: ortho_guard   # 缺省 / null = 无光环
```

挂载点（Phase A 样本建议）：

| 来源 | 怎么挂 |
| --- | --- |
| `pve_monsters.yaml` → `units[]` | 直接写 `taunt_aura_id` |
| 试炼傀儡 / 制造业傀儡面板 | `unit_defaults` 或物品/配方派生字段（可后接）；最小可用 **GM 发带光环的测试傀儡** |
| 阵法 `effect` | **Phase A 不**用阵法层冒充光环（避免与四象效果乘区纠缠）；Phase B 再评 |

`AutochessService` 组 setup 时把 id **解析成快照**写入单位（形状参数拷贝进 setup），保证引擎零读 Bundle / 零 IO。

### 4.4 ADM 字段（§0.0.1）

域 **`taunt_auras`**：

| 字段 | `label_zh` | `help_zh` 摘要 |
| --- | --- | --- |
| `id` | 光环 ID | 单位引用键；稳定勿随意改名 |
| `label_zh` | 中文名 | 面板与战报展示 |
| `help_zh` | 运营说明 | 后台编辑提示 |
| `summary` | 玩家摘要 | 短描述范围 |
| `shape` | 形状 | `ortho_adjacent` / `chebyshev` / `offsets` |
| `radius` | 半径 | 仅 chebyshev |
| `cells` | 偏移表 | 仅 offsets；dx/dy |
| `include_self_cell` | 含自身格 | 默认否 |
| `duration_rounds` | 持续回合 | 空=直到死亡；Phase B 生效 |

双写：条目表 + JSON/YAML 覆盖；发布清缓存。

---

## 5. 运行时模型

### 5.1 BattleState 增补

```text
taunt_target: list[int]          # 每槽；-1 = 无强制目标；存的是 **slot** 非 uid
taunt_aura_id: list[str | None]  # 开战快照
# 按「嘲讽者所在侧」聚合：side s 的光环覆盖格，用于检测 **1-s** 侧单位进入
taunt_aura_mask: list[int]       # 长度 2；int 位掩码
# 可选加速：格 → 嘲讽者 slot（重建掩码时写入；冲突用 §5.3）
aura_owner: list[int]            # 长度 49；-1 = 无
```

纯度：仅存在于单次 `simulate_battle` 栈上。

### 5.2 掩码重建（增量时机）

调用 `rebuild_taunt_auras(state) -> None`：

1. 清零 `taunt_aura_mask[0/1]` 与 `aura_owner[*]`；  
2. 遍历存活且 `taunt_aura_id` 非空的槽位；  
3. 按形状生成覆盖格，写入 `taunt_aura_mask[side] |= bit`；  
4. 对每一覆盖格按 §5.3 与现有 `aura_owner[cell]` 比较，保留胜者。

**必须重建** 的时机：

| 时机 | 原因 |
| --- | --- |
| 开战组完单位后 | 初始光环 |
| 嘲讽者死亡 | 覆盖消失；清 `taunt_target` |
| 嘲讽者格子变化（若未来有击退/force_shift 后仍存活） | Phase A：`force_shifts` 在 `battle_start` 前完成 → 在移位 **之后** 建一次即可 |
| Phase B：施加/到期/驱散 | 光环集合变 |

**不**因普通移动重建（嘲讽者自己移动时 Phase A 约定：**光环随人** → 嘲讽者每步移动后须重建或增量更新其旧格/新格）。  
Phase A 定案：**嘲讽者移动后立即增量更新自己的覆盖**（从掩码去掉旧覆盖、加上新覆盖，并重算冲突格），避免「人走了光环还在原地」。

### 5.3 多光环重叠（确定性归属）

同一格被多个敌对嘲讽者覆盖时，归属键取 **最小**：

```text
key = (CHEBYSHEV(cell, taunter_cell), taunter_uid)
```

平局用 `uid` 字典序。禁止哈希序 / 遍历 `set`。

进入该格的单位：`taunt_target = aura_owner[cell]`（已是确定性结果）。

### 5.4 触发与解除

| 事件 | 行为 |
| --- | --- |
| **进入** 敌方光环覆盖格 | 设 `taunt_target[mover] = owner_slot`；发射 `taunt`；`move.stop_reason=taunt`；**立即停止本段剩余步**；本回合剩余 AP 按新目标重规划 |
| 已在光环内但本段未「新进入」 | **不**重复触发（避免原地抖动刷事件） |
| 开战时已站在光环内 | Phase A：**不**自动嘲讽（只认移动进入）；可用样本让怪先走近战 |
| 嘲讽者死亡 | 所有 `taunt_target[i] == dead_slot` → `-1`；重建掩码；不强制立即 `ai_retarget`（下个 AP 自然走自由选目标） |
| 被嘲讽者死亡 | 清自身 `taunt_target`（无其它副作用） |
| 时长到期 / 驱散 | Phase B；接口预留 `clear_taunt(state, slot, reason)` |

### 5.5 与障/渊揭晓的次序

同一「踏入新格」循环内建议顺序：

```text
1. 占位 / 障碍撞停（已有）
2. 深渊飞跃 / 弹回（已有）
3. 陷阱（若有，M3-D03）
4. 嘲讽光环检查 ← 本设计
```

若本步因深渊弹回，**不**在弹回格上再判嘲讽（人以弹回后格为准；若弹回格本身在光环内且相对上一步是「新进入」，则判一次——实现时用「最终落格 vs 进入前格」统一判定，避免双判）。

**推荐实现**：每成功变更 `cur` 到非弹回逻辑的新格后检查一次；弹回只检查弹回后的 `cur` 是否相对 `prev_solid` 构成进入光环。

---

## 6. AI 决策与执行

### 6.1 决策层（对齐算法 §6.1）

```text
decide_action(st, slot):
  1) 嘲讽短路
     forced = st.taunt_target[slot]
     若 forced >= 0 且 st.alive[forced] 且为敌方:
         候选敌人集合 = {forced}   # 只打它 / 只朝它走
     否则:
         清掉失效 taunt_target；候选 = 全部存活敌人

  2) （可选）友军让路 — 见 §6.3

  3) 快速路径：当前能打到候选？→ ACT_ATTACK_UNIT

  4) 对候选打 approach 分 → ACT_MOVE / ACT_ATTACK_OBSTACLE / ACT_IDLE
```

不变式：**嘲讽 > 自由选目标**。即使另一敌人 `approach` 更低，只要嘲讽有效，不得改打别人。

### 6.2 执行层

`_execute_move` 在踏入新格后：

```text
if (taunt_aura_mask[1 - side] >> cur) & 1:
    owner = aura_owner[cur]
    if owner >= 0 and alive[owner]:
        old = taunt_target[slot]
        taunt_target[slot] = owner
        emit taunt { taunter_uid, victim_uid, cell }
        if old != owner:
            emit ai_retarget { from_uid, to_uid, reason: "taunt" }
        stop_reason = "taunt"
        break  # 剩余 mp 丢弃；上层 AP 循环继续 decide
```

返回后 `_unit_turn` **不**额外扣 AP（移动行动已耗 1 AP）；若还有剩余 AP，下一轮 `decide_action` 必走嘲讽短路。

### 6.3 与友军让路（算法 v2.5）的关系

| 情况 | 行为 |
| --- | --- |
| 有效嘲讽 | **不得**为接近其他敌人而 `pick_yield_cell` |
| 有效嘲讽但打不到 / 走不到嘲讽者，且友军堵唯一走廊 | **允许**让路（协助接近强制目标） |
| 无嘲讽 | 保持现行让路逻辑 |

实现建议：让路函数增加 `goal_slot: int | None`；嘲讽时传入 `forced`，只评估「是否挡向 forced 的路」。

### 6.4 失效嘲讽者仍被指向

若 `taunt_target` 指向已死亡槽位：决策步骤 1 清除并回退自由选目标（可发射一次 `ai_retarget reason=taunt_cleared`——Phase A **可选**；验收不强制）。

---

## 7. 战报与渲染

### 7.1 事件契约（与算法 §11.4 对齐）

| type | 字段 | 说明 |
| --- | --- | --- |
| `taunt` | `taunter`（uid）, `victim`（uid）, `cell` `{x,y}` | 强制生效瞬间 |
| `ai_retarget` | `from_uid`, `to_uid`, `reason: "taunt"` | 目标变更；`from_uid` 可为 `null`（首次强制） |
| `move` | `stop_reason` 增枚举值 **`taunt`** | 与 `arrived/budget/...` 并列 |

### 7.2 `battle_text` 文案

```text
【嘲讽】妖狼王 强制 本体 以自己为目标（格 (5,3)）
【换目标】本体：因嘲讽 → 妖狼王
【移动】本体：…（进入嘲讽中断）
```

`reason_map` 增 `"taunt": "进入嘲讽中断"`。

### 7.3 前端

Phase A：播放器已消费 `events[]` 即可；补类型与文案映射。  
可选：详细档高亮「嘲讽中」状态（读最近 `taunt` 至死亡/清除）——不阻塞验收。

---

## 8. 样本内容（验收用）

建议至少 1 条可 HTTP/单测驱动的样本：

| 样本 | 配置要点 |
| --- | --- |
| PVE `taunt_guardian`（或扩 `wild_wolves`） | 前排近战单位 `taunt_aura_id: ortho_guard`；后排输出无光环 |
| 布阵 | 进攻方本体从远处走向护关者邻格 |
| 期望 | 进入邻格 → `taunt` 事件 → 后续 AP 只攻击护关者，即使后排 `approach` 更优 |

单测可不用 HTTP：直接 `simulate_battle(setup, seed)` 断言事件序。

---

## 9. 竖切任务（Phase A）

| 序 | 任务 | 验收 |
| --- | --- | --- |
| A1 | `taunt_auras.yaml` + Bundle/加载校验 + ADM 域骨架 | 未知 shape 启动失败；未知 id 开战失败 |
| A2 | `taunt_aura.py`：形状→掩码、重建、归属键 | 单测形状与冲突 |
| A3 | `BattleState` 字段；开战移位后建掩码 | 初始掩码正确 |
| A4 | `_execute_move` 进入检测 + 事件 | `stop_reason=taunt`；同 seed 稳定 |
| A5 | `decide_action` 短路 + 让路约束 | 强制目标优先于更近敌人 |
| A6 | 死亡清除 + 掩码重建 | 死后可改打他人 |
| A7 | `battle_text` + 前端事件类型 | 详细日志可见 |
| A8 | 样本怪 + `tests/test_taunt_aura.py` | 覆盖 §10 |
| A9 | 文档：本文 §0 回填；`后续待完成` M3-D06 → 已消化；CHANGELOG/README | 同步 |

---

## 10. 验收钩子（对齐 M3-D06）

| # | 钩子 | 通过标准 |
| --- | --- | --- |
| 1 | 移入光环强制目标 | 进入后 `taunt_target` 指向嘲讽者；后续攻击/移动目标仅为它 |
| 2 | 优先级 | 存在更近/更低 approach 的其他敌人时仍打嘲讽者 |
| 3 | 死亡解除 | 嘲讽者 `death` 后，存活单位可自由选目标（同 seed 可断言换目标） |
| 4 | 战报 | 存在 `taunt`；存在 `ai_retarget` 且 `reason=taunt`；对应 `move.stop_reason=taunt` |
| 5 | 确定性 | 同 `setup+seed` 事件序列一致；`PYTHONHASHSEED` 变化不影响 |
| 6 | 性能直觉 | 进入格判定为位运算；无全场技能扫描 |
| 7 | 配置纪律 | 新增光环只改 YAML/ADM，不改引擎分支 |

算法文档 §17.1 用例 5（嘲讽）随 Phase A 一并变绿。

---

## 11. 风险与非目标

| 项 | 说明 |
| --- | --- |
| 与 M3-D03 耦合 | Phase A 刻意静态挂载，避免等技能 CD/驱散 |
| 嘲讽者被击退出原位 | Phase A 靠「移动后更新掩码」；若仅 `force_shifts` 开战一次则已覆盖 |
| 远程在光环外开火 | 允许：未进入则不嘲讽（风筝成立）；应用 `chebyshev` 扩大范围配表解决 |
| 玩家手动选目标 | 现行自走棋无手动点目标；未来若有，须服从同一 `taunt_target` |
| 灵宠回合制 | **无关**（PET 对战零 board）；勿把本设计塞进 `PetDuelService` |

---

## 12. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-10 | **v1.1.2 封装+挂账**：配置/列表进 Service；`clear_taunt` Phase B 钩子；登记 **M3-D06b** → M3-D03 |
| 2026-08-10 | **v1.1.1 显性收口**：播放器同步 `taunt`/`ai_retarget`；选怪 API/面板展示嘲讽摘要 |
| 2026-08-10 | **v1.1 Phase A 落地**：引擎短路/进入触发/死亡解除/ADM/样本怪/单测 7 例；M3-D06 已消化 |
| 2026-08-10 | **v1.0** 定案：静态光环 Phase A（=M3-D06）；掩码/进入触发/死亡解除/决策短路/战报/ADM；技能时长驱散 → Phase B（M3-D03） |

---

*冲突时：地形与 AP 会计以 `M3战斗成型设计.md` 为准；默认 AI 决策以 `M3自动移动攻击算法设计.md` 为准；嘲讽专有契约以 **本文** 为准。*

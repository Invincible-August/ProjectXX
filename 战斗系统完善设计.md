# 战斗系统完善设计

| 项 | 内容 |
| --- | --- |
| **针对** | 棋盘战斗数值、抗性、异常与诅咒分类；后续完善整套战斗系统的专栏 |
| **设计日期** | 2026-08-18 |
| **版本** | v0.1（口子：分类与 schema 占位；公式未开） |
| **依据** | [`ATTR战斗属性占位设计.md`](./ATTR战斗属性占位设计.md)；[`符箓叠层与异常状态设计.md`](./符箓叠层与异常状态设计.md)；[`M3战斗成型设计.md`](./M3战斗成型设计.md)；[`后续待完成.md`](./后续待完成.md) **M3-D03** |
| **原则** | 先锁分类与机读键；**不**假装命中/抗性/上状态已进引擎；完整演算仍挂 **M3-D03** |

---

## 0. 本版开口到哪

已落地（仅占位，`formula_enabled: false`）：

- 元素抗七维并入角色面板「战斗属性」，不再单独折叠「元素抗性」。
- 新增 `resist_ailment`（异常抗性）、`resist_dark`（暗抗 / 诅咒抗性）。
- 异常状态与诅咒两套清单冻结（见 §1）。

明确不做（本版）：

- 抗性参与命中、伤害、上状态概率的真实公式。
- 溺水 / 麻痹的回合行动细则（常量已登记，引擎行为未写）。
- 新伤害类型表、元素克制、诅咒扩展到全部战斗键的逐条公式。

心魔抗 / 天劫抗仍在 **根基栏展示**（schema 仍属 LifeAttrBlock），不进棋盘抗性栏。

---

## 1. 抗性三族

面板「战斗属性」按此顺序展示抗性；单位均为抗性点（int，默认 0）。

| 族 | 机读键 | 中文 | 对抗什么 |
| --- | --- | --- | --- |
| 元素 | `resist_metal` / `wood` / `water` / `fire` / `earth` / `wind` / `thunder` | 金木水火土风雷抗 | 对应元素伤害（公式未开） |
| 异常 | `resist_ailment` | 异常抗性 | §1.1 异常状态的施加 / 持续（公式未开） |
| 诅咒 | `resist_dark` | 暗抗 | §1.2 诅咒（降低战斗属性）的施加 / 幅度（公式未开） |

`toughness`（ATTR §4.4 韧性）仍关；控制向抗性以 `resist_ailment` 为准，勿双开。

### 1.1 异常状态

吃 `resist_ailment`。现行 id 在 `app.constants.status`。

| 中文 | 机读 id | 本版回合行为 |
| --- | --- | --- |
| 中毒 | `poison` | 已有：回合伤害（`STATUS_DOT`） |
| 灼烧 | `burn` | 已有：回合伤害 |
| 溺水 | `drown` | **仅登记**；回合细则后续专栏补 |
| 麻痹 | `paralyze` | **仅登记**；回合细则后续专栏补 |
| 睡眠 | `sleep` | 已有：无法行动，被打会醒 |
| 魅惑 | `charm` | 已有：打友方，被打会醒 |
| 眩晕 | `stun` | 已有：每回合仅 1 次行动 |
| 昏迷 | `coma` | 已有：无法行动，被打不醒 |

回合序纯函数仍见 [`符箓叠层与异常状态设计.md`](./符箓叠层与异常状态设计.md) §2；引擎接入 **M3-D03**。

### 1.2 诅咒（暗）

吃 `resist_dark`。定义：**降低战斗属性** 的效果（物/法攻防、速度、命中、闪避等），不是异常状态。

| 中文 | 机读 id | 本版 |
| --- | --- | --- |
| 降低攻击 | `atk_down` | 已有：直接改属性（`STATUS_CURSE`） |
| 降低防御 | `def_down` | 已有：直接改属性 |
| 其它降战斗属性 | （后续按键扩展，如速度/命中） | 未开口子以外的新 id |

减攻/减防 **不算** 异常，不吃 `resist_ailment`。

---

## 2. 与 ATTR / 面板

- 注册表：`combat_attrs.yaml`；`COMBAT_FINAL_KEYS` 含七维元素抗 + `resist_ailment` + `resist_dark`。
- 角色折叠栏只保留「战斗属性」；抗性行跟在命中/闪避之后。
- 生活栏的 `resist_heart_demon` / `resist_tribulation` **不**并入本栏。

---

## 3. 后续完善（本专栏待填，勿在别处另起一套）

按依赖从浅到深，实现时再拆验收钩子，登记见 [`后续待完成.md`](./后续待完成.md) **BATTLE-SYS-D01**：

1. 元素伤害类型与七维抗换算（伤害公式开时）。
2. 上异常 / 上诅咒的检定：成功率 vs `resist_ailment` / `resist_dark`。
3. 溺水、麻痹回合行动与被打规则。
4. 诅咒扩展到更多战斗键（仍走暗抗，不新开抗性族）。
5. 功法/符箓/道具施加链接到引擎（与 **M3-D03** 同迭代）。
6. 战报中文：抗性减免、异常、诅咒分条。

本专栏是战斗完善的权威分类；ATTR 只管键与叠层，M3 成型文档只管现行普攻闭环。

---

## 4. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-18 | **v0.1**：开口子——抗性三族、异常八项、诅咒=降战斗属性；公式关闭 |

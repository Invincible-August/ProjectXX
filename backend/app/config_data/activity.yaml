# =====================================================================
# 用途：活动/限时覆盖开关占位（ADM-10 最小）
# 加载时机：活动设施预览；完整多环境通道后置
# =====================================================================

# 活动条目：键=activity_id
activities:
  # —— 字段说明 ——
  # enabled     是否生效
  # title       展示名
  # note        玩家可见说明
  # overrides   可选：指向其它域的提示性覆盖说明（真覆盖仍走各域 publish）
  placeholder_double_idle:
    enabled: false
    title: 挂机双倍（占位）
    note: 未开放；开放后由运营后台开启
    overrides: {}

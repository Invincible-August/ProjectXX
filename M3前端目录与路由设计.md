# M3 前端目录与路由设计

| 项 | 内容 |
| --- | --- |
| **针对里程碑** | **M3（战斗成型）** |
| **设计日期** | 2026-08-04 |
| **依据** | `M3战斗成型设计.md`；`M2前端目录与路由设计.md`；`M1前端目录与路由设计.md`；`M0前端目录与路由设计.md`；`开发计划.md` §5 |
| **原则** | **首次新增玩法路由**（消化 M1-D31）；`/hall` 变为入口枢纽；布阵 / 战斗独立页；目录骨架继承 M0～M2 |

---

## 1. 设计目标

约定 M3 在 `frontend/src` 下的文件增量、**新路由表**、大厅枢纽信息架构，以及与后端布阵 / 阵法 / 快照 / 自走棋开战 / 战报接口的对接落点。

M3 可玩范围：布阵编辑器保存 ≥3 套预设、选阵法开战 PVE、战报逐步/跳过回放（**仅本会话**：战报只在开战响应中，浏览器 `sessionStorage` 保存，登出/关闭即销毁——成型设计 §7.8）、更新防守快照、攻打他人快照、体力条与不足提示。  
范围外：化身/灵宠页、工房、道主大厅、真 WS；以及 M3 延后项 **M3-D01～D10**（见 [`后续待完成.md`](./后续待完成.md)）。本文只描述已落地路由与组件。

---

## 2. 路由策略（相对 M0～M2：**新增玩法 path**）

| path | name | 文件 | M3 变化 |
| --- | --- | --- | --- |
| `/login` | `login` | `views/LoginView.vue` | 无 |
| `/register` | `register` | `views/RegisterView.vue` | 无 |
| `/create-character` | `create-character` | `views/CreateCharacterView.vue` | 无 |
| `/hall` | `hall` | `views/HallView.vue` | **枢纽**：养成保留；战斗区改为入口卡片 |
| `/formation` | `formation` | `views/FormationView.vue` | **新**：布阵编辑器 + 预设 + 阵法 + 快照更新 |
| `/battle` | `battle` | `views/BattleView.vue` | **新**：PVE/PVP 入口 + 体力条 + 本会话战报列表 + 播放器 |
| `/` | — | 重定向 | 逻辑不变（有角色仍优先去大厅） |
| `/test` | `test` | `test/apps.vue` | 可选；非验收 |

### 2.1 守卫逻辑（继承并扩展）

| 规则 | 说明 |
| --- | --- |
| 需登录 | `/hall` `/formation` `/battle` `/create-character` 均 `requiresAuth` |
| 无角色 | 访问上述玩法页 → `/create-character`（与大厅相同） |
| 有角色访创角 | → `/hall` |
| 已登录访登录/注册 | → `homePathAfterAuth()`；若 `redirect` 安全则尊重 |
| `redirect` 白名单 | `resolveSafeRedirect` **追加**允许 `/formation`、`/battle`（及带 query） |

```text
App.vue
  └── <RouterView />
        ├── /login | /register | /create-character
        ├── /hall → HallView.vue
        │     ├── CharacterPanel / Idle / Allocate / Constitution / Breakthrough（M2 保留）
        │     ├── HallBattleGate.vue（新：跳转布阵/战斗/快照冷却摘要）
        │     ├── BattlePanel.vue（降级为「快速教学战」或移除，见 §4.2）
        │     └── GameLogPanel.vue
        ├── /formation → FormationView.vue
        │     ├── FormationBoard.vue      （7×7；默认可部署 3×3 高亮）
        │     ├── FormationPresetBar.vue  （≥3 槽切换）
        │     ├── FormationPicker.vue     （阵法下拉）
        │     ├── UnitBench.vue          （可上阵棋子：本体 + 测试傀儡）
        │     └── SnapshotUpdateBar.vue  （手动更新 + 冷却）
        └── /battle → BattleView.vue
              ├── PveBattlePanel.vue
              ├── PvpAttackPanel.vue
              ├── BattleReportList.vue
              └── BattleReportPlayer.vue （逐步 / 跳过）
```

### 2.2 为何现在拆路由

| 理由 | 说明 |
| --- | --- |
| 对齐 M1-D31 | 布阵编辑器体量超出大厅折叠区；独立页利于联调与验收 |
| 信息架构 | 养成（大厅）与战前决策（布阵）/战中表现（战斗）分离 |
| 守卫复用 | 仍走同一套 JWT + `hasCharacter`；不新造鉴权 |

**不做**：`/idle` `/constitution` 等养成拆路由（M2 已稳定在大厅）；M3 只拆战斗相关。

### 2.3 可选 query（非新 path）

| 用法 | 说明 |
| --- | --- |
| `/formation?slot=1` | 打开指定预设槽 |
| `/battle?report=<session_key>` | 进页后自动打开 **本会话内** 该战报播放（key 为前端自生成，非服务端 id） |
| `/battle?mode=pvp` | 默认展开 PVP 区 |

---

## 3. `src` 目录增量

M0～M2 目录职责不变；M3 **追加文件与路由**，不推翻骨架。

```text
src/
├── api/
│   ├── formation.ts            # 新：presets / validate / board-meta
│   ├── snapshot.ts             # 新：defense me/update/preview
│   ├── battle.ts               # 扩展：pve 新契约、pvp、monsters、stamina（无 reports 端点）
│   ├── idle.ts / allocate.ts / …  # 复用
│   └── gm.ts                   # 扩展快照冷却等
├── components/
│   ├── formation/
│   │   ├── FormationBoard.vue
│   │   ├── FormationCell.vue       # 可选拆分
│   │   ├── FormationPresetBar.vue
│   │   ├── FormationPicker.vue
│   │   ├── UnitBench.vue
│   │   └── SnapshotUpdateBar.vue
│   ├── battle/
│   │   ├── PveBattlePanel.vue
│   │   ├── PvpAttackPanel.vue
│   │   ├── StaminaBar.vue          # 新：体力条 + 恢复倒计时 + 不足禁用开战
│   │   ├── BattleReportList.vue    # 本会话战报列表（sessionStorage，登出/关闭销毁）
│   │   ├── BattleReportPlayer.vue  # 数据源 = 开战响应（board_text + events + log_lines）
│   │   └── BattleBoardReplay.vue   # 可选：按事件在 7×7 上重放占位
│   ├── HallBattleGate.vue          # 大厅入口
│   ├── BattlePanel.vue             # 保留或改为薄入口（见 §4.2）
│   └── …（M2 面板保留）
├── stores/
│   ├── formation.ts            # 新：当前编辑草稿、presets、boardMeta
│   ├── battle.ts               # 新：本会话战报列表（挂 sessionStorage）、播放游标、体力状态
│   ├── character.ts            # 扩展快照摘要字段 apply
│   └── …
├── types/
│   ├── formation.ts
│   ├── snapshot.ts
│   ├── autochess.ts            # events / report payload
│   └── battle.ts               # 更新：弃用旧 rounds 类型或标 deprecated
├── utils/
│   ├── boardCoords.ts          # 分区判断、格 key
│   └── safeRedirect.ts         # 白名单扩展
├── router/
│   └── index.ts                # 登记 /formation /battle；守卫扩展
└── views/
    ├── HallView.vue            # 枢纽编排
    ├── FormationView.vue       # 新
    └── BattleView.vue          # 新
```

### 3.1 命名约定（继承）

- 页面：`XxxView.vue`（M3 **新增** Formation / Battle）
- 零件：`XxxPanel.vue` / `XxxBar.vue` / `XxxPlayer.vue`；战斗相关可放子目录 `components/formation|battle/`
- API 文件与后端模块同名
- 路由 path：小写短横线

### 3.2 子目录说明

M0 曾约定「views 不按功能建子目录」。M3 **components** 允许 `formation/`、`battle/` 分子夹，避免大厅根目录文件爆炸；`views/` 仍扁平（仅多 2 个整页）。

---

## 4. 信息架构

### 4.1 大厅（枢纽）布局

```text
┌──────────────────────────────────────────────────────────────┐
│ AuthSessionBar · 修仙大厅 · M3 枢纽                            │
│ [布阵] [战斗]  ← 顶栏或 HallBattleGate 主按钮                  │
├────────────────────────────┬─────────────────────────────────┤
│ CharacterPanel（M2）       │ GameLogPanel                    │
│ Idle / Allocate / …        │  · 养成事件                     │
│ Breakthrough               │  · 战斗摘要（开战回厅可 push）   │
│ HallBattleGate             │                                 │
│  · 快照冷却剩余             │                                 │
│  · 最近一战入口             │                                 │
└────────────────────────────┴─────────────────────────────────┘
```

移动端：顶栏两按钮固定可点；养成区仍单列折叠。

### 4.2 `BattlePanel` 处置

| 方案 | 说明 | 建议 |
| --- | --- | --- |
| A | 删除，只留 `HallBattleGate` | 干净 |
| B | 保留「一键教学 PVE」（无布阵，打 tutorial） | 利于回归 M1 闭环 |

**默认采用 B**：大厅可一键打教学怪（本体默认锚点 `(0,3)` 空盘开战）；完整布阵/PVP/历史在 `/battle`。

### 4.3 布阵页布局

```text
┌────────────────────────────────────────────────────────────┐
│ ← 回大厅 · 布阵 · 预设槽[0][1][2] · 阵法▼ · [保存] [更新快照] │
├──────────────┬─────────────────────────────────────────────┤
│ UnitBench    │  FormationBoard 7×7（左下=(0,0)；x 向右 y 向上）│
│  · 本体      │   默认高亮己方 6 格 (0,2)–(1,4)；中立列 x=3 禁落 │
│  · 试炼傀儡  │   敌对半区 x=4–6 只读预览（防守镜像）           │
│  · 化身/宠灰 │                                              │
├──────────────┴─────────────────────────────────────────────┤
│ SnapshotUpdateBar：上次更新 / 冷却倒计时 / 更新防守快照      │
└────────────────────────────────────────────────────────────┘
```

> 棋盘语义以成型设计 **v1.2 §3** 为准（DND+自走棋；非旧「上下 3×3」）。

### 4.4 战斗页布局

```text
┌────────────────────────────────────────────────────────────┐
│ ← 回大厅 · 战斗 · [去布阵]                                   │
├─────────────────────────────┬──────────────────────────────┤
│ PveBattlePanel              │ BattleReportList             │
│  · 选怪 · 选进攻预设 · 开战  │  · 最近 N 场 · 点开播放       │
│ PvpAttackPanel              │                              │
│  · 目标 id/道号 · 预览快照   │                              │
│  · 开战                     │                              │
├─────────────────────────────┴──────────────────────────────┤
│ BattleReportPlayer（有 report 时展开或 Dialog）             │
│  · 逐步 / 自动播放 / 跳过到底 · 可选棋盘回放                 │
└────────────────────────────────────────────────────────────┘
```

### 4.5 组件职责

| 组件 | 职责 | 不负责 |
| --- | --- | --- |
| `FormationBoard` | 渲染 7×7、高亮合法格、落子/移除交互 | 算伤害 |
| `FormationPresetBar` | 切槽、展示 role/name | 偷偷改快照 |
| `FormationPicker` | 选阵法；切换后请求 meta 刷新高亮 | 发明法外格子 |
| `SnapshotUpdateBar` | 调 update；展示冷却 | 改对方快照 |
| `BattleReportPlayer` | 按 `events[]` 播放；跳过 | 本地重算胜负 |
| `PvpAttackPanel` | 预览对方快照摘要；开战 | 修改防守方数据 |
| `HallBattleGate` | 路由跳转 + 冷却只读 | 大段布阵 UI |
| `FormationView` | 加载 presets、脏检查保存、离开确认 | 挂机预测 |
| `BattleView` | 编排开战 → 写入 store → 打开播放器 | settle 公式 |

### 4.6 关键时序

**保存布阵**

```text
编辑草稿 → PUT /formation/presets/{slot}
  → 成功：apply store；toast
  → 40041/42/43/44：ElMessage + 高亮冲突格（若有）
```

**更新快照**

```text
确认「将当前防守预设冻结为快照」→ POST /snapshot/defense/update
  → 成功：刷新冷却；GameLog（若回厅）
  → 40045：展示剩余秒
  → 40046：状态禁止文案
```

**开战 PVE**

```text
选怪 + 选预设（+ report_mode）→ POST /battle/pve
  → PlayGate 已在服务端处理 pending + 体力扣减
  → 40049：体力不足，显示恢复倒计时（next_point_in_seconds）
  → 返回完整战报（board_text + summary + events + log_lines? + stamina + character）
  → applyCharacter；存入 useBattleStore.sessionReports（sessionStorage）；打开 Player
```

**播放战报（无服务端拉取——响应即战报）**

```text
从 useBattleStore.sessionReports 取本会话战报（服务器不保留，无 GET 接口）
  → 先显示 board_text 开局棋盘（<pre> 等宽字体渲染）
  → Player 从 seq=0 起；VITE_BATTLE_PLAYBACK_MS 自动步进
  → 「跳过」直接到 battle_end；detailed 档另附逐回合文字日志
```

---

## 5. API / Store 对应

| 文件 | 函数（示意） |
| --- | --- |
| `api/formation.ts` | `fetchPresets` · `savePreset(slot, body)` · `fetchBoardMeta` · `validateFormation` |
| `api/snapshot.ts` | `fetchMySnapshot` · `updateDefenseSnapshot` · `previewSnapshot(characterId)` |
| `api/battle.ts` | `startPve` · `startPvp` · `fetchMonsters` · `fetchStamina`（**无** `fetchReports`——战报零保留） |

**`useFormationStore`**

| state / action | 说明 |
| --- | --- |
| `presets` / `activeSlot` | 服务端权威列表 + 当前编辑槽 |
| `draft` | 未保存 units / formation_id |
| `boardMeta` | zone + 当前阵法 extra_cells |
| `isDirty` | draft 相对已保存是否变化 |
| `load` / `selectSlot` / `place` / `remove` / `save` | |

**`useBattleStore`**

| state / action | 说明 |
| --- | --- |
| `lastReport` | 完整 payload（开战响应） |
| `sessionReports` | **本会话** 战报列表；同步到 `sessionStorage`（登出时显式清除） |
| `stamina` | `{ left, cap, next_point_in_seconds }`；开战响应/`fetchStamina` 更新 |
| `playbackIndex` | 当前事件游标 |
| `startPve` / `startPvp` / `play` / `skip` / `clearOnLogout` | 无 `loadHistory`（服务器无历史） |

**`useCharacterStore`**：`applyCharacter` 识别快照冷却字段；不把战报塞进 character。

---

## 6. 交互与文案约定

### 6.1 布阵

- 先点 Bench 棋子再点合法格；再点已占据格可「换下」或移到新格（实现二选一，文档默认：**点已方棋子选中 → 点空合法格移动**）。  
- 非法格：`cursor: not-allowed` + toast `40041`。  
- 离开页若 `isDirty`：`window` beforeunload 或 Vue 路由确认「放弃未保存？」。  
- 保存成功：「预设已保存（未自动更新防守快照）」——避免玩家误以为 PVP 已刷新。  

### 6.2 阵法

- 切换阵法立即刷新高亮；若已落子落在新区外，保存时由服务端拒绝并提示挪子。  
- 未解锁阵法禁用。  

### 6.3 快照

- 主按钮「更新防守快照」；冷却中显示 `mm:ss`。  
- 文案强调：快照不含挂机进度；更新读取 **防守预设**。  

### 6.4 战斗与战报

- 胜负结果用中文：「进攻方胜 / 防守方胜」。  
- 播放器默认自动播放；提供「跳过动画」。开局先展示 `board_text` 棋盘字符画（`<pre>` 等宽渲染）。  
- **明示留存策略**：战报列表页固定提示「战报仅本次登录有效，退出或关闭浏览器后将清除」。  
- **体力**：`StaminaBar` 常驻 `/battle` 与大厅入口卡片；不足时开战按钮禁用 + 「体力不足，X 秒后恢复 1 点」；`40049` 由拦截器特判同款文案。  
- 简易/详细档切换：开战面板一个开关（默认简易）；详细档展示逐回合 DND 式文字日志（折叠面板）。  
- PVP 目标输入：优先 `character_id`；可选道号搜索若后端提供（无则 M3 只做 id + GM 查 id）。  
- 旧 `rounds` 字段：类型标 `@deprecated`；UI 不再渲染。  

### 6.5 UI 基调

- 继承 M0～M2：功能优先；Element Plus；不为 M3 做终局战场美术。  
- 棋盘用 CSS Grid 即可；棋子可用色块 + 简称。  
- 至少保留简单过渡（如落子高亮、播放器行滚动），不做粒子特效。  

### 6.6 GM（DEV）

- 重置快照冷却、强制刷新快照、发放试炼傀儡。  
- 仅 `import.meta.env.DEV`。  

---

## 7. 类型对齐（要点）

`types/autochess.ts`（示意）：

```ts
export type BoardCoord = { x: number; y: number }


export type BattleEventType =
  | 'battle_start'
  | 'shift_noop'
  | 'shift'
  | 'attack'
  | 'death'
  | 'battle_end'

export interface BattleReport {
  schema_version: number
  seed: number
  mode: 'pve' | 'pvp_snapshot'
  result: string
  events: BattleEvent[]
  // …
}
```

`types/formation.ts`：`FormationPreset`、`FormationUnit`、`BoardMeta`。  
`types/battle.ts`：开战响应即完整战报（`board_text` / `summary` / `events` / `log_lines?` / `stamina`），**无 `report_id`**；旧 `rounds` 可选兼容字段勿再依赖。

统一错误：拦截器处理 `code !== 0`；`40045`/`40046` 在 Snapshot 条特判文案。

---

## 8. 与后续里程碑

| 里程碑 | 对本设计的影响 |
| --- | --- |
| **M3** | 本文范围 |
| **M4** | `UnitBench` 解锁化身/灵宠/真傀儡；快照 payload 扩编成 |
| **M5** | 渡劫禁更新/禁战 UI 真态；天气图标开战锁定 |
| **M6** | 道主挑战另开路由或 WS 房；不塞进 `/battle` 日常页 |
| 延后项 | 一律先查 [`后续待完成.md`](./后续待完成.md) |

---

## 9. 开发进度与步骤（对照 M3）

> 本节随实现同步更新；接口以 `M3战斗成型设计.md` 为准。

### 9.1 总览（对齐成型设计 §11 六步）

> 接口与竖切纪律以 [`M3战斗成型设计.md`](./M3战斗成型设计.md) **§11 / §12** 为准。  
> **S3 战斗全流程**为前后端共同重点板块，前端不得在 S3 未验收前铺开阵法/快照/PVP UI。

| 步 | 内容 | 状态 | 说明 |
| --- | --- | --- | --- |
| 0 | 设计文档（本文 + 成型设计含六步/战斗流程） | **已完成** | 2026-08-04 |
| **S1** | 棋盘 meta 类型 +（可随后端）格坐标工具 | **已完成** | 2026-08-05：`types/formation.ts` + `api/formation.ts`（board-meta） |
| **S2** | 路由 `/formation` `/battle` + FormationBoard + 预设联调 + 大厅枢纽 | **已完成** | 2026-08-05：`FormationView` + `FormationBoard/PresetBar/Picker` + `HallBattleGate` |
| **S3★** | 开战联调：先攻/移动/命中伤害摘要；对齐成型 §7.1～7.3 / §12 | **已完成** | 2026-08-05：`PveBattlePanel` + `useBattleStore.startPve`；`BattlePanel` 降级一键教学战 |
| **S4** | BattleReportPlayer + 本会话列表（sessionStorage）+ StaminaBar | **已完成** | 2026-08-05：播放器（图形化 7×7 棋盘随 `events` 回放 / 简易详细档 / 自动·单步·跳过）+ `BattleReportList` + `StaminaBar` |
| **S5** | UnitBench / 超员与未开放灰置 | **已完成** | 2026-08-05：`UnitBench`（上限提示 / 未启用种类灰置） |
| **S6** | 阵法高亮 + 快照冷却条 + PVP 入口 | **已完成** | 2026-08-05：地形/合法格高亮 + `SnapshotUpdateBar`（冷却倒计时 `40045/40046`）+ `PvpAttackPanel`（预览 `40047/40048`） |
| 收尾 | GameLog / GM / 总验收 | **已完成** | 2026-08-05：GM 类型同步 M3 字段；`vue-tsc -b && vite build` 通过 |

### 9.2 本地联调口令（实现后）

```powershell
# 后端
cd backend
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000

# 前端
cd frontend
npm run dev
```

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/formation/presets` | GET | 预设列表 |
| `/api/v1/formation/presets/{slot}` | PUT | 保存 |
| `/api/v1/formation/board-meta` | GET | 棋盘/区/阵法 meta |
| `/api/v1/snapshot/defense/update` | POST | 手动更新快照 |
| `/api/v1/battle/pve` | POST | 棋盘 PVE（响应即完整战报） |
| `/api/v1/battle/pvp/attack` | POST | 攻打快照（响应即完整战报） |
| `/api/v1/battle/stamina` | GET | 体力读数（惰性恢复后） |

### 9.3 下一步建议

1. 严格按成型设计 **S1→S6** 竖切；**S3 战斗全流程**单独排期与验收（§12）。  
2. S3 未打勾前，不要做 S6 的快照/PVP，也不要做精美战报动画（S4 再补）。  
3. 实现中保持 M2 大厅养成可用；战斗入口渐进替换。  
4. 数值/阵法只改 YAML；勿提前做化身页 / WS。  
5. 后端分层：新 Service + `domain/board|autochess`；配置热更仍见 **M2-D01**。  

---

## 10. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-04 | 初稿：新增 `/formation` `/battle`；大厅枢纽；components 子目录；对接 M3 战报/快照/阵法 |
| 2026-08-04 | 进度表对齐成型设计 v1.1：**S1～S6 六步**；S3 战斗全流程标为重点 |
| 2026-08-05 | 对齐成型设计 **v1.2**：`(x,y)` 左下锚点、默认 6 格高亮、中立列禁落；类型 `BoardCoord` 改 x/y |
| 2026-08-05 | 对齐成型设计 **v1.4.4**：战报零保留——移除 `reports` 端点/`report_id`，响应即战报，`sessionReports` 存 `sessionStorage`（登出/关闭销毁，UI 明示）；新增 `StaminaBar`（体力条/倒计时/`40049`）；开局 `board_text` 棋盘字符画 `<pre>` 渲染；简易/详细档开关 |
| 2026-08-05 | **前端实现完成**：§9.1 进度表 S1～S6 + 收尾全部标为已完成；新增路由/组件/双 store 落地，`vue-tsc -b && vite build` 通过 |
| 2026-08-05 | **战斗界面优化**：新增通用 `components/board/BoardGrid.vue`（7×7 固定格子层 + 棋子绝对定位加载层 + 移动/出入场动画 + 血条，深色战棋主题，本方在下敌方在上）；`BattleReportPlayer` 弃用 `board_text` 字符画，改为 `events` 重放驱动图形化棋盘（事件游标与日志行严格同步），详细档日志框高度随视口自适应（`calc(100vh - 340px)`）且自动滚底；`FormationBoard` 改基于 `BoardGrid`（接口不变）；后端 CORS 补 `PUT`（修复浏览器保存预设预检 400） |

---

*接口与引擎以 `M3战斗成型设计.md` 为准；本文只固化前端目录、路由增量与页面落点，并跟踪实现进度。*

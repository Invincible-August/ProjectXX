# M1 前端目录与路由设计

| 项 | 内容 |
| --- | --- |
| **针对里程碑** | **M1（核心循环 MVP）** |
| **设计日期** | 2026-08-03 |
| **依据** | `M1核心循环设计.md`；`M0前端目录与路由设计.md`；`开发计划.md` §3 |
| **原则** | **不新增玩法路由**；在 `/hall` 内升级面板与弹窗；目录骨架继承 M0 |

---

## 1. 设计目标

约定 M1 在 `frontend/src` 下的文件增量、大厅信息架构，以及与后端 `/idle`、`/breakthrough`、`/battle` 的对接落点。

M1 可玩范围：大厅内完成修灵挂机、突破、教学 PVE。  
范围外：独立玩法页、棋盘 UI、离线领取弹窗、布阵编辑器（见 [`后续待完成.md`](./后续待完成.md)）。

---

## 2. 路由策略（相对 M0 无新增）

| path | name | 文件 | M1 变化 |
| --- | --- | --- | --- |
| `/login` | `login` | `views/LoginView.vue` | 无 |
| `/register` | `register` | `views/RegisterView.vue` | 无（若仍合并进 LoginView 则保持现状） |
| `/create-character` | `create-character` | `views/CreateCharacterView.vue` | 无 |
| `/hall` | `hall` | `views/HallView.vue` | **主战场**：挂机/突破/战斗 |
| `/` | — | 重定向 | 逻辑不变 |

守卫逻辑不变：需登录；无角色→创角；有角色→大厅。

```text
App.vue
  └── <RouterView />
        └── /hall → HallView.vue
              ├── CharacterPanel.vue      （增强：修为进度、停滞、攻防展示）
              ├── IdlePanel.vue           （替换 IdlePanelPlaceholder）
              ├── BreakthroughPanel.vue   （按钮 + 预览；结果用对话框）
              ├── BattlePanel.vue         （开战 + 战报列表/对话框）
              └── GameLogPanel.vue        （追加挂机/突破/战斗事件）
```

---

## 3. `src` 目录增量

M0 目录职责不变；M1 **追加文件**，不推翻骨架。

```text
src/
├── api/
│   ├── idle.ts              # direction / sync
│   ├── breakthrough.ts      # preview / attempt
│   ├── battle.ts            # pve
│   └── gm.ts                # 仅开发联调；UI 入口可隐藏或加 dev 标记
├── components/
│   ├── IdlePanel.vue        # 真挂机（已替换 Placeholder；占位组件已删除）
│   ├── BreakthroughPanel.vue
│   ├── BreakthroughResultDialog.vue   # 可选拆分
│   ├── BattlePanel.vue
│   ├── BattleReportDialog.vue         # 可选拆分
│   ├── CharacterPanel.vue   # 增强
│   └── GameLogPanel.vue     # 复用
├── stores/
│   ├── character.ts         # 扩展：sync/poll、写入扩展字段
│   ├── idle.ts              # 可选；若逻辑少可并入 character
│   └── battle.ts            # 可选：最近一场战报
├── types/
│   ├── character.ts         # CharacterPublic 扩展字段
│   ├── idle.ts
│   ├── breakthrough.ts
│   └── battle.ts
└── views/
    └── HallView.vue         # 组装 startIdleRealtime、布局、把事件写入 GameLog
```

### 3.1 命名约定（继承 M0）

- 页面：`XxxView.vue`（M1 不新增）
- 零件：`XxxPanel.vue` / `XxxDialog.vue`
- API 文件与后端模块同名

---

## 4. 大厅信息架构

### 4.1 布局（在 M0 左右分栏上扩展）

```text
┌─────────────────────────────────────────────────────────┐
│ AuthSessionBar                                          │
│ 修仙大厅 · M1 核心循环                                  │
├──────────────────────────┬──────────────────────────────┤
│ CharacterPanel           │ GameLogPanel                 │
│  · 道号 / 境界 / 状态    │  · 欢迎语                    │
│  · 修为进度条            │  · 挂机结算摘要              │
│  · 灵石 / 停滞提示       │  · 突破结果                  │
│  · 攻防（base_atk/hp）   │  · 战斗结果摘要              │
├──────────────────────────┤                              │
│ IdlePanel                │                              │
│  · 当前方向              │                              │
│  · 预估速度 / tick       │                              │
│  · 开始修灵 / 停止       │                              │
│  · 同步按钮（可选）      │                              │
├──────────────────────────┤                              │
│ BreakthroughPanel        │                              │
│  · 预览差修为/成功率     │                              │
│  · 发起突破              │                              │
├──────────────────────────┤                              │
│ BattlePanel              │                              │
│  · 教学怪开战            │                              │
│  · 最近结果入口          │                              │
└──────────────────────────┴──────────────────────────────┘
```

移动端：单列自上而下 Character → Idle → Breakthrough → Battle → Log；不另做路由。

### 4.2 组件职责

| 组件 | 职责 | 不负责 |
| --- | --- | --- |
| `IdlePanel` | 展示方向、速率、停滞、片内进度；调用 `direction`；展示用 store.display | 自己当权威改修为 |
| `BreakthroughPanel` | 拉 preview；点 attempt；展示假读条 | 掷骰；跟预测刷预览 |
| `BattlePanel` | 选怪（M1 可写死教学怪）；展示战报 | 扣血计算 |
| `CharacterPanel` | 绑定权威字段 + display 进度条 | 直接改本地修为当权威 |
| `GameLogPanel` | 展示 `HallView` 推入的日志 | 调玩法 API |
| `HallView` | 生命周期 `startIdleRealtime`、编排日志文案、dev 下可选 GM 条 | 大段业务公式 |

### 4.3 修炼实时（预测 + tick 对齐 · 现行）

> 对齐 `M1核心循环设计.md` §4.0 / §4.5。**禁止**再实现为固定每 5s 轮询。

| 项 | 约定 |
| --- | --- |
| 展示 | `characterStore.display`（`idlePredict`）；片内条 + 总修为预测 |
| 权威入账 | 到点 `POST /idle/sync`；响应含 `next_tick_at` |
| 主排程 | 按 `next_tick_at` / `last_settled_at + tick` 对齐；0～2s jitter |
| 兜底对表 | `VITE_IDLE_POLL_MS` 默认 **120000**（最大间隔，非主路径） |
| 预测刷新 | `VITE_IDLE_PREDICT_MS` 默认 **250**（仅本地，不增 HTTP） |
| 条件 | 仅 `hall` + `visibilityState=visible` + 修灵中且未停滞 |
| 停表 | 离开大厅、`logout`、未修灵、停滞；sync 单飞 |

---

## 5. API / Store 对应

| 文件 | 函数（示意） |
| --- | --- |
| `api/idle.ts` | `setIdleDirection(direction)` · `syncIdle()` |
| `api/breakthrough.ts` | `previewBreakthrough()` · `attemptBreakthrough()` |
| `api/battle.ts` | `startPve(monsterId?)` |
| `api/gm.ts` | `gmSetCharacter(payload)` |
| `api/character.ts` | `fetchMyCharacter()` — 响应类型改为含扩展字段 |

**`useCharacterStore` 扩展建议**

| state / action | 说明 |
| --- | --- |
| `character` / `display` | 权威角色；预测快照（片内/整片） |
| `fetchMe` | 仍可用；进大厅首屏 |
| `applyCharacter(ch, nextTickAt?)` | 各玩法 API 返回的 character 统一写入 |
| `startIdleRealtime` / `stopIdleRealtime` | 大厅用（`startPolling` 为兼容别名） |
| `setDirection` / `syncNow` | 封装 idle API |

突破/战斗的瞬时 UI 状态（读条中、战报打开）放组件 local ref 或薄 store，避免污染角色权威数据。

---

## 6. 交互与文案约定

### 6.1 挂机

- 主按钮：未修灵时「开始修灵」→ `direction=spirit`；修灵中「停止挂机」→ `none`。  
- 炼体/制造业：按钮禁用或点击 toast「将在后续版本开放」（对应 `40020`）。  
- `is_stalled`：醒目提示「灵石不足，修炼停滞；可通过战斗获取灵石」。  

### 6.2 突破

- 不可突破时按钮 disabled，旁注 `preview.reason`。  
- 点击后前端假读条 0.8～1.5s，再展示结果对话框（成功/失败文案用服务端 `message`）。  
- 请求期间锁按钮，防双提交。  

### 6.3 战斗

- 默认怪：`tutorial_slime`。  
- 战后弹窗或折叠面板展示 `rounds[].text` 列表；可「跳过动画」直接显示全文。  
- 胜负与奖励变化同时 `pushLog` 到事件日志。  

### 6.4 UI 基调

- 继承 M0：功能优先；已用 Element Plus 的可继续，不引入第二套重型体系。  
- 不为 M1 做终局古风大场景；进度条与状态色足够表达即可。  

### 6.5 GM（可选 UI）

- 仅 `import.meta.env.DEV` 显示折叠「调参」：修为/灵石/境界快捷写入。  
- 生产构建不得出现可点入口（即使用户知 API）。  

---

## 7. 类型对齐（要点）

`types/character.ts` 在 M0 基础上追加：

```ts
cultivation_to_next: number | null
cultivation_progress_ratio: number
is_stalled: boolean
idle_cultivation_per_tick: number
idle_stones_per_tick: number
idle_tick_seconds: number
base_atk: number
base_hp: number
```

`types/battle.ts`：`BattleRound`、`PveBattleResult` 与 `M1核心循环设计.md` §6 / §8.5 一致。

统一错误：Axios 拦截器已处理 `code !== 0`；面板 catch 后 `ElMessage` + `pushLog`。

---

## 8. 与后续里程碑

| 里程碑 | 对本设计的影响 |
| --- | --- |
| **M1** | 本文范围 |
| **M2** | 可能新增「离线收益」对话框；IdlePanel 三向开放；不必改路由表 |
| **M3** | 预期新增 `/formation` 或布阵页与战报播放器；**那时才扩展路由** |
| 延后项 | 一律先查 [`后续待完成.md`](./后续待完成.md) |

---

## 9. 开发进度与步骤（对照 M1）

> 本节随实现同步更新；接口以 `M1核心循环设计.md` 为准。

### 9.1 总览

| 步 | 内容 | 状态 | 说明 |
| --- | --- | --- | --- |
| 0 | 设计文档（本文 + 核心循环 + 后续待完成） | **已完成** | 2026-08-03 |
| 1 | 类型扩展 + `CharacterPanel` 进度条 | **已完成** | 2026-08-03 |
| 2 | `api/idle` + `IdlePanel` + 预测/tick 对齐 | **已完成** | 替换 Placeholder；非 5s 轮询 |
| 3 | 突破 preview/attempt + 假读条弹窗 | **已完成** | |
| 4 | 战斗 PVE + 战报列表 | **已完成** | |
| 5 | GameLog 事件文案 + 总验收 | **已完成** | 联调按核心循环 §11 |
| 6 | 可选 Dev GM 条 | **已完成** | 仅 `import.meta.env.DEV` |
| 7 | M1 收口（文档 / 跨境单测 / 死代码） | **已完成** | 2026-08-04 |

### 9.2 本地联调口令（实现后）

```powershell
# 后端
cd backend
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000

# 前端
cd frontend
npm run dev
```

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/idle/direction` | POST | `{"direction":"spirit"}` |
| `/api/v1/idle/sync` | POST | 权威入账；大厅按 `next_tick_at` 到点调用 |
| `/api/v1/breakthrough/preview` | GET | 预览 |
| `/api/v1/breakthrough/attempt` | POST | 同步突破 |
| `/api/v1/battle/pve` | POST | `{"monster_id":"tutorial_slime"}` |
| `/api/v1/gm/character/set` | POST | 仅 development |

### 9.3 下一步建议

1. **M1 已收口**；陌生人按 README §联调路径可复现闭环即可。  
2. 开 **M2** 设计前先完整阅读 `后续待完成.md`（三向挂机 / 离线帽 / 品阶 / 体质等）。  
3. 数值只改 `backend/app/config_data/*.yaml`；勿提前做棋盘 / WS 推送。  

---

## 10. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-03 | 初稿：无新路由；大厅四面板；api/stores/types 增量；进度表 |
| 2026-08-03 | 实现完成：§9.1 步骤 1～6 标已完成 |
| 2026-08-04 | M1 收口：§9.1 步骤 7；删除 Placeholder；下一步改为开 M2 |
| 2026-08-04 | §4.3 正名：废弃「默认 5s 轮询」；改为预测 + tick 对齐 + 兜底对表；与 `M1核心循环设计.md` §4.0 对齐 |
| 2026-08-04 | 后端 OOP 同步说明：玩法走 `IdleService` / `BreakthroughService` / `BattleService` + `PlayGate`；本文前端目录与 HTTP 契约不变 |

---

*接口与结算以 `M1核心循环设计.md` 为准；本文只固化前端目录、路由（无新增）与大厅落点，并跟踪实现进度。*

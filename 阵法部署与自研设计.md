# 阵法部署区 · 强制移位 · 自研/后台配表设计

> **文档性质**：GDD §7.6 / §12.5 与 M3 棋盘规则的扩展真相源；消化 **M3-D08**，并为 **M8 板块 Z（自研阵法）** 与 ADM 域 `formations` 定契约  
> **版本**：v1.2 · 2026-08-10  
> **实现状态**：**Phase A（M3-D08）已落地**（2026-08-10）；Phase B（M8 自研设计器）未做  
> **关联**：[`M3战斗成型设计.md`](./M3战斗成型设计.md) §3 / §5 · [`开发计划.md`](./开发计划.md) §10 板块 Z · [`后台管理系统开发计划.md`](./后台管理系统开发计划.md) · [`后续待完成.md`](./后续待完成.md) **M3-D08** · GDD [`project修仙.md`](./project修仙.md) §7.6  
> **原则**：部署自由度在 **阵法定稿时写死**；出战只遵守该阵法契约；官方表与玩家自研 **共用同一 schema**；数值与模式 **全进配置 / 后台 / 自研草案**，禁止业务散落 if。

---

## 0. 落地对照（Phase A）

| 设计项 | 代码落点 |
| --- | --- |
| `deploy` 四模式 | `domain/formation_blueprint.py` · `formations.yaml` |
| `resolve_deploy` / 蓝图校验 | `resolve_formation_deploy` → `FormationDeploySnapshot`；`validate_formation_def` |
| 占位接 zone | `domain/board.py` · `FormationService.validate_units`（一次 snap） |
| presets 下发有效格 | `formation_public_dict` → `effective_deploy_cells` / `max_units_effective` |
| `force_shifts` | `ShiftBoardState` Protocol · `blocking_terrain_flat_cells` · `autochess` |
| 前端高亮 | `FormationView` / `FormationBoard`；换阵自动清洗非法格 |
| ADM 字段 | `admin_field_catalog` / `admin_field_schema` |
| 样本 | `wide_front` / `cloud_drift` / `left_wing_mask` / `shift_gust` |
| 单测 | `tests/test_formation_deploy.py` |

---

## 1. 定案与边界

### 1.1 一句话目标

阵法不仅改四象，还决定 **「棋子能摆哪、能摆几个、地形怎么落、开战是否挤人」**。  
这些规则在 **录入/自研定稿时固定**；玩家出战布阵时只在该契约内操作。既支持「死板固定格」的教学阵，也支持「只限数量、己方半区任意摆」的高自由度阵。

### 1.2 写死边界

| 做 | 不做 / 说明 |
| --- | --- |
| 部署模式可配置：`default` / `fixed` / `free_own` / `mask` | 禁止无上限全盘乱摆；**永不**默认开放中立列 / 敌方半区 |
| 地形模式可配置：`none` / `fixed` / `brush`（定稿时落笔） | 地形仍 **只写己方半区**；不可刷到对方半区（对齐 M3 §5.2） |
| `force_shifts` 可选列表；源空 noop、目标占取消 | M3 不做连锁挤退 / 推挤栈（GDD TBD 仍延后） |
| 官方阵（YAML ∪ ADM）与自研阵 **同一 `FormationBlueprint` schema** | 自研私有 id 前缀与权限另表；效果 id 走白名单（防外挂造载荷） |
| 出战校验与前端高亮 **同源**（服务端算有效部署格下发） | 前端不得本地发明可部署区 |
| 上阵数 ≤ `min(境界上限, 合法格数, 阵法 max_units)` | 合法格数随模式变化；`none` 阵回退现行默认 6 格 |

### 1.3 与现行落地的关系

| 已落地（保持） | 本文扩展 |
| --- | --- |
| 7×7、三区、默认 `(0,2)–(1,4)`、镜像存进攻方视角 | 有效部署格 = `resolve_deploy(board, formation)` |
| 地形障碍/深渊禁停；四象对抗 | 部署区可并/替；地形可 brush 定稿 |
| `formations.yaml` ×5 + 等级锁 | schema 增 `deploy` / `terrain_layout` / `force_shifts` |
| `/formation` 编辑器 + Bench | 按当前 `formation_id` 高亮有效格；自研工坊复用棋盘 |
| ADM 域 `formations` 可发布 | 表单暴露部署/地形/移位字段（中文 label） |

### 1.4 配置与后台纪律（§0.0）

| 必须 | 禁止 |
| --- | --- |
| 官方阵进域 **`formations`**：表格 + JSON/YAML；字段 `label_zh` / `help_zh` | 玩法代码写死「某阵法多 3 格」 |
| 自研定稿写入角色私有表，**校验器与官方同套** | 自研绕过 `own_half` / 中立闸 / 效果白名单 |
| 玩家服只读 Bundle + 私有阵合并视图 | 把 DEV `/gm` 当运营改阵入口 |

### 1.5 显性设计（§0.7）

玩家在布阵页 / 自研页必须能看到：

1. **当前阵法的部署模式**（固定格 / 半区自由 / 掩码区）与 **剩余可上阵数**；  
2. **高亮可部署格**（与服务器 `effective_deploy_cells` 一致）；  
3. **地形禁停格**（不可落子）与 brush 预算（若该阵允许定稿刷地形）；  
4. **开战移位预览**（若有 `force_shifts`：源→目标箭头 +「源空则无效」说明）；  
5. 非法落子时的 **明确原因**（越区 / 重叠 / 地形占用 / 超员），非静默灰格。

---

## 2. 术语

| 术语 | 含义 |
| --- | --- |
| **阵法蓝图** `FormationBlueprint` | 定稿后的阵法定义（官方或自研）；含四象 + 部署契约 + 地形布局 + 可选移位 |
| **部署模式** `deploy.mode` | 决定出战布阵时合法格集合如何计算 |
| **有效部署格** `effective_deploy_cells` | 某次校验使用的可落子格集合（进攻方视角） |
| **己方半区** | 进攻方视角 `x ∈ zones.own_x`（现行 `{0,1,2}`）；防守开战时镜像 |
| **默认部署区** | `board.yaml` 的 `default_deploy` 矩形（现行 6 格） |
| **地形布局** `terrain_layout` | 定稿时写入蓝图的障碍/沟壑/禁制格表 |
| **强制移位** `force_shift` | 开战瞬间：若源格有 **对方** 棋子，挪到目标格；否则 noop |
| **官方阵** | `formations.yaml` ∪ Overlay 发布产物 |
| **自研阵** | 角色私有定稿；`formation_id` 形如 `custom:{character_id}:{slug}`（实现可调，语义不变） |
| **草案** | 自研未定稿状态；可反复改笔刷与参数，不可出战 |

---

## 3. 部署模式（核心）

> 模式在 **阵法定稿时选定**，出战不可临时改模式。  
> 坐标一律 **进攻方视角**；防守方由引擎 `mirror_x`。

### 3.1 模式一览

| `deploy.mode` | 合法格怎么来 | 典型用途 |
| --- | --- | --- |
| **`default`** | = `board.default_deploy_cells`（现行行为） | `none`、教学阵、兼容旧表 |
| **`fixed`** | 显式 `cells[]`（可再并 `add_cells`）；缺省可 = default ∪ add | 「必须摆在这些点」的阵眼阵 |
| **`free_own`** | 己方半区全部格 − 禁停地形 −（可选）排除列表 | 「只规定数量、位置自选」的自由阵 |
| **`mask`** | `cells[]` 给出半区内任意子集（设计时画区） | 「左翼三列可摆、右翼不可」等异形区 |

### 3.2 公共约束（一切模式）

1. 合法格 ⊆ 己方半区（`x ∈ own_x`），除非显式 `allow_neutral: true`（**M3 默认 false，官方样本勿开**）。  
2. **禁止** 落入敌方半区。  
3. 与阵法地形禁停格（obstacle / ravine / seal 等）求差：禁停格 **不可部署棋子**。  
4. `max_units`（阵法可选）与境界 `max_units_by_major_realm`、合法格数三者取 **min**。  
5. 本体唯一、种类闸门、重叠规则不变（M3 §3.4）。

### 3.3 字段契约（`deploy`）

```yaml
deploy:
  mode: free_own          # default | fixed | free_own | mask
  max_units: 5            # 可选；缺省不额外收紧
  cells: []               # fixed / mask 必填（非空）；default / free_own 忽略或校验为空
  add_cells: []           # 仅 default 语义扩展时可用：default ∪ add；fixed 也可并入
  exclude_cells: []       # 从计算结果中扣除（free_own / mask 常用）
  allow_neutral: false    # true 才可含 x∈neutral_x；默认 false
```

**解析校验（启动 / 发布 / 自研定稿）**：

| 条件 | 失败 |
| --- | --- |
| `mode=fixed|mask` 且 `cells` 空 | 拒绝 |
| 任一 cell 越界或落入敌区 | 拒绝 |
| `allow_neutral=false` 却含中立列 | 拒绝 |
| `add_cells` 用于 `free_own` | 拒绝（语义冲突；应改 `exclude` 或换 `mask`） |
| `max_units < 1` 或 `> board.default_max_units` 的硬顶（可配） | 拒绝 |

### 3.4 解析伪代码

```text
resolve_deploy(board, blueprint) -> frozenset[(x,y)]:
  blocked = terrain_blocked_cells(blueprint)   # 禁停
  match blueprint.deploy.mode:
    default:
      cells = default_deploy_cells(board) ∪ add_cells
    fixed:
      cells = set(cells) ∪ add_cells
    free_own:
      cells = {(x,y) | x in own_x, y in 0..size-1}
    mask:
      cells = set(cells)
  cells = cells - exclude_cells - blocked
  if not allow_neutral:
    cells = cells - neutral_column_cells
  assert cells ⊆ own_half ∪ (neutral if allowed)
  return cells
```

出战 / 存预设：`validate_placement(..., deploy_zone=resolve_deploy(...))`。

### 3.5 与「只规定数量不规定位置」的对应

用户设想的自由上阵 = **`deploy.mode: free_own`** + 合适的 `max_units`：

- 不写死坐标；  
- 玩家在布阵 UI 于己方半区任意合法格落子；  
- 服务器只校验：半区内、非禁停、不重叠、不超员、种类合法。

固定教学阵 = **`default` 或 `fixed`**。

---

## 4. 地形布局（障碍等）

### 4.1 模式

| `terrain_layout.mode` | 含义 | 谁在何时落笔 |
| --- | --- | --- |
| **`none`** | 无地形 | — |
| **`fixed`** | 蓝图内写死 `terrain[]`（现行样本） | 管理员录入 / 自研定稿时已固定 |
| **`brush`** | 定稿流程中在 **允许刷区内** 放置，受预算约束；**定稿后冻结为普通 terrain[]** | 管理员或玩家在「阵法设计器」里画；**出战布阵不再改地形** |

> **定案**：出战页只摆 **棋子**，不临时改阵法地形。地形自由度体现在 **设计阵法时**，不是每场开战现场刷障碍。

### 4.2 字段契约

```yaml
terrain_layout:
  mode: brush                 # none | fixed | brush
  # brush 定稿约束（定稿后可只保留 terrain[]，constraints 可归档）
  brush:
    max_obstacles: 3
    max_ravines: 1
    allowed_types: [obstacle, ravine]   # seal 等随 M3-D07
    paint_zone: own_half                # own_half | mask
    paint_cells: []                     # paint_zone=mask 时必填
  # 定稿结果（与现行 terrain 对齐）
terrain:
  - { x: 2, y: 3, type: obstacle, subtype: destructible }
```

**规则**：

- 地形格必须落在己方半区（或 `paint_cells` ⊆ 己方半区）。  
- 地形格 **占用** 该格：不可再部署棋子；参与探路/LOS（现行引擎）。  
- `brush` 定稿时：超额 / 越区 / 类型不在白名单 → 拒绝定稿。  
- 官方 `fixed` 阵可在后台直接编辑 `terrain[]`，不必走 brush UI。

### 4.3 与部署区的交互

`resolve_deploy` **减去** 地形禁停格。  
因此 `free_own` 阵若刷了 2 个障碍，可摆格 = 半区格数 − 2；`max_units` 仍可再收紧。

---

## 5. 强制移位 `force_shifts`

### 5.1 语义（对齐 GDD §7.6 + M3 §5.5）

开战套用顺序（完整态）：

1. 加载双方单位并校验占位  
2. 布置双方地形  
3. 四象分层对抗  
4. 套用效果乘区等  
5. **`force_shifts`**：对 **敌方单位** 生效  
6. 进入回合循环  

单条规则：

| 情况 | 结果 | 战报 |
| --- | --- | --- |
| 源格无对方棋子 | 不移动 | `shift_noop` |
| 目标格已被占用 | 本条取消 | `shift_cancel_occupied` |
| 目标越界 / 禁停 / 非法 | 本条取消 | `shift_cancel_illegal` |
| 源有对方子且目标空且合法 | 移动该子 | `shift_applied` |

不做连锁、不做「挤退到邻格」。多条按数组 **顺序** 执行；前一条可能影响后一条的源/目标占用。

### 5.2 字段契约

```yaml
force_shifts:
  - from: [4, 3]    # 进攻方视角下的「敌方半区」坐标（对守方阵法则先镜像再解释）
    to:   [5, 3]
    # 可选：only_kinds: [main, puppet]  # 缺省=任意对方棋子
```

坐标约定：

- 蓝图一律存 **进攻方视角**；  
- 防守方施放该阵时：`from/to` 的 x 做 `mirror_x` 后再作用于战场。

### 5.3 自研白名单

自研可配移位，但：

- `from` 必须落在 **敌对半区**（对蓝图作者而言的「对方」）；  
- `to` 必须落在敌对半区或中立（默认 **禁止** 挪进己方半区，防「绑架」；若未来开放须单独 flag + 高危确认）；  
- 单阵 `force_shifts` 条数 ≤ 配置帽（如 3）。

---

## 6. 四象与其它载荷（边界）

本文 **不重做** 四象对抗（已落地）。蓝图仍含：

- `terrain` / `environment` / `weather` / `effect`  
- `level` / `required_array_level` / `unlocked_by_default`

内容加深（`seal` 禁制子类、天气载荷样本）见 **M3-D07** / [`四象禁制与环境天气载荷设计.md`](./四象禁制与环境天气载荷设计.md)，与部署契约正交，可并行。

效果 id、环境 id 等：**官方表任意；自研仅白名单**（对齐开发计划 AA 符箓纪律）。

---

## 7. 完整蓝图示意

### 7.1 官方：固定扩展部署 + 固定地形

```yaml
stone_wall_left:
  name: 左拒马阵
  level: 1
  unlocked_by_default: true
  deploy:
    mode: default
    add_cells: [[2, 3]]          # 多开阵眼一格
  terrain_layout:
    mode: fixed
  terrain:
    - { x: 2, y: 3, type: obstacle, subtype: destructible }
  # 注意：add 的 (2,3) 与障碍冲突 → 发布校验应拒绝或自动从 deploy 扣除
  environment: null
  weather: null
  effect: { id: guarded, force_apply: false, atk_mul: 0.98, hp_mul: 1.05 }
  force_shifts: []
```

> 发布校验：**部署格 ∩ 地形禁停 = ∅**（或自动扣除并告警）；推荐 **硬拒绝**，避免运营误配。

### 7.2 官方：半区自由数量阵

```yaml
cloud_drift:
  name: 流云散锋
  level: 2
  unlocked_by_default: false
  required_array_level: 2
  deploy:
    mode: free_own
    max_units: 4
    exclude_cells: []          # 可选再抠洞
    allow_neutral: false
  terrain_layout:
    mode: none
  terrain: []
  force_shifts: []
  environment: null
  weather: null
  effect: null
```

### 7.3 自研草案 → 定稿（逻辑等价）

玩家在阵法研发界面：

1. 选 `deploy.mode = mask`，在己方半区涂可部署区；设 `max_units = 5`；  
2. `terrain_layout.mode = brush`，预算 2 障碍，在允许区落笔；  
3. 可选加 1 条 `force_shift`；  
4. 选效果白名单 id；  
5. 定稿 → 私有蓝图；出战下拉可选。

定稿后存储形态与官方 YAML 条目同构（多 `owner_character_id` / `source=custom`）。

---

## 8. 运行时与 API

### 8.1 领域函数（建议落点）

| 模块 | 职责 |
| --- | --- |
| `domain/board.py` | `resolve_deploy_cells(...)`；`validate_placement` 改为吃 `deploy_zone` |
| `domain/formation_blueprint.py`（新） | 解析/校验蓝图；地形禁停；移位合法性纯函数 |
| `domain/formation_rules.py` | 四象不变；开战管线调用 `apply_force_shifts` |
| `services/formation_service.py` | 存预设时按当前 `formation_id` resolve；返回有效格 |
| `services/autochess_service.py` | 开战第 5 步套用移位 |

### 8.2 API 契约增量

| 接口 | 增量 |
| --- | --- |
| `GET /formation/board-meta` | 仍返回默认区；**可选**保留兼容 |
| `GET /formation/presets` | 每条预设 / 当前选中阵附带：`deploy_mode`、`effective_deploy_cells`、`terrain_cells`、`force_shifts_preview`、`max_units_effective` |
| `PUT /formation/presets/{slot}` | body 含 `formation_id` + units；服务端按该阵 resolve 校验 |
| （M8）`GET/POST /formation/craft/...` | 草案 CRUD、定稿；详见 §10 |

前端：`FormationBoard` 高亮改为 **`effective_deploy_cells`**，不再写死 `default_deploy_cells`。

### 8.3 错误码（沿用 / 扩展）

| 码 | 场景 |
| --- | --- |
| `40041` | 非法占位 / 超员 / 重叠 / 地形占用 |
| `40044` | 阵法不存在 |
| `40054` | 阵法等级不足 |
| `40055`（建议） | 蓝图校验失败（自研定稿 / 坏表）——细节 message |

移位失败不阻断开战，只记账（noop/cancel）。

---

## 9. 管理员后台（域 `formations`）

### 9.1 录入体验

后台编辑单条阵法时，除现有四象字段外，增加分组：

1. **部署契约**：mode 下拉；`fixed/mask` 时棋盘点选 `cells`；`free_own` 时只填 `max_units` / `exclude`；`allow_neutral` 开关（高危，默认关，改动需确认）。  
2. **地形**：`fixed` 表格编辑或小型棋盘点选；`brush` 仅作「设计提示」时，官方更常用 `fixed` 直接提交 `terrain[]`。  
3. **强制移位**：列表编辑 from/to；棋盘预览箭头。  
4. **校验面板**：一键跑 `validate_blueprint`，展示有效部署格数、与地形冲突、移位非法。

### 9.2 发布

与现行 Overlay 一致：草稿 → 发布 → 清 Bundle。  
坏蓝图 **拒绝发布**（同启动加载硬失败策略）。

### 9.3 字段中文（catalog 摘要）

| 键 | 中文 |
| --- | --- |
| `deploy.mode` | 部署模式 |
| `deploy.max_units` | 阵法上阵上限 |
| `deploy.cells` | 固定/掩码可部署格 |
| `deploy.add_cells` | 追加可部署格 |
| `deploy.exclude_cells` | 排除格 |
| `deploy.allow_neutral` | 允许中立列 |
| `terrain_layout.mode` | 地形布局模式 |
| `force_shifts` | 开战强制移位列表 |

---

## 10. 玩家自研（M8 板块 Z）

> 开发计划 Z1～Z4；本文先定 **数据与校验契约**，UI 竖切挂 M8。

### 10.1 流程

```text
材料/钻研等级门槛
  → 创建草案（空蓝图，默认 deploy.mode=default）
  → 阵法设计器（复用 BoardGrid）：
        选部署模式 → 画 mask / 设 free_own 上限
        brush 地形（预算内）
        选四象白名单载荷
        可选 force_shifts
  → 保存草案（多次）
  → 定稿（validate_blueprint + 扣材料/记等级）
  → 私有 formation_id 可出战 / 进快照
```

### 10.2 与出战编辑器的关系

| 界面 | 改什么 |
| --- | --- |
| **阵法研发** `/workshop` 阵法分支或独立 `/formation/craft` | 改 **蓝图**（模式、区、地形、移位、四象） |
| **布阵** `/formation` | 只改 **棋子落点**（遵守已定稿蓝图） |

禁止在出战页改 `deploy.mode` 或临时加障碍（防每场现场改规则导致快照不一致）。

### 10.3 快照 / PVP

快照序列化必须带完整蓝图快照或 `formation_id` + 内容哈希：

- 官方阵：id + 配置版本哈希；  
- 自研阵：**内联蓝图** 或不可变私有版本号，避免对方攻打时作者改草案影响已发布快照。

定案建议：**定稿后蓝图不可变**；再改只能「另存新版 / 新 id」。

---

## 11. 竖切与阶段

### 11.1 Phase A — 消化 M3-D08（建议下一并行包）

| 步 | 内容 | 验收 |
| --- | --- | --- |
| A1 | schema：`deploy` / `force_shifts`；旧阵缺省 `mode=default` | 旧 YAML 无感兼容 |
| A2 | `resolve_deploy` + `validate_placement` 接 zone | 单测 fixed / free_own / 越区拒绝 |
| A3 | 样本：`free_own` ×1、`default+add_cells` ×1、`force_shifts` ×1 | 开战战报可见 shift_* |
| A4 | presets API 下发 `effective_deploy_cells`；前端高亮切换 | 换阵法高亮变 |
| A5 | ADM 字段 catalog + 基础表单（可先 JSON） | 发布校验拒绝冲突蓝图 |
| A6 | 文档：M3-D08 → 已消化；本文状态更新 | README/CHANGELOG |

**本阶段不做**：自研草案 UI、brush 完整设计器（官方用 `fixed` terrain 即可）。

### 11.2 Phase B — M8 自研

| 步 | 内容 | 验收 |
| --- | --- | --- |
| B1 | 草案表 + 定稿 API + 同套校验器 | Z1 |
| B2 | 阵法设计器（mask 涂区 + brush 地形） | Z3 |
| B3 | 出战可选自研；快照内联/版本冻结 | Z4 |
| B4 | 效果白名单；审核池占位 | 防外挂 |

### 11.3 明确后置

| 项 | 原因 |
| --- | --- |
| 出战中临时改地形 | 与快照/PVP 一致性冲突 |
| 移位挤退连锁 | GDD TBD；规则爆炸 |
| 默认开放中立列 | 破坏三区教学语义；仅显式 flag |
| 把敌方单位挪进己方半区 | 平衡与规则风险；需单独高危旗 |

---

## 12. 验收钩子汇总

### 12.1 M3-D08（Phase A）

1. YAML `deploy.mode=free_own` 时，己方半区非禁停格可落子；敌区 / 中立（默认）拒绝。  
2. `fixed` / `mask` / `add_cells` 与前端高亮一致。  
3. 地形禁停格不可部署；与 deploy 冲突的蓝图发布失败。  
4. `force_shifts`：源空 noop、目标占取消、成功移动进战报。  
5. `none` 与旧阵缺省行为与现行一致（回归绿）。

### 12.2 M8（Phase B）

1. 玩家可定稿至少 1 个 `free_own` 或 `mask` 自研阵并出战。  
2. 定稿后改草案不影响已写入快照的版本。  
3. 非法半区 / 超预算 brush / 非白名单效果 → 定稿拒绝。

---

## 13. 对既有文档的指针

| 文档 | 如何引用本文 |
| --- | --- |
| `后续待完成.md` **M3-D08** | 细则以本文为准；验收钩子 §12.1 |
| `M3战斗成型设计.md` §1.3 / §3.3 | 「阵法改部署区」→ 本文 §3；实现后回写现行行为 |
| `开发计划.md` 板块 Z | Z1 校验 = 本文 `validate_blueprint` |
| `后台管理系统开发计划.md` | 域 `formations` 表单加深 → 本文 §9 |
| GDD §7.6 | 非常规占位 / 强制移位的工程展开 |

---

## 14. 修订记录

| 日期 | 版本 | 说明 |
| --- | --- | --- |
| 2026-08-07 | v1.0 | 初稿：部署四模式、地形 brush 定稿、force_shifts、ADM/自研同 schema、Phase A/B 竖切 |
| 2026-08-10 | v1.1 | **Phase A 落地**：schema/校验/样本/API 有效格/前端高亮/force_shift 战报；M3-D08 已消化 |
| 2026-08-10 | v1.2 | **封装优化**：`FormationDeploySnapshot` 一次解析；`validate_formation_def`；禁停常量与 terrain 同源；换阵清洗非法格；`max_units_effective` |

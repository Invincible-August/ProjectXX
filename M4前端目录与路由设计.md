# M4 前端目录与路由设计

| 项 | 内容 |
| --- | --- |
| **针对里程碑** | **M4（双线程成长）** |
| **设计日期** | 2026-08-05 |
| **依据** | `M4双线程成长设计.md`；`M3前端目录与路由设计.md`；`M2`/`M1`/`M0` 前端目录设计；`开发计划.md` §6 |
| **原则** | **再拆三条玩法路由**（`/avatar` `/workshop` `/pets`）；`/hall` 仍为养成枢纽；`/formation` `/battle` 保留并扩展 Bench；目录骨架继承 M0～M3 |

---

## 1. 设计目标

约定 M4 在 `frontend/src` 下的文件增量、**新路由表**、大厅枢纽信息架构，以及与后端化身 / 双挂机 / 工坊 / 背包 / 灵宠 / 神识 / Bench 接口的对接落点。

M4 可玩范围：金丹凝练化身、双线程挂机与修为互传、工坊选配方进度领取、灵兽园持有与上阵勾选、布阵 Bench 接入真棋子、快照含化身/宠/傀儡。  
范围外：~~多化身~~（**已取消**，见 [`化身系统设计.md`](./化身系统设计.md)）、化身独立跨境、完整捕获生态、道值炼丹、宗门化身任务、真 WS；以及 M3 延后打磨 **M3-D01～D10**（见 [`后续待完成.md`](./后续待完成.md)）。本文只描述 M4 主路径路由与组件。

---

## 2. 路由策略（相对 M3：**再增三条玩法 path**）

| path | name | 文件 | M4 变化 |
| --- | --- | --- | --- |
| `/login` | `login` | `views/LoginView.vue` | 无 |
| `/register` | `register` | `views/RegisterView.vue` | 无 |
| `/create-character` | `create-character` | `views/CreateCharacterView.vue` | 无 |
| `/hall` | `hall` | `views/HallView.vue` | **枢纽扩展**：双线程入口 + 工坊/灵宠摘要 |
| `/formation` | `formation` | `views/FormationView.vue` | Bench 接真棋子；阵法等级锁 |
| `/battle` | `battle` | `views/BattleView.vue` | 无强制改版；开战仍走原契约 |
| `/avatar` | `avatar` | `views/AvatarView.vue` | **新**：凝练 / 化身挂机 / 传修为 / 神识 |
| `/workshop` | `workshop` | `views/WorkshopView.vue` | **新**：配方 / 队列 / 领取 / 背包摘要 |
| `/pets` | `pets` | `views/PetsView.vue` | **新**：灵兽园 / 升级占位 / 上阵偏好 |
| `/` | — | 重定向 | 逻辑不变（有角色仍优先去大厅） |
| `/test` | `test` | `test/apps.vue` | 可选；非验收 |

### 2.1 守卫逻辑（继承并扩展）

| 规则 | 说明 |
| --- | --- |
| 需登录 | `/hall` `/formation` `/battle` `/avatar` `/workshop` `/pets` `/create-character` 均 `requiresAuth` |
| 无角色 | 访问上述玩法页 → `/create-character` |
| 有角色访创角 | → `/hall` |
| 已登录访登录/注册 | → `homePathAfterAuth()`；若 `redirect` 安全则尊重 |
| `redirect` 白名单 | `resolveSafeRedirect` **追加** `/avatar`、`/workshop`、`/pets`（及带 query） |
| 境界门禁 | **不做路由级硬拦**；页内按 API 错误展示（未金丹访 `/avatar` 仍可进，凝练按钮禁用 + 文案） |

```text
App.vue
  └── <RouterView />
        ├── /login | /register | /create-character
        ├── /hall → HallView.vue
        │     ├── CharacterPanel / Idle / Allocate / Constitution / Breakthrough（保留）
        │     ├── HallBattleGate.vue（保留：布阵/战斗）
        │     ├── HallDualThreadGate.vue（新：化身/工坊/灵宠入口 + 双线程摘要）
        │     ├── DualIdleSummary.vue（可选：本体∥化身速率一行）
        │     └── GameLogPanel.vue
        ├── /formation → FormationView.vue
        │     ├── UnitBench.vue（扩展：拉 /formation/bench）
        │     ├── FormationPicker.vue（扩展：array_craft_level 锁）
        │     └── …（Board / Preset / Snapshot 保留）
        ├── /battle → BattleView.vue（保留）
        ├── /avatar → AvatarView.vue
        │     ├── AvatarCondensePanel.vue
        │     ├── AvatarIdlePanel.vue
        │     ├── AvatarTransferPanel.vue
        │     └── DivineSenseBar.vue
        ├── /workshop → WorkshopView.vue
        │     ├── RecipeList.vue
        │     ├── CraftJobQueue.vue
        │     ├── CraftClaimBar.vue
        │     └── InventoryPanel.vue（可复用于侧栏）
        └── /pets → PetsView.vue
              ├── PetList.vue
              ├── PetDetailPanel.vue
              └── PetDeployToggle.vue
```

### 2.2 为何现在再拆路由

| 理由 | 说明 |
| --- | --- |
| 对齐开发计划 L5/M3/N2 | 化身管理、工坊、灵兽园均为独立操作面，塞进大厅会挤爆 |
| 继承 M3 拆法 | 养成仍在 `/hall`；「第二线程与生产」独立；战斗链路不动 |
| 守卫复用 | 同一套 JWT + `hasCharacter`；不新造鉴权 |

**不做**：把分配/体质再拆路由；不把工坊做进化身页内嵌 Tab 作为唯一入口（允许化身页链到工坊，但 `/workshop` 必须可直达）。

### 2.3 可选 query（非新 path）

| 用法 | 说明 |
| --- | --- |
| `/avatar?tab=transfer` | 打开传修为区 |
| `/workshop?branch=alchemy` | 默认选中炼丹分支 |
| `/workshop?actor=avatar` | 默认化身队列 |
| `/pets?focus=<pet_id>` | 高亮某宠 |
| `/formation?bench=pet` | 打开 Bench 并滚到灵宠区 |

---

## 3. `src` 目录增量

M0～M3 目录职责不变；M4 **追加文件与路由**，不推翻骨架。

```text
src/
├── api/
│   ├── avatar.ts               # 新：me / condense / idle / transfer / sense
│   ├── craft.ts                # 新：recipes / jobs / start / claim
│   ├── inventory.ts            # 新：list / use（可选）
│   ├── pets.ts                 # 新：list / capture_test / upgrade / patch
│   ├── idle.ts                 # 扩展：双线程 sync / offline 分列
│   ├── formation.ts            # 扩展：fetchBench；recipes 等级字段
│   ├── character.ts            # 扩展 apply 新摘要字段
│   └── gm.ts                   # 扩展金丹/材料/测试宠/神识
├── components/
│   ├── hall/
│   │   ├── HallDualThreadGate.vue   # 新：三入口卡片
│   │   └── DualIdleSummary.vue      # 可选
│   ├── avatar/
│   │   ├── AvatarCondensePanel.vue
│   │   ├── AvatarIdlePanel.vue
│   │   ├── AvatarTransferPanel.vue
│   │   └── DivineSenseBar.vue
│   ├── workshop/
│   │   ├── RecipeList.vue
│   │   ├── RecipeCard.vue           # 可选拆分
│   │   ├── CraftJobQueue.vue
│   │   ├── CraftClaimBar.vue
│   │   └── InventoryPanel.vue
│   ├── pets/
│   │   ├── PetList.vue
│   │   ├── PetDetailPanel.vue
│   │   └── PetDeployToggle.vue
│   ├── formation/
│   │   ├── UnitBench.vue            # 改：数据源 bench API
│   │   └── FormationPicker.vue      # 改：等级锁
│   └── …（M3 battle / M2 面板保留）
├── stores/
│   ├── avatar.ts               # 新
│   ├── craft.ts                # 新：recipes / jobs / 本地进度预测
│   ├── pets.ts                 # 新
│   ├── inventory.ts            # 新或并入 craft
│   ├── character.ts            # 扩展摘要
│   ├── formation.ts            # 扩展 bench 列表
│   └── idle 相关               # 扩展 avatar 预测字段
├── types/
│   ├── avatar.ts
│   ├── craft.ts
│   ├── inventory.ts
│   ├── pets.ts
│   ├── divineSense.ts
│   └── formation.ts            # bench unit 联合类型
├── utils/
│   ├── safeRedirect.ts         # 白名单扩展
│   └── craftProgress.ts        # 本地进度条预测（只展示）
├── router/
│   └── index.ts                # 登记三路由；守卫扩展
└── views/
    ├── HallView.vue            # 挂 DualThreadGate
    ├── AvatarView.vue          # 新
    ├── WorkshopView.vue        # 新
    ├── PetsView.vue            # 新
    ├── FormationView.vue       # 改 Bench
    └── BattleView.vue          # 基本不动
```

### 3.1 命名约定（继承）

- 页面：`XxxView.vue`（M4 **新增** Avatar / Workshop / Pets）
- 零件：`XxxPanel.vue` / `XxxBar.vue` / `XxxGate.vue`；按域放 `components/avatar|workshop|pets|hall/`
- API 文件与后端模块同名
- 路由 path：小写短横线（本里程碑均为单段英文词）

### 3.2 子目录说明

- `views/` 仍扁平（再多 3 个整页，可接受）。  
- `components/` 继续按域分子夹，避免根目录爆炸（与 M3 一致）。

---

## 4. 信息架构

### 4.1 大厅（枢纽）布局

```text
┌──────────────────────────────────────────────────────────────┐
│ AuthSessionBar · 修仙大厅 · M4 双线程                          │
│ [布阵] [战斗]  [化身] [工坊] [灵兽园]                           │
├────────────────────────────┬─────────────────────────────────┤
│ CharacterPanel             │ GameLogPanel                    │
│  · 原 M2 字段              │  · 养成 / 战斗                   │
│  · 神识摘要一行            │  · 凝练 / 工坊领取 / 超载        │
│  · 阵法等级                │                                 │
│ IdlePanel（本体三向）      │                                 │
│ DualIdleSummary（可选）    │                                 │
│  · 本体速率 ∥ 化身速率     │                                 │
│ HallBattleGate             │                                 │
│ HallDualThreadGate         │                                 │
│  · 化身状态/未解锁提示     │                                 │
│  · 工坊可领取角标          │                                 │
│  · 灵宠持有数              │                                 │
│ Allocate / Constitution /  │                                 │
│ Breakthrough …             │                                 │
└────────────────────────────┴─────────────────────────────────┘
```

移动端：顶栏入口可收入「更多」折叠；优先保证布阵/战斗/化身三键可点。

### 4.2 化身页布局

```text
┌────────────────────────────────────────────────────────────┐
│ ← 回大厅 · 化身 · DivineSenseBar（容量/负载/超载提示）      │
├────────────────────────────┬───────────────────────────────┤
│ 未凝练：AvatarCondensePanel │ 已凝练：状态 / 属性 / 修为池   │
│  · 门槛说明 · 费用 · 凝练  │ AvatarIdlePanel               │
│                            │  · 修灵/炼体/制造业/停止      │
│                            │ AvatarTransferPanel           │
│                            │  · 方向 · 数量 · 确认         │
│                            │ [去工坊·化身队列] [去布阵]    │
└────────────────────────────┴───────────────────────────────┘
```

### 4.3 工坊页布局

```text
┌────────────────────────────────────────────────────────────┐
│ ← 回大厅 · 工坊 · actor 切换[本体|化身] · 体力条（可复用）   │
├──────────────┬─────────────────────────┬───────────────────┤
│ RecipeList   │ CraftJobQueue           │ InventoryPanel    │
│  · 五分支 Tab│  · running 进度预测     │  · 丹/符/傀儡…    │
│  · 材料需求  │  · ready → 领取         │                   │
│  · 效率提示  │ CraftClaimBar           │                   │
└──────────────┴─────────────────────────┴───────────────────┘
```

文案必须区分：**「制造业挂机（涨经验池）」在大厅 Idle** vs **「配方队列」在本页**。

### 4.4 灵兽园布局

```text
┌────────────────────────────────────────────────────────────┐
│ ← 回大厅 · 灵兽园 · 持有 n/cap · [测试捕获]                  │
│ （N4）种族筛选 Tab/下拉 · 稀有度角标 · [图鉴入口·占位灰置]   │
├────────────────────────────┬───────────────────────────────┤
│ PetList                    │ PetDetailPanel                │
│  · 物种 / 种族 / 等级      │  · 升级占位                   │
│  · 战力摘要                │  · 获取来源文案（途径 ID）    │
│                            │  · PetDeployToggle 偏好上阵   │
│                            │  · [去布阵]                   │
└────────────────────────────┴───────────────────────────────┘
```

> N4/N5 信息架构增量见成长设计 §7.4～7.5；**完整图鉴页**可后置独立路由，本阶段仅占位入口。

### 4.5 组件职责

| 组件 | 职责 | 不负责 |
| --- | --- | --- |
| `HallDualThreadGate` | 跳转三页 + 只读摘要角标 | 凝练/开工表单 |
| `AvatarCondensePanel` | 调 condense；展示门槛 | 改本地境界 |
| `AvatarIdlePanel` | 设化身方向；展示预测 | 自己 settle 入账 |
| `AvatarTransferPanel` | 传修为；非法资源不展示或禁用 | 传炼体/制造业 |
| `DivineSenseBar` | 展示容量/负载/超载 | 发明衰减公式 |
| `RecipeList` | 选配方；展示锁与材料 | 扣背包 |
| `CraftJobQueue` | 进度条预测 + 列表 | 权威完成判定 |
| `InventoryPanel` | 只读/使用消耗品入口 | 交易 |
| `PetDeployToggle` | 偏好勾选 | 直接改预设坐标 |
| `UnitBench` | 展示 bench API 棋子并落子 | 绕过校验本地造子 |
| `AvatarView` 等 | 编排拉数、日志、脏提示 | 大段公式 |

### 4.6 关键时序

**凝练**

```text
确认费用 → POST /avatar/condense
  → 成功：applyCharacter + avatar store；pushLog
  → 40050/40051：ElMessage
```

**化身挂机**

```text
选方向 → POST /avatar/idle
  → 服务端 settle_dual 后改方向
  → 大厅 DualIdleSummary / 化身页刷新预测
```

**传修为**

```text
选方向+数量 → POST /avatar/transfer
  → 40052：不应在 UI 可选到非法 resource
  → 成功：刷新两侧池
```

**开工 / 领取**

```text
选配方+actor → POST /craft/start
  → 40049 体力 / 40053 队列满 / 40055 材料
  → jobs 刷新；本地用 finish_at 预测进度
领取 → POST /craft/claim → inventory 刷新；阵法等级若变则 applyCharacter
```

**布阵 Bench**

```text
进入 /formation → GET /formation/bench
  → UnitBench 分区：本体 | 化身 | 灵宠 | 傀儡
  → 灰置：未凝练 / 渡劫禁化身 / 无库存傀儡
保存预设 → 原 PUT；服务端校验持有与神识策略
```

---

## 5. API / Store 对应

| 文件 | 函数（示意） |
| --- | --- |
| `api/avatar.ts` | `fetchAvatar` · `condenseAvatar` · `setAvatarIdle` · `transferCultivation` · `fetchDivineSense` |
| `api/craft.ts` | `fetchRecipes` · `fetchJobs` · `startCraft` · `claimCraft` |
| `api/inventory.ts` | `fetchInventory` · `useItem` |
| `api/pets.ts` | `fetchPets` · `captureTestPet` · `upgradePet` · `patchPet` |
| `api/formation.ts` | `fetchBench` ·（原 presets / board-meta / validate） |
| `api/idle.ts` | `syncIdle` 响应含 `avatar`；offline preview 分列 |

**`useAvatarStore`**

| state / action | 说明 |
| --- | --- |
| `avatar` | 服务端权威；null=未凝练 |
| `sense` | 神识读数 |
| `load` / `condense` / `setIdle` / `transfer` | |

**`useCraftStore`**

| state / action | 说明 |
| --- | --- |
| `recipes` / `jobs` / `actor` | |
| `localProgress` | 仅 UI；以 `finish_at` 为锚 |
| `start` / `claim` / `tickLocal` | |

**`usePetsStore`**

| state / action | 说明 |
| --- | --- |
| `pets` / `cap` | |
| `captureTest` / `upgrade` / `setDeployPreferred` | |

**`useFormationStore`**：增加 `benchUnits`；`loadBench`；落子前本地粗检（最终以服务端为准）。

**`useCharacterStore`**：`applyCharacter` 识别 `has_avatar`、`divine_sense`、`array_craft_level`、`craft_jobs_summary` 等。

---

## 6. 交互与文案约定

### 6.1 化身

- 未达金丹：页可进；主按钮禁用：「金丹境后方可凝练化身」。  
- 凝练成功：「化身凝练完成；可并行安排挂机」。  
- 传修为区只暴露修为；帮助文案写明炼体度/制造业经验不可传。  
- 渡劫（钩子）：化身页提示「渡劫中不可上阵，挂机仍可用」。  

### 6.2 工坊

- 效率提示：「本体正在制造业挂机：配方速度 ×1.25（示例）」。  
- 领取成功列出获得物；失败：「炼制失败，材料已耗」。  
- 与大厅 Idle 的「制造业」用不同标题，避免玩家以为切方向=开工。  

### 6.3 灵宠

- 列表标明「不可替代本体」。  
- 测试捕获仅 DEV 或显式测试按钮文案「测试捕获（非正式野外）」。  
- **N4**：展示种族 / 稀有度；测试捕获文案不得写成「野外捕获」。  
- **N5**：孵化进度与蛋来源提示；与工坊「制造业挂机」文案区分。  

### 6.4 布阵

- Bench 灰置给 tooltip：未开放原因（未凝练/等级不足/无库存）。  
- 阵法下拉：`required_array_level` 不足显示锁与「工坊钻研阵法」。  
- 保存成功仍强调「未自动更新防守快照」。  

### 6.5 神识

- 超载：Warn 色；严重超载：文案「神识反噬：化身修炼减速（占位）」。  
- 不在前端重算 atk；以服务端返回的衰减后摘要为准。  

### 6.6 UI 基调

- 继承 M0～M3：功能优先；Element Plus；不为 M4 做终局古风工坊美术。  
- 至少 2～3 处轻量动效：入口卡片 hover、工坊进度条、凝练成功短暂高亮。  

### 6.7 GM（DEV）

- 一键金丹、发材料、发测试宠、设神识、清空工坊队列。  
- 仅 `import.meta.env.DEV`。  

---

## 7. 类型对齐（要点）

```ts
export type AvatarIdleDirection = 'none' | 'spirit' | 'body' | 'crafting'

export interface AvatarPublic {
  id: number
  status: 'idle' | 'crafting' | 'disabled'
  idle_direction: AvatarIdleDirection
  cultivation_points: number
  body_tempering_points: number
  crafting_exp: number
  base_stats: Record<string, number>
}

export type CraftBranch = 'alchemy' | 'smithing' | 'talisman' | 'array' | 'puppet'
export type CraftActor = 'main' | 'avatar'

export interface BenchUnit {
  unit_uid: string
  unit_kind: 'main' | 'avatar' | 'pet' | 'puppet'
  ref_id?: number
  display_name: string
  enabled: boolean
  disabled_reason?: string
  stats_preview?: { atk: number; hp: number; speed: number }
}
```

---

## 8. 与后续里程碑

| 里程碑 | 对本设计的影响 |
| --- | --- |
| **M4** | 本文范围；按上表建齐三路由与门闸 |
| **M5** | 渡劫真态驱动 `disabled`；天气/六时对工坊提示；顶栏时辰 |
| **M6** | 道值运用入口可能挂工坊；勿提前塞 WS |
| **M7** | 交易/宗门；背包面板或迁独立经济页 |
| 延后项 | 一律先查 [`后续待完成.md`](./后续待完成.md) |

---

## 9. 开发进度与步骤（对照 M4）

> 本节随实现同步更新；接口以 `M4双线程成长设计.md` 为准。

### 9.1 总览（对齐成长设计 §11 T1～T6）

| 步 | 内容 | 状态 | 说明 |
| --- | --- | --- | --- |
| 0 | 设计文档（本文 + 成长骨架） | **已完成** | 2026-08-05 |
| **T1** | 类型 + `api/avatar` 凝练联调壳 | **已完成** | 2026-08-05 |
| **T2** | Idle sync 双线程字段 + DualIdleSummary | **已完成** | |
| **T3** | 传修为 UI + DivineSenseBar | **已完成** | |
| **T4** | 路由三页壳 + AvatarView 闭环 | **已完成** | 守卫白名单同步 |
| **T5** | WorkshopView + Inventory + Formation 阵法锁/傀儡 Bench | **已完成** | |
| **T6** | PetsView + Bench 全源 + 快照联调 + 大厅门闸角标 | **已完成** | |
| 收尾 | GameLog / GM / 总验收 / smoke | **已完成** | `vue-tsc` + `vite build` |
| **N4** | 种族/多物种展示 + 筛选 + 图鉴入口占位 | **设计中** | 成长设计 §7.4；实现未开 |
| **N5** | 蛋孵 UI（队列/领取入园） | **设计中** | 成长设计 §7.5；须在 N4 后 |

### 9.2 本地联调口令（实现后）

```powershell
# 后端
cd backend
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000

# 前端
cd frontend
npm run dev
```

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/avatar/me` | GET | 化身 |
| `/api/v1/avatar/condense` | POST | 凝练 |
| `/api/v1/avatar/idle` | POST | 化身方向 |
| `/api/v1/avatar/transfer` | POST | 传修为 |
| `/api/v1/craft/recipes` | GET | 配方 |
| `/api/v1/craft/start` | POST | 开工 |
| `/api/v1/craft/claim` | POST | 领取 |
| `/api/v1/pets` | GET | 灵宠列表 |
| `/api/v1/formation/bench` | GET | 可上阵源 |

### 9.3 下一步建议

1. M4 主路径（T1～T6）已收口；化身深化见 [`化身系统设计.md`](./化身系统设计.md) **AVATAR-D***；打磨项见 [`后续待完成.md`](./后续待完成.md)。  
2. **可并行**：N4 物种种族表前端（种族筛选、稀有度、测试捕获文案）；勿提前做完整图鉴页。  
3. 联调优先：GM 一键金丹 → 凝练 → 双挂机 → 工坊领取 → 灵兽园 → 布阵含化身/宠 → 更新快照。  
4. 下一主线里程碑：**M5**；野外捕获 UI → M9。  

---

## 10. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-05 | 初稿：新增 `/avatar` `/workshop` `/pets`；大厅 DualThreadGate；Bench/阵法锁扩展；对齐 T1～T6 |
| 2026-08-05 | **实现收口**：§9.1 T1～T6 + 收尾全部完成；`vue-tsc` + `vite build` 通过 |
| 2026-08-05 | **物种与获取途径规划**：§4.4 / §6.3 补种族·图鉴·来源；§9.1 增 N4/N5「设计中」 |

---

*接口与领域以 `M4双线程成长设计.md` 为准；本文只固化前端目录、路由增量与页面落点，并跟踪实现进度。*

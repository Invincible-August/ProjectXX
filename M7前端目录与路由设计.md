# M7 前端目录与路由设计

| 项 | 内容 |
| --- | --- |
| **针对里程碑** | **M7（宗门、社交、经济与商业化）** |
| **设计日期** | 2026-08-11（**v1.0**：依据 `M7宗门社交与经济设计.md` v1.0） |
| **依据** | `M7宗门社交与经济设计.md`；`M6前端目录与路由设计.md`；`M5`/`M4`/`M3`/`M2`/`M1`/`M0` 前端目录设计；`开发计划.md` §9 |
| **原则** | **再拆五条玩法路由**（`/sect` `/market` `/social` `/dual-cultivation` `/shop`）；**聊天坞为横切浮层**（非独立 path，复用 M6 `ws/`）；`/hall` 仍为养成枢纽；M6 大道/道主与 M5 渡劫/轮回保留；**成交/抢传承/双修结算权威来自服务端**；地图/势力频 UI **不做**（→ M9） |

---

## 1. 设计目标

约定 M7 在 `frontend/src` 下的文件增量、**新路由表**、大厅枢纽与聊天坞信息架构，以及与后端宗门 / 交易 / 邮件 / 聊天 / 传承 / 师徒 / 双修 / 商业化接口的对接落点。

M7 可玩范围（L1～L8）：宗门大厅与兑宠、道友、交易行/拍卖/面交、邮件/赠送、五频道聊天坞与传承、师徒、真引渡入口、双修会话与四榜、会员与天道商店壳。  
范围外：地图频/势力频实体 UI、战场队、化身双修、真支付收银台、宗门大比完整赛季、敏感词运营台（见 [`后续待完成.md`](./后续待完成.md)）。

### 1.1 与检定 / 经济纪律（前端）

双修掷骰结果、传承拆分金额、交易成交价、会员剩余时长，**只展示服务端返回字段**；客户端禁止本地 `Math.random` 决定传承份额或双修档位。区间骰若出现在响应中，按 [`骰子系统设计.md`](./骰子系统设计.md) 与 §0.0.2 展示中文说明。

### 1.2 与 WS / 聊天纪律

- 复用 `src/ws/`；新增订阅辅助，**不**新建第二套连接。  
- Pinia 可乐观插入聊天行，必须以 HTTP history / `seq` 校准。  
- `VITE_WS_ENABLED=false` 或推送关闭时：聊天/双修/面交降级轮询；入口不整体隐藏。  
- 聊天坞**永不**挡住挂机主界面操作（可折叠；默认贴边）。

---

## 2. 路由策略（相对 M6：**再增五条玩法 path + 聊天坞横切**）

| path | name | 文件 | M7 变化 |
| --- | --- | --- | --- |
| `/login` | `login` | `views/LoginView.vue` | 无 |
| `/register` | `register` | `views/RegisterView.vue` | 无 |
| `/create-character` | `create-character` | `views/CreateCharacterView.vue` | **扩展**：必选性别 |
| `/hall` | `hall` | `views/HallView.vue` | **枢纽**：角色摘要 → `/character`；社交/宗门/商店入口 |
| `/character` | `character` | `views/CharacterView.vue` | 左属性（折叠详参）/ 右体质装备 + 功法 |
| `/formation` | `formation` | `views/FormationView.vue` | 无强制改版 |
| `/battle` | `battle` | `views/BattleView.vue` | 无 |
| `/avatar` | `avatar` | `views/AvatarView.vue` | 可选：宗门任务入口提示 |
| `/workshop` | `workshop` | `views/WorkshopView.vue` | 无 |
| `/pets` | `pets` | `views/PetsView.vue` | 可选：兑宠跳转宗门 |
| `/tribulation` | `tribulation` | `views/TribulationView.vue` | 无 |
| `/reincarnation` | `reincarnation` | `views/ReincarnationView.vue` | 预览追加贡献归零/道友保留说明 |
| `/dao` | `dao` | `views/DaoView.vue` | 无 |
| `/dao-lord` | `dao-lord` | `views/DaoLordView.vue` | 无 |
| `/dao-lord/arena` | `dao-lord-arena` | （既有） | 无 |
| `/sect` | `sect` | `views/SectView.vue` | **新**：宗门大厅/任务/商店/魂灯/兑宠 |
| `/market` | `market` | `views/MarketView.vue` | **新**：交易行/拍卖/面交 |
| `/social` | `social` | `views/SocialView.vue` | **新**：邮件/赠送/师徒/引渡（道友可跳独立页） |
| `/friends` | `friends` | `views/FriendsView.vue` | **新**：独立道友页（修为/在线/动作） |
| `/party` | `party` | `views/PartyView.vue` | **新**：独立队伍页（建队/邀请/踢人/队友摘要） |
| `/dual-cultivation` | `dual-cultivation` | `views/DualCultivationView.vue` | **新**：双修会话与四榜 |
| `/shop` | `shop` | `views/ShopView.vue` | **新**：会员与天道商店 |
| `/` | — | 重定向 | 逻辑不变；双修进行中可优先进 `/dual-cultivation` |
| `/test` | `test` | `test/apps.vue` | 可选；非验收 |

### 2.1 守卫逻辑（继承并扩展）

| 规则 | 说明 |
| --- | --- |
| 需登录 | 既有玩法页 + 五条新路由均 `requiresAuth` |
| 无角色 | → `/create-character` |
| 有角色访创角 | → `homePathAfterAuth()` |
| 已登录访登录/注册 | → `homePathAfterAuth()` |
| `redirect` 白名单 | `resolveSafeRedirect` **追加** `/sect` `/market` `/social` `/friends` `/party` `/dual-cultivation` `/shop` |
| 待引渡 / 新生 | 仍优先 `/reincarnation`（M5）；待引渡允许 `/social?mode=ferry` 救援 |
| 渡劫中 | 允许社交只读与聊天坞；禁上架/发传承/开双修（页内 + API） |
| 双修进行中 | `homePathAfterAuth` 可优先 `/dual-cultivation?mode=session` |
| 无性别补选 | 访双修/四榜时若缺性别 → 强制补选对话框（或回创角闸）；**不做**整站死锁 |
| 境界硬拦 | **不做**「未入宗禁止进 `/sect`」路由级硬拦；页内空态引导拜入 |

```text
App.vue
  └── 布局壳
        ├── WorldClockBar.vue（保留）
        ├── WsStatusBadge.vue（保留）
        ├── ChatDock.vue（新：横切浮层；不含私聊页签）
        ├── DmDialog.vue（私聊弹窗：左列表右对话）
        └── <RouterView />
              ├── /login | /register | /create-character（性别）
              ├── /hall → HallView.vue
              │     ├── CharacterPanel（compact）
              │     ├── HallDaoGate / HallSocialGate
              │     └── GameLogPanel.vue
              ├── /character → CharacterView.vue
              │     ├── CharacterPanel（详参折叠）
              │     ├── ConstitutionPanel
              │     └── CharacterTechniquesPanel
              ├── /formation | /battle | /avatar | /workshop | /pets
              ├── /tribulation | /reincarnation
              ├── /dao | /dao-lord | /dao-lord/arena
              ├── /sect → SectView.vue
│     ├── SectStatusPanel.vue
│     ├── SectJoinCreatePanel.vue
│     ├── SectOverviewPanel.vue
│     ├── SectCouncilPanel.vue
│     ├── SectQuestPanel.vue
│     ├── SectTreasuryPanel.vue
│     ├── SectScripturePanel.vue
│     ├── SectWorkshopPanel.vue
│     ├── SectFormationPanel.vue
│     ├── SectMinePanel.vue
│     ├── SectHerbPanel.vue
│     ├── SectShopPanel.vue
│     ├── SectSoulLampPanel.vue
│     └── SectPetExchangePanel.vue
              ├── /market → MarketView.vue
              │     ├── TradeListingPanel.vue
              │     ├── AuctionPanel.vue
              │     └── FaceTradePanel.vue
              ├── /social → SocialView.vue
              │     ├── FriendListPanel.vue
              │     ├── MailBoxPanel.vue
              │     ├── GiftPanel.vue
              │     ├── MentorPanel.vue
              │     └── FerryRescuePanel.vue
              ├── /friends → FriendsView.vue
              │     └── FriendListPanel.vue
              ├── /party → PartyView.vue
              │     └── PartyPanel.vue
              ├── /dual-cultivation → DualCultivationView.vue
              │     ├── DualTechniquePanel.vue
              │     ├── DualInvitePanel.vue
              │     ├── DualSessionPanel.vue
              │     ├── DualDiceResult.vue
              │     └── DualRankBoard.vue
              └── /shop → ShopView.vue
                    ├── MembershipPanel.vue
                    └── TiandaoShopPanel.vue
```

### 2.2 为何现在再拆路由

| 理由 | 说明 |
| --- | --- |
| 对齐开发计划 V/W/WD/X | 宗门、交易、双修、商店均为独立决策面，不宜全部塞进大厅折叠 |
| 社交枢纽合一 | 道友/邮件/师徒/引渡同属弱社交，收拢到 `/social` + query，避免路由爆炸 |
| 聊天是横切 | 多页都要发消息/抢传承；ChatDock 挂 App，**不**为聊天单开 path |
| 守卫复用 | 同一套 JWT + `hasCharacter`；仅追加双修会话与待引渡救援分流 |

**不做**：把聊天做成唯一全屏页（允许从坞弹出历史，但主路径是浮层）；不做地图频页（→ M9）。

### 2.3 可选 query（非新 path）

| 用法 | 说明 |
| --- | --- |
| `/sect?mode=join` | 拜入/建宗（须选专精） |
| `/sect?mode=overview` | 总览（等级/设施/buff；默认入宗落点） |
| `/sect?mode=council` | 议事厅（俸禄/任命/公告/战占位） |
| `/sect?mode=quests` | 任务殿 |
| `/sect?mode=treasure` | 藏宝阁 |
| `/sect?mode=scripture` | 藏经阁 |
| `/sect?mode=forge` / `alchemy` / `talisman` | 锻造工坊 / 炼丹阁 / 服务工坊（代工 + 上缴/兑换图纸） |
| `/sect?mode=formation` | 宗门大阵（兑换/上缴；有权选阵/启停/加点） |
| `/sect?mode=mine` | 矿脉（被动入库 + 采矿挂机） |
| `/sect?mode=herbs` | 灵药园（兑换 / 托管种植） |
| `/sect?mode=shop` | 贡献商店（兼容） |
| `/sect?mode=lamps` | 魂灯 |
| `/sect?mode=exchange` | 兑宠 |
| `/sect?mode=status` | 兼容旧链 → 总览面板 |
| `/market?mode=listings` | 交易行（默认） |
| `/market?mode=auction` | 拍卖 |
| `/market?mode=face&peer=<id>` | 面交会话 |
| `/friends` | 独立道友页（修为/在线；私聊/赠礼/跳转队伍/助战/面交） |
| `/party` | 独立队伍页（建队/接受邀请；队长邀请·踢人；队友公开摘要） |
| `/party?invite=<道号>` | 预填队长邀请道号 |
| `/social?mode=friends` | 道友（兼容；可跳 `/friends`） |
| `/social?mode=mail` | 邮件 |
| `/social?mode=gift` | 赠送 |
| `/social?mode=mentor` | 师徒 |
| `/social?mode=ferry` | 引渡救援 |
| `/dual-cultivation?mode=session` | 当前会话 |
| `/dual-cultivation?mode=ranks` | 四榜 |
| `/dual-cultivation?mode=invite&to=<id>` | 邀约预填 |
| `/shop?mode=member` | 会员 |
| `/shop?mode=tiandao` | 天道商店 |
| `/hall?hint=social` | 滚动到社交门闸 |
| ChatDock 内部 | `channel=world\|sect\|mentor\|party`；`heritage=<id>` 高亮卡片 |
| DmDialog | 独立私聊弹窗；按会话选人；服务端持久最近 `dm_history_limit` 条 |

### 2.4 `homePathAfterAuth` 扩展

```text
if !hasCharacter → /create-character
if status == awaiting_ferry → /reincarnation?mode=ferry
   （允许用户从门闸进 /social?mode=ferry）
if status == reincarnating → /reincarnation?mode=newborn
if status == tribulation → /tribulation
if active_dual_session → /dual-cultivation?mode=session
if active_dao_contest_match → /dao-lord/…（M6 既有优先于社交）
→ /hall
```

### 2.5 `PLAY_ROUTE_NAMES` / 待引渡拦截

- 追加：`sect`、`market`、`social`、`friends`、`party`、`dual-cultivation`、`shop`。  
- `FERRY_BLOCKED_ROUTES`：待引渡时 **禁** `market` 上架类操作、`dual-cultivation` 开局、`sect` 建宗；**允许** `/social?mode=ferry` 与只读邮件；`/sect` 魂灯只读可开。  
- 定案：待引渡 **允许** ChatDock 只读收消息；**禁** 发传承与上架。
- ChatDock：**不**承载建队/邀请/离队；组队管理在 `/party`；待邀可显示角标链到队伍页。
- ChatDock：**不**承载私聊页签；私聊用 `DmDialog`（道友「私聊」/ 坞未读角标打开）；私聊服务端持久。

---

## 3. `src` 目录增量

M0～M6 目录职责不变；M7 **追加文件与路由**，不推翻骨架。

```text
src/
├── api/
│   ├── sect.ts
│   ├── friends.ts
│   ├── trade.ts
│   ├── mail.ts
│   ├── chat.ts
│   ├── heritage.ts
│   ├── mentor.ts
│   ├── dualCultivation.ts
│   ├── commerce.ts
│   ├── ferry.ts                 # 扩展救援
│   ├── character.ts             # 性别 / social_badges
│   └── gm.ts                    # 扩展社交/经济 GM
├── ws/
│   ├── client.ts                # 保留；订阅扩展
│   ├── protocol.ts              # 增补 chat/heritage/dual/trade.face
│   └── channels.ts              # 新：频道 subscribe/unsubscribe 辅助
├── components/
│   ├── layout/
│   │   ├── ChatDock.vue         # 横切浮层（无私聊页签）
│   │   ├── ChatChannelTabs.vue
│   │   ├── ChatMessageList.vue
│   │   ├── ChatComposer.vue
│   │   └── HeritageCard.vue     # 发/抢机缘（灵石或背包点选）
│   ├── hall/
│   │   └── HallSocialGate.vue   # 新
│   ├── sect/
│   │   ├── SectStatusPanel.vue
│   │   ├── SectJoinCreatePanel.vue
│   │   ├── SectQuestPanel.vue
│   │   ├── SectShopPanel.vue
│   │   ├── SectSoulLampPanel.vue
│   │   └── SectPetExchangePanel.vue
│   ├── market/
│   │   ├── TradeListingPanel.vue
│   │   ├── AuctionPanel.vue
│   │   └── FaceTradePanel.vue
│   ├── social/
│   │   ├── FriendListPanel.vue
│   │   ├── DmDialog.vue         # 私聊弹窗
│   │   ├── MailBoxPanel.vue
│   │   ├── GiftPanel.vue
│   │   ├── MentorPanel.vue
│   │   └── FerryRescuePanel.vue
│   ├── party/
│   │   └── PartyPanel.vue
│   ├── dual/
│   │   ├── DualTechniquePanel.vue
│   │   ├── DualInvitePanel.vue
│   │   ├── DualSessionPanel.vue
│   │   ├── DualDiceResult.vue
│   │   └── DualRankBoard.vue
│   ├── shop/
│   │   ├── MembershipPanel.vue
│   │   └── TiandaoShopPanel.vue
│   └── create/
│       └── GenderSelectField.vue  # 创角性别
├── stores/
│   ├── sect.ts
│   ├── friends.ts
│   ├── trade.ts
│   ├── mail.ts
│   ├── chat.ts                  # 频道/未读/历史
│   ├── heritage.ts
│   ├── mentor.ts
│   ├── dualCultivation.ts
│   ├── commerce.ts
│   ├── character.ts             # 扩展 badges / gender / sect
│   └── ws.ts                    # 扩展订阅
├── types/
│   ├── sect.ts / trade.ts / mail.ts / chat.ts
│   ├── heritage.ts / mentor.ts / dualCultivation.ts / commerce.ts
├── utils/
│   ├── safeRedirect.ts          # 白名单扩展
│   └── socialLabel.ts           # 频道/错误码→中文（优先信服务端）
├── router/
│   └── index.ts                 # 登记五路由；双修回跳
└── views/
    ├── HallView.vue
    ├── CreateCharacterView.vue
    ├── SectView.vue             # 新
    ├── MarketView.vue           # 新
    ├── SocialView.vue           # 新
    ├── DualCultivationView.vue  # 新
    ├── ShopView.vue             # 新
    └── …（其余保留）
```

### 3.1 命名约定（继承）

- 页面：`XxxView.vue`（M7 **新增** Sect / Market / Social / DualCultivation / Shop）
- 零件：`XxxPanel.vue` / `XxxCard.vue` / `XxxGate.vue` / `XxxDock.vue`；按域放 `components/sect|market|social|dual|shop|layout|hall/`
- API 文件与后端模块同名（`dualCultivation.ts` ↔ `dual_cultivation`）
- 路由 path：小写短横线（`/dual-cultivation`）
- 聊天组件放 `layout/`（横切），**不要**塞进单一玩法域导致循环依赖

### 3.2 子目录说明

- `views/` 仍扁平（再多 5 个整页；若后续膨胀再按域分子目录，M7 不强制）。  
- `ws/channels.ts` 与 `api/chat.ts` 分工：前者订阅生命周期，后者 HTTP。  
- ChatDock 在 `meta.showWorldBar` 玩法布局挂载；登录/注册/创角不挂。

### 3.3 聊天坞挂载

| 方案 | 说明 | M7 选择 |
| --- | --- | --- |
| 独立 `/chat` 全屏页 | 打断挂机 | 不采用作主路径 |
| 仅宗门页内嵌 | 世界/私聊难用 | 不采用 |
| App 级可折叠浮层 | 多页可达、不挡主操作 | **采用** |
| 未读角标 | 顶栏或坞折叠钮 | 数字 + 中文 tooltip |

---

## 4. 信息架构

### 4.1 大厅（枢纽）布局增量

```text
┌──────────────────────────────────────────────────────────────┐
│ WorldClockBar · WsStatusBadge · Mail/Chat 未读角标            │
│ AuthSessionBar · 修仙大厅 · 养成枢纽                          │
│ [角色][布阵][战斗][宗门][社交][道友][商店]                    │
├────────────────────────────┬─────────────────────────────────┤
│ CharacterPanel             │ GameLogPanel                    │
│ Idle / Allocate / 突破     │                                 │
│ HallBattleGate             │                                 │
│ （已移除 DEV 调参 / 教学战 / │                                 │
│  M4～M7 侧栏门闸）          │                                 │
└────────────────────────────┴─────────────────────────────────┘
右下/侧边：ChatDock（折叠条 → 展开频道）
```

### 4.2 宗门页布局

```text
┌────────────────────────────────────────────────────────────┐
│ ← 回大厅 · 宗门                                             │
│ SectStatusPanel：宗名 · 职位 · 贡献 · 祖师功能锁说明         │
├────────────────────────────────────────────────────────────┤
│ mode=join：拜入 NPC / 自建（费用中文）                       │
│ mode=quests：本体/化身任务列表                               │
│ mode=shop：贡献商店                                          │
│ mode=lamps：魂灯（散修空态：「入宗后可查看同门状态」）         │
│ mode=exchange：兑宠白名单                                    │
└────────────────────────────────────────────────────────────┘
```

### 4.3 商店中心 / 坊市 / 拍卖行

```text
/shop?mode=bazaar   NPC 坊市：固定货架购买 + 玩家按收购价出售换灵石
/shop?mode=auction  拍卖行（嵌入）：一口价 / 竞拍 / 面交（sub=listings|auction|face）
/shop?mode=tiandao  天道商店：会员 / 货架 / 沙盒
/market             拍卖行独立入口（兼容深链；mode=listings|auction|face）
```

### 4.4 社交页布局

```text
mode=friends | party | dual | mail | gift | mentor | ferry
道友：列表 · 私聊 · 赠礼 · 面交入口
队伍：建队 / 邀请 / 踢人
双修：会话 + 四榜（DualCultivationPanel）
邮件：列表 · 读信 · 一键领附件（红点）
师徒：申请 · 名录 · 任务 · 传功
引渡：可选道友/同门 · 成本对比自救
```

### 4.5 双修页布局

```text
左：功法选择（mode 中文：双人同增 / 传修为）
中：邀约道友 · 会话状态 · 掷骰按钮/结果动画
右：四榜 Tab（男一号|男零号|女一号|女零号）· 本人名次
空态无性别：「请先补全道途阴阳（性别）」
亦可从 /social?mode=dual 进入同一工作台
```

### 4.6 商店页布局（已并入 §4.3）

```text
见 §4.3；旧 member/tiandao 深链兼容跳转天道商店 Tab
```

### 4.7 聊天坞与机缘

```text
折叠：[聊] 未读 n
展开：
  Tabs：世界 | 宗门 | 师承 | 队伍
  （私聊 → DmDialog 弹窗）
  MessageList（中文名/时间）
  机缘卡片区：发机缘（灵石 | 背包点选）· 进行中包 · [开缘]领取
  Composer：输入 · 限速提示
```

> 落地补充：机缘卡片并入消息流时间线；消息区可滚动；新内容自底部顶上。

### 4.8 组件职责

| 组件 | 职责 | 不负责 |
| --- | --- | --- |
| `HallSocialGate` | 入口跳转 + 未读角标 | 执行成交 |
| `SectStatusPanel` | 展示权威宗门摘要 | 本地改贡献 |
| `SectJoinCreatePanel` | 提交拜入/建宗 | 伪造解锁 |
| `SectPetExchangePanel` | 兑宠确认 | 改 catalog |
| `TradeListingPanel` | 上架/购买 | 客户端改手续费 |
| `FaceTradePanel` | 锁定/确认 UI | 本地原子交换 |
| `MailBoxPanel` | 列表/领取 | 伪造附件 |
| `GiftPanel` | 从背包选赠 | 绕过限额 |
| `MentorPanel` | 申请/任务/传功 | 自算奖励 |
| `FerryRescuePanel` | 发起救援 | 本地清 ferry |
| `ChatDock` | 频道切换/未读/降级轮询 | 敏感词终审库 |
| `HeritageCard` | 发机缘（灵石/背包点选）/抢/结果摘要 | 本地拆随机份额；手填 item_id |
| `DualSessionPanel` | 会话态机展示 | 本地 settle |
| `DualDiceResult` | 展示服务端 dice 块 | `Math.random` |
| `DualRankBoard` | 四榜只读 | 直写榜分 |
| `MembershipPanel` | 开通/展示帽 | 本地改离线公式 |
| `SectView` 等 View | 编排拉数、日志 | 大段公式 |

### 4.9 关键时序

**交易行购买**

```text
/market → POST buy → 刷新背包/灵石 → GameLog「购得××」
  → 失败绑定：40111 中文
```

**面交**

```text
invite → 双方 lock → confirm → committed
  → WS trade.face.state 或轮询
```

**聊天 + 传承**

```text
ChatDock → POST /chat/send → ws chat.message
发传承 → POST /heritage → heritage.created
开缘 → POST claim → 入包摘要；非成员 40140
```

**双修**

```text
选功法 → invite → 对方 confirm → roll（展示 dice）→ settle
  → 刷新修为池与四榜
```

**引渡**

```text
待引渡角色：/social?mode=ferry
  → 道友选人 → POST /ferry/rescue
  → 成功回大厅；日志中文
```

---

## 5. API / Store / WS 对应

| 文件 | 函数（示意） |
| --- | --- |
| `api/sect.ts` | `fetchSectMe` · `joinSect` · `createSect` · `fetchQuests` · `exchangePet` |
| `api/trade.ts` | listings CRUD · auction · face session |
| `api/mail.ts` | `fetchMail` · `claimMail` · `sendGift` |
| `api/chat.ts` | `fetchChannels` · `fetchHistory` · `sendMessage` · `partyCreateLeave` |
| `api/heritage.ts` | `createHeritage` · `claimHeritage` |
| `api/mentor.ts` | apply / me / quests / graduate |
| `api/dualCultivation.ts` | invite / confirm / roll / settle / ranks |
| `api/commerce.ts` | shop / membership / sandboxGrant |
| `ws/channels.ts` | `subscribeChannel` · `unsubscribeChannel` |

**`useChatStore`**

| state / action | 说明 |
| --- | --- |
| `activeChannel` / `messages` / `unread` | |
| `send` / `loadHistory` / `applyPush` / `startPollingFallback` | |

**`useHeritageStore`**：进行中包列表；claim；过期提示。

**`useDualCultivationStore`**：`session` / `lastDice` / `ranks`；invite/confirm/roll/settle。

**`useMailStore`**：列表；claim；badge 同步 `character.social_badges`。

**`useCharacterStore`**：识别 `gender`、`sect`、`membership`、`social_badges`。

---

## 6. 交互与文案约定

### 6.1 宗门

- 散修空态：「散修可通关核心循环；入宗可获更高传承与魂灯便利」。  
- 功能锁：「祖师修为不足，×× 尚未开启」（禁止只甩英文 key）。

### 6.2 交易

- 易物手续费：**显性**展示高于灵石一口价。  
- 绑定物：按钮禁用 + 「绑定灵物不可上架」。

### 6.3 聊天 / 机缘

- 频道 Tab 用中文：世界 / 宗门 / 私聊 / 师承 / 队伍。  
- 机缘：「发机缘」「开缘」；模式「拼手气」「定额均分」；内容「灵石」或「背包物品」点选（禁手填 id）。  
- 过期：「未领机缘已退回，请查收邮件」。

### 6.4 双修

- 功法流向中文：「双人同增」「甲传乙修为」。  
- 掷骰结果展示档位与时长来自服务端。  
- 四榜名：男修一号榜 / 男修零号榜 / 女修一号榜 / 女修零号榜（可配置显示名）。

### 6.5 商业化

- 会员：「过期后挂机有效时长回落十二时辰」。  
- 商店页固定边界句：**不出售指定本命大道**。  
- 沙盒加点仅 DEV 或显式开关时可见。

### 6.6 UI 基调

- 继承 M0～M6：功能优先；Element Plus；不为 M7 做终局社交特效。  
- 至少 2～3 处轻量动效：传承卡片滑入、双修掷骰翻面、聊天未读脉冲。

### 6.7 GM（DEV）

- 灌贡献、加好友、塞邮件、推聊天、开双修会话、加天道点、改性别（审计）。  
- 仅 `import.meta.env.DEV`。

---

## 7. 类型对齐（要点）

```ts
export type Gender = 'male' | 'female'

export type ChatChannelType =
  | 'world'
  | 'sect'
  | 'dm'
  | 'mentor'
  | 'party'
  | 'region' // reserved M9
  | 'faction' // reserved M9

export interface ChatMessagePublic {
  id: number
  channel_type: ChatChannelType
  channel_ref: string
  sender_name: string
  content: string
  created_at: string
  seq: number
}

export type HeritageMode = 'random' | 'fixed'

export interface HeritagePublic {
  id: number
  mode: HeritageMode
  mode_label: string
  channel_type: ChatChannelType
  remain: number
  total: number
  expires_at: string
  payload_summary: string // 中文摘要
}

export type DualMode = 'mutual_gain' | 'transfer'

export interface DualSessionPublic {
  id: number
  phase: 'inviting' | 'confirmed' | 'running' | 'settled' | 'cancelled'
  technique_label: string
  mode: DualMode
  mode_label: string
  dice?: {
    purpose: 'dual_cultivation'
    roll: number
    effect_tier_label: string
    duration_label: string
  }
}

export interface DualRankBoardPublic {
  board:
    | 'male_primary'
    | 'male_secondary'
    | 'female_primary'
    | 'female_secondary'
  board_label: string
  entries: { rank: number; name: string; score: number }[]
  me?: { rank: number; score: number }
}

export interface MembershipPublic {
  tier: 'none' | 'i' | 'ii'
  tier_label: string
  idle_cap_hours: number
  expires_at: string | null
}
```

---

## 8. 与后续里程碑

| 里程碑 | 对本设计的影响 |
| --- | --- |
| **M7** | 本文范围；按上表建齐五路由 + ChatDock |
| **M9** | ChatDock 增地图/势力 Tab；引渡同图去桩；魂灯接真坐标 |
| **M11** | 宗门战入口可挂 `/sect`；战场队 ≠ 最小 party |
| **M13** | 真支付替换沙盒；双修/交易数值满表 |
| 延后项 | 一律先查 [`后续待完成.md`](./后续待完成.md) |

---

## 9. 开发进度与步骤（对照 M7）

> 本节随实现同步更新；接口以 `M7宗门社交与经济设计.md` 为准。

### 9.1 总览（对齐核心设计 §14 L1～L8）

| 步 | 内容 | 状态 | 说明 |
| --- | --- | --- | --- |
| 0 | 设计文档（本文 + 核心骨架） | **已完成** | 2026-08-11 v1.0 |
| **L1** | 类型 + `api/sect` + SectView 壳 | **已完成** | 2026-08-11 |
| **L2** | 道友 + MarketView 交易/面交 | **已完成（前端）** | `/market` `/social?mode=friends` |
| **L3** | 邮件/赠送 + Social 基础 | **已完成** | 2026-08-11 |
| **L4** | ChatDock + 五频道 + party | **已完成** | 2026-08-11 |
| **L5** | 机缘发/抢 UI（背包点选） | **已完成** | 2026-08-11 |
| **L6** | 师徒 + 引渡面板 | **已完成** | 2026-08-11 |
| **L7** | 创角色性别 + DualCultivationView | **已完成** | 2026-08-11 |
| **L8** | ShopView + HallSocialGate + 守卫 + GM + 总验收 | **已完成** | 2026-08-11 |

### 9.2 本地联调口令（实现后）

```powershell
# 后端
cd backend
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000

# 前端
cd frontend
npm run dev
```

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/sect/me` | GET | 我的宗门 |
| `/api/v1/trade/listings` | GET/POST | 交易行 |
| `/api/v1/mail` | GET | 邮件 |
| `/api/v1/chat/history` | GET | 历史 |
| `/api/v1/heritage` | POST | 发传承 |
| `/api/v1/dual/ranks` | GET | 四榜 |
| `/api/v1/commerce/membership` | POST | 会员 |
| `/api/v1/ws` | WS | 复用 M6 通道 |

---

## 10. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-12 | **私聊弹窗**：`DmDialog`；ChatDock 去 dm 页签；`dm_history_limit` 持久裁剪 |
| 2026-08-12 | **独立队伍页**：`/party` + `PartyPanel`；ChatDock 去组队控件；大厅「队伍」入口；队长踢人/队友摘要 |
| 2026-08-11 | **L8 前端落地**：`ShopView` 会员/货架/沙盒；五路由齐 |
| 2026-08-11 | **L7 前端落地**：创角性别；`DualCultivationView` 会话/四榜 |
| 2026-08-11 | **L6 前端落地**：`MentorPanel` / `FerryRescuePanel`；`/social?mode=mentor\|ferry`；待引渡「去求援」 |
| 2026-08-11 | 初稿 **v1.0**：相对 M6 增五路由 + ChatDock 横切；对齐 L1～L8；创角性别；待引渡救援分流 |

---

*玩法与接口以 `M7宗门社交与经济设计.md` 为准；排期以 `开发计划.md` §9 为准；延后以 `后续待完成.md` 为准。*

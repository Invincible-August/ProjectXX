# M0 前端目录与路由设计

| 项 | 内容 |
| --- | --- |
| **针对里程碑** | **M0（工程骨架）** |
| **设计日期** | 2026-07-27 |
| **依据** | `M0工程骨架设计.md` §2.3 / §6；`开发计划.md` §2 |
| **原则** | 功能壳优先；UI 用原生按钮即可，美术后置 |

---

## 1. 设计目标

约定 `frontend/src` 下文件夹名称与职责，以及 M0 各路由对应的页面文件，便于边学边实现、前后端按契约联调。

M0 可玩范围：注册 / 登录 / 创建角色 / 空大厅读面板。  
范围外：挂机结算、突破、战斗、WebSocket、终局美术。

---

## 2. `src` 目录结构

```text
src/
├── api/           # 请求后端的函数（按模块拆文件）
├── assets/        # 图片、图标（M0 可几乎为空）
├── components/    # 可复用零件（不是整页）
├── router/        # 路由表（通常仅 index.ts）
├── stores/        # Pinia 全局状态
├── types/         # TypeScript 类型
├── utils/         # 小工具（如 token 存取）
├── views/         # 整页组件（与路由一一对应）
├── test/          # 学习/演示页（可选，不纳入 M0 验收）
├── App.vue        # 根壳：放置 <RouterView />
├── main.ts        # 入口：挂载 Vue、Router、Pinia
└── style.css      # 全局样式（M0 保持轻量）
```

### 2.1 各目录职责

| 文件夹 | 放什么 | 不放什么 |
| --- | --- | --- |
| `views/` | 一整屏页面 | 小按钮、小面板 |
| `components/` | 多页复用的 UI 块 | 带独立路由的整页 |
| `api/` | `http.get` / `http.post` 封装 | 页面模板 |
| `stores/` | token、当前用户、角色等跨页状态 | 与 UI 强绑定的一次性逻辑（可先放页面） |
| `router/` | 路由表配置 | 页面 `.vue` 本体 |
| `types/` | 接口与领域类型 | 业务请求实现 |
| `utils/` | 如 `storage.ts` | 页面 |
| `assets/` | 静态资源 | 业务代码 |
| `test/` | 学习用演示（如资源/请求练习） | 正式玩法验收页 |

### 2.2 命名约定

- 页面文件：`XxxView.vue`
- 零件组件：`XxxPanel.vue` 或功能名
- 路由 path：小写 + 短横线（如 `/create-character`）
- 目录名：全小写英文，不用中文路径

---

## 3. M0 路由与页面文件

路由统一登记在 `router/index.ts`；页面组件统一放在 `views/`（不再按功能建子目录，M0 体量不需要）。

| path | name | 文件 | 守卫（概要） | 职责 |
| --- | --- | --- | --- | --- |
| `/login` | `login` | `views/LoginView.vue` | 已登录则去大厅或创角 | 登录表单 |
| `/register` | `register` | `views/RegisterView.vue` | 同上 | 注册表单；成功后跳登录 |
| `/create-character` | `create-character` | `views/CreateCharacterView.vue` | 需登录；已有角色则去大厅 | 输入角色名并创建 |
| `/hall` | `hall` | `views/HallView.vue` | 需登录且已有角色 | 展示角色面板 + 挂机占位 |
| `/` | — | 无独立页面 | 重定向 | 未登录→登录；无角色→创角；有角色→大厅 |

### 3.1 可选学习路由（非 M0 验收）

| path | 文件 | 说明 |
| --- | --- | --- |
| `/test` | `test/test.vue` | 本地学习资源与请求；可随时移除 |

### 3.2 大厅使用的组件（非路由）

| 文件 | 说明 |
| --- | --- |
| `components/CharacterPanel.vue` | 角色属性窗：道号、境界、灵石、状态、挂机方向、修为等 |
| `components/IdlePanelPlaceholder.vue` | 挂机区占位：「挂机系统将在 M1 开放」 |
| `components/GameLogPanel.vue` | 事件日志窗：进入大厅欢迎语与面板摘要（M0 客户端本地流） |

### 3.3 关系示意

```text
App.vue
  └── <RouterView />
        ├── /login            → LoginView.vue
        ├── /register         → RegisterView.vue
        ├── /create-character → CreateCharacterView.vue
        └── /hall             → HallView.vue
                                  ├── CharacterPanel.vue（属性）
                                  ├── IdlePanelPlaceholder.vue（挂机占位）
                                  └── GameLogPanel.vue（事件日志）
```

---

## 4. `api` / `stores` 与页面对应

| 文件 | 主要服务 |
| --- | --- |
| `api/http.ts` | Axios 实例（全站）；Bearer 注入；401 自动 refresh 一次 |
| `api/auth.ts` | 注册（含核验字段 + 三票）、登录、refresh、`/auth/me` |
| `api/verification.ts` | modes、短信/邮件发码与确认、身份证提交 |
| `api/character.ts` | 创建角色、获取我的角色 |
| `stores/auth.ts` | access/refresh token、当前用户、`loadFromStorage` / `login` / `refreshTokens` |
| `stores/character.ts` | 当前角色、loading |
| `utils/storage.ts` | 按「记住登录」在 localStorage / sessionStorage 间持久化双令牌 |

---

## 5. UI 约定（M0）

- 不引入重型 UI 框架；不追求古风终局美术。
- 交互控件使用原生即可，例如 `<button></button>`、`<input>`（当前登录页已用 Element Plus）。
- 能完成联调与验收即可；视觉迭代放在功能稳定之后。

---

## 6. 与后续里程碑

| 里程碑 | 对本设计的影响 |
| --- | --- |
| **M0** | 本文范围；按上表建齐目录与路由即可 |
| **M1+** | 可在 `views/` 增加挂机/突破/战斗页；`api/`、`stores/` 按模块追加文件；**不必推翻**本文目录骨架 |

---

## 7. 开发进度与步骤（对照 M0）

> 本节随实现同步更新；接口字段以 `M0工程骨架设计.md` 为准。

### 7.1 总览

| 步 | 内容 | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 脚手架 + health 联调 | **已完成** | `GET /health`、`LoginView`「检测状态」 |
| 2 | 注册 API + 登录页表单壳 | **已完成** | `POST /auth/register`；同页登录/注册切换 |
| 3 | 登录 / refresh / me + 保存登录态 | **已完成（2026-07-28）** | 见 §7.2 |
| 3.5 | 注册核验（邮箱/手机/身份证 A·B·C）+ 超级密码 | **已完成（2026-07-28）** | 后端见 §7.3；前端注册表单已对接发码/三票（§7.3 前端清单） |
| 4 | 创角 API + 创角页 + 路由守卫 | **已完成（2026-08-03）** | 后端 `POST /characters` + 前端真实联调；已移除 Demo |
| 5 | 大厅属性窗 + 日志窗绑真实字段 | **已完成（2026-08-03）** | `GET /characters/me`；HallView 左属性/挂机、右日志 |
| 6 | 守卫细化、文案、总验收 | **部分完成** | 创角/大厅按 `hasCharacter` 分流；可做总验收打勾 |

### 7.2 步骤 3 完成清单（登录与保存登录状态）

**后端（`backend/app`）**

| 项 | 状态 | 落点 |
| --- | --- | --- |
| JWT 编解码（access / refresh） | ✅ | `core/security.py`（PyJWT + HS256） |
| `POST /api/v1/auth/login` | ✅ | 返回双令牌 + `user`；支持 `remember_me` |
| `POST /api/v1/auth/refresh` | ✅ | body 带 `refresh_token`；错误码 `40101` |
| `GET /api/v1/auth/me` | ✅ | Bearer；含 `has_character`（角色表未上线前为 `false`） |
| `get_current_user` 依赖 | ✅ | `core/deps.py` |
| 密码错误不枚举账号 | ✅ | 统一 `40002` |
| 禁用账号 | ✅ | `40300` |

**前端（保存登录状态）**

| 项 | 状态 | 落点 |
| --- | --- | --- |
| 登录勾选「记住登录」 | ✅ | `LoginView` → `remember_me` |
| 双令牌持久化 | ✅ | `utils/storage.ts`：记住→localStorage；否则→sessionStorage |
| Pinia `useAuthStore` | ✅ | `loadFromStorage` / `login` / `refreshTokens` / `logout` |
| 启动恢复令牌 | ✅ | `main.ts` 挂载前 `loadFromStorage()` |
| Axios 401 → refresh 一次 | ✅ | `api/http.ts` |
| 依赖 Pinia | ✅ | `npm` 已加入 `pinia` |

**「保存登录状态」工作原理（简图）**

```text
登录成功
  ├─ remember_me=true  → 长效 refresh（默认 14 天）+ localStorage
  └─ remember_me=false → 短效 refresh（1 天）+ sessionStorage
        ↓
刷新页面 → main.ts loadFromStorage → 请求带 Bearer
        ↓
access 过期 → 401 → /auth/refresh → 写入新双令牌 → 重试原请求
        ↓
refresh 也过期 → 清 Storage → 需重新登录
```

### 7.3 步骤 3.5 完成清单（核验 + 超级密码，2026-07-28）

**后端（`backend/app`）**

| 项 | 状态 | 落点 |
| --- | --- | --- |
| Provider 层（A format / B·C stub / 短信邮件 debug） | ✅ | `services/verification/providers/` |
| 编排服务（发码 / 确认 / ticket / 注册校验） | ✅ | `VerificationService`（`services/verification/service.py`） |
| `GET /api/v1/verification/modes` | ✅ | `api/verification.py` |
| `POST /api/v1/verification/sms/send\|confirm` | ✅ | 确认 → `sms_ticket` |
| `POST /api/v1/verification/email/send\|confirm` | ✅ | 确认 → `email_ticket` |
| `POST /api/v1/verification/id/submit` | ✅ | → `id_ticket` |
| 注册消费三票 + email/phone 查重 | ✅ | `AuthService`（`services/auth_service.py`）→ `40013` / `40017` |
| `SUPER_PASSWORD` 超级密码登录 | ✅ | WARNING 审计；禁用号 `40300` |
| 集成测试 | ✅ | `tests/test_verification_auth.py`（7）+ `tests/test_id_format.py`（13） |
| Provider Protocol + 应用服务类（OOP） | ✅ | `verification/protocols.py`；`VerificationService` / `AuthService`；路由 `Depends`（2026-08-04） |

**前端（`LoginView` 注册表单，2026-07-28）**

| 项 | 状态 | 落点 |
| --- | --- | --- |
| 邮箱 / 手机 / 实名 / 身份证字段 | ✅ | `views/LoginView.vue` |
| 「发送验证码」+ 60s 冷却 | ✅ | 调 `api/verification` send |
| 提交前 confirm + id/submit 换三票再注册 | ✅ | `obtainRegisterTickets` → `registerApi` |
| 拉取 `/verification/modes` 提示 DEBUG | ✅ | 进入注册模式时 |
| `api/verification.ts` / `types/verification.ts` | ✅ | 新建 |

**规格与设计**

- 全文：`docs/superpowers/specs/2026-07-28-verification-super-password-design.md`（状态：**已实现**）
- DEBUG 跳过：后端 `DEBUG=true` 时可省略 ticket；前端联调仍要求填码（可用 `000000`）

**本地联调 API 速查**

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/verification/modes` | GET | 当前 debug / id_verify_mode / providers |
| `/api/v1/verification/sms/send` | POST | `{"phone":"13800138000"}` |
| `/api/v1/verification/sms/confirm` | POST | `{"phone":"...","code":"000000"}` → ticket |
| `/api/v1/verification/email/send` | POST | `{"email":"a@b.com"}` |
| `/api/v1/verification/email/confirm` | POST | `{"email":"...","code":"000000"}` → ticket |
| `/api/v1/verification/id/submit` | POST | `{"real_name":"...","id_card":"..."}` → ticket |
| `/api/v1/auth/register` | POST | 正式需三票 + email/phone/实名；DEBUG 可省略 |
| `/api/v1/auth/login` | POST | 可用 `SUPER_PASSWORD` 作为 password |

### 7.4 下一步建议

1. ~~后端创角 + 关 Demo~~ **已完成（2026-08-03）**。  
2. 按 `M0工程骨架设计.md` §9 总验收清单打勾。  
3. ~~进入 M1 设计~~ **已完成（2026-08-03）**：见 `M1核心循环设计.md` / `M1前端目录与路由设计.md` / `后续待完成.md`；下一步按设计实现修灵挂机。
### 7.5 本地联调口令（步骤 3）

```powershell
# 后端
cd backend
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000

# 前端
cd frontend
npm run dev
```

| 接口 | 方法 | Body / Header |
| --- | --- | --- |
| `/api/v1/auth/register` | POST | `{"username":"player01","password":"password123"}` |
| `/api/v1/auth/login` | POST | `{"username":"player01","password":"password123","remember_me":true}` |
| `/api/v1/auth/refresh` | POST | `{"refresh_token":"<jwt>"}` |
| `/api/v1/auth/me` | GET | `Authorization: Bearer <access>` |

---

## 8. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-07-27 | 初稿：M0 `src` 目录职责与路由—文件对照 |
| 2026-07-28 | 增补 §7 开发进度：登录 / refresh / me / 记住登录已完成；下一步创角 |
| 2026-07-28 | §7 步骤 3.5 标 **已完成**：核验 API + 注册改造 + 超级密码；§7.3 完成清单与 API 速查 |
| 2026-07-28 | 前端注册表单对接邮箱/手机发码与身份证；新增 `api/verification.ts` |
| 2026-08-03 | 创角/大厅正式联调：后端 characters API；大厅属性窗+事件日志；移除 Demo |
| 2026-08-04 | 后端 OOP 同步：鉴权/核验改为 Application Service + Protocol；前端 API 契约不变 |

---

*玩法与接口细节以 `M0工程骨架设计.md` 为准；本文只固化前端目录与路由落点，并跟踪实现进度。*

---
description: Project修仙 横切强制信条（后台适配、中文、显性、竖切）
alwaysApply: true
---

# 核心信条

权威：`设计文档/开发计划.md` §0.0 / §0.0.2 / §0.1 / §0.7。总索引：`约束/AGENTS.md`。

## 必须

- 新配置进可后台 CRUD 的注册表/覆盖层（YAML 也须 schema+校验器）；玩家服只读 `GameConfigBundle`
- 玩家可见文案、战报、设计文档给人读的段落用中文；内容 id 配 `label_zh`
- 加成/乘区/概率对玩家可见：有效值 + 来源或 catalog；预览与 settle 同一套服务端公式
- 竖切闭环后再下一块：后端可结算 + 前端可操作 + 联调
- 里程碑验收同步 `README.md` 与 `CHANGELOG.md`

## 禁止

- `if species_id == "xxx"` 或前端写死下拉枚举当内容源
- 把英文 id（如 `mist` / `seal`）直接甩给玩家或战报播放器
- 「只有结算、没有展示」的半截功能；前后端各算一套展示公式
- 整端堆完再对接；把 GM 面板当运营后台
- 用户未要求时 `git commit` / `git push`
- 拍板前开 M9（见开发计划 §20）

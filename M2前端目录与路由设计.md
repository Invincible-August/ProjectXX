# M2 前端目录与路由设计

| 项 | 内容 |
| --- | --- |
| **针对里程碑** | **M2（成长深度）** |
| **设计日期** | 2026-08-04 |
| **依据** | `M2成长深度设计.md`；`M1前端目录与路由设计.md`；`M0前端目录与路由设计.md`；`开发计划.md` §4 |
| **原则** | **不新增玩法路由**；在 `/hall` 内扩展面板与对话框；目录骨架继承 M0/M1 |

---

## 1. 设计目标

约定 M2 在 `frontend/src` 下的文件增量、大厅信息架构，以及与后端离线领取 / 分配 / 体质 / 品阶接口的对接落点。

M2 可玩范围：大厅内完成三向修炼、离线领取、资源分配、体质镶嵌、跨境品阶展示。  
范围外：独立布阵页、棋盘、化身、配方工房、真 WS（见 [`后续待完成.md`](./后续待完成.md)）。

---

## 2. 路由策略（相对 M0/M1 无新增玩法 path）

| path | name | 文件 | M2 变化 |
| --- | --- | --- | --- |
| `/login` | `login` | `views/LoginView.vue` | 无 |
| `/register` | `register` | `views/RegisterView.vue` | 无（若仍合并进 LoginView 则保持现状） |
| `/create-character` | `create-character` | `views/CreateCharacterView.vue` | 无 |
| `/hall` | `hall` | `views/HallView.vue` | **主战场**：三向 / 离线弹窗 / 分配 / 体质 / 品阶 |
| `/` | — | 重定向 | 逻辑不变 |
| `/test` | `test` | `test/apps.vue` | 可选学习页；非验收 |

守卫逻辑不变：需登录；无角色→创角；有角色→大厅。

```text
App.vue
  └── <RouterView />
        └── /hall → HallView.vue
              ├── CharacterPanel.vue         （增强：池/境界进度/品阶/神通空槽）
              ├── IdlePanel.vue              （三向开放 + 速率/耗石）
              ├── AllocatePanel.vue          （新：修为/炼体/制造业分配）
              ├── ConstitutionPanel.vue      （新：背包 + 主副格）
              ├── BreakthroughPanel.vue      （增强：读 realm_progress；品阶预告文案）
              ├── BattlePanel.vue            （复用；战力展示可跟品阶/体质）
              ├── GameLogPanel.vue           （追加离线/分配/体质/品阶事件）
              ├── OfflineClaimDialog.vue     （新：预览 + 一键领取）
              ├── BreakthroughResultDialog.vue（增强：展示本次品阶）
              └── GmDevPanel.vue             （DEV：realm_progress / 池 / 会员档 / 清 pending）
```

### 2.1 为何仍不拆路由

| 理由 | 说明 |
| --- | --- |
| 与 M1-D31 一致 | M2 若大厅挤，优先对话框/折叠区，不先开 `/idle` `/constitution` |
| 联调成本 | 守卫与会话恢复已稳定；少路由少分叉 |
| M3 再评估 | 布阵编辑器体量足够大时再增 `/formation` 等 |

若实现中体质/分配 UI 严重超出单屏，允许 **大厅内 Tab**（仍是 `/hall` 查询参数如 `?tab=constitution`，**不是**新 `path`）。默认首版用折叠 `<details>` / Element Collapse，不强制 query tab。

---

## 3. `src` 目录增量

M0/M1 目录职责不变；M2 **追加文件**，不推翻骨架。

```text
src/
├── api/
│   ├── idle.ts                 # 扩展；+ offlinePreview / offlineClaim
│   ├── allocate.ts             # 新
│   ├── techniques.ts           # 新
│   ├── constitution.ts         # 新
│   ├── breakthrough.ts         # 扩展 history；preview 字段
│   ├── battle.ts               # 复用
│   └── gm.ts                   # 扩展字段
├── components/
│   ├── IdlePanel.vue           # 三向按钮
│   ├── AllocatePanel.vue       # 新
│   ├── ConstitutionPanel.vue   # 新
│   ├── ConstitutionSlotGrid.vue    # 可选拆分：主/副格
│   ├── ConstitutionBagList.vue     # 可选拆分：背包
│   ├── OfflineClaimDialog.vue  # 新
│   ├── DivineSlotRow.vue       # 可选：空神通槽展示
│   ├── CharacterPanel.vue      # 增强
│   ├── BreakthroughPanel.vue   # 增强
│   ├── BreakthroughResultDialog.vue
│   ├── BattlePanel.vue
│   ├── GameLogPanel.vue
│   └── GmDevPanel.vue
├── stores/
│   ├── character.ts            # 扩展 applyCharacter；进厅检测 offline_pending
│   ├── techniques.ts           # 可选薄 store
│   └── constitution.ts         # 可选薄 store
├── types/
│   ├── character.ts            # 池/进度/品阶/pending
│   ├── idle.ts                 # 三向；offline pending
│   ├── allocate.ts
│   ├── techniques.ts
│   ├── constitution.ts
│   └── breakthrough.ts         # grade 字段
└── views/
    └── HallView.vue            # 编排：进厅 preview、弹窗、日志、折叠布局
```

### 3.1 命名约定（继承 M0/M1）

- 页面：`XxxView.vue`（M2 **不新增**）
- 零件：`XxxPanel.vue` / `XxxDialog.vue`
- API 文件与后端模块同名

---

## 4. 大厅信息架构

### 4.1 布局（在 M1 左右分栏上扩展）

```text
┌──────────────────────────────────────────────────────────────┐
│ AuthSessionBar · 修仙大厅 · M2 成长深度                         │
├────────────────────────────┬─────────────────────────────────┤
│ CharacterPanel             │ GameLogPanel                    │
│  · 道号 / 境界 / 状态       │  · 欢迎 / 结算 / 突破 / 战斗     │
│  · 境界进度条(realm_progress)│  · 离线领取 / 分配 / 体质      │
│  · 三池：修为/炼体/制造业   │  · 品阶结果                     │
│  · 品阶 + 神通空槽          │                                 │
│  · 攻防（含品阶/体质修正展示）│                                 │
├────────────────────────────┤                                 │
│ IdlePanel                  │                                 │
│  · 修灵 / 炼体 / 制造业     │                                 │
│  · 停止；速率；耗石；停滞    │                                 │
├────────────────────────────┤                                 │
│ AllocatePanel（可折叠）     │                                 │
│  · 投入境界 / 升功法        │                                 │
├────────────────────────────┤                                 │
│ ConstitutionPanel（可折叠） │                                 │
│  · 主1 + 副2 格；背包镶嵌   │                                 │
├────────────────────────────┤                                 │
│ BreakthroughPanel          │                                 │
│  · preview；假读条；品阶提示 │                                 │
├────────────────────────────┤                                 │
│ BattlePanel                │                                 │
└────────────────────────────┴─────────────────────────────────┘
         ▲
         └── OfflineClaimDialog（有 pending 时模态）
```

移动端：单列 Character → Idle → Allocate → Constitution → Breakthrough → Battle → Log；离线对话框全屏宽。

### 4.2 组件职责

| 组件 | 职责 | 不负责 |
| --- | --- | --- |
| `IdlePanel` | 三向切换、速率/耗石、停滞提示 | 自己算 tick 收益 |
| `OfflineClaimDialog` | 展示 pending 明细；claim；关闭 | 改本地池数字 |
| `AllocatePanel` | 选目标、输入 amount、调用 allocate | 代选最优分配 |
| `ConstitutionPanel` | 展示格与背包；equip/unequip；升品/融合入口 | 算突破权重 |
| `CharacterPanel` | 只读绑定 store：池、进度、品阶、空槽 | 直接改权威字段 |
| `BreakthroughPanel` | preview/attempt；展示门槛差 | 掷品阶 |
| `HallView` | 进厅拉 me/preview、自动弹离线窗、pushLog、`startIdleRealtime` | 大段公式 |
| `GameLogPanel` | 展示日志 | 调玩法 API |

### 4.3 进厅与离线弹窗时序

```text
进入 /hall
  → fetchMe / sync（服务端可能生成 pending）
  → 若 character.offline_pending 或 preview.has_pending
        → 打开 OfflineClaimDialog（VITE_OFFLINE_AUTO_OPEN）
        → 用户「领取」→ claim → applyCharacter → pushLog → 关闭
  → 再 start 在线预测 / tick 对齐调度（有 pending 时不预测入账）
```

### 4.4 在线实时 / 离线冻结（相对 M1 §4.0）

| 项 | 约定 |
| --- | --- |
| 有 `offline_pending` | **停止** 片内预测入账与 tick 对齐 sync；仅展示「请先领取离线收益」 |
| claim 成功后 | 恢复 M1：`startIdleRealtime` = 预测 + `next_tick_at` 对齐 `/idle/sync` |
| 兜底间隔 | 仍用 `VITE_IDLE_POLL_MS`（默认 120s，**非**主路径短轮询） |
| 预测刷新 | `VITE_IDLE_PREDICT_MS`（默认 250） |
| 可见性 | 仅 `hall` + `visibilityState=visible` |

---

## 5. API / Store 对应

| 文件 | 函数（示意） |
| --- | --- |
| `api/idle.ts` | `setIdleDirection` · `syncIdle` · `previewOffline` · `claimOffline` |
| `api/allocate.ts` | `allocateResource({ target_type, target_id, amount })` |
| `api/techniques.ts` | `fetchMyTechniques()` |
| `api/constitution.ts` | `fetchConstitution` · `equip` · `unequip` · `upgrade` · `fuse` |
| `api/breakthrough.ts` | `preview` · `attempt` · `fetchGradeHistory()` |
| `api/gm.ts` | 扩展 set 字段 |

**`useCharacterStore` 扩展建议**

| state / action | 说明 |
| --- | --- |
| `character` | 含三池、`realm_progress`、品阶、`offline_pending` |
| `applyCharacter(ch)` | 各 API 返回统一写入 |
| `claimOffline()` | 调 API 后 apply + 清本地 pending 标记 |
| `setDirection` | 若 40030，转译为「请先领取」并打开对话框 |

体质/功法列表可放薄 store，避免 `character` 过胖；以联调时体感为准。

---

## 6. 交互与文案约定

### 6.1 三向修炼

- 三个主按钮互斥高亮：修灵 / 炼体 / 制造业；另有「停止修炼」。  
- 配置未开放的方向：禁用 + toast（对应 `40020`）。  
- 停滞文案随方向：「灵石不足，修炼停滞…」。  

### 6.2 离线领取

- 明细至少展示：有效时长、是否触帽、方向、各池获得、灵石消耗、停滞。  
- 主按钮「领取收益」；领取中锁按钮。  
- 关闭未领取：允许关掉对话框，但 Idle/分配等入口持续提示红点，直至 claim。  

### 6.3 分配

- 默认 Tab 或分段：投入境界 | 功法。  
- 功法列表展示等级、下一级消耗、所属 track。  
- 输入校验：正整数、不超过池；失败用服务端 `message`。  
- **不做**「一键最优」或自动分配。  

### 6.4 体质

- 主格 1、副格 2 视觉区分；点击背包 → 选空格镶嵌（拖拽可选，非必须）。  
- 格满 / 类型不符：toast `40034`。  
- 升品/融合：二次确认对话框；结果用 `ElMessage` + GameLog。  

### 6.5 品阶与突破

- CharacterPanel 展示 `breakthrough_grade_name`；无则「尚未跨境品阶」。  
- 神通槽：按 `divine_ability_slots` 渲染空位，文案「暂无神通（占位）」。  
- 突破成功对话框：若本次跨境，高亮品阶名称。  
- 可选折叠「品阶历史」调用 `fetchGradeHistory`。  

### 6.6 UI 基调

- 继承 M0/M1：功能优先；继续 Element Plus，不引入第二套重型体系。  
- 不为 M2 做终局古风大场景；折叠与对话框足够承载复杂度。  

### 6.7 GM（DEV）

- 增：三池、`realm_progress`、品阶、`membership_tier`、清除 pending。  
- 仅 `import.meta.env.DEV`。  

---

## 7. 类型对齐（要点）

`types/character.ts` 在 M1 基础上追加（示意）：

```ts
realm_progress: number
breakthrough_grade: string
breakthrough_grade_name: string
divine_ability_slots: number
membership_tier: 'free' | 'tier1' | 'tier2' | string
offline_cap_hours: number
offline_pending: OfflinePending | null
```

`types/idle.ts`：`OfflinePending`、三向 `IdleDirection`。  
`types/allocate.ts` / `constitution.ts` / `techniques.ts`：与 `M2成长深度设计.md` §8 一致。

统一错误：拦截器处理 `code !== 0`；面板 catch 后 `ElMessage` + `pushLog`；`40030` 特判打开离线窗。

---

## 8. 与后续里程碑

| 里程碑 | 对本设计的影响 |
| --- | --- |
| **M2** | 本文范围 |
| **M3** | 预期新增 `/formation` 等；大厅变为入口枢纽；再评估 M1-D31 |
| **M4** | 制造业面板可能升级为工房；仍可先对话框后路由 |
| 延后项 | 一律先查 [`后续待完成.md`](./后续待完成.md) |

---

## 9. 开发进度与步骤（对照 M2）

> 本节随实现同步更新；接口以 `M2成长深度设计.md` 为准。

### 9.1 总览

| 步 | 内容 | 状态 | 说明 |
| --- | --- | --- | --- |
| 0 | 设计文档（本文 + 成长深度 + 待完成状态） | **已完成** | 2026-08-04 |
| 1 | 类型扩展 + CharacterPanel 三池/进度/品阶位 | **已完成** | 2026-08-04 |
| 2 | IdlePanel 三向 + 后端 F1 联调 | **已完成** | |
| 3 | OfflineClaimDialog + preview/claim | **已完成** | |
| 4 | AllocatePanel + techniques | **已完成** | |
| 5 | ConstitutionPanel 镶嵌/卸下 | **已完成** | |
| 6 | 突破品阶展示 + 空神通槽 | **已完成** | 历史 API 已接，UI 可后续折叠 |
| 7 | GameLog 文案 + GM 扩展 + 总验收 | **已完成** | vue-tsc 通过；后端 56 passed |

### 9.2 本地联调口令（实现后）

```powershell
# 后端
cd backend
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000

# 前端
cd frontend
npm run dev
```

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/idle/direction` | POST | `spirit`/`body`/`crafting`/`none` |
| `/api/v1/idle/offline/preview` | GET | 离线明细 |
| `/api/v1/idle/offline/claim` | POST | 领取 |
| `/api/v1/allocate` | POST | 分配 |
| `/api/v1/techniques/me` | GET | 功法 |
| `/api/v1/constitution/me` | GET | 体质 |
| `/api/v1/constitution/equip` | POST | 镶嵌 |
| `/api/v1/breakthrough/grades/history` | GET | 品阶历史 |

### 9.3 下一步建议

1. **M2 前后端已落地**；按 README 联调路径复现三向 / 离线领取 / 分配 / 体质 / 跨境品阶。  
2. 开 **M3** 前完整阅读 `后续待完成.md`（棋盘 / 布阵等）。  
3. 数值只改 `backend/app/config_data/*.yaml`；勿提前做棋盘 / WS。配置热更/DB 覆盖见 `后续待完成.md` **M2-D01**。  
4. 后端分层现状：Application Service + `domain/`（见 README「后端架构」）；前端无需改目录。  

---

## 10. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-04 | 初稿：无新玩法路由；大厅增分配/体质/离线对话框；api/stores/types 增量；进度表 |
| 2026-08-04 | §4.4 与 M1 结算模型正名对齐：在线为预测 + tick 对齐，非固定短轮询 |
| 2026-08-04 | §9.1 步骤 1～7 标已完成；与后端 M2 联调落地 |
| 2026-08-04 | 后端 OOP 同步说明：分配/体质/离线等走对应 `*Service` + `PlayGate`；前端 API 路径与字段不变 |

---

*接口与结算以 `M2成长深度设计.md` 为准；本文只固化前端目录、路由（无新增玩法 path）与大厅落点，并跟踪实现进度。*

# 双轨整改与配置兼容方案

> **状态**：**v1.0.3**（2026-08-14）· **S1 收口** + **CONTENT_STORE_MODE 已接通玩法 Bundle**  
> **配套真理**：实体/Ability 类层次见 [`统一实体类层次与重构方案.md`](./统一实体类层次与重构方案.md) **v1.0**  
> **排期**：**ARCH 夯实优先于 M8**  
> **配置现状**：[`后台管理系统开发计划.md`](./后台管理系统开发计划.md) · 主计划 **§0.0 / §0.0.1** · `app.game.content_store.ContentStore`  
> **目标**：把落地工作拆成 **两步**——① 已实现功能按新思路整改；② 未开发内容按新规范设计与实现；并保证 **测试继续只改 YAML**、正式环境可切 **数据库权威存储**，业务代码无感知。

---

## 0. 总览：两步 + 一条配置脊柱

```text
┌─────────────────────────────────────────────────────────────┐
│  步骤 1 · 存量整改（已实现玩法）                                │
│  按新 ABC / Ability / GrantSource 改代码与接线；行为尽量等价   │
└──────────────────────────────▲──────────────────────────────┘
                               │ 同一套契约
┌──────────────────────────────┴──────────────────────────────┐
│  步骤 2 · 增量规范（未开发 / 设计中）                           │
│  新设计文档与实现必须从第一天遵守 game.* ABC + ContentStore     │
└──────────────────────────────▲──────────────────────────────┘
                               │
┌──────────────────────────────┴──────────────────────────────┐
│  配置脊柱 · ContentStore（大兼容）                             │
│  测试：YAML 底表（可 + 本地 overlay）                          │
│  正式：DB 权威文档（可仍保留 YAML 作种子/导出）                  │
│  对上：永远吐出「同一套 domain raw dict」给 Bundle / OOP 工厂   │
└─────────────────────────────────────────────────────────────┘
```

| 轨道 | 做什么 | 不做什么 |
| --- | --- | --- |
| **步骤 1 存量** | 已上线竖切按新类层次整改；shim 保绿；HTTP/`domain_id` 稳定 | 借整改改玩法数值、改前端路由 |
| **步骤 2 增量** | 新功能设计/编码强制新规范；后台域同步登记 | 再写一套平行属性/特效引擎 |
| **配置兼容** | 业务只依赖 `ContentStore.load(domain_id)` | 业务 `open("xxx.yaml")` 或写死 SQL 读设定 |

---

## 1. 兼容与扩展总原则（两轨共用）

### 1.1 兼容（Compatibility）

| # | 原则 | 落地 |
| --- | --- | --- |
| C1 | **API 面稳定** | 玩家 HTTP 路径/主字段、战报事件名不因重构改语义 |
| C2 | **admin `domain_id` 稳定** | 已有域 ID 不改名；新能力用新域（如 `abilities`/`equipment`） |
| C3 | **配置契约稳定** | 域 raw JSON Schema（或等价校验器）是唯一内容契约；存储后端可换 |
| C4 | **测试工作流稳定** | 本地/测试服 **继续改 `config_data/*.yaml` 即可验证**；不必为测一数值先建库表行 |
| C4b | **运行时库可换** | 账号/角色等实体只走 `DATABASE_URL`（测试 SQLite / 正式 PG）；业务不感知方言；多份 `xiuxian.db` 由 `runtime_url` 选用有账号的文件 |
| C5 | **行为先等价** | 存量整改默认「结构换、玩法数字与规则结果不变」；规则变更单独立项 |
| C6 | **双读过渡** | ORM 名、旧 service 函数可 shim；删除旧路径须单测+冒烟全绿 |

### 1.2 扩展（Extensibility）

| # | 原则 | 落地 |
| --- | --- | --- |
| E1 | **加内容不改代码** | 新装备/能力/怪物进注册表；禁止 `if id ==` |
| E2 | **加类型靠子类/组件** | 新 `Character`/`Item` 子类或 Component；不膨胀基类 abstract |
| E3 | **加效果靠 op** | 新特效优先新 `Ability.op`；解释器注册表可插拔 |
| E4 | **加存储后端靠 Source** | 实现 `ContentBackend` 即可；上层 Bundle/OOP 不动 |
| E5 | **schema_version** | 每域带版本；迁移脚本/兼容读明确 |

---

## 2. 步骤 1：已实现功能整改方案

### 2.1 目标

把**已经落地**的玩法代码，整改到与 [`统一实体类层次与重构方案.md`](./统一实体类层次与重构方案.md) 一致：

- 可参战对象 → `Character` 子类门面  
- 背包物 → `Item` 子类门面  
- 属性组装 → ATTR +（逐步）`GrantSource`  
- 开战 → `on_battle_enter` → 现有 `simulate_battle`  
- **不**在本步骤强行上齐装备特效战斗链（那是 M3-D03 / 步骤 2 增量）

### 2.2 存量范围清单（按现行已落地）

| 包块 | 已实现代表 | 整改切入 |
| --- | --- | --- |
| 角色成长 | 挂机/突破/体质/功法/ATTR-D01 | `PlayerCharacter` + 现有 `build_combat_attrs` 委托 |
| 化身 | AVATAR-* | `AvatarCharacter` |
| 灵宠 | PET-* / 孵化/喂养/词条/技能 | `PetCharacter`；技能栏暂保持现 API，Grant 化可后置 |
| 工坊/背包 | craft / inventory / 傀儡入包上阵 | `Item` 门面；傀儡 → 双切面迁移（模型 A） |
| 布阵/战斗 | formation / autochess / 赛会 | 组装改经 Character 工厂；引擎纯度保留 |
| 环境 | 时辰/天气/切段 idle | `Environment` 门面 + side_mod；规则函数可仍 domain |
| 渡劫/轮回/引渡 | tribulation / reincarnation / ferry | 弱依赖 OOP；按需挂 Grant/旗标 |
| 大道/道主 | dao / lord / contest | `LawEntity`；特权布尔 → 兼容投影到 privilege Ability |
| 宗门/社交/经济 | sect / trade / mail / chat… | `Organization` 与社交服务渐进；非战斗优先不动热路径 |
| 后台 | ADM 域编辑/发布 | 继续打 ConfigSource；新域登记 schema |

### 2.3 存量整改分期（与 OOP 文档 R 对齐，强调「改旧」）

| 期 | 名称 | 内容 | 兼容验收 |
| --- | --- | --- | --- |
| **S1-0** | 契约冻结 | 拍板槽位/傀儡 A/去重；升 OOP 文档 v1.0 | ✅ 已完成 |
| **S1-1** | 门面骨架 | `app/game/**` ABC + `from_orm`/`from_template`；ContentStore；旧路径 shim | ✅ 单测 `test_game_arch_abc` |
| **S1-2** | 开战统一 | `AutochessService`/`FormationService` 经 Character 组装 seed | ✅ 已落地 |
| **S1-3** | ATTR 源门面 | technique/constitution GrantSource；数字不变 | ✅ 骨架已落地（ability_ids 可空） |
| **S1-4** | 傀儡双切面 | 背包傀儡补 actor 行；试炼傀保留 ephemeral | ✅ `PuppetActor` / `test_s1_4_5` |
| **S1-5** | 道主特权投影 | 布尔 ↔ privilege grants 双写读 | ✅ `normalize_privileges_payload` |
| **S1-6** | 收敛删除 shim | 删 `domain.m4_constants`；ORM `CharacterRow` 别名；`app.constant` 保留为长期入口 | ✅ 2026-08-14 |

> **S1 与 M8**：装备数值喂入（ATTR-D02）属于「半存量半增量」——**存量通道打开 + 新 equipment 域**；归 **S1-3 延伸 / 与步骤 2 的 M8 清单交界**，单独验收。

### 2.4 存量整改纪律

1. **先包装、后搬迁、最后删**：禁止上来挪光 `domain/`。  
2. **每期必须**：单测 + 相关 `smoke_m*` 绿。  
3. **玩法变更另开 PR/条目**：整改 PR 描述写「结构等价」。  
4. **前端**：步骤 1 默认零改；除非 DTO 合法加字段。  

### 2.5 存量「完成定义」

某竖切算整改完成，当且仅当：

- [ ] 有对应 `Character`/`Item`/… 门面类  
- [ ] 服务层创建/开战/面板主路径走门面（或明确委托）  
- [ ] 不新增第二套属性键  
- [ ] 配置仍经 ContentStore（见 §4），无新的裸读 YAML  
- [ ] 文档勾选「已对接 ARCH」

---

## 3. 步骤 2：未开发部分的设计与开发规范

### 3.1 目标

凡 **尚未实现** 或 **仅有延后设计** 的内容（装备特效、套装、M3-D03 技能链、M6-D04 天道技能与秘境、M8 自研、M9 地图等），**设计和编码从第一天**按新定义，不再先写旧式 service 散逻辑再二次整改。

### 3.2 强制检查清单（开新设计 / 开新竖切前）

| # | 检查项 | 不合格则 |
| --- | --- | --- |
| 1 | 落入哪一 `domain_root` / 哪个 ABC 子类？ | 补类或证明用 Component |
| 2 | 数值是否走 ATTR / Ability `stat_mod`？ | 禁止自造平行面板 |
| 3 | 特效是否 `Ability` + `grants`？ | 禁止装备 id 写死分支 |
| 4 | 非战斗权限是否 `privilege` domain？ | 禁止塞进 `simulate_battle` |
| 5 | 配置是否新 `domain_id` + schema + 校验器？ | 禁止只改代码常量 |
| 6 | 是否声明 ContentStore 可读（YAML 种子 + 未来 DB）？ | 见 §4 |
| 7 | HTTP/`domain_id` 兼容策略？ | 写进设计 §边界 |
| 8 | 后台双写 + `label_zh`（主计划 §0.0.1）？ | 竖切未完成 |

### 3.3 增量优先队列（与排期对齐，不另开平行主线）

| 优先级 | 内容 | 规范要点 |
| --- | --- | --- |
| P0 | **M8 + ATTR-D02** | `EquipmentItem` + grants；打开 equipment 通道 |
| P0 | **abilities / equipment / sets 域** | 新 YAML + 注册表；测试只改 YAML |
| P1 | **M3-D03** | BattleUnit + AbilityRuntime；战报事件 |
| P1 | **套装** | `equipment_sets` → grants |
| P2 | **M6-D04** | privilege Ability；秘境入口鉴权 |
| P2 | **M8 自研** | 产出进 Content/Item + 可 grants |
| 后置 | M9 地图/Boss 模板等 | `MonsterCharacter`/`BossCharacter` 模板行 |

### 3.4 增量设计文档模板（追加一节）

新设计文必须含：

```markdown
## ARCH 对齐
- domain_root / 类：
- GrantSource / Ability：
- ContentStore domain_id：
- 存量依赖 / 是否需先完成 S1-x：
- 兼容：API / 存档 / 配置种子：
```

---

## 4. 大兼容：YAML（测试）↔ 数据库（正式）

### 4.1 你的诉求（写死）

| 环境 | 期望 |
| --- | --- |
| **测试 / 本地** | 继续用 **YAML 改设定**；不想为联调改流程、不想「测个数还得写库」 |
| **正式** | 设定类数据以 **数据库为权威**（运营后台发布、审计、回滚、多实例一致） |
| **代码** | 玩法 / OOP / Bundle **不关心**数据存在文件还是库里 |

### 4.2 与现状的关系（不是推倒）

现行已具备雏形：

```text
config_data/*.yaml  ──YamlConfigSource──┐
                                        ├─ deep_merge → realm_config 解析 → GameConfigBundle
DB config_published ──OverlayStore──────┘
```

差距：正式侧目前是 **「YAML 底表 + DB 覆盖」**，不是「DB 整域权威」；测试侧已经满足「改 YAML 即可」（无 overlay 时）。

本方案升级为显式 **ContentStore 模式**，兼容三种部署，且 **测试默认保持 YAML 权威**：

| 模式 | 环境建议 | 行为 |
| --- | --- | --- |
| **`yaml_authority`** | 本地 / 测试服默认 | 只读 YAML；可选忽略 DB overlay（或测试库空 overlay） |
| **`yaml_base_db_overlay`** | 现行后台已落地；预发可沿用 | YAML 底 ∪ 已发布 overlay（**现状**） |
| **`db_authority`** | 正式目标 | 整域以 DB 文档为准；YAML 仅作 **种子导入 / 导出备份 / 灾备** |

### 4.3 目标架构

```text
                    ┌─────────────────────────┐
                    │   ContentStore (门面)     │
                    │   load(domain_id)->dict  │
                    │   list_domains()         │
                    └───────────┬─────────────┘
                                │
              ┌─────────────────┼─────────────────┐
              ▼                 ▼                 ▼
     YamlFileBackend    MergeBackend      DbDocumentBackend
     (读 config_data)   (yaml∪overlay)    (读 config_documents)
              │                 │                 │
              └─────────────────┴─────────────────┘
                                │
                                ▼
                    同一 raw dict 契约
                                │
              ┌─────────────────┼─────────────────┐
              ▼                 ▼                 ▼
        GameConfigBundle   game.* 工厂      Admin 预览/校验
```

**环境变量（已落地 · ``backend/.env`` / ``.env.example``）**

```text
CONTENT_STORE_MODE=yaml_authority|yaml_base_db_overlay|db_authority
# 本地/单测推荐：yaml_authority（只改 YAML，忽略 DB 发布）
# 现行默认：yaml_base_db_overlay（玩家服吃后台发布）
# 正式目标：db_authority（无发布则回退 YAML 种子）
```

玩法入口：``realm_config._load_yaml`` → ``ContentStore.load_filename``（启动日志会打印当前 mode）。

### 4.4 测试「不想改动」如何保证

1. **测试/本地设 `CONTENT_STORE_MODE=yaml_authority`**：改 `backend/app/config_data/*.yaml` → 存盘 → 清 Bundle 缓存/重启 → 立即生效，**不吃** DB 发布层。  
2. **单测**：继续用临时目录注入 `YamlConfigSource(config_dir=tmp)`；不强制 DB。  
3. **不要求**测试人员使用后台发布流才能改怪/改宠。  
4. **新域**仍先落 YAML 种子文件 + registry；正式再 import 进 DB。  
5. **禁止**在步骤 1/2 业务代码中新增「只认文件路径」的读取；一律 `ContentStore`（底层仍可是 YAML）。

### 4.5 正式 DB 权威（扩展路径，可后置实现）

建议表（可与现 `config_published` 演进，不必一口吃）：

| 概念 | 说明 |
| --- | --- |
| `config_document` | `domain_id` + `payload_json` + `schema_version` + `version` + `updated_at` |
| `config_revision` | 已有审计/回滚可复用 |
| 发布 | 后台发布 = 写 document +  bump version + `RuntimeConfigReloader` |
| 种子 | `scripts/seed_config_from_yaml.py`：YAML → DB（正式首装） |
| 导出 | `scripts/export_config_to_yaml.py`：DB → YAML（备份/给测试包） |

**兼容要点**：`DbDocumentBackend.load` 返回的 dict **必须**通过与 YAML 相同的校验器；字段契约零分叉。

### 4.6 从「底表+覆盖」迁到「DB 权威」的兼容阶梯

| 阶段 | 模式 | 说明 |
| --- | --- | --- |
| 现在 | `yaml_base_db_overlay` | **已接通** `realm_config`；测试可强制 `yaml_authority` |
| 过渡 | 发布时 **物化整域** 进 DB（底∪overlay 写回 document） | 读路径仍 Merge，写路径开始整域 |
| 正式 | `db_authority` | 运行时只读 DB；YAML 退出热路径 |
| 测试包 | 永远可 `yaml_authority` | 你的核心诉求 |

业务与 OOP **在过渡全程无需改调用方式**。

### 4.7 扩展：多环境 / 多服

| 能力 | 做法 |
| --- | --- |
| 测试服 A/B 不同数值 | 不同 YAML 目录或不同 DB 文档集；同 mode |
| 正式多 worker | DB 权威 + 发布后广播 reload（现行 Overlay 内存模型需配套；可挂 **PRESENCE/配置总线** 类延后项） |
| 热更 | 仍只走 `RuntimeConfigReloader` |

---

## 5. 两步与配置脊柱的交叉矩阵

| 工作 | ContentStore | OOP/ABC |
| --- | --- | --- |
| S1 存量门面 | 读路径改经 Store（底层仍 YAML） | 包装现逻辑 |
| M8 装备域 | 新 YAML + registry；测试改 YAML | `EquipmentItem` + Ability |
| 正式切 DB | 只改 Backend + 种子脚本 | **零改** game 类 |
| M3-D03 | 能力表同 Store | `AbilityRuntime` |

---

## 6. 风险

| 风险 | 缓解 |
| --- | --- |
| 整改范围过大拖死 M8 | S1-1/1-2 小步；装备进 M8 交界单独立项 |
| 过早切 `db_authority` 害测试手感 | **测试默认永 Lock `yaml_authority`** |
| Bundle 解析与 Store 分叉 | 唯一入口 `load(domain_id)`；禁止双读 |
| schema 漂移 | 每域校验器 + CI 用 YAML 跑同一套 |

---

## 7. 待拍板

1. 测试服是否 **强制** `yaml_authority`（推荐：是）？  
2. 正式切 `db_authority` 的目标里程碑（建议 **不早于 M8 装备域稳定**，可挂 M11/M13 运维）？  
3. 现行 `config_published` overlay 是否在过渡期 **物化整域** 再切权威？  
4. 步骤 1 是否允许与 M8 **并行**（推荐：S1-1/1-2 并行，S1-3 装备与 M8 合并）？  

---

## 8. 文档挂钩

| 文档 | 关系 |
| --- | --- |
| [`统一实体类层次与重构方案.md`](./统一实体类层次与重构方案.md) | 类/Ability **是什么** |
| **本文** | **怎么分两步落地** + **YAML/DB 怎么兼容** |
| [`后台管理系统开发计划.md`](./后台管理系统开发计划.md) | 后台编辑/发布；Store 模式升级后需修订 §配置契约 |
| [`后续待完成.md`](./后续待完成.md) | **ARCH-R01**（OOP）+ **ARCH-R02**（本双轨与 ContentStore） |

---

## 9. 修订记录

| 日期 | 版本 | 说明 |
| --- | --- | --- |
| 2026-08-14 | v0.1 | 首版：步骤1存量整改、步骤2增量规范、ContentStore 三模式（测试 YAML 权威写死） |
| 2026-08-14 | **v1.0** | 与 OOP v1.0 同步；S1-1 落地；排期 ARCH 优先于 M8 |
| 2026-08-14 | **v1.0.1** | S1-4/S1-5 勾选完成；下一主线 M8/ATTR-D02 |
| 2026-08-14 | **v1.0.2** | **S1-6**：删除 `domain.m4_constants` shim；`CharacterRow` 别名；ARCH 存量整改 S1 收口 |
| 2026-08-14 | **v1.0.3** | `CONTENT_STORE_MODE` 接通 `realm_config`；三模式单测 `test_content_store_mode` |

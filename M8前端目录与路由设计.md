# M8 前端目录与路由设计

| 项 | 内容 |
| --- | --- |
| **针对里程碑** | **M8（自研与内容管线）** |
| **设计日期** | 2026-08-20（**v1.1.30**：自研迁入洞府二级页「研究室」；顶栏「洞府」） |
| **依据** | `M8自研与内容管线设计.md`；`M7前端目录与路由设计.md`；`M6`/`M5`/`M4`/`M3`/`M2`/`M1`/`M0` 前端目录设计；现行 `HallView` / `CharacterView` / `WorkshopView` / `FormationView` / `SectView` / `ShopView`；`开发计划.md` §10；[`阵法部署与自研设计.md`](./阵法部署与自研设计.md) §10.2 |
| **原则** | **洞府为一级玩法页**（`/cave`）；研究室为二级（`/cave/lab`，query 三分支）；旧 `/research` 重定向；`/workshop` 仍为生产与符预载；`/formation` **只摆棋子**；`/character` 增 **17 区穿戴栏**；`/hall` 仍为养成枢纽；**穿戴/定稿/画符权威来自服务端**；**样式继承现行玩法壳**（§4.8 / §6.5），不为 M8 另起皮肤；审核台 / 地图 / 时效 buff UI **不做** |

---

## ARCH 对齐（2026-08-17）

> 工程重构指针：[`统一实体类层次与重构方案.md`](./统一实体类层次与重构方案.md) · [`双轨整改与配置兼容方案.md`](./双轨整改与配置兼容方案.md)  
> **ARCH S1 已收口**；本里程碑前端只消费装备/自研 DTO，**不**在客户端复刻叠层公式。

| 项 | 对齐 |
| --- | --- |
| 前端边界 | 洞府 `/cave` + 研究室 `/cave/lab`；API 权威 `/cave/lab/*`（旧 `/research` 兼容）；UI 绑定 DTO |
| 领域对象 | 装备槽 / 自研会话 / 私有功·阵·符列表；开战仍只消费战报 |
| 配置 | 图鉴与白名单来自 GET catalog；禁止前端写死效果枚举当内容源 |
| 战斗 | 只渲染服务端 `combat.breakdown` 与战报 `*_label_zh` |
| 增量 | 禁止按装备 id 写死分支；词条/效果下拉只渲染 catalog |

---

## 1. 设计目标

约定 M8 在 `frontend/src` 下的文件增量、**新路由表**、大厅枢纽与自研/装备信息架构、**各页布局与样式文字方案**（对齐 M3～M7 已落地的玩法页改法），以及与后端装备 / 自研 / 工坊画符 / 阵法蓝图接口的对接落点。

M8 可玩范围（R1～R6）：角色页 **17 区穿戴栏**并看到装备/傀儡来源拆解；自研功法/阵法/符箓定稿；工坊批量画符与预载；布阵可选自研阵；宗门工坊真图纸；大厅自研入口。  
范围外：全服审核台 UI、套装 2/4 件完整矩阵、完整技能施放编辑器、符箓完整陷阱链、丹药/符时效 buff 倒计时、化身双修、地图（见 [`后续待完成.md`](./后续待完成.md)）。

### 1.1 与检定 / 属性纪律（前端）

自研预览出目、装备改挂机乘区、骰子区间、战斗面板 final，**只展示服务端返回字段**。禁止本地按装备 `stats` 再算一套 `phys_atk`。区间骰按 [`骰子系统设计.md`](./骰子系统设计.md) 与 §0.0.2 展示中文说明。

### 1.2 与工坊 / 布阵纪律

- `/workshop`：**生产**（配方队列、领取）；两列配方+队列，**不**展示背包栏；符箓与其它分支同布局，**无**画符/预载带（出战符在装备栏）。  
- `/formation`：**出战布阵**（棋子 + 高亮 `effective_deploy_cells`）；不改蓝图。  
- `/cave/lab?mode=formation`：**设计蓝图**（复用 `FormationBoard` / 笔刷，不复用「保存进攻预设」主按钮语义）。  
- 自研进行中不挡挂机主界面（可离开页；会话服务端保留至过期）。

---

## 2. 路由策略（相对 M7：**增一级洞府 `/cave`，研究室为其二级页**）

| path | name | 文件 | M8 变化 |
| --- | --- | --- | --- |
| `/login` | `login` | `views/LoginView.vue` | 无 |
| `/register` | `register` | `views/RegisterView.vue` | 无 |
| `/create-character` | `create-character` | `views/CreateCharacterView.vue` | 无 |
| `/hall` | `hall` | `views/HallView.vue` | **枢纽**：工坊/洞府入口摘要 |
| `/character` | `character` | `views/CharacterView.vue` | **扩展**：17 区穿戴栏 + 战斗拆解含装备/傀儡 |
| `/formation` | `formation` | `views/FormationView.vue` | 下拉含自研阵；链到研究室；**不**开放 brush |
| `/battle` | `battle` | `views/BattleView.vue` | 战报渲染 `source_label_zh` / `item_trigger` 中文 |
| `/avatar` | `avatar` | `views/AvatarView.vue` | 无强制改版（化身默认不吃本体装备） |
| `/workshop` | `workshop` | `views/WorkshopView.vue` | 四分支配方队列（无阵法）；符箓不再挂画符/预载 |
| `/pets` | `pets` | `views/PetsView.vue` | 无 |
| `/tribulation` | `tribulation` | `views/TribulationView.vue` | 无 |
| `/reincarnation` | `reincarnation` | `views/ReincarnationView.vue` | 可选文案：自研默认不带出 |
| `/dao` | `dao` | `views/DaoView.vue` | 无 |
| `/dao-lord` | `dao-lord` | `views/DaoLordView.vue` | 无 |
| `/dao-lord/arena` | `dao-lord-arena` | （既有） | 整备文案「装备」改为真槽（若已穿戴） |
| `/sect` | `sect` | `views/SectView.vue` | 工坊 Tab：真材料/捐赠图纸 |
| `/market` | `market` | `views/MarketView.vue` | 装备可上架（非绑定）；无新 path |
| `/social` | `social` | `views/SocialView.vue` | 无 |
| `/friends` | `friends` | `views/FriendsView.vue` | 无 |
| `/party` | `party` | `views/PartyView.vue` | 无 |
| `/dual-cultivation` | `dual-cultivation` | `views/DualCultivationView.vue` | 无 |
| `/shop` | `shop` | `views/ShopView.vue` | 无 |
| `/account` | `account` | `views/AccountView.vue` | 无 |
| `/cave` | `cave` / `cave-hub` | `views/AbodeView.vue` + `AbodeHubView.vue` | **新**：洞府一级页 |
| `/cave/lab` | `cave-lab` | `views/ResearchView.vue` | **新**：研究室（功法/阵法/符箓） |
| `/research` | — | redirect → `/cave/lab` | 兼容旧链接 |
| `/` | — | 重定向 | 逻辑不变；有进行中自研会话 **不** 抢 `homePathAfterAuth`（避免打断渡劫/双修优先） |
| `/test` | `test` | `test/apps.vue` | 可选；非验收 |

### 2.1 守卫逻辑（继承并扩展）

| 规则 | 说明 |
| --- | --- |
| 需登录 | 既有玩法页 + `/cave` `/cave/lab` 均 `requiresAuth` |
| 无角色 | → `/create-character` |
| 有角色访创角 | → `homePathAfterAuth()` |
| 已登录访登录/注册 | → `homePathAfterAuth()` |
| `redirect` 白名单 | `resolveSafeRedirect` **追加** `/cave` `/cave/lab`（旧 `/research` 仍允许） |
| `PLAY_ROUTE_NAMES` | 追加 `cave` / `cave-hub` / `cave-lab` |
| `FERRY_BLOCKED_ROUTES` | **追加** `cave` / `cave-hub` / `cave-lab`（待引渡禁定稿/设计；只读邮件/求援仍走社交） |
| 待引渡 / 新生 | 仍优先 `/reincarnation`（M5） |
| 渡劫中 | 允许打开 `/cave/lab` **只读**已定稿列表；禁新开会话/定稿/穿装备（页内禁用 + API） |
| 双修进行中 | 不改变 M7 优先回跳双修页 |
| 境界硬拦 | **不做**「未金丹禁止进研究室」路由级硬拦；页内按 catalog 门槛禁用定稿 |

```text
App.vue
  └── 布局壳
        ├── WorldClockBar.vue（保留）
        ├── WsStatusBadge.vue（保留）
        ├── ChatDock.vue（保留）
        ├── DmDialog.vue（保留）
        └── <RouterView />
              ├── /login | /register | /create-character
              ├── /hall → HallView.vue
              │     ├── CharacterPanel（compact）
              │     ├── HallResearchGate.vue（新）
              │     └── GameLogPanel.vue
              ├── /character → CharacterView.vue
              │     ├── CharacterPanel（详参折叠：战斗/根基/生产属性）
              │     ├── EquipmentSlotsPanel.vue（新）
              │     ├── ConstitutionPanel
              │     ├── CharacterTechniquesPanel（含自研功）
              │     └── DivineAbilityPanel（点槽从神通池装备）
              ├── /cave → AbodeView.vue
              │     ├── /cave → AbodeHubView.vue（房间：研究室）
              │     └── /cave/lab → ResearchView.vue
              │           ├── TechniqueResearchPanel.vue
              │           ├── FormationResearchPanel.vue
              │           ├── TalismanResearchPanel.vue
              │           └── ResearchMineList.vue
              ├── /workshop → WorkshopView.vue
              │     ├── RecipeList / CraftJobQueue / InventoryPanel（保留）
              │     ├── TalismanScribePanel.vue（新）
              │     └── TalismanPreloadBar.vue（新）
              ├── /formation → FormationView.vue
              │     ├── FormationPicker（扩展自研阵）
              │     ├── FormationBoard（高亮有效格；无 brush）
              │     └── 链到 /cave/lab?mode=formation
              ├── /battle | /avatar | /pets | …
              └── /sect → SectWorkshopPanel（真材料/图纸）
```

### 2.2 为何现在只再拆一条路由

| 方案 | 说明 | M8 选择 |
| --- | --- | --- |
| A. `/cave` 洞府 + `/cave/lab` 研究室（query 三模式） | 设计台属驻地房间；与工坊生产分离；顶栏只占一格「洞府」 | **采用** |
| B. 全部塞进 `/workshop?branch=` | 阵法设计器与配方列表抢屏；易在出战语义上混淆 | 不采用 |
| C. 三条新 path（功/阵/符） | 守卫与大厅入口膨胀，收益低 | 不采用 |
| D. `/formation/craft` 独立 path | 与专文备选等价，但会让「布阵/设计」两个 formation 根路径易混 | 不采用；用 `/cave/lab?mode=formation` |
| E. 顶栏一级「自研」 | 研究室不是与大厅并列的驻地玩法 | 不采用 |

**不做**：为装备单开 `/equipment` 全屏页（穿戴栏放角色页右侧，与体质并列）；不做审核后台玩家页。

### 2.3 可选 query（非新 path）

| 用法 | 说明 |
| --- | --- |
| `/cave/lab?mode=technique` | 研究室·功法（默认） |
| `/cave/lab?mode=formation` | 研究室·阵法设计器 |
| `/cave/lab?mode=talisman` | 研究室·符箓图纸 |
| `/cave/lab?session=<id>` | 打开进行中会话 |
| `/workshop?branch=talisman` | 符箓配方队列（与其它分支同布局） |
| `/workshop?branch=alchemy` 等 | 既有配方分支 |
| `/character?focus=equipment` | 滚到穿戴栏 |
| `/formation?formation=<id>` | 选中官方或自研阵（仍只摆子） |
| `/hall?hint=research` | 滚动到自研门闸 |
| `/sect?mode=forge` / `alchemy` / `talisman` | 既有；R5 接真图纸 |

### 2.4 `homePathAfterAuth`（不抢社交/渡劫优先）

```text
if !hasCharacter → /create-character
if status == awaiting_ferry → /reincarnation?mode=ferry
if status == reincarnating → /reincarnation?mode=newborn
if status == tribulation → /tribulation
if active_dual_session → /dual-cultivation?mode=session
if active_dao_contest_match → /dao-lord/…（M6 既有）
→ /hall
```

进行中的研究室会话只在大厅「继续草案」与 `/cave/lab` 恢复，**不**改变登录落地页。

### 2.5 `PLAY_ROUTE_NAMES` / 待引渡拦截

- 追加：`cave` / `cave-hub` / `cave-lab`。  
- 待引渡：**禁** `/cave/lab` 写操作与 `/workshop` 画符；**允许**角色页只读已穿戴外观（卸下可延后到新生后）。  
- 渡劫：装备槽只读；研究室只读。

---

## 3. `src` 目录增量

M0～M7 目录职责不变；M8 **追加文件与一条路由**，不推翻骨架。

```text
src/
├── api/
│   ├── equipment.ts            # 新：slots / equip / unequip / catalog
│   ├── cave.ts                 # 新：洞府枢纽 + 研究室 catalog / sessions / finalize / mine
│   ├── research.ts             # 兼容再导出 cave.ts 研究室函数
│   ├── craft.ts                # 扩展：scribe / preload
│   ├── formation.ts            # 扩展：自研阵出现在 picker
│   ├── character.ts            # 扩展 apply combat.breakdown 装备行
│   ├── sect.ts                 # 扩展 donate-blueprint
│   └── gm.ts                   # 扩展发测试装备/材料
├── components/
│   ├── hall/
│   │   └── HallResearchGate.vue
│   ├── character/
│   │   ├── EquipmentSlotsPanel.vue
│   │   └── CombatBreakdownList.vue   # 可选：抽出装备/傀儡行
│   ├── research/
│   │   ├── ResearchKindTabs.vue
│   │   ├── TechniqueResearchPanel.vue
│   │   ├── ResearchAffixPreview.vue
│   │   ├── FormationDesignerPanel.vue
│   │   ├── TerrainBrushToolbar.vue
│   │   ├── TalismanResearchPanel.vue
│   │   └── ResearchMineList.vue
│   ├── workshop/
│   │   ├── TalismanScribePanel.vue
│   │   └── TalismanPreloadBar.vue
│   └── formation/
│       └── FormationPicker.vue       # 改：分组「官方 / 自研」
├── stores/
│   ├── equipment.ts
│   ├── research.ts
│   ├── craft.ts                      # 扩展 preload
│   └── character.ts                  # 穿戴后刷新 combat
├── types/
│   ├── equipment.ts
│   ├── cave.ts
│   └── research.ts
├── utils/
│   └── safeRedirect.ts               # 白名单追加 /cave /cave/lab
├── router/
│   └── index.ts                      # 登记 cave / cave-lab；FERRY_BLOCKED
└── views/
    ├── AbodeView.vue                 # 洞府壳
    ├── AbodeHubView.vue              # 洞府枢纽
    ├── ResearchView.vue              # 研究室
    ├── CharacterView.vue             # 改
    ├── WorkshopView.vue              # 改
    ├── FormationView.vue             # 改
    └── HallView.vue                  # 改门闸
```

### 3.1 命名约定（继承）

- 页面：`AbodeView.vue` / `AbodeHubView.vue` / `ResearchView.vue`（研究室）  
- 零件：`XxxPanel.vue` / `XxxBar.vue` / `XxxGate.vue`；研究室面板仍放 `components/research/`  
- API 文件与后端模块同名（`cave.ts` ↔ `/cave`、`/cave/lab`）  
- 路由 path：小写短横线（`/cave`、`/cave/lab`）  
- 设计器棋盘复用 `components/formation/` 的 Board，**不要**复制第二套格子组件

### 3.2 子目录说明

- `views/` 仍扁平（只多 1 个整页）。  
- `FormationDesignerPanel` 可内嵌既有 `FormationBoard`，通过 prop `interactiveMode: 'place-units' | 'paint-blueprint'` 区分，禁止在 place-units 模式改地形。

---

## 4. 信息架构

### 4.0 既往改法对照（M8 只继承，不另起皮肤）

实现时以**现行 Vue 为准**，不要回退到登录演示稿或早期设计里的「侧栏大门闸」。对照如下：

| 既往改法 | 现行落点 | M8 怎么跟 |
| --- | --- | --- |
| 玩法页脱离 `.game-shell` / `.game-card` 金边暗底 | 各玩法 View：`max-width: 1100px; margin: 0 auto; padding: 1rem 1rem 2rem` | `/cave`、装备卡、画符带一律此壳 |
| 标题行折行 + `el-button size="small"` | `HallView` `.hall-title` / `.hall-nav` | 新页标题、mode-nav、穿戴钮同尺寸 |
| 大厅门闸收进顶栏，侧栏只留养成 | `HallView`：nav 按钮；`hall-side` 仅 Character + Idle | 「工坊」「洞府」进 nav；进行中草案用 warning 钮，**禁止**再塞 `HallSocialGate` 式大卡 |
| 页内多分支用按钮 mode-nav，不用整页 Tabs | `SectView` / `ShopView` / `DaoView`：当前项 `type="primary"` | `/cave/lab?mode=`、工坊 `branch` 同款 |
| 角色页左属性 / 右体质+功法；体质本源/旁支小方格；功法主/技方格 | `CharacterView` 网格 `minmax(280px,420px) 1fr`；`ConstitutionPanel` `.cons-cell`；`CharacterTechniquesPanel` | 17 区穿戴栏插在体质之上；穿戴/体质/功法三栏默认同套收起 |
| 工坊两列配方/队列；`≤900px` 单列 | `WorkshopView` `.workshop-grid` | 页内不展示背包栏 |
| 布阵 toolbar + 部署 alert + Bench 260px + 深色棋盘 | `FormationView`；`BoardGrid` 盘底 `#12121c`、可部署 `#14262e`/`#2a5f66` | 设计器换左栏工具，棋盘复用；出战页**禁止** brush |
| 错误不可关 `el-alert`；空态 `el-empty`；卡 `shadow="never"` | 全玩法页 | 通道关闭、校验失败、无自研同此 |
| 顶栏账号条 + 世界时辰条 | `AuthSessionBar`（内嵌 `PlayNav`，账号与其它标签同一组）+ `WorldClockBar` | 玩法页挂账号条；不抢时辰条 |
| 本页日志侧栏约 220～300px | `SectView` `1fr minmax(220px, 300px)` | 自研页右侧日志同宽 |
| 离开脏页确认 | `FormationView` `onBeforeRouteLeave` | 设计器 dirty 同样确认 |
| 轻量反馈，无新动画库 | 棋子选中金描边 `#c9930f`；工坊领取 toast；双修出目块 | 穿戴槽金边回落、定稿 Message、落笔变色 |

细则令牌见 **§4.8**；交互禁止项见 **§6.5**。

### 4.1 大厅（枢纽）布局增量

> **对齐现行 `HallView`**：M7 出口打磨已把侧栏门闸收进顶栏按钮；M8 **不要**再往 `hall-side` 塞一张大 `HallSocialGate` 式卡片，以免把养成区挤出首屏。

```text
┌──────────────────────────────────────────────────────────────┐
│ WorldClockBar + WsStatusBadge（App 玩法壳，全玩法页共用）      │
│ AuthSessionBar：PlayNav [大厅]…[商店][账号]（同一组、同风格） │
│ 修仙大厅 · 养成枢纽                                           │
│   有进行中自研会话时：标题行 warning 钮「继续草案」（同离线领取） │
├────────────────────────────┬─────────────────────────────────┤
│ hall-side 280～380px       │ hall-main sticky                │
│ CharacterPanel compact     │ GameLogPanel 半窗高             │
│ IdlePanel                  │ HallInviteList                  │
└────────────────────────────┴─────────────────────────────────┘
```

`HallResearchGate`：**不是**大厅主栏大卡。实现为：

1. `PlayNav` 增加「工坊」「洞府」两键（研究室走洞府二级页）；  
2. 进行中会话时，标题行出现 `el-button type="warning" size="small"`「继续草案」，跳 `/cave/lab?session=`；  
3. 可选一行 `el-alert`（`type="info"`，不可关）贴在 `hall-title` 下，文案不超过一句。

### 4.2 角色页布局（装备）

> **对齐现行 `CharacterView`**：`character-grid` 左 `minmax(280px,420px)` / 右 `1fr`；`≤800px` 单列。装备插在右侧 **体质之上**（先穿戴再看体质，避免「装备」一词与体质格混淆）。

```text
character-page（max-width:1100px）
┌ AuthSessionBar（PlayNav：[大厅]…[商店][账号]，同一组按钮）
┌ 标题：角色 · [← 大厅]
├──────────────────┬─────────────────────────────────────────┤
│ CharacterPanel   │ EquipmentSlotsPanel（el-card，默认收起） │
│  非 compact      │  标题：已穿 n/16 · 展开/收起             │
│  简介：轮回点/体力│  展开：示意剪影 + 左右列小方槽 + 底排武器 │
│  折叠：战斗属性  │  点槽详情 / 背包 / 傀儡编成板             │
│   （含元素/异常  │ ConstitutionPanel（默认收起；已镶 n/总；i 购槽） │
│     /暗抗）      │ CharacterTechniquesPanel（默认收起；主+技）│
│        根基      │ CharacterTechniquesPanel（默认收起；主+技）│
│        生产属性  │  主功法大方格 + 技法多格（随修为增槽）    │
│                  │ DivineAbilityPanel（默认收起；无主副）   │
│                  │  点槽打开紧凑已学格；悬停固定 format     │
│                  │ 本页操作提示卡（最多 5 行）               │
└──────────────────┴─────────────────────────────────────────┘
```

穿戴栏视觉（示意剪影，中间不放槽；小方格约 52px；词条在点选详情）：

```text
[头]                        [手]
[项链]                      [腿]
[胸]         修士剪影        [鞋]
[法宝一]                    [戒指一]
[法宝二]                    [戒指二]
[本命法宝]                  [饰品]
[符宝]                      [灵宠]
           [主手]  [副手]   ← 双手时两格都显示武器名，副手框为红色
┌ [上阵傀儡（n/3）] [i] ─────────────────────────────────────┐
│ 未点开：只显示已上阵名单（或「尚未上阵傀儡」）               │
│ 点开后：可上阵列表 + 发挥% / 神识占用；[上阵] 才保存         │
└──────────────────────────────────────────────────────────┘
```

通道关闭：槽位仍可穿，卡头右侧 `el-tag type="info" effect="plain"`「属性通道未启用」；拆解折叠项内第一行重复该句。

### 4.3 自研页布局

> **对齐现行 `SectView` / `ShopView`**：标题行 + **按钮式 mode-nav**（当前 mode `type="primary"`），**不要**用整页 `el-tabs` 当主导航（灵兽园 Tab 是页内子功能，自研三分支是路由 query，与宗门 mode 同类）。

```text
research-page（max-width:1100px）
┌ AuthSessionBar
┌ ← 回大厅 · 自研 · 副标题「功法 / 阵盘 / 符箓图纸」
│  mode-nav 靠右：[功法][阵法][符箓]
├────────────────────────────────────────┬──────────────────┤
│ main-left                              │ main-side 220～  │
│  mode=technique：                      │ 300px            │
│   上：材料多选 + 投入数字（el-form）    │ 本页日志卡        │
│   中：预览词条 el-tag + dice 出目块    │ （同 SectView）   │
│   下：[重投][定稿] + 命名输入           │                  │
│  mode=formation：见 §4.9 设计器         │                  │
│  mode=talisman：白名单下拉 + 材料 + 定稿│                  │
│  底部：ResearchMineList（已定稿列表）   │                  │
└────────────────────────────────────────┴──────────────────┘
空态 el-empty：「自研产出仅自己可用；定稿后不可改，只能另开新稿」
```

功法工作台再拆为 **上表单 / 下预览** 两张 `el-card`，不要一屏长表单。定稿用 `ElMessageBox.confirm`（文案见 §6.2），成功 `ElMessage.success`。

### 4.4 工坊页增量

> **对齐现行 `WorkshopView`**：标题 + **外层** branch 钮 + actor 单选 + 体力条 + 两列 `workshop-grid`（配方 / 队列）。符箓分支与其它分支同布局，**不再**叠画符 / 出战预载全宽带（出战符在装备栏设置）。页顶不展示开工天气占位行。页内**不**展示背包栏。

```text
┌ ← 回大厅 · 工坊 · 副标题「配方」
│ branch 钮（回大厅下方，size=small）：炼丹|炼器|符箓|傀儡
│ toolbar：本体|化身 · StaminaBar
├──────────────────────┬──────────────────────────┤
│ RecipeList           │ CraftJobQueue            │
│  可点选一列           │                          │
│  悬停：名/等级/属性芯片│                          │
│  所需材料（材料+灵石） │                          │
│  所需体力             │                          │
│  [开工][赋予道韵]      │                          │
└──────────────────────┴──────────────────────────┘
```

`≤900px` 仍单列堆叠（现行 media）。  
**赋予道韵**：开工按钮右侧勾选（同一行、不折到下方）；扣当前队列主体（本体/化身）道值，为造物附加本命道之力。配方是可点选列表；选中后「所需材料」只含材料与灵石，「所需体力」另起一栏。悬停走功法同款卡片：左上名称、右上制作等级、单一属性芯片（金木水火土风雷暗）、功效、使用条件（仅境界）、说明。同属性灵根额外收益本版只挂字段、不结算。

### 4.5 布阵页增量

> **对齐现行 `FormationView`**：toolbar（预设 / 名 / 攻守 / 阵法 / 保存）+ 部署 `el-alert` + `formation-grid`（Bench 260px + 棋盘）。

```text
FormationPicker 用 el-option-group：
  官方样本
  我的自研（无则组隐藏）
toolbar 右侧增加 link 钮「去研究室」→ /cave/lab?mode=formation
部署 alert 维持一行：模式中文 · 可部署 n 格 · 上阵上限
棋盘高亮仍只吃 effective_deploy_cells（青绿 deployable）
禁止出现 brush 工具条
```

### 4.6 组件职责

| 组件 | 职责 | 不负责 |
| --- | --- | --- |
| `HallResearchGate` | 顶栏「洞府/工坊」+ 进行中草案 warning 钮；可选一句 alert | 大厅侧栏大卡、本地 settle |
| `EquipmentSlotsPanel` | 穿脱 UI、槽位中文名 | 本地加 phys_atk |
| `CombatBreakdownList` | 渲染服务端 breakdown | 发明乘区 |
| `TechniqueResearchPanel` | 材料/预览/定稿 | `Math.random` 词条 |
| `FormationDesignerPanel` | 画区/brush/保存草案 | 写出战预设 units |
| `TerrainBrushToolbar` | 预算内选地形类型 | 刷到敌方半区（按钮禁用） |
| `TalismanResearchPanel` | 选白名单效果、定稿图纸 | 手填效果 id |
| `TalismanScribePanel` | 批量画符 | 伪造库存 |
| `TalismanPreloadBar` | 预载确认 | 跨场无限用 |
| `ResearchMineList` | 已定稿列表、跳转装备/出战 | 改冻结蓝图 |
| `ResearchView` | 编排拉数、日志 | 大段公式 |

### 4.7 关键时序

**穿戴装备**

```text
/character → POST /equipment/equip
  → 刷新 CharacterPublic.combat / idle_env
  → 拆解出现「装备」；失败 40205 中文
```

**自研功法**

```text
/cave/lab?mode=technique → POST /cave/lab/sessions
  → 展示 dice 与词条预览（label_zh）
  → finalize 命名 → 出现在功法面板
```

**自研阵法**

```text
画 mask / brush → save-draft → finalize
  → /formation 下拉可见 → 存预设校验有效格
  → 更新防守快照（内联蓝图）
```

**画符出战**

```text
定稿图纸 → /workshop?branch=talisman scribe
  → preload → /battle 开战
  → 战报「符箓：××」（禁止裸 effect id）
```

### 4.8 视觉令牌与组件惯例（继承现行，禁止另起皮肤）

玩法页自 M1 起已从登录壳的 `game-card` 演示稿，收敛为 **居中文档流 + Element Plus**。M8 **必须**复用下列惯例，禁止把 `style.css` 里 `.game-shell` / `.game-card` / 金色印章顶栏搬进 `/cave`。

| 令牌 | 现行值 | M8 用法 |
| --- | --- | --- |
| 页宽 | `max-width: 1100px; margin: 0 auto; padding: 1rem 1rem 2rem` | 所有新 View |
| 标题行 | `display:flex; flex-wrap; gap: 0.5rem 0.75rem; margin: 0.75rem 0 1rem` | `page-title` / `character-title` |
| 主按钮尺寸 | `el-button size="small"` | 导航、穿戴、定稿、画符 |
| 卡片 | `el-card shadow="never"` | 装备槽、自研表单、日志、预载 |
| 危险/提示 | `el-alert` 不可关；错误 `type="error"` | 通道关闭、校验失败 |
| 空态 | `el-empty`，`image-size` 48～64 | 无自研、无图纸、散修工坊 |
| 表格 | `el-table size="small" stripe` | 功法列表、已定稿列表 |
| 属性表 | `el-descriptions border size="small"` | 装备预览一行属性 |
| 折叠 | `el-collapse` | 角色详参；自研「词条说明」可折叠 |
| 点选背包 | `el-check-tag` | 装备背包、自研材料 |
| 槽位框 | 虚线小方格 `border: 1px dashed`；选中金描边 `#c9930f` | 穿戴栏指针槽、体质本源/旁支、符预载格 |
| 折列断点 | 大厅/角色/布阵/宗门 **800px**；工坊 **900px** | 新网格同样 |
| 顶栏账号 | `AuthSessionBar` 底边 `var(--el-border-color-lighter)` | 玩法页必挂；内嵌 PlayNav |
| 世界顶栏 | `WorldClockBar` 时辰切换 `shichen-fade` | 不改；自研不抢顶栏 |

**棋盘色（设计器必须跟布阵同一套 `BoardGrid`）**：

| 语义 | 现行（`BoardGrid`） | 设计器增量 |
| --- | --- | --- |
| 盘底 | `#12121c` / 格 `#1a1a28` | 不变 |
| 可部署 | `#14262e` 边 `#2a5f66` | 出战页；设计器 mask 涂区用同一 deployable |
| 选中棋子 | 金描边 `#c9930f`（`.token-selected`） | 穿戴成功槽位可短借此金色，0.4s 回落 |
| 敌方半区 | `#251722` | 设计器 **禁止** 落地形 |
| 笔刷预算内可涂 | — | 复用既有 `path`/`to` 高亮思路：可涂格用青绿，越区格不可点 |
| 障碍/沟壑/禁制 | 既有 terrain 叠层 | brush 落笔后立刻画上，与出战地形同色 |

**大厅导航色（沿用现行语义，勿新造第五套 type）**：

| 按钮 | 现行 | M8 |
| --- | --- | --- |
| 角色 | `primary` + `plain` | 不变 |
| 战斗 | `danger` | 不变 |
| 化身 | `warning` | 不变 |
| 宗门 | `success` + `plain` | 不变 |
| 社交 | `primary` | 不变 |
| 商店 | `warning` + `plain` | 不变 |
| **工坊** | （现行无） | `warning` + `plain` |
| **洞府** | （新，原自研） | `success` |

### 4.9 阵法设计器布局（`/cave/lab?mode=formation`）

与 `/formation` **同构不同语义**：左栏换工具，棋盘复用，保存按钮文案换成「保存草案 / 定稿」。

```text
┌ 部署模式 el-select · max_units 数字 · 四象白名单 el-select
│ TerrainBrushToolbar：
│   [障碍 n/max] [沟壑 n/max] [擦除]
│   预算用 el-tag：未超=info，满额=warning
├─────────────────────┬──────────────────────────────────────┤
│ 工具说明卡          │ FormationBoard                        │
│  · 当前模式中文     │  interactiveMode=paint-blueprint      │
│  · 移位条数 0/帽    │  己方半区可点；敌区禁点 toast         │
│  · [保存草案]       │                                      │
│  · [定稿][去布阵]   │                                      │
└─────────────────────┴──────────────────────────────────────┘
下方 el-alert：定稿后地形冻结，出战页只摆棋子
```

左栏宽度与布阵 Bench 一致：`minmax(200px, 260px)`。窄屏先工具条后棋盘。

### 4.10 显性数值怎么摆（对齐挂机环境行）

挂机区已用「一行基础→有效，细节悬停 i」。装备/自研同样：

| 场景 | 默认看见 | 展开后才看见 |
| --- | --- | --- |
| 穿戴后 | 方槽短名；点选后详情出词条与卸下 | 完整 stats；breakdown 折叠 |
| 挂机 | `IdleEnvPanel` 乘区出现「装备」 | 悬停 i 列出 idle_mult |
| 自研预览 | 词条中文 tag + 出目/阈值 | schema 英文 id 仅 DEV 或折叠「机读」 |
| 通道关 | tag「未启用」 | 不假装数字变了 |

---

## 5. API / Store 对应

| 文件 | 函数（示意） |
| --- | --- |
| `api/equipment.ts` | `fetchSlots` · `equipItem` · `unequipSlot` · `fetchEquipmentCatalog` |
| `api/cave.ts` | `fetchCaveOverview` · 研究室 catalog / sessions / draft / finalize / mine |
| `api/research.ts` | 再导出研究室函数（兼容 store） |
| `api/craft.ts` | `scribeTalisman` · `putTalismanPreload` |
| `api/formation.ts` | 既有 presets；picker 识别 `source=custom` |
| `api/sect.ts` | `donateBlueprint` |

**`useEquipmentStore`**

| state / action | 说明 |
| --- | --- |
| `slots` / `catalog` | |
| `equip` / `unequip` / `refresh` | 成功后 `character.fetchMe()` |

**`useResearchStore`**

| state / action | 说明 |
| --- | --- |
| `kind` / `session` / `mine` / `catalog` | |
| `create` / `reroll` / `saveDraft` / `finalize` | 定稿后清 session |

**`useCharacterStore`**：识别 `combat.breakdown` 中 `equipment` / `puppet`；通道关闭文案优先信 `channels_label_zh`。

---

## 6. 交互与文案约定

### 6.1 装备

- 槽位中文：主手 / 副手 / 头 / 项链 / 胸 / 手 / 腿 / 鞋 / 戒指 / 饰品 / 法宝 / 本命法宝 / 符宝 / 灵宠（id 仍为 `weapon_1` 等）。  
- 点槽：列出该部位可装的背包装备；已穿件排第一、金色边框，可「卸下」；其余点「装备」。再点同一槽取消选中并隐藏背包栏。空背包用带边框栏位「此部位背包暂无可用装备」。不再展示无筛选总背包。  
- 穿戴失败：「该灵物无法装入此槽」或「境界不足」（信服务端 message）。  
- 通道关闭：「装备属性通道未启用，穿戴仅作外观/预留」。

### 6.2 自研

- mode-nav 中文：功法 / 阵法 / 符箓（**不要**写成整页 `el-tabs`）。  
- 定稿确认：「定稿后不可再改此稿，只能另开新研。」  
- 审核按钮若展示：点击后「审核池尚未开放」（40210），不要做假进度条。  
- 词条/效果只从 catalog 点选，禁止手填英文 id。

### 6.3 阵法设计器

- 部署模式中文：默认区 / 固定格 / 己方半区自选 / 掩码区（对应 `default`/`fixed`/`free_own`/`mask`）。  
- 越区落笔即时提示，与服务器 `40202` 一致。  
- 「去布阵」与「保存蓝图」分按钮，避免误存进攻预设。

### 6.4 战报

- 「官方样本」/「自研」来源标签。  
- 符箓触发只播中文名。

### 6.5 UI 基调（视觉方案 · 对齐既往改法）

> 既往改法摘要：登录演示壳（`game-card` 金边暗底）**不**用于玩法页；玩法页 = 文档流 1100px + Element Plus；M3 棋盘单独深色战棋；M5 顶栏时辰淡入；M7 大厅导航收敛、聊天坞浮层、骰子翻面。M8 只在该骨架上加槽位/设计器/画符带。

**做**

- 组件优先：`el-card` / `el-button size="small"` / `el-alert` / `el-empty` / `el-table` / `el-select` / `el-check-tag` / `el-descriptions` / `el-collapse` / `ElMessage` / `ElMessageBox`。  
- 间距用 `rem`（0.35 / 0.5 / 0.75 / 1），与大厅、角色、工坊、宗门一致。  
- 自研三分支用 **按钮 mode-nav**（宗门/商店同款），当前项 `type="primary"`。  
- 装备槽为示意剪影 + 左右列虚线小方格（中间不叠部位槽）；点槽列出该部位可装备，不把登录页纸娃娃立绘搬进角色页。  
- 阵法设计器复用 `BoardGrid` 深色格，不把 Element 浅色表单画进格子里。  
- 通道关闭、校验失败用 **alert / tag 中文**，不用红字散落。  
- 本页日志侧栏（自研）宽度 `minmax(220px, 300px)`，与宗门一致。

**不做**

- 不为 M8 引入新主题色盘、新字体、全屏炼器 CG、拖拽人形换装。  
- 不把 ChatDock 做成自研页内嵌。  
- 不用 `el-tabs` 当研究室主分发（query mode 才是权威，刷新可还原）。  
- 不在出战布阵页出现笔刷图标。  
- 不把装备「属性通道未启用」做成隐藏槽位。

**轻量动效（至少 3 处，沿用既有节奏）**

| 处 | 做法 | 参照 |
| --- | --- | --- |
| 穿戴成功 | 对应槽 `box-shadow` 金边约 0.4s 后回落 | 棋子 `selected` 金描边，勿新动画库 |
| 定稿成功 | `ElMessage.success`；列表插入一行 | 工坊领取 toast |
| 设计器落笔 | 格背景切到 terrain 色（无额外粒子） | BoardGrid 既有格状态 |
| 自研出目 | 复用双修骰结果块的数字展示，**可**短闪；禁止另做 3D 骰 |

**响应式**

- `≤800px`：角色左右栏、自研主栏/日志、设计器工具/棋盘改为单列。  
- `≤900px`：工坊两列改为单列。  
- 大厅 `hall-nav` 已 `flex-wrap`；新增键跟着折行，不引入「更多」菜单（M4 文档曾提过，现行未做，M8 也不做）。

**可达与防误触**

- 定稿、卸下、画符批量：主操作 `primary`，破坏性（卸下/放弃草案）用 `link` + `type="danger"`（体质卸下已如此）。  
- 离开设计器且草案 dirty：`onBeforeRouteLeave` 确认（布阵页已有）。  
- 预载符与画符按钮在 `busy` 时 `loading`，防连点。

### 6.6 GM（DEV）

- 发测试装备、自研材料、抬 `array_craft_level`、开关通道（审计）。  
- 仅 `import.meta.env.DEV`。

---

## 7. 类型对齐（要点）

```ts
/** 与后端 app.constants.equipment 对齐 */
export type EquipmentSlot =
  | 'weapon'
  | 'armor'
  | 'accessory_1'
  | 'accessory_2'
  | 'fabao'

export interface EquipmentSlotPublic {
  slot: EquipmentSlot
  slot_label_zh: string
  item_uid: string | null
  item_label_zh: string | null
  stats_preview?: Record<string, number>
}

export type ResearchKind = 'technique' | 'formation' | 'talisman'

export type ResearchPhase =
  | 'drafting'
  | 'previewed'
  | 'finalized'
  | 'cancelled'
  | 'expired'

export interface ResearchSessionPublic {
  id: number
  kind: ResearchKind
  kind_label_zh: string
  phase: ResearchPhase
  phase_label_zh: string
  dice?: {
    purpose: 'research_technique' | 'research_formation' | 'research_talisman'
    roll: number
    label_zh: string
  }
  affix_previews: { id: string; label_zh: string }[]
}

export interface PrivateContentPublic {
  id: string
  source: 'custom'
  source_label_zh: '自研'
  label_zh: string
  revision: number
  kind: ResearchKind
}
```

前端协议枚举须注释「与后端 constants 对齐」，避免分叉。

---

## 8. 与后续里程碑

| 里程碑 | 对本设计的影响 |
| --- | --- |
| **M8** | 本文范围；建齐 `/research` + 装备槽 + 画符预载 |
| **M9** | 野外掉落装备/图纸可挂同一背包类型；不改路由 |
| **M11** | 战场不复用设计器 |
| **M13** | 填装备/词条正式曲线；UI 字段名不改 |
| 延后项 | 一律先查 [`后续待完成.md`](./后续待完成.md) |

---

## 9. 开发进度与步骤（对照 M8）

> 本节随实现同步更新；接口以 `M8自研与内容管线设计.md` 为准。

### 9.1 总览（对齐核心设计 §14 R1～R6）

| 步 | 内容 | 状态 | 说明 |
| --- | --- | --- | --- |
| 0 | 设计文档（本文 + 核心骨架） | **已完成** | 2026-08-17 v1.0；**v1.1 补视觉布局** |
| **R1** | 类型 + `api/equipment` + 角色页穿戴栏 + breakdown | **已实现** | 2026-08-17；槽表后扩为 17 区 |
| **R2** | `ResearchView` 功法分支 | **已实现** | 2026-08-17；mode-nav + 侧栏日志；角色页「去自研」 |
| **ITEM-0 → R0** | 道具 ABC（无新路由） | **进行中** | 专文 [`道具与装备实体设计.md`](./道具与装备实体设计.md)；**竖切最高优先** |
| **R3** | 阵法设计器 + picker 自研分组 | **已实现** | 2026-08-17；复用 BoardGrid；工具条换 Bench；`/formation` 只摆子 |
| **R4** | 符图纸 + 工坊画符/预载 + 战报中文 | **已实现** | 2026-08-17；三列网格上加全宽带；无时效 buff UI |
| **R5** | 宗门工坊真图纸捐赠 | **已实现** | 2026-08-17；`manual`+`manual_kind`；背包上缴/领取；无图纸属性 UI |
| **R6** | 大厅 nav 补工坊/自研 + 继续草案钮 + 守卫 + GM | **已实现** | 不塞大厅大卡 |

### 9.2 本地联调口令（实现后）

```powershell
# 后端
cd backend
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000

# 前端
cd frontend
npm run dev
```

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/equipment/slots` | GET | 17 区指针槽（含 `pet`）+ `puppet_sense` |
| `/api/v1/equipment/equip` | POST | 穿戴 |
| `/api/v1/equipment/puppet-loadout` | PUT | 整表保存上阵傀儡（`item_uids`） |
| `/api/v1/cave` | GET | 洞府房间列表（研究室） |
| `/api/v1/cave/lab/catalog` | GET | 材料与白名单 |
| `/api/v1/cave/lab/sessions` | GET | 进行中草案（继续草案） |
| `/api/v1/cave/lab/sessions` | POST | 开会话 |
| `/api/v1/cave/lab/sessions/{id}/draft` | POST | 保存阵法草案或符箓效果 |
| `/api/v1/cave/lab/sessions/{id}/finalize` | POST | 定稿 |
| `/api/v1/craft/talisman/scribe` | POST | 画符 |
| `/api/v1/craft/talisman/preload` | GET/PUT | 出战预载栏 |
| `/api/v1/formation/presets` | GET | 含自研阵 |

---

## 10. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-20 | **v1.1.30**：自研迁入洞府；顶栏「洞府」；`/cave/lab` 研究室；API `/cave` + `/cave/lab/*`；旧 `/research` 重定向/别名 |
| 2026-08-20 | **v1.1.29**：工坊去掉阵法分支（不属于制造业）；阵法等级仍在角色面板/自研门槛 |
| 2026-08-19 | **v1.1.28**：工坊去掉背包栏（配方+队列）；材料=材料+灵石，体力另栏；悬停单一属性（金木水火土风雷暗），无抗性 |
| 2026-08-19 | **v1.1.27**：工坊图纸点选列表、材料在选中后展示、开工在列表底；悬停条件栏读物品 inspect |
| 2026-08-19 | **v1.1.26**：工坊符箓页去掉画符带与出战预载；配方只显示名称、悬停详情；开工旁「赋予道韵」 |
| 2026-08-19 | **v1.1.25**：工坊五分支外层钮（回大厅下方）；去掉页顶天气占位；消耗道值勾选挪到开工后 |
| 2026-08-18 | **v1.1.24**：体质/功法/神通/装备选择栏改紧凑方格；悬停走装备属性 format |
| 2026-08-18 | **v1.1.23**：神通栏点槽从池选装（无主副）；格数=修为基数+品阶 |
| 2026-08-18 | **v1.1.22**：右侧神通栏按品阶槽装备已学神通；根基神通与耐力对调 |
| 2026-08-18 | **v1.1.21**：悟性/耐力/神通进根基；战斗按资源-攻防-机动-抗性排；生成属性按工坊分支成对 |
| 2026-08-18 | **v1.1.20**：简介含轮回点/体力；折叠改「生产属性」；天劫抗/心魔抗/吐纳进根基；根骨格不拉满 |
| 2026-08-18 | **v1.1.19**：角色页功法栏改主功法大方格 + 技法多格；悬停名/lv/属性/来源；灵根上面板 |
| 2026-08-18 | **v1.1.18**：账号并入 `PlayNav` 同一组按钮，不再靠右单独一颗 |
| 2026-08-18 | **v1.1.17**：`PlayNav` 迁入 `AuthSessionBar` 与「账号」同级；去掉顶栏第二套账号；大厅/账号页挂账号条 |
| 2026-08-18 | **v1.1.16**：`PlayNav` 挂玩法壳，任意玩法页互跳；装备栏化身开关 + 符箓编成；工坊预载改为指向装备栏；阵法只布阵 |
| 2026-08-18 | **v1.1.15**：角色面板取消「其它」；轮回点/神通/制作等级并入生活属性 |
| 2026-08-18 | **v1.1.12**：体质栏改本源/旁支紧凑格；点槽打开收藏区；i「可以通过轮回点购买更多体质槽」 |
| 2026-08-18 | **v1.1.10**：傀儡编成板改为「上阵傀儡」勾选栏（点选/取消实时算发挥%，上阵才保存）；旁 i 标签说明神识规则 |
| 2026-08-17 | **v1.1.9**：角色页穿戴栏改为示意剪影（左右列 + 底排主手/副手）；槽名项链/饰品/符宝；穿戴/体质/功法默认收起 |
| 2026-08-17 | **v1.1.8**：**出口打磨**（`usePlayWriteGate`；阵法脏离开；切 mode 清 session；材料中文名/空态；「后续开放」文案） |
| 2026-08-17 | **v1.1.7**：**R6 已实现**（大厅 nav 工坊/自研；继续草案；`session` query；不塞大卡） |
| 2026-08-17 | **v1.1.6**：**R5 已实现**（宗门工坊兑换入包、背包上缴、代工领取；无图纸品阶 UI） |
| 2026-08-17 | **v1.1.5**：**R4 已实现**（`TalismanResearchPanel` + 工坊全宽画符/预载带；战报 `item_trigger` 中文） |
| 2026-08-17 | **v1.1.4**：**R3 已实现**：`/research?mode=formation` 设计器；布阵 picker 官方/自研分组；「去设计新阵盘」 |
| 2026-08-17 | **v1.1.2**：废除五槽 UI 表述；角色页穿戴栏改为 17 区分组虚线槽 + 傀儡编成板说明 |
| 2026-08-17 | **R0 开工**：背包展示四页标签与占用标；无新路由 |
| 2026-08-17 | **ITEM-0 设计开篇**：指针 [`道具与装备实体设计.md`](./道具与装备实体设计.md)；无新玩法路由 |
| 2026-08-17 | **R2 已实现**：`/research?mode=technique` + 功法来源列；符仍占位；大厅 nav 仍待 R6 |
| 2026-08-17 | **R1 已实现**：后端 `/equipment/*` + 前端穿戴栏；通道已开（槽表后扩 17 区） |
| 2026-08-17 | **v1.1**：按现行玩法页改法补布局/样式文字方案（1100px 壳、mode-nav、虚线槽、BoardGrid 色、大厅不塞大卡、工坊全宽画符带） |
| 2026-08-17 | 初稿 **v1.0**：相对 M7 增 `/research` 三分支；角色页穿戴栏；工坊画符/预载；布阵只摆子；对齐 R1～R6 |

---

*玩法与接口以 `M8自研与内容管线设计.md` 为准；排期以 `开发计划.md` §10 为准；延后以 `后续待完成.md` 为准。*

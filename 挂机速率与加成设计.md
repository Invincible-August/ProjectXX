# 挂机速率与内外部加成 — 设计说明

> 依据：`project修仙.md` §9 · `M2成长深度设计.md` §4.3 · 开发计划 §0.7 显性设计 · M5 环境乘区  
> 版本：v1.2 · 2026-08-07（ADM：`idle` 为高危域，发布须 `confirm_high_risk`；M2-D01 覆盖已通）  
> 目标：统一「**按境界基础速率 × 内部加成 × 外部加成**」公式；配置驱动；面板可拆解研究  
> 本期落地：境界基础表 + 既有时辰/天气/灵根·功法 tag + 体质钩子；装备/丹药/符箓/灵眼/洞府为通道占位（见 `后续待完成.md`）  
> **后台**：正式改 `idle.yaml` 走运营后台域 `idle`（balance）；DEV 改 YAML 仍可用，但须遵守主计划 §0.0。

---

## 1. 设计目标

1. **不同大境界基础修炼速度不同**（修灵 / 炼体 / 制造业各自一张表）。
2. **内部加成**：灵根、功法、体质、装备（后两者本期钩子/延后）。
3. **外部加成**：时辰/天气环境、丹药、符箓、灵眼、洞府道具（后四者本期通道占位）。
4. **显性**：`idle_env.breakdown` 列出各来源乘区；修炼区「本片预计」与 settle 同式。
5. **数值占位**：曲线可改 YAML，不定死最终平衡（GDD §23 TBD）。

---

## 2. 核心公式

```text
base = realm_base[direction][major_realm]
       # 缺省回落 idle.yaml directions.*.*_per_tick

internal_mult = Π(灵根×功法 tag 乘区已并入 env 层时可不重复,
                  constitution_idle, equipment_idle, …)
external_mult = Π(shichen, weather, buff_pill, buff_talisman, spirit_eye, cave, …)

# 与现网 env_preview 对齐的实用写法（本期）：
#   env_mult = clamp(tag_product × shichen × weather, env_clamp)
#   channel_mult = Π(enabled bonus_channels 的实际乘区，默认 1.0)
#   bonus_clamp = idle.yaml clamp_min/max（作用于 channel×env 的再钳制可选）
#
# 本期默认实现：
effective = floor(base × channel_mult × env_mult)
```

其中 `env_mult` 沿用 M5：`combine_env_multipliers(tag_product, shichen, weather)` + calendar/weather clamp。

`channel_mult` 为本期新增：体质等内部通道 × 外部时效/驻地通道（未启用或无实例则为 `1.0`）。

### 2.1 叠乘分层

| 层 | 来源 | 本期状态 |
| --- | --- | --- |
| A 基础 | `gain_per_tick_by_realm[direction][major]` | **实现** |
| B 内部 | 灵根/功法 `env_tags`（并入 env）；体质 `constitution_idle`；装备 `equipment_idle` | tag **已有**；体质 **钩子**；装备 **延后关** |
| C 外部·环境 | 时辰 / 天气 | **已有** |
| C 外部·时效 | `buff_pill` / `buff_talisman` | **通道关**，恒 1 |
| C 外部·驻地 | `spirit_eye` / `cave` | **通道关**，恒 1 |

---

## 3. 配置（`idle.yaml`）

| 键 | 含义 |
| --- | --- |
| `directions.*.*_per_tick` | 回落基础速率 |
| `gain_per_tick_by_realm.<spirit\|body\|crafting>.<major>` | 按大境界基础产出 |
| `spirit_stone_cost_by_realm` | 每 tick 灵石（已有） |
| `bonus_channels.<id>.enabled` | 通道是否参与乘积 |
| `bonus_channels.<id>.default_mult` | 无实例时的乘区（通常 1.0） |
| `clamp_min` / `clamp_max` | 通道总乘区建议钳制（与 env clamp 分工见实现注释） |

体质词条 / 装备 meta 约定（钩子）：实例带 `idle_mult`（float）时乘入对应通道。

---

## 4. API / 面板契约

`CharacterPublic.idle_env.<direction>`：

| 字段 | 说明 |
| --- | --- |
| `base_per_tick` | **境界基础**（A 层），非最终有效值 |
| `effective_per_tick` | `floor(base × …)` 最终展示/结算用 |
| `total_mult` | 相对 base 的总乘区 |
| `breakdown[]` | `{ source, id, label, mult }` |

新增 / 约定 `source`：

| source | 含义 |
| --- | --- |
| `realm_base` | 标明基础取自哪一境界（mult 恒 1，id=major） |
| `shichen` / `weather` | 环境 |
| `tag_shichen` / `tag_weather` | 灵根/功法标签 |
| `constitution` | 体质挂机乘区 |
| `equipment` | 装备（延后） |
| `buff_pill` / `buff_talisman` | 丹药/符箓 buff |
| `spirit_eye` / `cave` | 灵眼/洞府 |

前端「本片预计」可用 `/world/env.idle_preview` + 角色拆解在浏览器重算（仅展示）；**入账以服务端 settle 为准**。

---

## 5. 与里程碑关系

| 已有 | 本设计动作 |
| --- | --- |
| M2 三向 settle | base 改查境界表 |
| M5 env 乘区 | 保留；breakdown 扩展 |
| 体质系统 | 读已装备词条 `idle_mult` 钩子 |
| 装备 / 丹药 / 符箓 / 灵眼 / 洞府 | `后续待完成` **IDLE-R01～R04** |

---

## 6. 验收清单

- [x] 锻体与金丹等不同 `major_realm` 的 `base_per_tick` / settle 产出可测不同
- [x] 无加成时 effective ≈ base × env_mult（与改表前行为兼容路径可测）
- [x] `idle_env.breakdown` 含 `realm_base`；启用体质钩子时可出现 `constitution`
- [x] 关闭的 bonus_channels 不改变乘积（×1）
- [x] README / CHANGELOG 已同步；延后项已登记

---

## 7. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-06 | v1.0 初稿并落地境界基础表 + 通道骨架 |
| 2026-08-07 | v1.1：`idle.yaml` 锻体/炼气 `spirit_stone_cost=0`；cost=0 时 settle 按满 tick 推进（勿再走「else 0」）；跨时辰切段仍属 **M5-D11** |

# 异步真读条突破设计（M5-D05 / M1-D20）

> **文档性质**：突破体验竖切真相源；消化 [`后续待完成.md`](./后续待完成.md) **M5-D05** / **M1-D20**  
> **版本**：v1.2 · 2026-08-10  
> **实现状态**：**Phase A 已落地**；**产品定案：默认关闭读条**（`async_channel.enabled=false`，点击同步出结果）  
> **关联**：[`开发计划.md`](./开发计划.md) **§0.0 / §0.0.1 / §0.0.2 / §0.7 / §0.8 / §0.9** · [`M1核心循环设计.md`](./M1核心循环设计.md) §5 · [`M5环境与轮回外环设计.md`](./M5环境与轮回外环设计.md) · [`骰子系统设计.md`](./骰子系统设计.md) · [`后台管理系统开发计划.md`](./后台管理系统开发计划.md)  
> **原则**：服务端权威；机读英文、人读中文（§0.0.2）。读条能力保留为配置开关，默认不启用。

---

## 0. 产品定案（2026-08-10）

| 项 | 说明 |
| --- | --- |
| **默认体验** | **无读条**：玩家点「发起突破」→ `POST /breakthrough/attempt` → **立即**弹结果（或分流渡劫） |
| **配置** | `breakthrough.yaml` → `async_channel.enabled: false` |
| **前端** | `BreakthroughPanel` 只走同步 attempt；去掉假读条与真进度条 UI |
| **后端** | `/breakthrough/channel/*` 与会话表 **保留**（ADM 可再开；单测仍强制 enabled）；默认路径不占用 `breaking_through` 跨请求 |
| **旧档** | 若角色仍停在 `breaking_through`，预览/面板会懒结算一次 |

下文 §1～§n 描述 **enabled=true** 时的真读条契约（运营再开时仍适用）。

---

## 0.1 落地对照（Phase A）

| 设计项 | 代码落点 | 状态 |
| --- | --- | --- |
| 读条时长 / 文案 / 开关进配置 | `breakthrough.yaml` `async_channel` + Bundle | **已落地**（默认 **关**） |
| 突破进行中会话 | `breakthrough_sessions` ORM | **已落地**（可选路径） |
| 开读条 / 查进度 / 到期结算 | `BreakthroughService` + `/breakthrough/channel/*` | **已落地**（可选） |
| 跨请求 `status=breaking_through` | 仅 `enabled=true` | **可选** |
| 前端同步出结果 | `BreakthroughPanel` → `attempt` | **现行默认** |
| ADM 字段中文 schema | 域 `breakthrough` | **已落地** |
| 边界单测 | `tests/test_breakthrough_async_channel.py`（monkeypatch 强制开） | **已落地** |

---

## 1. 定案与边界

### 1.1 一句话目标

把「假读条（结果已在同一 HTTP 内算完，前端再演 0.8～1.5s）」升级为：**服务端真实占用 `breaking_through`，跨请求可观测进度，到期权威结算成败**；玩家刷新/重连仍看到进阶中，期间不能开战、挂机零产出。

### 1.2 要解决的体验问题（目的）

| 现状（M1～M5） | 问题 | 本竖切要达成 |
| --- | --- | --- |
| `POST /breakthrough/attempt` **同步**掷骰并立刻改境界/扣费 | 弱网重试、多标签、切页后「读条」与权威不同步；假条可被跳过感知为动画 | **权威读条窗口**：未到期前角色就是进阶中 |
| 前端 `runFakeProgress` 与结果并行 | 读条不占用状态机；互斥只在 attempt 瞬间 | **跨请求互斥**：`activity.mode=breaking_through` 可被大厅轮询看见 |
| 渡劫已有「进行中会话」心智 | 若把普通突破也塞进渡劫会话会拖垮 E4 | **独立轻量会话**；雷劫门槛仍走 `needs_tribulation` → `/tribulation/*` |

**本功能目的一句话**：让「突破」从「点一下立刻出结果 + 演出」变成「闭关进阶一段时间、世界规则在期间生效、时间到了才揭晓」——与挂机/渡劫同一套服务端权威与活动互斥哲学，但**不**做成渡劫那么重。

### 1.3 写死边界

| 做 | 不做 / 说明 |
| --- | --- |
| 无劫突破（层进阶 / 未达雷劫门槛的跨境）走异步读条 | **不**把渡劫准备/开渡并进读条；达劫仍 `needs_tribulation`，不创建读条会话 |
| `status=breaking_through` 跨请求可见直至结算 | **不**引入第五种 status；**不**把挂机升成 status |
| 到期自动结算（懒结算：任意相关 API / 角色拉取时 resolve） | Phase A **不**上 WebSocket 推送（**M5-D04**）；可用大厅轮询 |
| 读条期间：禁开战、禁再开突破、禁改挂机进产出；settle **只推锚点零产出** | **不做**读条中途取消退费（承诺闭关）；取消若以后要做 → 另登记 |
| 成败仍走 `DiceService.roll_breakthrough`（§0.9） | **禁止**业务内再发明 `random()` |
| 时长 / 开关 / 中文文案进 `breakthrough` 配置 + ADM | 禁止前端写死 800～1500ms 当权威时长 |
| 幂等：开读条沿用 `Idempotency-Key`（M1-D30） | 结算接口同样防双结 |

### 1.4 与现行落地的关系

| 已落地（保持） | 本文扩展 |
| --- | --- |
| `attempt` 内校验门槛、灵石、互斥、雷劫分流 | 拆为 **start（开读条）** + **resolve（到期结算）**；`attempt` 可保留为兼容壳或 Feature Flag 切回同步 |
| 成功/失败扣修为·灵石·写品阶 | **结算时刻**执行；开读条时**锁定意图并扣灵石**（见 §4.3） |
| `breaking_through` 在同步路径内闪现 | 变为真实持续状态 |
| 前端假读条 + 结果弹窗 | 真进度条（按 `ends_at`）；结算后再弹窗 |
| `activity_mutex` 已拦截 `breaking_through` | 补「跨请求仍拦截」单测与 UI 续条 |
| 渡劫 `tribulation_sessions` | **不复用**该表；另开轻量突破会话 |

### 1.5 配置与后台纪律（§0.0）

| 必须 | 禁止 |
| --- | --- |
| 域 **`breakthrough`** 增加读条段：`async_channel.enabled`、时长规则、`label_zh` / `help_zh` / 玩家提示文案 | 引擎硬编码「一律 3 秒」且无法运营改 |
| 时长可按 `layer_advance` / `major_advance` 分设，或统一 `channel_seconds` + 可选倍率 | 前端按境界写死毫秒表 |
| ADM：表格+JSON 双写；字段中文 schema（§0.0.1） | 只给 raw YAML、无中文列 |
| 玩家服只读 Bundle | 把 DEV `/gm` 当运营改读条入口 |

**§0.0 自检**：运营把「层进阶读条」从 5s 改成 12s → 只改 `breakthrough` 发布，玩家端无发版。

### 1.6 显性设计与中文信条（§0.7 · §0.0.2）

玩家必须能感知：

1. **当前在闭关**：大厅 `ActivityStatusBanner` / `activity.mode_label` 显示「进阶中」（已有中文映射）；并显示 **剩余时间或进度百分比**（新增）；  
2. **读条进度**：突破面板进度条跟服务端 `progress_ratio` / `ends_at`，禁止只播本地假条却声称「天机运转」；  
3. **结算结果**：到期后弹窗仍展示成败、修为/灵石变化、`dice` 出目与阈值（接 M5-D12）；文案中文；  
4. **拦截原因**：读条中点开战/突破 → 明确「进阶中不可…」；挂机面板提示零产出（若仍显示方向）。

禁止：结果已入库、界面还在播无关假动画；或界面显示进阶中但服务端已是 `normal`。

### 1.7 Feature Flag（降低风险）

| 配置键 | 含义 | 默认建议 |
| --- | --- | --- |
| `async_channel.enabled` | `true`：走异步读条；`false`：同步 `attempt` **立即出结果**（无假条） | **产品定案默认 `false`**（2026-08-10）；运营可开 |

同步路径为现行玩家体验；读条为可选能力。**不得**两套规则长期分叉（扣费时机、骰子、雷劫分流须一致，仅「是否跨请求占用 status」不同）。

---

## 2. 术语

| 术语 | 英文键 | 含义 |
| --- | --- | --- |
| **假读条** | fake channel | 现行：HTTP 已返回结果，前端本地动画 |
| **真读条** | async channel | 服务端 `breaking_through` 持续至 `ends_at` |
| **开读条** | start / begin channel | 校验通过后创建会话、置状态、扣灵石、锁定结算意图 |
| **进度** | progress | `(now - started_at) / duration`，钳制 `[0,1]` |
| **到期结算** | resolve / settle channel | 掷骰并应用成败；清会话；`status→normal` |
| **懒结算** | lazy resolve | 不依赖独立 cron；在 preview/me/attempt/idle settle 等路径入口先 resolve |
| **锁定意图** | locked intent | 开读条时固化 `advance_type`、规则快照、有效成功率等，避免读条中途改配表导致前后不一致 |
| **渡劫分流** | tribulation divert | 达劫门槛不建读条会话，返回 `needs_tribulation` |

---

## 3. 分相交付

| 相 | 范围 | 状态目标 |
| --- | --- | --- |
| **Phase A（= M5-D05 / M1-D20）** | Flag + 会话 + start/status/resolve；跨请求互斥；前端真条；ADM 字段；单测 | **已落地（2026-08-10）** |
| **Phase B（可选）** | 定时任务主动扫到期会话；读条中「天象文案」轮播；GM 强制结算/跳过读条 | 需要时另开登记 |
| **不做** | WS 推送读条结束（→ **M5-D04**）；读条中取消退费；化身独立突破读条（→ **M5-D07** / M4-D02） | — |

---

## 4. 流程与状态机

### 4.1 状态转移

```text
normal
  --(start, 无劫门槛)--> breaking_through
  --(start, 达劫)--> 仍 normal，响应 needs_tribulation（不建会话）

breaking_through
  --(lazy resolve, now >= ends_at)--> normal（成功或失败结果已写入）
  --(其它积极玩法)--> 409 拦截
```

与渡劫正交：

```text
normal --tribulation start-prep--> tribulation → …
```

**禁止**在 `breaking_through` 时进入渡劫准备；须先等读条结算（或未开读条）。

### 4.2 开读条（start）步骤

1. `resolve_pending_before_play` + `settle_idle`  
2. **先** `lazy_resolve_breakthrough_channel`（若已有到期未结会话，先结掉）  
3. `assert_activity(BREAKTHROUGH)`（须 `normal` + 未修炼 + 无冲突）  
4. 门槛：`realm_progress` / 灵石 / 配置存在  
5. 雷劫分流：若 `needs_tribulation` → **不**改 status，返回引导（同现行）  
6. 创建会话：写入 `started_at` / `ends_at` / 锁定意图快照  
7. **扣灵石**（见 §4.3）；**不**在此时扣修为、不改境界  
8. `character.status = breaking_through`；flush  
9. 返回：`channel` 进度信封 + `CharacterPublic`（无最终 `success` 字段，或 `success: null`）

### 4.3 扣费与掷骰时机（定案）

| 资源 | 时机 | 理由 |
| --- | --- | --- |
| **灵石** | **开读条时**按规则扣除（失败是否扣：沿用 `fail_still_charge_stones`——若失败也扣，则开读条即扣全额；若失败不扣，则开读条扣「成功价」并在失败结算时退回差额——**Phase A 简化：无论成败均在开读条扣 `spirit_stone_cost`，与现行「失败也扣」配置默认一致**；若配置 `fail_still_charge_stones: false`，则开读条扣费，失败结算时退回） | 防止读条窗口刷灵石；闭关有代价 |
| **修为 / 境界 / 品阶** | **到期结算时** | 未揭晓前面板境界不变；进度条期间 `realm_progress` 仍显示开读条前值 |
| **骰子** | **到期结算时**掷 `roll_breakthrough`（使用锁定的 `effective_rate`） | 开读条不预掷，避免「结果已定却还装读条」的信任问题；时长本身是玩法占用 |

> 若未来要「天机已定」叙事，可 Phase B 改为开读条锁定 `seed`+预掷但 UI 仍等到点才揭晓；**Phase A 不做预掷**。

### 4.4 懒结算（resolve）步骤

触发点（任一即可，须幂等）：

- `GET /breakthrough/preview` / `GET /breakthrough/channel`（或 status）  
- `GET` 角色 / 大厅轮询刷新 character  
- `settle_idle` 入口  
- 再次 `POST` start / 旧 `attempt`  
- 显式 `POST /breakthrough/resolve`（可选，便于前端读条结束主动拉）

步骤：

1. 若无进行中会话或 `status != breaking_through` → no-op  
2. 若 `now < ends_at` → 返回进行中进度，不改结果  
3. 若 `now >= ends_at`：  
   - 用锁定意图调用骰子 → 成功/失败分支（复用现行 `_apply_success` / fail keep_ratio）  
   - `status=normal`；关闭会话；`last_settled_at=now`（避免读条窗口被补算产出——期间本已零产出）  
   - 返回结果信封（供弹窗）

### 4.5 读条期间挂机

沿用 M1：`status != normal` 时 settle **只推进时间锚点、零产出零消耗**。  
开读条前须已停修炼（互斥）；若异常残留 `idle_direction≠none`，settle 仍零产出，且 UI 应提示进阶中。

---

## 5. 数据模型

### 5.1 推荐：独立会话表（与渡劫同构、字段更少）

表名：`breakthrough_sessions`

| 列 | 说明 |
| --- | --- |
| `id` | PK |
| `character_id` | FK；同一角色至多一条 `status=active` |
| `status` | `active` / `resolved` / `cancelled`（Phase A 不用 cancel） |
| `advance_type` | `layer` / `major` |
| `started_at` / `ends_at` | UTC |
| `duration_seconds` | 开读条时写入的权威时长 |
| `spirit_stones_charged` | 已扣灵石 |
| `effective_success_rate` | 锁定有效成功率 |
| `rule_snapshot_json` | keep_ratio、是否失败扣石等快照 |
| `from_major` / `from_stage` / `realm_progress_at_start` | 审计 |
| `resolved_at` | 可空 |
| `result_json` | 结算后缓存（success/dice/deltas），便于幂等重读 |

角色：`characters.status` 仍为权威互斥轴；会话表承载进度与快照。

### 5.2 备选（更轻、不推荐长期）

仅在 `characters` 上增 `breakthrough_ends_at` 等列。Phase A 若赶工可用，但审计与幂等重放弱于会话表；**默认按 §5.1**。

---

## 6. API 契约（草案）

> 前缀：`/api/v1/breakthrough`；均需登录；错误码延续既有 4002x / 409。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/preview` | 先 lazy resolve；返回 `can_attempt`、门槛；若进行中附带 `channel` |
| POST | `/channel/start` | 开读条；`Idempotency-Key`；响应 `channel` + character |
| GET | `/channel` | 当前读条进度；到期则 resolve 并返回 `result` |
| POST | `/channel/resolve` | 显式结算（未到期 → 409/`40028`「尚未完成闭关」） |
| POST | `/attempt` | **兼容**：`async_channel.enabled=true` 时等价 start（或 301 语义说明）；`false` 时保持同步旧行为 |

### 6.1 `channel` 进度信封（玩家可见字段须可中文展示）

```json
{
  "state": "in_progress",
  "progress_ratio": 0.42,
  "started_at": "...",
  "ends_at": "...",
  "remaining_seconds": 7,
  "duration_seconds": 12,
  "advance_type": "layer",
  "advance_type_label_zh": "层进阶",
  "hint_zh": "闭关突破中，请稍候…"
}
```

结算后：

```json
{
  "state": "resolved",
  "result": { "success": true, "dice": {}, "message": "…", "character": {} }
}
```

### 6.2 错误码（增量）

| 码 | 场景 |
| --- | --- |
| `40024` | 已在突破中（开读条重复）— 已有 |
| `40022` | 进阶中不可其它操作 — 已有 |
| `40028`（新） | 读条未到期不可强制结算（若开放 resolve） |
| `40029`（新） | 无进行中读条却调用 resolve |

（具体码号实现时可与现有表对齐，避免冲突。）

---

## 7. 配置草案（`breakthrough.yaml` 增补）

```yaml
# 异步真读条（M5-D05）；false 时回退同步 attempt
async_channel:
  enabled: true
  # 玩家可见总述（§0.0.2）
  label_zh: 闭关突破
  hint_zh: 突破需闭关片刻，期间无法开战或有效挂机。
  # 权威时长（秒）；可按进阶类型分设
  duration_seconds:
    layer_advance: 8
    major_advance: 15
  # 前端轮询建议间隔（毫秒，仅提示；权威仍以 ends_at）
  client_poll_ms: 500
```

数值仅占位；正式曲线可 M13 再调。ADM schema 为每个字段提供 `label_zh` / `help_zh`。

---

## 8. 前端（大厅）

| 点 | 行为 |
| --- | --- |
| 点「发起突破」 | 调 `channel/start`（或 attempt 兼容）；展示真进度条 |
| 进度 | 用 `ends_at` 本地插值 + 定时 `GET /channel` 校准；**禁止**再使用与结果并行的假条作为唯一进度 |
| 结束 | `state=resolved` → 结果弹窗（含 dice）；刷新 character / 品阶历史 |
| 重连 / 刷新 | `character.status===breaking_through` 或 preview 带 `channel` → 自动续条，不丢会话 |
| 活动条 | `ActivityStatusBanner` 显示进阶中 + 剩余秒数（从 channel 或 character 扩展字段） |
| 雷劫 | 仍展示「需渡劫」CTA；不进入读条 UI |

路由：养成仍留 `/hall`（与 M1-D31 一致）；不新开突破页。

---

## 9. 与渡劫 / 互斥 / 骰子的边界清单

| 主题 | 定案 |
| --- | --- |
| 渡劫 | 达门槛只分流；读条与 `tribulation_sessions` 互斥且数据结构分离 |
| §0.8 | `breaking_through` 禁止开战/突破/渡劫/进秘境；开读条前须停修炼 |
| §0.9 | 仅结算时 `DiceService.roll_breakthrough` |
| 幂等 | start 与 resolve 均支持 Idempotency-Key 或会话单活约束 |
| 化身 | Phase A **仅本体**；化身突破不在范围 |

---

## 10. 验收钩子（对齐后续待完成）

| # | 钩子 | 验证方式 |
| --- | --- | --- |
| 1 | `breaking_through` 跨请求可见 | start 后另开 GET character，status 仍为进阶中 |
| 2 | 可查询进度 | `GET /channel` 返回 `progress_ratio` / `remaining_seconds` |
| 3 | 到期自动（懒）结算 | 拨钟 `ends_at` 后任意 preview/me → 境界/修为变化且 status=normal |
| 4 | 期间禁开战 | PVE/PVP → 409 / 进阶中文案 |
| 5 | 期间有效挂机零产出 | 读条窗口内 settle 修为不涨（锚点可推进） |
| 6 | 达劫不建读条 | 元婴圆满 attempt/start → `needs_tribulation`，status 仍 normal |
| 7 | Flag 关闭回退同步 | `enabled:false` 时旧同步单测全绿 |
| 8 | 中文显性 | UI/错误/hint 无裸 `breaking_through` 给玩家 |
| 9 | ADM | 改 `duration_seconds` 发布后新开读条用新时长 |

---

## 11. 测试计划（Phase A）

| 用例 | 期望 |
| --- | --- |
| `test_channel_start_sets_status` | status=breaking_through；灵石已扣；境界未变 |
| `test_channel_progress_midway` | 未到期 resolve 不升境 |
| `test_channel_lazy_resolve_success` | 拨钟后成功升境；status=normal；会话 resolved |
| `test_channel_lazy_resolve_fail_keep_ratio` | 失败 keep_ratio；石策略符合快照 |
| `test_channel_blocks_battle` | 读条中开战 409 |
| `test_channel_idle_zero_gain` | 读条中 settle 零产出 |
| `test_channel_tribulation_divert` | 达劫不建会话 |
| `test_channel_idempotent_start` | 同 Key 不双扣石 |
| `test_sync_fallback_when_disabled` | flag off 走旧 attempt |

---

## 12. 风险与缓解

| 风险 | 缓解 |
| --- | --- |
| 与渡劫「进行中」心智混淆 | 文案区分「闭关突破」vs「渡天劫」；API/表分离 |
| 懒结算依赖玩家在线 | Phase A 可接受；角色一拉就结；Phase B 可加扫描 |
| 读条中改 YAML 成功率 | 开读条写 `effective_success_rate` + rule 快照 |
| 双请求并发 start | DB 唯一 active 会话 + status 校验 + 幂等键 |
| 拖垮主线 | Flag + 不进 M5 出口；默认可关 |

---

## 13. 文档与登记同步

| 文档 | 动作 |
| --- | --- |
| `后续待完成.md` | M5-D05 / M1-D20 → **设计中**；笔记指向本文 |
| `M1核心循环设计.md` §5 | 保留同步说明 + 一行指针「真读条见本文」 |
| `M5环境与轮回外环设计.md` | M5-D05 行指向本文 |
| `README.md` / `CHANGELOG.md` | 登记设计开篇 |
| 实现完成后 | 状态 → **已消化**；§0 落地对照打勾；同步 README API 表 |

---

## 14. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-10 | **v1.0** 开篇定案：异步真读条 Phase A；与假读条/渡劫边界；懒结算；配置+ADM；验收与单测表 |
| 2026-08-10 | **v1.1** Phase A 落地：`breakthrough_sessions`、channel API、前端真条、单测 8 例；Flag 可回退同步 |
| 2026-08-10 | **v1.2 产品关读条**：默认 `enabled=false`；前端点突破同步弹结果；channel 能力保留可再开 |

# 四象禁制与环境·天气载荷设计（M3-D07）

> **文档性质**：阵法四象内容加深真相源；消化 [`后续待完成.md`](./后续待完成.md) **M3-D07**，细则对齐 [`M3战斗成型设计.md`](./M3战斗成型设计.md) §5  
> **版本**：v1.1.1 · 2026-08-10  
> **实现状态**：**Phase A（M3-D07）已落地**；载荷/LOS 已从对抗结算与引擎内联中解耦  
> **关联**：[`开发计划.md`](./开发计划.md) **§0.0 / §0.0.1 / §0.0.2 / §0.7** · [`阵法部署与自研设计.md`](./阵法部署与自研设计.md) · [`M5环境与轮回外环设计.md`](./M5环境与轮回外环设计.md)（**世界天气 ≠ 阵法天气层**）· [`后台管理系统开发计划.md`](./后台管理系统开发计划.md)  
> **原则**：对抗算法已落地，本文只加深 **禁制子类** 与 **环境/天气可测战斗载荷**；机读英文、人读中文（§0.0.2）；数值全进配置 / 后台。

---

## 0. 落地对照（Phase A）

| 设计项 | 代码落点 | 状态 |
| --- | --- | --- |
| 禁制子类掩码 / LOS 按 `attack_kind` 过滤 | `domain/terrain.py` · `line_of_sight.py` · `combat_ai` / `autochess` | **已落地** |
| 环境 / 天气战斗载荷注册表 | `formations.yaml` `*_catalog` · `FormationsConfig.to_engine_catalogs` | **已落地** |
| 载荷套用（命中/伤害等乘区） | `domain/layer_payloads.py`（与 `formation_rules` 对抗结算解耦） | **已落地** |
| 战报中文映射 | `layer_payloads.enrich_*`（含 `layer_label_zh`）· `battle_text` · 播放器 | **已落地** |
| 样本阵法 | `seal_phys_curtain` / `seal_spell_curtain` / `seal_all_curtain`；mist/storm 加深 | **已落地** |
| 对抗边界单测 | `tests/test_m3_d07_four_aspects.py` | **已落地** |
| ADM | 域 `formations` catalog 字段 + 中文 help | **已落地** |

---

## 1. 定案与边界

### 1.1 一句话目标

在 S6 已落地的「四象对抗 + 障/渊揭晓 + 效果 atk/hp 乘区」之上，补齐：

1. **禁制 `seal`（禁制）**：样本阵法 + 按子类挡住对应远程攻击；  
2. **环境 / 天气载荷**：对抗胜出后除写入 `resolved_*` id 外，还有 **可单测的战斗数值/规则修正**；  
3. **战报与文档**：玩家只见中文（§0.0.2）。

### 1.2 写死边界

| 做 | 不做 / 说明 |
| --- | --- |
| `seal` 子类：`ranged_physical`（禁远程物理）/ `ranged_magic`（禁远程法术）/ `ranged_all`（禁全部远程） | Phase A **不做**「禁近战」；标签列表 `block_tags` → 可选 Phase B |
| 中间格上的禁制参与最小 LOS（与障碍同轴对齐+对角） | **不**提前做完整任意角度 LOS（**M3-D02**） |
| 禁制格 **不可部署、不可停留**（与障/渊同属禁停） | 禁制 **不**挡移动穿越？→ **定案：不可停留、不可作为移动终点；正交移动可视为软墙（与障碍一致，路径不可踏入）** |
| 环境/天气 `combat` 载荷表驱动 | **不**把世界天气 `weather.yaml` 与阵法 `weather` 层混为一谈（见 §1.6） |
| 载荷至少覆盖：命中乘区、伤害乘区（物理/法术可分） | Phase A **不做**改 AP、改先攻骰面、改地形生成 |
| `label_zh` 进配置；战报只播中文 | 禁止战报裸打 `mist` / `thunderstorm` / `seal` |
| 平局分区 / `force_apply`（强制生效）/ 双强制互抵有单测 | 对抗公式本身不重写（保持 §5.3） |

### 1.3 与现行落地的关系

| 已落地（保持） | 本文扩展 |
| --- | --- |
| `resolve_battlefield` 三层：效果→环境→天气 | 环境/天气 resolved 后查 catalog 套 `combat` |
| `effect_multipliers_for_side` → atk/hp | 新增 `env_weather_modifiers_for_side`（或等价） |
| `TerrainState.seal_mask` 一律挡远程 | 改为按子类 + `attack_kind`（攻击类别）过滤 |
| `formations.yaml`：mist / thunderstorm 仅 id | 补 catalog 载荷 + seal 样本阵 |
| `battle_text`：层名中文；resolved 打 id | resolved → `label_zh`；禁制格图例「禁=禁制」 |
| 部署禁停：obstacle / ravine | seal 纳入禁停（与 [`阵法部署与自研设计.md`](./阵法部署与自研设计.md) brush `allowed_types` 对齐） |

### 1.4 配置与后台纪律（§0.0）

| 必须 | 禁止 |
| --- | --- |
| 禁制子类、环境/天气 catalog、乘区系数进 **`formations` 域**（或独立域但开战只读 Bundle） | 引擎写死「迷雾一定 -15% 命中」而无表 |
| 每个环境/天气/禁制子类有 `label_zh` / `help_zh`（§0.0.1） | 只有英文 id 无中文列 |
| 阵法 `terrain[].type=seal` + `subtype` 外键到子类枚举 | `if formation_id == "mist_domain"` 特判载荷 |

**§0.0 自检**：运营加「禁远程法术帘」→ 只改 YAML/后台发布 + 挂到某阵，玩家端无发版。

### 1.5 显性设计与中文信条（§0.7 · §0.0.2）

玩家必须能感知：

1. **布阵**：选带禁制的阵法时，棋盘高亮禁制格，并说明挡哪种远程（中文）；  
2. **开战四象行**：环境/天气/效果结算结果显示 **中文名**（如「迷雾」「雷暴」），可附一句 `summary`；  
3. **战斗过程**：因禁制导致 `los_blocked`（视线被遮挡）时，详细日志说明「被禁制挡住」，而非只说「被地形阻挡」且不区分；  
4. **载荷生效**：若迷雾降低远程命中，战报命中检定行或四象摘要能看出修正来源（至少：四象行写明「远程命中×0.85」占位，或事件带 `modifier_notes`）。

禁止：对抗赢了只改内部状态、战报仍写英文 id、界面无中文说明。

### 1.6 与世界天气的边界

| 概念 | 来源 | 作用 |
| --- | --- | --- |
| **世界天气** `locked_world_weather` | M5 `weather.yaml` / 时辰池 | 挂机、工坊、开战修为环境乘区等 |
| **阵法天气层** `formations.*.weather` | 布阵四象 | 本场棋盘对抗 + **本文战斗载荷** |

二者可同时存在；**不得**用世界天气 catalog 冒充阵法天气载荷，也不得反向覆盖。战报若同时提及，须用不同措辞：「天地天气：…」vs「阵法天气：…」。

---

## 2. 术语

| 术语 | 英文键 | 含义 |
| --- | --- | --- |
| **四象** | battlefield layers | 地形 / 环境 / 天气 / 效果 四层战场修饰 |
| **禁制** | `seal` | 地形类型：使特定攻击无法穿越该格 |
| **禁制子类** | `seal.subtype` | 决定挡哪类攻击（见 §4） |
| **环境** | `environment` | 四象对抗层；载荷见 catalog |
| **天气（阵法）** | `weather`（formation layer） | 四象对抗层；≠ 世界天气 |
| **效果** | `effect` | 已有 atk/hp 乘区；本文不改算法 |
| **强制生效** | `force_apply` | 单方 true → 无视比分全场；双方 true → 互抵为无 |
| **战斗载荷** | `combat` payload | 对抗 resolved 之后套用的可测修正表 |
| **攻击类别** | `attack_kind` | `melee_physical` / `melee_magic` / `ranged_physical` / `ranged_magic` |
| **中文名** | `label_zh` | 玩家与战报唯一展示名 |

---

## 3. 分相交付

| 相 | 范围 | 状态目标 |
| --- | --- | --- |
| **Phase A（= M3-D07）** | 禁制三子类 + 样本阵；环境/天气 combat 载荷 + 样本加深；战报中文；对抗边界单测；ADM 字段；部署禁停含 seal | **已落地（2026-08-10）** |
| **Phase B（可选）** | `block_tags`（禁特定攻击标签）；更复杂载荷（改移动力、回合开始伤害等）；禁制揭晓叙事 | 需要时另开登记项 |
| **不做** | 完整 LOS（M3-D02）；用阵法天气替换世界天气；微操 | — |

---

## 4. 禁制 `seal`（禁制）规则

### 4.1 子类枚举（Phase A）

| `subtype` | 中文 | 阻挡条件 |
| --- | --- | --- |
| `ranged_physical` | 禁远程物理 | 中间格命中该禁制，且攻击方 `attack_kind == ranged_physical` |
| `ranged_magic` | 禁远程法术 | 同上，`attack_kind == ranged_magic` |
| `ranged_all` | 禁全部远程 | 任一 `attack_kind` 以 `ranged_` 开头 |

近战（`melee_*`）**一律不**被上述子类阻挡。

### 4.2 空间与部署

- 坐标书写规则与障/渊相同：只写 **己方半区**；防守方镜像。  
- 禁制格 ∈ 部署禁停集（不可落子）。  
- 移动：禁制格视为 **不可进入**（与障碍相同，计入 wall/软墙规划）；**不可**作为停留格。  
- 远程 LOS：直线中间格若存在「对该 `attack_kind` 生效的禁制」，则攻击失败，事件 `blocked` + `reason=los_blocked`，并建议子原因 `seal`（机器字段）→ 战报译「被禁制挡住」。

### 4.3 数据结构（建议）

现行单一 `seal_mask` 不足以表达子类，改为其一：

```text
seal_subtype: dict[cell, subtype]   # 推荐
# 或三套掩码 seal_mask_phys / seal_mask_magic / seal_mask_all
```

LOS 伪代码：

```text
block_mask = obstacle_mask()
for cell in line_between(attacker, target):
    if cell in seal_subtype and seal_blocks(seal_subtype[cell], attack_kind):
        block
```

决策层 `ranged_attack_blocked` / `_los_block_mask` 必须传入 **当前单位的 `attack_kind`**，禁止再对所有远程共用一张「全禁制」掩码（除非该场只有 `ranged_all`）。

### 4.4 信息与 AI

- 禁制 **无**「可破/不可破」揭晓（与障碍不同）；开战即可知格上有禁制，但 **子类对敌方是否开战前隐藏**：**Phase A 定案为可见**（简化；若要隐藏则 Phase B）。  
- 默认 AI：远程单位规划时按真实子类过滤 LOS；被挡则换站位或改近战目标估价（沿用现行 blocked 回流）。

### 4.5 样本阵法（示意）

```yaml
# 金锁障 —— 禁远程物理帘（己方前列）
seal_phys_curtain:
  name: 金锁障
  level: 1
  unlocked_by_default: true
  deploy: { mode: default }
  terrain_layout: { mode: fixed }
  terrain:
    - { x: 2, y: 2, type: seal, subtype: ranged_physical }
    - { x: 2, y: 3, type: seal, subtype: ranged_physical }
    - { x: 2, y: 4, type: seal, subtype: ranged_physical }
  environment: null
  weather: null
  effect: null

# 咒缄幕 —— 禁远程法术
seal_spell_curtain:
  name: 咒缄幕
  level: 1
  unlocked_by_default: true
  deploy: { mode: default }
  terrain_layout: { mode: fixed }
  terrain:
    - { x: 2, y: 3, type: seal, subtype: ranged_magic }
  environment: null
  weather: null
  effect: null
```

---

## 5. 环境 / 天气战斗载荷

### 5.1 Catalog 契约

在 `formations.yaml`（推荐同文件，ADM 同域）增加：

```yaml
environment_catalog:
  mist:
    label_zh: 迷雾
    summary: 雾气弥漫，远程命中下降
    combat:
      ranged_hit_mul: 0.85          # 远程命中率乘区（对 base_rate）
      # 缺省字段 = 1.0 / 不生效

  fire_field:
    label_zh: 火域
    summary: 灼热场域（克制表示意用）
    combat:
      physical_damage_mul: 1.05

weather_catalog:
  thunderstorm:
    label_zh: 雷暴
    summary: 雷霆加持，法术伤害上升
    combat:
      magic_damage_mul: 1.10
```

阵法层仍只写：

```yaml
environment:
  id: mist
  force_apply: false
  counter_group: fog
```

**载荷不写在阵法行内副本**，避免同 id 多处不一致；开战用 `id` 查 catalog。

### 5.2 套用时机与作用域

1. `resolve_battlefield` 得到每层 `coverage` / `resolved_*`（逻辑不变）。  
2. 对 **环境**、**天气** 两层，按与 `effect_multipliers_for_side` **相同的覆盖规则** 解析该侧生效的 id（全场赢家 / split 用己方 / none·cancelled → 无）。  
3. 查 catalog 得 `combat`；与效果层乘区 **分别乘入**（先效果面板，再环境/天气战斗修正；顺序写死并单测）。  
4. **命中**：`base_rate * ranged_hit_mul`（仅远程攻击吃 `ranged_hit_mul`；近战忽略）。  
5. **伤害**：在现行伤害骰结果上乘 `physical_damage_mul` 或 `magic_damage_mul`（按 `attack_kind` 是否含 `physical` / `magic`）。

Phase A 最小字段集（可扩展，缺省 1.0）：

| 字段 | 中文含义 | 作用对象 |
| --- | --- | --- |
| `ranged_hit_mul` | 远程命中乘区 | 远程攻击命中检定 |
| `physical_damage_mul` | 物理伤害乘区 | `*_physical` 伤害 |
| `magic_damage_mul` | 法术伤害乘区 | `*_magic` 伤害 |

### 5.3 加深现有样本

| 阵法 id | 现状 | Phase A |
| --- | --- | --- |
| `mist_domain`（迷雾境） | 仅环境 id | catalog 挂 `ranged_hit_mul` |
| `storm_force`（强制雷暴） | 仅天气 force_apply | catalog 挂 `magic_damage_mul` |
| `stone_wall_left` 等 | 效果乘区 | 保持；不强迫改 |

### 5.4 与效果层关系

- **效果**：继续用阵法行内 `atk_mul` / `hp_mul`（开战改面板）。  
- **环境/天气**：不进面板 HP/ATK 永久值，只进 **命中/伤害结算**（避免与效果职责混淆）。  
- 若未来要「天气改面板」，另开字段并显性说明；Phase A 禁止混用。

---

## 6. 战报与中文渲染（§0.0.2）

### 6.1 事件（机读可英文）

`battlefield_layer` 可增：

```json
{
  "type": "battlefield_layer",
  "layer": "weather",
  "resolved_full": "thunderstorm",
  "resolved_full_label_zh": "雷暴",
  "combat_notes": ["法术伤害×1.10"],
  "coverage": "full_attacker",
  "...": "既有 dice/score 字段"
}
```

`blocked` 可增 `block_source: "seal" | "obstacle"`（可选，便于译文）。

### 6.2 渲染（人读必须中文）

| 场景 | 禁止 | 必须 |
| --- | --- | --- |
| 四象对抗行 | `… → thunderstorm` | `… → 雷暴` 或「雷暴覆盖全场」 |
| 平局分区 | 打印英文 id | `己方 迷雾 / 敌方 火域 / 中立 迷雾` |
| 棋盘图例 | `seal` | `禁=禁制（挡对应远程）`；格标记建议用「禁」 |
| LOS 阻挡 | 含糊「被地形阻挡」且无法区分 | 详细档：「远程攻击被禁制挡住」 |
| 载荷 | 静默改命中 | 四象行或首回合摘要列出中文乘区说明 |

`battle_text` 查表优先级：`event["*_label_zh"]` → Bundle catalog[`id`].label_zh → 最后才允许 fallback（fallback 须打日志告警，验收视为失败）。

### 6.3 文档备注纪律

本设计与计划中凡写英文键，须保持 `英文（中文）` 形式；实现 PR / CHANGELOG 玩家可见描述用中文。

---

## 7. 对抗边界单测（验收钩子对齐）

| 用例 | 期望 |
| --- | --- |
| `test_seal_blocks_ranged_physical_only` | 禁远程物理挡弓箭；同线法术远程仍可打穿 |
| `test_seal_blocks_ranged_magic_only` | 对称 |
| `test_seal_ranged_all_blocks_both` | 全禁远程挡两种；近战不受影响 |
| `test_seal_cell_not_deployable` | 落子禁制格 → 占位错误码 |
| `test_mist_ranged_hit_mul_applied` | 迷雾生效后远程命中率可断言 |
| `test_thunderstorm_magic_damage_mul` | 雷暴生效后法术伤害可断言 |
| `test_layer_split_half_payloads` | 平局分区：两侧各用己方环境载荷 |
| `test_dual_force_apply_cancel` | 双方 `force_apply` → coverage cancelled，无载荷 |
| `test_single_force_apply_ignores_score` | 单方强制全场，无视比分 |
| `test_battle_text_uses_zh_labels` | `render_detailed` 不含 `mist`/`thunderstorm`/`seal` 裸词（可用正则白名单测） |

种子固定，保证可复现。

---

## 8. 前端与 ADM

### 8.1 前端

- 布阵：地形图例增加「禁」；tooltip 中文子类。  
- 战报播放器：四象行与 `blocked` 文案与后端 detailed 一致；**禁止**把 `resolved_full` 原文上屏。  
- 阵法下拉：已有 `name` 中文则保持。

### 8.2 ADM（域 `formations`）

| 加深点 | 说明 |
| --- | --- |
| terrain type 枚举含 `seal`（禁制） | `label_zh` / `help_zh` |
| subtype 下拉三子类 | 中文列头 |
| `environment_catalog` / `weather_catalog` | 表格+JSON 双写；combat 字段中文说明 |
| probe | 校验未知 seal subtype、catalog 缺 label_zh 拒绝发布 |

---

## 9. 开战管线顺序（完整态修订）

对齐成型设计 §5.5，插入载荷步：

1. 加载单位与坐标（含部署禁停：障/渊/**禁制**）。  
2. 布置双方地形。  
3. 分层对抗 → `battlefield_layer` 事件（带 `label_zh`）。  
4. 套用 **效果** 面板乘区。  
5. 解析环境/天气 **combat 载荷** 写入 `BattleState`（或等价修饰器）。  
6. 可选 `force_shifts`（强制移位）。  
7. 回合循环：命中/伤害读取修饰器；攻击查询障碍 LOS + **按 attack_kind 的禁制**。

---

## 10. 验收清单（M3-D07 Done）

- [x] 至少 2 个禁制样本阵法可在布阵页选择并开战  
- [x] 物理帘 / 法术帘行为符合 §4，有单测  
- [x] `mist_domain` / `storm_force` 载荷可测，战报中文名正确  
- [x] 平局分区 / 双强制互抵 / 单强制 有命名单测  
- [x] `render_detailed` 与播放器无裸英文内容 id  
- [x] 部署与 brush `allowed_types` 含 seal（禁制）  
- [x] ADM 字段中文 + §0.0 自检通过  
- [x] `后续待完成.md` **M3-D07 → 已消化**；本文 §0 对照表回填；CHANGELOG / README 同步  

---

## 11. 风险与非目标

| 风险 | 缓解 |
| --- | --- |
| 远程 AI 被禁制后空耗 AP | 沿用 blocked 回流；单测「有禁制走廊时会换位或改近战」 |
| 世界天气与阵法天气文案混淆 | §1.6 措辞分离；UI 分栏 |
| catalog 与阵法 id 漂移 | 启动/发布硬校验：阵法引用的 env/weather id 必须存在于 catalog |
| 乘区叠乘过猛 | Phase A 占位系数温和（0.85 / 1.10）；§0.7 显性露出 |

**非目标**：技能禁制 Buff、可破坏禁制、完整 LOS、阵法自研 brush 画禁制 UI（brush 允许类型可先开，设计器属 M8）。

---

## 12. 修订记录

| 日期 | 版本 | 说明 |
| --- | --- | --- |
| 2026-08-10 | v1.0 | 初稿：禁制三子类、环境/天气 combat catalog、战报中文信条对齐 §0.0.2、验收与样本 |
| 2026-08-10 | v1.1 | Phase A 落地：引擎/配置/ADM/前端/单测；M3-D07 已消化 |
| 2026-08-10 | v1.1.1 | 封装解耦：`layer_payloads` / `line_of_sight`；对抗与载荷分模块；战报 `layer_label_zh` 收口 |

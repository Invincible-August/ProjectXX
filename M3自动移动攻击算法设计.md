# M3 自动移动攻击算法设计

| 项 | 内容 |
| --- | --- |
| **针对** | M3 战斗内默认 AI（无玩家微操） |
| **依据** | [`M3战斗成型设计.md`](./M3战斗成型设计.md) §5.2 地形探路 · §7.0～§7.3 距离/行动/移动/攻击 |
| **版本** | **v2.5** · 2026-08-10（v2.4 收口；**v2.5 友军占位：软墙绕行 + 让路** §5.4） |
| **目标** | 规则等价前提下，做到 **CPython 友好、低分配、可预算、易横向扩展** 的默认 AI |

---

## 0. 现行落地范围 vs 延后

| 范围 | 内容 |
| --- | --- |
| **已落地** | 位棋盘 / 静态表；Tier B 距离表；AP=2 决策；障/渊揭晓与换目标；轴对齐+对角最小 LOS；双档日志渲染；战报零保留；**友军软墙绕行 + 窄廊让路** |
| **延后** | 完整 LOS / 飞跃 → **M3-D02**；技能道具陷阱 → **M3-D03**；嘲讽 → **M3-D06**；Tier A 短路 / Tier C / 基准 / 完整修正栈 → **M3-D10**；`report_mode` 请求参数 → **M3-D09**（现行响应全量+前端切档） |

细则与验收钩子见 [`后续待完成.md`](./后续待完成.md)。下文含未落地设计处仅作指针，**实现以现行代码为准**。

---

## 0. v2.0 优化总览

### 0.1 v1.0 的性能问题

| 问题 | 后果 |
| --- | --- |
| 搜索状态含 `belief_hash`（`(pos, belief)`） | 状态空间随未知格数指数膨胀；无法缓存 |
| **每个候选目标各跑一次** `estimate_approach` | 单次决策 = `N_targets` 次图搜索；12 子对局每回合上百次搜索 |
| 用 `Dijkstra/heapq` + 节点对象 | CPython 里堆操作与对象分配是主要开销 |
| 每次决策重建图/清空数组 | 高频 `O(V)` 清零与临时 list 分配 |
| 战报事件即时拼中文文案 | 字符串格式化常成为热点 |

### 0.2 v2.0 的六条替换

| # | 优化 | 收益 |
| --- | --- | --- |
| O1 | **扁平索引 + 位棋盘**：`cell = y*7+x`，49 格塞进一个 Python `int` | 集合运算 = 单次整数位运算 |
| O2 | **模块级预计算静态表**（邻接 / 切比雪夫球 / 曼哈顿 / 射线） | 决策期只做查表，不做几何计算 |
| O3 | **一次全源距离表，替代每目标一次搜索** | 单次决策的搜索次数 `N_targets → 0`（纯查表） |
| O4 | **空盘闭式距离**（曼哈顿）+ 地形版本号懒重建 | S3 空盘 **零建表**；有地形时每战最多重建几次 |
| O5 | **每战一份 `Scratch` + 版本戳数组**，`heapq → 桶队列` | 每次决策 0 分配、0 清零 |
| O6 | **纯函数引擎 + 惰性事件文案** | 可进程池横向扩展；文案只在序列化时生成 |

### 0.3 关键洞察：因为「每次行动都重规划」，估价可以不精确

成型设计要求：**打障揭晓 / 深渊揭晓 / 嘲讽触发后立即重规划**，且每个 AP 只执行 **一个** 原子动作。

⇒ 规划器只需要 **第一步是对的**，不需要一条全局最优的完整计划。  
⇒ 允许用 **乐观、可采纳（admissible）的下界估价** 替代精确的信念状态搜索；估价错了下一次行动自然修正。  
⇒ 这就是能把 v1.0 的指数状态搜索降级为 **查表** 的正当性来源。

> 精确 AP 搜索仍保留为 **Tier C 参考实现**（§5.3），只用于单测断言近似误差，不进热路径。

---

## 1. 问题陈述（规则，与 v1.0 一致）

棋子每个 **行动回合** 有 **2 点行动机会（AP）**；每次 **移动行动** 或 **攻击行动**（普攻 / 主技能 / 主动技能 / 打障碍）各耗 **1 AP**。  
默认策略：向「当前信念下、耗 AP 折合回合数最少就能打到」的敌人施压；途中探明 **障碍不可破** / **深渊禁飞** / 踩到 **嘲讽** ⇒ 立即重规划，必要时换目标。

### 1.1 与规则文档的接口（不重复定义）

| 概念 | 来源 | 本文用法 |
| --- | --- | --- |
| 正交移动、禁止斜走 | 成型 §7.0 | 四邻接图 |
| 陆地 2 / 飞行 4 移动力 | 成型 §7.2 | 一次移动行动内的步数预算 `mp` |
| 每回合 2 AP | 成型 §7.2 | 决策与估价的唯一代价单位 |
| 近战正交 / 远程切比雪夫 | 成型 §7.3.1 | 攻击位掩码 |
| 障碍可破/不可破（攻击前未知），命中一次即破 | 成型 §5.2.1 | 乐观视为可破 |
| 深渊可飞/禁飞（踏上前未知），不可停留，踏入需剩余步数 > 1 | 成型 §5.2.2 | 乐观视为可飞；失败弹回 |
| 嘲讽 | 技能/效果 | 强制目标，短路选目标 |

---

## 2. AP 模型（规则口径，写死）

| 项 | 约定 |
| --- | --- |
| `AP_MAX` | **2** |
| 一次移动行动 | 沿正交路径连续走 **最多 `mp` 步**；耗 1 AP |
| 一次攻击行动 | 对单位或障碍结算一次攻击；耗 1 AP |
| 组合 | 移+攻 / 移+移 / 攻+攻 / 攻+移 / 只用 1 AP |

**估价口径（唯一标准，避免两处不一致）**：

```text
approach_ap(T) = 从当前位置移动到「可攻击 T 的落脚格」所需的最少 AP（不含打 T 那一次）
approach_turns(T) = ceil(approach_ap(T) / AP_MAX)
```

**目标优先键（升序，全程只用这一个）**：

```text
(approach_turns, approach_ap, chebyshev(cur, T), target_uid)
```

`approach_turns` 天然表达了用户口述的「少了一个回合就换目标」，无需特判。

---

## 3. 数据表示（O1）

### 3.1 扁平索引与位棋盘

```python
BOARD_SIZE = 7
CELL_COUNT = BOARD_SIZE * BOARD_SIZE          # 49
FULL_MASK = (1 << CELL_COUNT) - 1


def cell_of(x: int, y: int) -> int:
    """Convert board coordinate to flat cell index (left-bottom anchored)."""
    return y * BOARD_SIZE + x
```

49 格 < 64 bit，**一个 Python `int` 即可表示任意格集合**；交集/并集/补集都是单条位运算，比 `set` 快一个数量级且零分配。

| 掩码 | 含义 |
| --- | --- |
| `wall_mask` | 已知不可通行（不可破障碍、对非飞行单位的深渊、已知禁飞深渊） |
| `breakable_mask` | 未知 / 已知可破障碍（乐观可清） |
| `abyss_mask` | 深渊格（不可停留） |
| `occupied_mask` | 任意单位占用 |
| `standable_mask` | `FULL_MASK & ~wall_mask & ~abyss_mask & ~occupied_mask` |

### 3.2 单位状态用 SoA（Struct of Arrays）

```python
class BattleUnits:
    """Struct-of-arrays unit state; index is a dense slot id (0..n-1)."""

    __slots__ = ("cell", "hp", "side", "mp", "can_fly", "atk_range", "is_melee", "uid")

    def __init__(self, n: int) -> None:
        self.cell = [0] * n
        self.hp = [0] * n
        self.side = [0] * n          # 0=attacker, 1=defender
        # …其余同构 list
```

- 用 **稠密整数 slot** 索引，不用对象引用/字典查找。  
- 禁止在热路径构造 `dataclass` / `namedtuple` 临时对象。  
- 需要对象语义时用 `__slots__` 普通类（无 `eq/order`，避免 `__hash__`/比较开销）。

---

## 4. 预计算静态表（O2）

**模块导入时构建一次，全部 `tuple` / `int`（不可变）** ⇒ 多线程多进程共享安全、无锁、fork 后 COW 不复制。

```python
def _build_tables() -> None:
    """Build immutable lookup tables at import time (shared read-only)."""
    ...


ORTHO_MASK: tuple[int, ...]          # [cell] -> 正交邻格掩码（近战 reach=1 攻击位）
ORTHO_NEIGHBORS: tuple[tuple[int, ...], ...]   # [cell] -> 邻格索引，供逐步走子
RANGE_MASK_EX: tuple[tuple[int, ...], ...]     # [r][cell] -> 切比雪夫 ≤ r 且排除自身
MANHATTAN_ROW: tuple[tuple[int, ...], ...]     # [a][b] -> 曼哈顿距离（空盘闭式距离）
CHEBYSHEV_ROW: tuple[tuple[int, ...], ...]     # [a][b] -> 切比雪夫距离（平局键）
RAY_BETWEEN: tuple[tuple[int, ...], ...]       # [a][b] -> 直线中间格掩码；非共线为 0
```

| 表 | 规模 | 说明 |
| --- | --- | --- |
| `ORTHO_MASK` | 49 | 位掩码 |
| `RANGE_MASK_EX` | 7 × 49 | 射程 0～6 |
| `MANHATTAN_ROW` / `CHEBYSHEV_ROW` | 49 × 49 = 2401 | **查表比现算 `abs()` 更快**（少几条字节码） |
| `RAY_BETWEEN` | 49 × 49 | 仅共行/共列/共对角非 0；其余按成型 §5.2 简化为不遮挡 |

内存合计几百 KB 量级，可忽略。

**位迭代**（热路径内联，不用生成器）：

```python
m = pos_mask
while m:
    low = m & -m
    c = low.bit_length() - 1
    m ^= low
    # …use c
```

---

## 5. 距离与 AP 估价：三层实现（O3 / O4）

**核心替换**：不再「每目标一次搜索」，而是先拿到 **当前地形下的全源步距离**，然后所有目标都靠查表打分。

```text
approach_ap(T) = min over c in AttackPos(T)∩standable  of  ceil(STEP[cur][c] / mp)
```

移动行动内可连续走 `mp` 步且中途无额外代价 ⇒ `ceil(step/mp)` 对 **纯移动是精确值**，不是估计。

### 5.1 Tier A · 空盘闭式（S3 默认，零建表）

无地形（`wall_mask == breakable_mask == abyss_mask == 0`）时：

```python
STEP[a][b] == MANHATTAN_ROW[a][b]
```

⇒ **完全不建表**，直接查静态表。S3 全程走这条路径。

### 5.2 Tier B · 有地形则按版本号懒建表（M3 主路径）

```python
class TerrainDistance:
    """All-pairs step distance under current belief terrain, lazily rebuilt."""

    __slots__ = ("version", "rows_land", "rows_fly")
```

- 键：`terrain_version`（**只有** 障碍破坏 / 揭晓不可破 / 深渊揭晓 才 `+1`）。  
- 建表：对 49 个源各跑一次四邻接 BFS（`deque`），得到 `rows[src]` 为长度 49 的 `list[int]`。  
- 一场战斗地形变化通常 0～3 次 ⇒ **每战最多重建几次**，其余全是 `O(1)` 查表。  
- 飞行/陆地通行性不同 ⇒ 分别缓存 `rows_land` / `rows_fly`（两张表足够，因为差异只来自深渊与飞行标签）。

**乐观通行约定（可采纳下界）**：

| 格类型 | 建表时视作 |
| --- | --- |
| 未知 / 可破障碍 | **可通行**（真实清除要额外 1 AP，靠「撞到就打」在执行期兑现） |
| 已知不可破障碍 | 墙 |
| 未知深渊 | 对飞行单位可通行；对陆地单位是墙 |
| 已知禁飞深渊 | 对所有单位是墙 |
| 其它单位 | **全源表不算墙**；热路径局部软墙 BFS 绕行 + 友军让路（§5.4） |

### 5.3 Tier C · 精确 AP 搜索（仅测试/调参，不进热路径）

状态 `(cell, budget)`，`budget ∈ [0, mp]`，共 `49 × (mp+1) ≤ 245` 个状态；边权 ∈ {0,1,2} ⇒ 用 **桶队列（dial）**，不用 `heapq`：

```python
def plan_ap_exact(start: int, mp: int, standable: int, abyss: int,
                  breakable: int, can_fly: bool, scratch: "Scratch") -> list[int]:
    """
    Reference exact AP-distance planner (Tier C).

    Weights are tiny non-negative ints, so a dial/bucket queue beats heapq:
    - step inside current move action -> +0 AP (consumes 1 budget)
    - start a new move action         -> +1 AP (budget resets to mp)
    - break a breakable obstacle      -> +1 AP (attack action)

    深渊约束：仅当剩余 budget >= 2 时可踏入（踏入后仍须能走出）；
    深渊格不可作为终点，因此不允许从深渊状态开启新的移动行动。
    """
```

用途：单测断言 `Tier A/B` 的近似值与精确值的差 ≤ 容差（默认 1 AP），防止近似导致明显蠢走位。

### 5.4 单位占位：软墙绕行 + 友军让路（2026-08-10 修订）

全源地形距离表**仍不把单位算进墙**（避免每次移子重建 Tier B 缓存）。占位冲突改由热路径上的**局部软墙 BFS**与**让路**处理：

| 机制 | 说明 |
| --- | --- |
| 软墙绕行 | `decide_action` / `execute_move` 把「其它存活单位」并入局部 `blocked_mask`，对 49 格做单源 BFS；被挡单位优先改道 |
| 落脚过滤 | `AttackPos(T) & standable_mask`（含 `~occupied`）不变，不规划到别人头上 |
| 友军让路 | 当同侧后排软墙不可达、但地形贪心路径经过前排格时，前排优先侧移到空闲邻格（优先仍可攻击的格） |
| 空耗防护 | 无空闲可迈步时改 `ACT_IDLE`，避免反复 `move` + `blocked_unit` 烧 AP |

**不再依赖**「撞人即停 + 下 AP 重规划 ≈ 绕行」——该假设在友军占住最短路且自身不挪时不成立。

---

## 6. 单次行动决策（热路径，每 AP 调一次）

```python
def decide_action(st: "BattleState", slot: int) -> tuple[int, int]:
    """
    Decide one atomic action for a unit, costing exactly 1 AP.

    Returns (ACTION_KIND, payload):
      ACT_ATTACK_UNIT     -> target slot
      ACT_ATTACK_OBSTACLE -> obstacle cell
      ACT_MOVE            -> goal cell (execution layer walks up to mp steps)
      ACT_IDLE            -> no payload
    """
```

### 6.1 五步流程（每步都是查表或位运算）

```text
1) 嘲讽短路
   forced = st.taunt_target[slot]
   若存在且存活 → 候选集合只有它

2) 快速路径：当前就能打？
   reach = ORTHO_MASK[cur] if 近战 else RANGE_MASK_EX[r][cur]
   if reach & enemy_occupancy_mask:
       → 在候选里挑一个（同一优先键）→ ACT_ATTACK_UNIT
   # 绝大多数行动止于此处；成本 ~1 次位运算

3) 选目标：对每个存活敌人查表打分
   row = STEP_ROW(cur)                       # Tier A 闭式 / Tier B 缓存行
   pos = AttackPos(T) & standable | 自身位
   ap  = min(ceil(row[c] / mp) for c in bits(pos))
   key = (ceil(ap/2), ap, CHEBYSHEV_ROW[cur][T], uid)   # 线性取最小，不排序

4) 求首步：从 cur 沿「使 STEP[n][goal] 严格减小」的正交邻格贪心
   若首格是 breakable → ACT_ATTACK_OBSTACLE
   否则 → ACT_MOVE(goal)

5) 无可达目标 → ACT_IDLE（并记 blocked 事件）
```

### 6.2 成本

| 环节 | 操作量 |
| --- | --- |
| 快速路径命中 | 1 次 `&`，1 次真值判断 |
| 选目标 | `N_targets × |AttackPos|` 次 **list 下标查表**（≤ 12 × 25） |
| 首步 | ≤ 4 次查表 |
| 搜索 | **0 次**（Tier A/B） |

对比 v1.0：单次决策由 `N_targets` 次图搜索降为 **零搜索**。

---

## 7. 移动执行与地形揭晓

规划只给 `goal`；真实走子在执行层，逐格揭晓：

```python
def execute_move(st: "BattleState", slot: int, goal: int) -> int:
    """
    Walk at most `mp` orthogonal steps toward goal, revealing terrain on the way.

    返回本次移动的中断原因，供上层决定是否立刻重规划：
      STOP_ARRIVED / STOP_BUDGET / STOP_BLOCKED_UNIT
      STOP_ABYSS_BOUNCE  -> 揭晓禁飞，弹回上一格，terrain_version += 1
      STOP_TAUNT         -> 进入嘲讽范围，强制改目标
    """
```

**逐步规则**（与成型 §5.2.2 对齐）：

| 情况 | 处理 |
| --- | --- |
| 下一格被单位占用 | 规划层应已绕开；执行层若仍撞上 → `STOP_BLOCKED_UNIT`（本 AP 结束；友军堵死时决策侧改让路 / IDLE） |
| 下一格是深渊且 **剩余步数 ≤ 1** | 不得踏入 → 停止 |
| 下一格是深渊且剩余步数 ≥ 2 | 揭晓：可飞则进入（**不可停在此处**，必须继续）；禁飞则 **弹回上一格** + `abyss_bounce` 事件 + 升 `terrain_version` |
| 进入新格后处于嘲讽范围 | 设 `taunt_target`，立即中断（剩余 AP 按新目标重规划） |
| 走完 `mp` 步或已到 `goal` | 正常结束 |

**不变式（加断言）**：移动结束时 `cell not in abyss_mask`。

### 7.1 打障碍

```text
ACT_ATTACK_OBSTACLE(cell) → 结算一次攻击
  命中且可破 → 清除该格；breakable_mask &= ~bit；terrain_version += 1
  揭晓不可破 → wall_mask |= bit；breakable_mask &= ~bit；terrain_version += 1
              → 下个 AP 重规划时 approach 变大，可能自动换目标（用户 A/B 例）
  未命中     → 不揭晓（保持未知），本 AP 消耗
```

---

## 8. 嘲讽（已落地 · M3-D06 Phase A）

> **已落地。** 实现真理与竖切以 [`嘲讽光环设计.md`](./嘲讽光环设计.md) **v1.1** 为准；验收见该文 §10 与 [`后续待完成.md`](./后续待完成.md) **M3-D06（已消化）**。


| 项 | 约定 |
| --- | --- |
| 触发 | 移动 **进入** 嘲讽单位的触发范围（邻接或配置光环掩码） |
| 判定 | 进入新格后 `taunt_aura_mask & (1 << cell)`，**一次位运算** |
| 效果 | `taunt_target[slot] = 嘲讽者`；解除条件：嘲讽者死亡（时长/驱散 → Phase B） |
| 优先级 | **嘲讽 > 自由选目标**（§6.1 步骤 1 短路） |

实现：维护 **按侧聚合的 `taunt_aura_mask`**；静态 YAML 挂载，不必等 M3-D03 技能树。

---

## 9. 确定性（CPython 陷阱清单）

同一 `seed` + 同一输入必须逐事件一致，否则战报回放与单测都不可信。

| 规则 | 原因 |
| --- | --- |
| 每场一个 `random.Random(seed)` 实例；**禁用**模块级 `random.*` | 全局状态在并发下互相污染 |
| **决策路径禁止 `set` / `frozenset` 迭代**（哈希序不稳定） | 用位掩码或有序 `list` 替代 |
| 平局一律用显式键 `(…, uid)` 收尾 | 消除任何「碰巧的顺序」 |
| 规划器 **完全无随机** | 随机只出现在命中/伤害/释放/先攻平票 |
| 先攻平票桶洗牌用 `rng.randrange` + **swap-pop**（`O(1)`） | `rng.choice(list)+list.remove` 是 `O(n)` 且易改序 |
| 不依赖 `dict` 之外的隐式顺序；不用 `hash()` 参与决策 | `PYTHONHASHSEED` 影响 |
| 全程迭代实现，无递归 | 线程栈安全，避免 `RecursionError` |

### 9.1 先攻平票桶（对齐成型 §7.1，O(1) 取子）

```python
def iter_tie_bucket(bucket_by_side: list[list[int]], rng, last_side: int):
    """
    Yield units from one initiative tie bucket, forcing side alternation.

    双方交错：一方动完后，只要另一方仍有同点单位，下一动必须来自另一方；
    某方清空后，另一方可连续清完。
    """
    while bucket_by_side[0] or bucket_by_side[1]:
        side = 1 - last_side if bucket_by_side[1 - last_side] else last_side
        pool = bucket_by_side[side]
        i = rng.randrange(len(pool))
        pool[i], pool[-1] = pool[-1], pool[i]   # swap-pop：O(1) 且顺序确定
        yield pool.pop()
        last_side = side
```

---

## 10. Scratch 与零分配（O5）

```python
class Scratch:
    """
    Per-battle reusable buffers. 每场创建一次，供上千次决策复用。

    版本戳技巧：用 stamp[i] != cur 判断「未访问」，避免每次 O(V) 清零。
    """

    __slots__ = ("dist", "stamp", "cur", "buckets", "queue", "path")

    def __init__(self, states: int, max_ap: int) -> None:
        self.dist = [0] * states
        self.stamp = [0] * states
        self.cur = 0
        self.buckets = [[] for _ in range(max_ap + 3)]
        self.queue = deque()
        self.path = []

    def begin(self) -> None:
        """Invalidate all buffers in O(1)."""
        self.cur += 1
```

| 约束 | 说明 |
| --- | --- |
| `Scratch` **不可** 放模块级全局 | 线程不安全；必须每场一个（或 `threading.local()`） |
| 每次决策不新建 list/dict | 复用 `scratch.path` 等，用切片长度控制有效区 |
| 桶队列替代 `heapq` | 边权是小整数，桶 `O(1)` 入出 |

---

## 11. 事件、双档战斗日志与开局棋盘渲染（O6）

### 11.1 分层原则（引擎永远只记结构化事件）

```python
# 热路径：只 append 原始元组（全是 int / 短 str 常量）
events.append((SEQ, EV_ATTACK, actor_slot, target_slot, damage, hp_after))
```

| 层 | 职责 | 位置 |
| --- | --- | --- |
| **引擎** | **无条件全量** 记录结构化事件元组（复现与校验本来就需要） | `autochess.py` 热路径 |
| **渲染器** | 事件元组 → 中文文本 / 棋盘字符画，**纯函数、非热路径** | `domain/battle_text.py` |
| **服务层** | 按请求参数 `report_mode` 决定渲染档位与持久化策略 | `AutochessService` |

**「简易 / 详细」是渲染档位，不是引擎分支**——引擎不因档位少记一条事件，否则复现与校验就废了。

| 规则 | 收益 |
| --- | --- |
| 战斗中 **不拼中文文案**，只存结构化元组 | 去掉热点里的字符串格式化 |
| 文案在 **响应/回放时** 由渲染器按 `type` 生成 | 同时便于将来做 i18n |
| `logger.debug` 前加 `if logger.isEnabledFor(DEBUG)` | 避免无谓 f-string 求值 |
| 事件字段全为原始类型 | 直接 `json.dumps`，可 pickle，可跨进程 |

### 11.2 两档日志定义

| 档位 | 响应内容 | 用途 |
| --- | --- | --- |
| **`simple`（简易）** | 开局棋盘字符画 + 结果摘要（胜负 / 回合数 / 双方存活与剩余 HP / 击杀列表） | 挂机刷本、批量战斗 |
| **`detailed`（详细）** | 开局棋盘字符画 + **逐回合 DND 式文本日志**（§11.4 全事件渲染） | 玩家看戏、调试、对局复盘 |

两档都以 **开局棋盘渲染（§11.5）开头**。`report_mode` 为开战请求参数，默认 `simple`。

### 11.3 响应即战报：服务器 **零保留**（v2.3 定稿）

**产品决策（写死）**：战斗日志 **只存在于开战响应中**；浏览器自行保存（内存 / `sessionStorage`），退出登录或关闭浏览器即销毁；**服务器输出即完事，不落任何战斗日志**。

```text
POST /battle/… → 演算 → 渲染（按 report_mode）→ 响应返回 → 服务器侧日志生命周期结束
服务器只持久化【结算】：体力扣减、奖励入账、胜负计数、快照结果 —— 全是小事务行更新
```

| 层 | 保留什么 | 生命周期 |
| --- | --- | --- |
| 服务器 DB | **无任何日志/事件/棋盘文本**；只有结算行更新 | — |
| 服务器内存 | 事件元组仅存在于单次请求栈上 | 请求结束即释放 |
| 浏览器 | 响应里的棋盘 + 日志文本，存 Pinia + `sessionStorage` | 退出登录 / 关标签页销毁 |

**已知取舍（明示）**：

| 后果 | 说明 |
| --- | --- |
| 无历史战报页 | 只能回看本会话内打过的场次（浏览器侧列表） |
| PVP 防守方看不到被打过程 | 只能看到结果通知（荣誉/胜负变化）；若未来要补，登记 **M3-D04**（给守方投递 `setup+seed`，用确定性重演再渲染，仍不用存日志） |
| 客服/审计无过程数据 | `seed` 与 `setup` 摘要可写入 **运行日志**（logging，滚动删除），与玩家战报无关 |

**确定性（§9）仍然必须**：不是为了回放持久化，而是为了 ① 单测与基准可断言 ② 出问题时用请求参数 + seed 在开发环境重演复现 ③ M3-D04 这类未来能力不被堵死。

### 11.4 事件全集（DND 明细字段，渲染器输入）

与成型 §7.7 合并为同一 `schema_version`。每类事件必须带齐「掷骰点数 + 计算中间量 + 最终值」：

| type | 关键字段（渲染详细日志所需） |
| --- | --- |
| `battle_start` | 初始棋子位置/属性摘要、可见地形格（不含未知属性）、天气/环境/效果结算结果、seed |
| `round_start` | 回合号 |
| `initiative` | `uid`, `speed`, `dice`, `initiative = speed×dice`, 本回合行动序 |
| `move` | `uid`, `path[]`, `ap_spent`, `stop_reason ∈ {arrived, budget, blocked_unit, abyss_bounce, taunt}` |
| `hit_check` | 攻守 uid、`base_rate`, `extra_rate`, `dodge_rate`, 合成命中率, `dice`, `forced ∈ {none, sure_hit, sure_dodge}`, 结果 |
| `damage` | `P`, `C_atk`, `dice`, `D`, `C_def`, `raw`, `final`, `shield_before/after`, `hp_before/after` |
| `skill_cast` / `skill_fail` | `skill_id`, 检定明细（CD/射程/法力/禁制各✓✗）, `base_prob`, `dice`, 合成概率, 掷点, 结果（释放 / 回退主技能） |
| `skill_degrade` | 沉默→物理 / 缴械→近战（成型 §7.4） |
| `passive_trigger` | `uid`, `skill_id`, 触发条件（如「对方闪避」） |
| `item_trigger` | `uid`, `item_id`, 触发条件与效果 |
| `stat_change` | `uid`, `stat_id`, 修正量, 来源（§12 修正栈落账） |
| `abyss_pass` / `abyss_bounce` | `uid`, `cell`, 揭晓结果（可飞通过 / 禁飞弹回 `back_to`） |
| `obstacle_hit` → `obstacle_break` / `obstacle_immune` | `uid`, `cell`, 揭晓结果（击破 / 无法破坏） |
| `terrain_reveal` | `cell`, `kind`, `subtype` |
| `ai_retarget` | `from_uid`, `to_uid`, `reason ∈ {obstacle_indestructible, abyss_nofly, taunt, cheaper}` |
| `taunt` | 嘲讽者、被强制者 |
| `blocked` | `cell` 或 `target_uid`（地形/禁制导致无法行动） |
| `death` / `battle_end` | 常规 |

### 11.5 开局棋盘渲染（两档通用，字符画）

渲染器从 `battle_start` 事件画 7×7 字符棋盘：**棋子名直接写在格内**，地形写「障 / 渊 / 封」（**不标注可破/禁飞**——对进攻方未知，见成型 §5.2）；天气/环境/效果用文字行说明。y 大在上（左下锚点 `(0,0)` 在左下）。

```text
天气：烈日（火系法伤 +10%）   环境：无   场上效果：无
y\x │  0   │  1   │  2   │  3   │  4   │  5   │  6   │
 6  │      │      │      │      │      │      │      │
 5  │      │      │ 障   │      │      │      │      │
 4  │ 本体 │      │      │      │      │      │ 妖狼 │
 3  │ 木傀 │      │ 渊   │      │      │      │ 妖王 │
 2  │      │      │      │      │      │      │      │
 1  │      │      │      │      │      │      │      │
 0  │      │      │      │      │      │      │      │
图例：障=障碍（能否破坏未知） 渊=深渊（能否飞越未知） 封=封印
进攻方：本体(0,4) 木傀(0,3)   防守方：妖狼(6,4) 妖王(6,3)
```

- 格宽固定（全角 2 列对齐）；同格优先显示棋子名（棋子不会与禁停地形同格，成型 §3.4）。  
- S3 空盘：只有棋子行与「天气：无 环境：无 效果：无」。

### 11.6 详细日志渲染样例（DND 式）

```text
—— 第 3 回合 ——
【先攻】青云子：速度 8 × d20=15 → 先攻 120
【先攻】妖狼：速度 6 × d20=18 → 先攻 108
【行动】青云子（AP 2）
  移动：(1,3)→(2,3)→(3,3)，耗 1 AP（剩 1）
  攻击 妖狼【主技能·剑气】：命中 = 75%(远程物理) + 10%(装备) − 20%(闪避) = 65%，掷 d100=42 → 命中
  伤害 = 威力30 × 克制1.5 × d20点数12 = 540(破盾前) − 防御80 × 克制1.0 = 460 → 妖狼 HP 1200→740
【行动】妖狼（AP 2）
  主动技【撕咬】检定：冷却✓ 射程✓ 法力✓ 禁制✓ → 释放率 = 40% + d20=12 ×1% = 52%，掷 d100=77 → 不释放，回退主技能
  移动：(6,4)→(5,4)，撞上障碍 (4,4) 中断，耗 1 AP
  攻击障碍 (4,4)：揭晓【无法破坏】→ 重选目标：原目标 青云子 绕行需 3 回合 > 木傀 2 回合 → 改打 木傀
【探路】木傀 尝试进入深渊 (3,2)：揭晓【禁飞】→ 弹回 (3,3)，本次移动结束
【被动】妖王【荆棘甲】触发（条件：受到近战攻击）：反弹伤害 45 → 青云子 HP 980→935
```

渲染器按事件 `type` 查一张「模板函数表」逐条生成；模板集中在 `battle_text.py`，引擎零改动即可调文案。

---

## 12. 战斗内动态属性（修正栈 + 脏标记 + 触发索引）

> 属性在战斗中会被功法触发、道具、对方陷阱等改变。两个极端都不对：  
> ① 每次读属性都从养成数据全量重算（重复劳动 + 回读 DB）；② 预计算所有可能状态（组合爆炸，不可能）。  
> 正解是 **快照分层 + 修正栈 + 脏标记缓存**——单机和万人在线用的是 **同一套** 机制，成本瓶颈从来不在「算一次属性」，而在「重复算」和「战斗中回读 DB」。

### 12.1 三层属性模型

| 层 | 内容 | 生命周期 | 存储 |
| --- | --- | --- | --- |
| **L0 养成基线** | `build_combat_stats`（M2）聚合境界/功法/体质/品阶 | **开战时算一次**，战斗内不可变 | `base` 扁平数组（per-unit per-stat） |
| **L1 修正栈** | buff/debuff/陷阱效果等动态修正 | 随战斗事件增删 | per-unit `list[tuple]` |
| **L2 生效值缓存** | L0 + L1 的合成结果 | 修正栈变化时失效 | `eff` 扁平数组 + `dirty` 位 |

```python
# 属性用小整数 id 索引，不用字符串键
STAT_ATK, STAT_DEF, STAT_SPEED, STAT_MP, STAT_HIT, STAT_DODGE, STAT_RANGE = range(7)

# 一条修正 = 一个原始元组，零对象开销
# (stat_id, op, value, source_uid, expire_round)
#   op ∈ {OP_FLAT, OP_PCT}
```

```python
def get_stat(st: "BattleState", slot: int, stat: int) -> int:
    """Read effective stat with lazy recomputation (dirty-flag cache)."""
    if st.dirty[slot]:
        _recompute_effective(st, slot)   # O(#modifiers)，通常个位数
        st.dirty[slot] = False
    return st.eff[slot * STAT_COUNT + stat]
```

- **PVE**：双方 L0 均来自开战时一次聚合；**PVP**：防守方 L0 来自快照。战斗内 **绝不回读 DB**。  
- 一回合内 `get_stat` 被读几十次，但只在修正栈变化后重算一次 ⇒ 读是 `O(1)` 数组下标。

### 12.2 求值公式（写死，顺序无关）

```text
eff = clamp( (base + Σ flat) * (1 + Σ pct) , stat_min, stat_max )
```

| 约束 | 理由 |
| --- | --- |
| 只允许 `FLAT` / `PCT` 两种 op，**禁止 `SET`** | 加法与乘法各自可交换 ⇒ 结果与修正的插入顺序无关 ⇒ 天然确定性，无「谁先谁后」的顺序 bug |
| 需要「锁定/固定值」语义时 | 用独立标志位（如 `rooted`、`silenced`）表达，不混进数值栈 |
| `clamp` 边界进配置 | 防止负攻/负速等未定义行为 |

### 12.3 修正的增删

| 时机 | 操作 |
| --- | --- |
| 施加 buff/debuff/陷阱效果 | `mods[slot].append(tuple)` + `dirty[slot] = True` |
| 大回合开始 | 扫一遍 `expire_round <= round` 的条目，**swap-pop** 删除 + 置 `dirty`（`O(#mods)`，个位数） |
| 来源死亡需移除（如光环） | 按 `source_uid` 过滤重建该单位的 `mods` + 置 `dirty` |

### 12.4 触发索引（被动功法 / 道具 / 陷阱）

**反模式**：每发生一个事件就扫描全场所有单位的所有技能问「你触发吗」——`O(事件数 × 全场技能数)`。

**正解**：开战时建一次 **按事件类型分桶的触发索引**：

```python
# 开战时构建一次；战斗中只增删极少（单位死亡时摘除）
trigger_index: dict[int, list[tuple[int, int]]]   # EV_TYPE -> [(slot, ability_id), ...]
```

- 事件发射时只遍历 `trigger_index.get(ev_type)` 那一小桶；**绝大多数事件类型桶为空**，成本≈一次 dict 查找。  
- 触发后的释放判定（CD/范围/法力/禁制/概率骰）仍按成型 §7.4，不在此重复。  
- **陷阱**：布阵期落位后合成 `trap_mask`（位掩码）；移动逐格检查 `trap_mask & (1 << cell)`，与嘲讽光环同款 `O(1)`，已在 §7 的走子循环里顺带完成。

### 12.5 与规划器的关系（为什么属性变化不拖慢 AI）

| 属性变化 | 对规划的影响 |
| --- | --- |
| 移动力 `mp` 变化 | **零表失效**——距离表存的是「步数」，`ceil(step/mp)` 在查询时才除，读的是当下 `eff` |
| 射程变化 | 同上，`RANGE_MASK_EX[r]` 按当下 `eff` 取行 |
| 速度变化 | 只影响先攻，而先攻 **每大回合重掷重排**（成型 §7.1），天然吸收 |
| 地形被改（破障/揭晓） | 才需要重建距离表（§5.2 的 `terrain_version`，每战 0～3 次） |

⇒ **属性系统与距离表解耦**是本设计的关键取舍：动态属性再频繁，也不会触发任何图重算。

### 12.6 反模式清单（代码评审对照用）

| 反模式 | 替代 |
| --- | --- |
| 每次命中重跑 `build_combat_stats` | L0 只在开战算一次 |
| `deepcopy` 单位对象来施加临时 buff | 修正栈 append + 到期 sweep |
| 修正存 `dict` 嵌套 `dict` | 原始元组 list |
| 每事件全场扫描被动技能 | 开战建 `trigger_index` |
| 战斗中按 `character_id` 查库补属性 | `setup` 里带全，引擎零 IO |

---

## 13. 并发与部署模型

### 13.1 引擎必须是纯函数

```python
def simulate_battle(setup: dict, seed: int) -> dict:
    """
    Pure CPU battle simulation: no DB, no network, no asyncio, no globals.

    入参/返回值均为可 pickle 的原始数据，因此既能内联调用，
    也能整体丢进线程池或进程池，无需改动引擎。
    """
```

| 约束 | 原因 |
| --- | --- |
| 引擎内 **不出现 ORM 对象 / `AsyncSession` / `await`** | 便于卸载到线程或进程；也让单测不需要 DB |
| 入参在 `AutochessService` 边界转成 `dict` / `tuple` | 避免惰性加载与跨进程不可序列化 |
| **禁止模块级可变状态**（缓存、计数器、`Scratch`） | 并发下相互污染且破坏确定性 |
| 静态表是不可变 `tuple`/`int` | 只读共享，无锁，fork 后 COW 不复制 |

### 13.2 GIL 与横向扩展（实话）

| 手段 | 是否真并行 | 建议 |
| --- | --- | --- |
| `async def` 内直接跑引擎 | 否，且会阻塞事件循环 | 仅当单场耗时远小于请求预算时可用 |
| `anyio.to_thread.run_sync` | **不解 GIL**（纯 Python CPU） | 只用于「别让单个长战斗卡死事件循环」 |
| **多 uvicorn worker（多进程）** | 是 | **首选横向扩展手段** |
| `ProcessPoolExecutor` | 是 | 仅当单场进入几十毫秒级再引入（有序列化开销） |

**推荐姿势**：把单场成本压到毫秒级 ⇒ 直接内联跑，用 **进程级 worker** 扩并发；不要一上来就上进程池。

### 13.3 可选缓存（回放，不是发奖）

`(setup_hash, seed)` 完全确定结果 ⇒ 可缓存 **战报**用于重复回放；  
**奖励入账绝不走缓存**（仍由服务层事务权威处理），避免刷奖。

---

## 14. 性能预算与压测方法

### 14.0 万人在线容量估算（口径：**人均 10～30 秒一场 + 体力上限 + 战报零保留**）

**关键事实**：本战斗是 **请求驱动的一次性演算**——收到开战请求，服务端一口气模拟完整场并返回战报；**没有** tick/帧循环，**没有** 战斗常驻内存状态。所以容量只取决于 **每秒开战次数 × 单场 CPU 耗时**，与「多少人挂着在线」无关。

**体力系统是权威限流器**（成型 §12.9）：战斗消耗体力，体力只按时间恢复（+道具），**每人每天可打场次有硬上限**。因此 10 秒/场只是短时爆发（清体力），不可持续——容量按「爆发扛得住、日总量被体力封顶」设计即可。

| 假设 | 爆发峰值（清体力时段） | 日均 |
| --- | --- | --- |
| 活跃玩家 | 10 000 | 10 000 |
| 人均频率 | 每 **10 s** 一场（体力耗尽即停） | 由体力经济决定，如 **≤ 100 场/人/天** |
| ⇒ 开战速率 | ≈ **1 000 场/s**（短时） | ≈ 100 万场/天 ≈ 平均 12 场/s |
| 单场 CPU（§14.1 预算） | 25 ms（含地形，最坏） | 5 ms（空盘为主） |
| ⇒ 演算核数 | ≈ 25 核（最坏×最坏，短时） | **≈ 0.06～0.3 核** |

**演算 CPU 结论**：日均近乎免费；爆发峰值最坏 25 核，可由多 worker 横向摊（纯函数引擎，§13.2），且体力校验失败的请求在 P0 就被拒、不进引擎。

**存储结论（v2.3，比 v2.2 更进一步）**：战报 **零落库**（§11.3）——日志写入量为 **0 字节/s**，没有任何 retention/裁剪问题。DB 侧只剩小事务：

| DB 负载（1 000 场/s 爆发时） | 量级 | 对策 |
| --- | --- | --- |
| ① 体力扣减 + 奖励入账 | 每场 1～2 行 **UPDATE**（同一角色行可合并为一次） | 单角色事务已有 `FOR UPDATE`；行更新极小 |
| ② 组装 `setup` 的 DB 读（角色/功法/快照） | 每场 1 次聚合查询 | 一次取齐杜绝 N+1；快照就是为此设计的 |
| ③ 响应 JSON 序列化 | `detailed` 档 50～200 KB 文本 **只进响应不进库** | `simple` 默认档极小；详细档是玩家显式选择 |

**前提是不犯 §12.6 / §13.1 的反模式**（战斗中回读 DB、每次命中全量重算属性、deepcopy、每事件拼文案）——这些反模式任何一条都可能把单场从 5 ms 拖到 500 ms，那才是「吃不消」的真实来源。

### 14.1 预算目标（**需实测填数，勿当已测结论**）

| 指标 | 目标 |
| --- | --- |
| 单次 `decide_action`（快速路径命中） | ≤ 10 µs |
| 单次 `decide_action`（需选目标+首步） | ≤ 60 µs |
| Tier B 距离表重建（49 源 BFS） | ≤ 8 ms，**每战 ≤ 3 次** |
| 单场战斗（12 子 / 30 回合 / 空盘） | **≤ 5 ms** |
| 单场战斗（含地形） | ≤ 25 ms |
| 每场分配的临时对象 | 与回合数近似 **无关**（靠 `Scratch`） |

### 14.2 压测与防回退

```powershell
# 基准：pytest-benchmark 或 timeit 直调纯函数（不经 HTTP）
python -m pytest backend/tests/test_autochess_bench.py -q

# 热点定位
python -m cProfile -s tottime -m scripts.bench_battle
```

| 手段 | 用途 |
| --- | --- |
| `cProfile` + `tottime` | 找热点函数 |
| `tracemalloc` | 抓每场分配是否随回合线性增长（应当不增长） |
| 基准阈值断言 | 单测里断言单场耗时上限，防性能回退 |

### 14.3 逃生舱（现在不做，但别堵死）

| 选项 | 前提 |
| --- | --- |
| `mypyc` / `Cython` 编译该模块 | 引擎 **零外部依赖、纯整数运算**（本设计已满足） |
| PyPy 跑独立演算服务 | 引擎是纯函数，可独立部署 |
| Rust/C 扩展重写 | 位棋盘 + 扁平数组的表示天然可移植 |

⇒ 因此约定：`domain/combat_ai.py` 与 `domain/board_tables.py` **不得 import** FastAPI / SQLAlchemy / pydantic。

---

## 15. 代码落点与模块划分

```text
backend/app/domain/
├── board_tables.py     # O2：静态表；导入期构建；纯 int/tuple；零依赖
├── combat_ai.py        # 决策：decide_action / select_target / first_step / Scratch
├── autochess.py        # 引擎：simulate_battle（纯函数）、先攻、执行层、事件
├── terrain.py          # 位掩码地形 + TerrainDistance（版本号懒建表）
└── battle_text.py      # 渲染器（非热路径）：开局棋盘字符画 + 事件→DND 式中文日志模板表
```

| 函数 | 签名要点 |
| --- | --- |
| `decide_action(st, slot) -> (kind, payload)` | 无 IO、无随机 |
| `execute_move(st, slot, goal) -> stop_reason` | 逐格揭晓，可改 `terrain_version` |
| `simulate_battle(setup, seed) -> result` | 唯一对外入口；`AutochessService` 调它 |
| `render_board(battle_start_ev) -> str` | 开局棋盘字符画（§11.5，两档通用） |
| `render_detailed(events) -> list[str]` | 逐回合 DND 式日志（§11.6，仅 `detailed` 档调用） |
| `render_summary(events) -> dict` | 结果摘要（`simple` 档） |

`AutochessService` 只负责：体力校验扣减（P0）→ 读 DB 组 `setup` → 调 `simulate_battle` → 渲染进响应（**现行：summary + detailed_log + events 全给**；请求参数选档 → **M3-D09**）→ 结算。**战报不落库**（§11.3），浏览器自存自灭。

---

## 16. 里程碑切片

| 阶段 | 状态 | 交付 |
| --- | --- | --- |
| **S3** | 已落地 | `board_tables` + 快速路径 + 选目标 + AP=2；空盘；`battle_text` 双档 |
| **S6** | 已落地 | 位掩码地形 + Tier B + 揭晓/弹回/破障 + 换目标 + 最小 LOS |
| **M3-D02** | 待做 | 完整任意角 LOS / 飞跃寻路 |
| **M3-D03** | 待做 | 主动/被动技能与道具接入同一 AP 会计 |
| **M3-D06** | **已落地** | 嘲讽光环 Phase A |
| **M3-D10** | 待做 | Tier A 短路、Tier C 基线、基准阈值、完整修正栈 |

---

## 17. 验收与测试

### 17.1 规则用例（与 v1.0 一致）

1. **优先近目标**：A 需 1 移 + 1 破障，B 需 4 移 → 选 A。  
2. **障不可破换目标**：打 A 障揭晓不可破，重算后 `approach_turns(A) > approach_turns(B)` → 改打 B。  
3. **深渊禁飞**：乐观飞穿，踏入弹回并揭晓，重算后绕路更亏 → 换目标。  
4. **剩余移动力**：`budget == 1` 时不得踏入深渊。  
5. **嘲讽**：移动进入光环 → 强制目标，即使他人 `approach` 更低。  
6. **同 seed**：事件序列逐条一致。

### 17.2 v2.0 追加用例（护性能与近似质量）

7. **近似质量**：随机 200 张地形，`Tier A/B` 的 `approach_ap` 与 `Tier C` 精确值差 ≤ 1。  
8. **表缓存有效性**：一场战斗内 `TerrainDistance` 重建次数 == 地形变化次数。  
9. **零分配**：`tracemalloc` 断言单场峰值分配不随回合数线性增长。  
10. **确定性**：同 `setup+seed` 跑 100 次结果哈希一致；`PYTHONHASHSEED` 变化不影响。  
11. **并发一致**：4 进程并发跑同一 `setup+seed`，结果与单进程逐字节一致。  
12. **性能阈值**：空盘单场 ≤ 预算（§14.1），超出即失败。

### 17.3 v2.1 追加用例（动态属性）

13. **脏标记正确性**：施加/到期/来源死亡各路径后 `get_stat` 与全量重算逐值一致。  
14. **顺序无关**：同一组修正以随机顺序插入，`eff` 结果一致（FLAT/PCT 可交换性）。  
15. **触发索引等价**：`trigger_index` 路径与「全场扫描」朴素实现的触发集合一致。  
16. **零表失效**：整场只改属性不改地形时，`TerrainDistance` 重建次数 == 0。  
17. **战斗零 IO**：`simulate_battle` 执行期间断言无任何 DB 会话/网络调用（打桩计数）。

### 17.4 v2.2/v2.3 追加用例（双档日志与零保留）

18. **重演一致**（开发环境复现用）：同 `setup+seed` 重演渲染的详细日志与首次响应逐字节一致。  
19. **棋盘快照**：固定 `setup` 的开局棋盘字符画与基准文件（golden file）一致；地形只显示「障/渊/封」，不泄露可破/禁飞属性。  
20. **档位不影响引擎**：同 `setup+seed` 下 `simple` 与 `detailed` 的事件流一致（渲染档位零副作用）。  
21. **明细字段齐全**：`hit_check`/`damage`/`skill_cast`/`initiative` 事件均含骰点与全部中间量，渲染不需要回查任何状态。  
22. **渲染性能**：渲染是非热路径，但详细渲染 30 回合 ≤ 5 ms（防模板写出 O(n²) 拼接，用 `list.append + ''.join`）。  
23. **零保留**：开战请求结束后，断言 DB 中不存在任何战斗日志/事件行；只有体力/奖励等结算行变化。  
24. **体力门禁**：体力不足时开战被 P0 拒绝（专用错误码），**引擎与渲染均未被调用**（打桩计数为 0）。

---

## 18. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-10 | **嘲讽落地**：§8 / §16 **M3-D06 Phase A 已落地**（指向设计 v1.1） |
| 2026-08-10 | **嘲讽指针**：§8 指向 [`嘲讽光环设计.md`](./嘲讽光环设计.md) v1.0（M3-D06 设计中） |
| 2026-08-10 | **v2.5 友军占位协调**：§5.4 修订——全源表仍不含单位；决策/执行局部软墙 BFS 改道；窄廊前排让路；消除反复 `blocked_unit` 空耗 |
| 2026-08-05 | v1.0 初稿：AP=2、乐观探路、破障/深渊揭晓重规划、嘲讽、与成型 §5/§7 对齐 |
| 2026-08-05 | **v2.0 实现方案重写（规则不变）**：位棋盘 + 预计算表；每目标搜索 → 全源距离表/闭式；桶队列替代 heapq；`Scratch` + 版本戳零分配；纯函数引擎 + 惰性事件文案；CPython 确定性陷阱清单；GIL/并发部署与性能预算 |
| 2026-08-05 | **v2.1 动态属性与容量**：新增 §12 三层属性模型（L0 开战快照 / L1 修正栈 / L2 脏标记缓存）、顺序无关求值公式、触发索引、属性系统与距离表解耦；§14.0 万人在线容量估算（演算 CPU 非瓶颈，真瓶颈是战报落库与 setup 读）；§17.3 动态属性用例 |
| 2026-08-05 | **v2.2 双档日志与棋盘渲染**：§11 重写——引擎无条件全量记事件，`simple`/`detailed` 为渲染档位；详细日志 **重演再生不落库**（只存 summary+setup+seed，≈2～5 KB/场）；事件全集补齐 DND 明细字段（先攻/命中/伤害/技能检定的骰点与中间量）；开局棋盘字符画（两档通用，地形不泄露未知属性）；§14.0 频率口径改 **10～30 s/场**（峰值 1000 场/s：演算 2～25 核可横向摊，硬约束在战报写入 → 落库方案定为 summary+setup+seed）；新增 `battle_text.py` 渲染器落点与 §17.4 用例 |
| 2026-08-05 | **v2.4 文档收口**：§0 现行 vs 延后；§8 嘲讽标 M3-D06；§16 里程碑对齐已落地/待做；`report_mode` 对齐现行 |
| 2026-08-05 | **v2.3 战报零保留 + 体力限频**：§11.3 定稿「响应即战报」——服务器不落任何战斗日志，浏览器 `sessionStorage` 自存、退出/关闭即销毁（v2.2 的 summary+setup+seed 落库方案随之废止）；明示取舍（无历史战报页、PVP 守方看不到过程 → 登记 M3-D04）；§14.0 引入 **体力系统为权威限流器**（战斗耗体力、按时恢复+道具、日上限 ⇒ 10 s/场只是清体力的短时爆发），DB 负载只剩体力/奖励小事务，日志写入 0 字节；§17.4 增零保留与体力门禁用例 |

---

*冲突时以 `M3战斗成型设计.md` 地形与战斗演算章节为准（规则）；本文为默认 AI 的 **决策算法与实现约束**，规则语义与 v1.0 完全一致。*

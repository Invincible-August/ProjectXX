# 测试服更新指令

面向当前 Ubuntu 测试机部署方式：**conda + screen + Nginx**。  
币安监控占用 `:5000`，本项目勿动该端口。

| 用途 | 端口 | 说明 |
| --- | --- | --- |
| 币安监控（已有） | **5000** | 全程不要改、不要停 |
| 本项目后端 | **8100** | 仅本机 `127.0.0.1`，screen 内 uvicorn |
| 本项目对外 | **8080** | Nginx：玩家前端静态 + 反代 API/后台 |

访问地址：

- 玩家：`http://101.36.105.109:8080/`
- 运营后台：`http://101.36.105.109:8080/management/`
- 健康检查：`http://101.36.105.109:8080/health`

本地开发端口（8000 / 5173）与测试服无关。

---

## 一、完整更新（前后端 + Admin 都改了）

SSH 登录服务器后按顺序执行。

### 1. 进入项目并拉取代码

```bash
cd ~/ProjectXX
git pull
# 若不用 git：用本机 scp/rsync 覆盖代码后跳过本步
```

### 2. 后端：依赖 + 重启

```bash
source ~/miniconda3/etc/profile.d/conda.sh
conda activate projectxx

cd ~/ProjectXX/backend
pip install -r requirements.txt
```

重启 uvicorn（screen）：

```bash
screen -ls
screen -r xiuxian-api
# 进会话后 Ctrl+C 停掉旧进程，再执行：

source ~/miniconda3/etc/profile.d/conda.sh
conda activate projectxx
cd ~/ProjectXX/backend
uvicorn app.main:app --host 127.0.0.1 --port 8100 --workers 1

# 脱离：Ctrl+A，再按 D
```

若没有名为 `xiuxian-api` 的会话，先新建：

```bash
source ~/miniconda3/etc/profile.d/conda.sh
conda activate projectxx
cd ~/ProjectXX/backend
screen -S xiuxian-api
uvicorn app.main:app --host 127.0.0.1 --port 8100 --workers 1
# Ctrl+A，再 D
```

自检：

```bash
curl -s http://127.0.0.1:8100/health
```

### 3. 构建运营后台 Admin

```bash
cd ~/ProjectXX/admin
npm ci
npm run build
# 产物：admin/dist ；由后端提供 /management/
```

说明：只更新了 `admin/dist` 时，多数情况**不必**再重启 uvicorn；若后台页面仍是旧版，再执行一次步骤 2 的重启。

### 4. 构建玩家前端

```bash
cd ~/ProjectXX/frontend
# 确认 .env.production 存在（见文末模板）
npm ci
npm run build
chmod -R a+rX ~/ProjectXX/frontend/dist
# 家目录部署时确保 nginx(www-data) 能穿越路径：
chmod o+x /home/ubuntu /home/ubuntu/ProjectXX /home/ubuntu/ProjectXX/frontend 2>/dev/null || true
```

### 5. 重载 Nginx

```bash
sudo nginx -t && sudo systemctl reload nginx
```

### 6. 验收

```bash
curl -s -o /dev/null -w "frontend:%{http_code}\n" http://127.0.0.1:8080/
curl -s http://127.0.0.1:8080/health
curl -s -o /dev/null -w "management:%{http_code}\n" http://127.0.0.1:8100/management/
```

浏览器建议强刷（Ctrl+F5）：

- `http://101.36.105.109:8080/`
- `http://101.36.105.109:8080/management/`
- `http://101.36.105.109:5000/`（币安应仍正常）

---

## 二、按改动范围的精简更新

| 只改了什么 | 做什么 |
| --- | --- |
| 仅 `backend/` | `pip install -r requirements.txt` + screen 里重启 uvicorn |
| 仅 `admin/` | `cd admin && npm ci && npm run build`（必要时重启后端） |
| 仅 `frontend/` | `cd frontend && npm ci && npm run build` + `chmod` + `nginx reload` |
| 仅 Nginx 配置 | `sudo nginx -t && sudo systemctl reload nginx` |
| 三边都改 | 走「一、完整更新」 |

**不要**把 `frontend/dist` 复制到 `admin/dist`，两套产物相互独立。

---

## 三、首次部署备忘（已上线可跳过）

### conda 环境

```bash
source ~/miniconda3/etc/profile.d/conda.sh
conda create -y -n projectxx python=3.11
conda activate projectxx
cd ~/ProjectXX/backend
pip install -r requirements.txt
cp .env.example .env
# 用 vim 编辑 .env（生产项见下）
```

### `backend/.env` 测试服关键项

```env
APP_ENV=production
DEBUG=false
HOST=127.0.0.1
PORT=8100
CORS_ORIGINS=http://101.36.105.109:8080
GM_ENABLED=false
DAO_LORD_FORCE_WINDOW=false
WORLD_EVENTS_ENABLED=false
# JWT_SECRET_KEY / ADMIN_JWT_SECRET_KEY 用 openssl rand -hex 32 生成后写入
```

### `frontend/.env.production`

```env
VITE_API_BASE_URL=/api/v1
VITE_HEALTH_URL=/health
VITE_WS_URL=ws://101.36.105.109:8080/api/v1/ws
VITE_WS_ENABLED=true
```

改过 `VITE_*` 后必须重新 `npm run build`。

### Nginx 要点

- 监听 **8080**（不要占用 5000）
- `root` 指向 `.../frontend/dist`
- `/api/`、`/admin/`、`/management/`、`/health`、`/api/v1/ws` 反代到 `http://127.0.0.1:8100`
- `location /` 使用 `try_files $uri $uri/ /index.html;`（且 `dist/index.html` 必须存在）

编辑配置：

```bash
sudo vim /etc/nginx/sites-available/xiuxian
sudo ln -sf /etc/nginx/sites-available/xiuxian /etc/nginx/sites-enabled/xiuxian
sudo nginx -t && sudo systemctl reload nginx
```

### 防火墙 / 安全组

- 放行 **8080**；保持 **5000**
- **不要**对公网放行 **8100**

---

## 四、常见故障

### 前端 `npm run build` 报 TS 错误

在开发机修好并同步到服务器后再 build。不要用 `npm audit fix` 当构建失败的解决办法。

### `uvicorn` 启动报缺 `config_data/*.yaml`

同步完整 `backend/app/config_data/`（勿漏文件），再重启后端。

### `http://…:8080/` 返回 500，`error.log` 出现 `redirection cycle ... /index.html`

`frontend/dist/index.html` 不存在或 Nginx `root` 指错 → 重新 build 前端并核对 `root`。

### `www-data` 读不了 dist（`READ_FAIL` / `HOME_EXEC_FAIL`）

```bash
chmod o+x /home/ubuntu /home/ubuntu/ProjectXX /home/ubuntu/ProjectXX/frontend
chmod -R a+rX /home/ubuntu/ProjectXX/frontend/dist
sudo -u www-data test -r /home/ubuntu/ProjectXX/frontend/dist/index.html && echo READ_OK
```

### screen 会话找不到

```bash
screen -ls
# 没有则按「一、2」新建 xiuxian-api
```

### 机器重启后

Nginx 一般会自启；**screen 里的 uvicorn 不会自动起来**，需重新 `conda activate` + `screen` 启动后端。

---

## 五、一键复制：完整更新命令块

```bash
cd ~/ProjectXX && git pull

source ~/miniconda3/etc/profile.d/conda.sh
conda activate projectxx
cd ~/ProjectXX/backend && pip install -r requirements.txt
# 然后手动：screen -r xiuxian-api → Ctrl+C → 再开 uvicorn → Ctrl+A D

cd ~/ProjectXX/admin && npm ci && npm run build

cd ~/ProjectXX/frontend && npm ci && npm run build
chmod -R a+rX ~/ProjectXX/frontend/dist

sudo nginx -t && sudo systemctl reload nginx

curl -s http://127.0.0.1:8100/health
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8080/
```

编辑本文或配置文件请用 **vim**（例如 `vim ~/ProjectXX/backend/.env`）。

# M6 前端目录与路由设计

| 项 | 内容 |
| --- | --- |
| **针对里程碑** | **M6（大道、道主与 WebSocket 强交互）** |
| **设计日期** | 2026-08-10（**v1.1**：对齐大道设计 v1.1 赛会定案；v1.0 依据大道设计 v1.0） |
| **依据** | `M6大道与道主设计.md`；`M5前端目录与路由设计.md`；`M4`/`M3`/`M2`/`M1`/`M0` 前端目录设计；`开发计划.md` §8 |
| **原则** | **再拆两条玩法路由**（`/dao` `/dao-lord`）；WS 为横切客户端（非独立 path）；`/hall` 仍为养成枢纽；M5 渡劫/轮回与 M4 三路由保留；**开道结果与赛会/挑战胜负权威来自服务端**；聊天/传承 UI **不做**（→ M7） |

---

## 1. 设计目标

约定 M6 在 `frontend/src` 下的文件增量、**新路由表**、大厅枢纽信息架构，以及与后端大道 / 道主 / WebSocket / 世界事件骨架接口的对接落点。

M6 可玩范围（W1～W6）：开道三选一、道池图鉴、道值与运用开关（战斗/工坊）、上位克制战报展示、WS 连接状态与掉线重连、道主榜（空位自动就任）、过渡期单挑挑战、实时战况、Boss/秘境入口骨架。  
**赛会 UI（M6-D06）**：报名、对阵树、半决/决赛直播（不可跳过）、战报回放——定案见大道设计 §5；**本轮仅文档**。  
范围外：完整 37 道图鉴美术、天道技能/秘境成型、多频道聊天坞与传承红包、修炼结算真推送完整面板（M1-D33 / M6-D07）、属性满表（ATTR）。

### 1.1 与检定 / 随机纪律（前端）

开道三选项、运用成败、挑战胜负、克制是否触发，**只展示服务端返回字段**；客户端禁止本地 `Math.random` 决定本命道或挑战结果。区间骰/权重抽若出现在响应中，按 [`骰子系统设计.md`](./骰子系统设计.md) 与 §0.0.2 展示中文说明。

### 1.2 与 WS 纪律

- 连接状态、房间快照、`seq` 补洞以服务端为准。  
- Pinia 可乐观更新 UI，但必须以 `room.state` / HTTP `GET challenges/{id}` 校准。  
- `VITE_WS_ENABLED=false` 时：开道/道池仍可用 HTTP；道主挑战入口展示「强交互通道未开启」。

---

## 2. 路由策略（相对 M5：**再增两条玩法 path + WS 横切**）

| path | name | 文件 | M6 变化 |
| --- | --- | --- | --- |
| `/login` | `login` | `views/LoginView.vue` | 无 |
| `/register` | `register` | `views/RegisterView.vue` | 无 |
| `/create-character` | `create-character` | `views/CreateCharacterView.vue` | 无 |
| `/hall` | `hall` | `views/HallView.vue` | **枢纽扩展**：大道/道主入口 + WS 状态角标 |
| `/formation` | `formation` | `views/FormationView.vue` | 可选：道主战前编成提示 |
| `/battle` | `battle` | `views/BattleView.vue` | 开战「运用本命道」开关 + 克制摘要 |
| `/avatar` | `avatar` | `views/AvatarView.vue` | 无强制改版 |
| `/workshop` | `workshop` | `views/WorkshopView.vue` | 开工「耗道值运用」开关与预览 |
| `/pets` | `pets` | `views/PetsView.vue` | 无 |
| `/tribulation` | `tribulation` | `views/TribulationView.vue` | 无 |
| `/reincarnation` | `reincarnation` | `views/ReincarnationView.vue` | 预览清单追加道池保留/卸道主说明 |
| `/dao` | `dao` | `views/DaoView.vue` | **新**：开道 / 道池 / 道资源 |
| `/dao-lord` | `dao-lord` | `views/DaoLordView.vue` | **新**：道主榜 /（过渡）挑战 / **赛会**报名·赛程·观战 / 事件骨架 |
| `/` | — | 重定向 | 逻辑不变；挑战进行中可优先进 `/dao-lord` |
| `/test` | `test` | `test/apps.vue` | 可选；非验收 |

### 2.1 守卫逻辑（继承并扩展）

| 规则 | 说明 |
| --- | --- |
| 需登录 | 既有玩法页 + `/dao` `/dao-lord` 均 `requiresAuth` |
| 无角色 | → `/create-character` |
| 有角色访创角 | → `homePathAfterAuth()` |
| 已登录访登录/注册 | → `homePathAfterAuth()` |
| `redirect` 白名单 | `resolveSafeRedirect` **追加** `/dao`、`/dao-lord` |
| 待引渡 / 新生 | 仍优先 `/reincarnation`（M5）；**禁止**进挑战 |
| 渡劫中 | 允许 `/dao` 只读；禁挑战 join（页内 + API） |
| 挑战进行中 | `homePathAfterAuth` 可优先 `/dao-lord?mode=challenge` |
| 境界硬拦 | **不做**「未真仙禁止进 `/dao`」路由级硬拦；页内空态 + API `40080` |

```text
App.vue
  └── 布局壳
        ├── WorldClockBar.vue（保留；可订阅 world.env）
        ├── WsStatusBadge.vue（可选：连接态）
        └── <RouterView />
              ├── /login | /register | /create-character
              ├── /hall → HallView.vue
              │     ├── …（M1～M5 面板保留）
              │     ├── HallEnvGate.vue（保留）
              │     ├── HallDaoGate.vue（新：开道/道主入口）
              │     └── GameLogPanel.vue
              ├── /formation | /battle | /avatar | /workshop | /pets
              ├── /tribulation | /reincarnation
              ├── /dao → DaoView.vue
              │     ├── DaoStatusPanel.vue
              │     ├── DaoOpenPanel.vue          # 三选一
              │     ├── DaoPoolGallery.vue        # 道池图鉴
              │     ├── DaoUsageToggle.vue        # 默认运用偏好（本地+同步）
              │     └── DaoRestraintHint.vue      # 只读克制说明
              └── /dao-lord → DaoLordView.vue
                    ├── DaoLordBoardPanel.vue     # 各道道主
                    ├── DaoLordWindowBanner.vue   # 开窗倒计时
                    ├── DaoLordClaimPanel.vue
                    ├── DaoLordChallengePanel.vue
                    ├── DaoLordBattleFeed.vue     # WS 战况
                    └── WorldEventSkeletonPanel.vue
```

### 2.2 为何现在再拆路由

| 理由 | 说明 |
| --- | --- |
| 对齐开发计划 S4 / U3 | 开道与道主挑战均为独立决策面，不宜塞进大厅折叠 |
| 强交互全屏 | 挑战倒计时与战况流需要整页注意力；独立路由便于守卫与重连回跳 |
| WS 是横切 | 连接层挂 App/store，**不**为 WS 单开 path |
| 守卫复用 | 同一套 JWT + `hasCharacter`；仅追加挑战会话分流 |

**不做**：把开道做成唯一弹窗入口（大厅可跳转，但 `/dao` 必须可直达）；不做聊天坞路由（→ M7）。

### 2.3 可选 query（非新 path）

| 用法 | 说明 |
| --- | --- |
| `/dao?mode=open` | 打开开道三选一区 |
| `/dao?mode=pool` | 道池图鉴 |
| `/dao?focus=<dao_id>` | 高亮某道 |
| `/dao-lord?mode=board` | 道主榜（默认） |
| `/dao-lord?mode=challenge` | （过渡）单挑会话区；赛会落地后可改为 `match` |
| `/dao-lord?mode=contest` | **赛会**：报名 / 对阵 / 本场状态（M6-D06） |
| `/dao-lord?mode=spectate` | **观战**：直播（半决/决赛/道主战）或战报回放 |
| `/dao-lord?mode=events` | Boss/秘境骨架 |
| `/dao-lord?dao=<dao_id>` | 聚焦某道道主 |
| `/battle?use_dao=1` | 预勾选运用道 |
| `/workshop?use_dao=1` | 预勾选耗道值 |
| `/hall?hint=dao` | 滚动到大道门闸 |

### 2.4 `homePathAfterAuth` 扩展

```text
if !hasCharacter → /create-character
if status == awaiting_ferry → /reincarnation?mode=ferry
if status == reincarnating → /reincarnation?mode=newborn
if status == tribulation → /tribulation
if active_dao_challenge → /dao-lord?mode=challenge
→ /hall
```

### 2.5 `PLAY_ROUTE_NAMES` / 待引渡拦截

- 追加：`dao`、`dao-lord`。  
- `FERRY_BLOCKED_ROUTES` 追加：`dao-lord`（待引渡不可挑战）；`dao` **允许只读**（看池）或一并拦截——**定案：允许 `/dao` 只读，禁一切 POST**（按钮禁用）。

---

## 3. `src` 目录增量

M0～M5 目录职责不变；M6 **追加文件与路由**，不推翻骨架。

```text
src/
├── api/
│   ├── dao.ts                   # 新：catalog / me / pool / open
│   ├── daoLord.ts               # 新：board / windows / claim / challenges
│   ├── worldEvents.ts           # 新：骨架
│   ├── battle.ts                # 扩展：use_dao、克制展示字段
│   ├── craft.ts                 # 扩展：use_dao 预览与开工
│   ├── character.ts             # 扩展 apply dao 摘要
│   ├── reincarnation.ts         # 展示卸主/留池文案字段（若有）
│   └── gm.ts                    # 扩展真仙/道池/道主/开窗
├── ws/
│   ├── client.ts                # 新：连接、鉴权、心跳、重连
│   ├── protocol.ts              # 新：type 常量与类型守卫
│   └── rooms.ts                 # 新：join/leave 辅助
├── components/
│   ├── layout/
│   │   ├── WorldClockBar.vue    # 扩展：可吃 world.env
│   │   └── WsStatusBadge.vue    # 新：连接态（可选挂顶栏旁）
│   ├── hall/
│   │   └── HallDaoGate.vue      # 新
│   ├── dao/
│   │   ├── DaoStatusPanel.vue
│   │   ├── DaoOpenPanel.vue
│   │   ├── DaoPoolGallery.vue
│   │   ├── DaoUsageToggle.vue
│   │   └── DaoRestraintHint.vue
│   ├── daoLord/
│   │   ├── DaoLordBoardPanel.vue
│   │   ├── DaoLordWindowBanner.vue
│   │   ├── DaoLordClaimPanel.vue
│   │   ├── DaoLordChallengePanel.vue
│   │   ├── DaoLordBattleFeed.vue
│   │   └── WorldEventSkeletonPanel.vue
│   ├── battle/
│   │   └── BattleDaoUsageLine.vue  # 可选
│   ├── workshop/
│   │   └── CraftDaoUsageLine.vue   # 可选
│   └── …（M5 tribulation/reincarnation 等保留）
├── stores/
│   ├── ws.ts                    # 新：连接态 / lastError / seq
│   ├── dao.ts                   # 新
│   ├── daoLord.ts               # 新：board / session / feed
│   ├── worldEvents.ts           # 新（可极简）
│   ├── world.ts                 # 扩展：接受 WS env 校准
│   ├── character.ts             # 扩展 dao 摘要
│   └── battle / craft           # 扩展 use_dao 偏好
├── types/
│   ├── dao.ts
│   ├── daoLord.ts
│   ├── ws.ts
│   └── worldEvents.ts
├── utils/
│   ├── safeRedirect.ts          # 白名单扩展
│   └── daoLabel.ts              # id→中文（优先信服务端 label）
├── router/
│   └── index.ts                 # 登记两路由；挑战回跳
└── views/
    ├── HallView.vue             # 挂 DaoGate
    ├── DaoView.vue              # 新
    ├── DaoLordView.vue          # 新
    ├── BattleView.vue           # 运用道
    ├── WorkshopView.vue         # 耗道值
    ├── ReincarnationView.vue    # 道相关文案
    └── …（其余保留）
```

### 3.1 命名约定（继承）

- 页面：`XxxView.vue`（M6 **新增** Dao / DaoLord）
- 零件：`XxxPanel.vue` / `XxxBanner.vue` / `XxxGate.vue` / `XxxFeed.vue`；按域放 `components/dao|daoLord|layout|hall/`
- API 文件与后端模块同名（`daoLord.ts` 对应 `dao_lord`）
- 路由 path：小写短横线（`/dao` `/dao-lord`）
- WS 模块放 `src/ws/`，**不要**塞进 `api/`（api 保留 HTTP）

### 3.2 子目录说明

- `views/` 仍扁平（再多 2 个整页，可接受）。  
- `ws/` 与 `api/` 并列，避免 Axios 与 WebSocket 生命周期缠在一起。  
- `components/` 继续按域分子夹。

### 3.3 顶栏与 WS 徽标

| 方案 | 说明 | M6 选择 |
| --- | --- | --- |
| WS 独立全屏页 | 过度设计 | 不采用 |
| 仅挑战页连接 | 环境广播困难 | 不采用 |
| 玩法布局自动连 | `meta.showWorldBar` 路由挂载后 `ws.connect()` | **采用** |
| 徽标位置 | 顶栏右侧或 Auth 旁 | 小圆点 + tooltip |

登录/注册/创角：**不**强制连 WS。

---

## 4. 信息架构

### 4.1 大厅（枢纽）布局增量

```text
┌──────────────────────────────────────────────────────────────┐
│ WorldClockBar · WsStatusBadge                                │
│ AuthSessionBar · 修仙大厅 · M6 大道与道主                       │
│ [布阵][战斗][化身][工坊][灵兽园][渡劫][轮回]  [大道][道主]      │
├────────────────────────────┬─────────────────────────────────┤
│ CharacterPanel             │ GameLogPanel                    │
│  · 本命道 / 道值 / 道等级   │  · 开道 / 运用 / 挑战更替        │
│ HallBattleGate             │                                 │
│ HallDualThreadGate         │                                 │
│ HallEnvGate                │                                 │
│ HallDaoGate（新）          │                                 │
│  · 可开道：去开道           │                                 │
│  · 已是道主：道主印记       │                                 │
│  · 开窗中：挑战高亮         │                                 │
└────────────────────────────┴─────────────────────────────────┘
```

### 4.2 大道页布局

```text
┌────────────────────────────────────────────────────────────┐
│ ← 回大厅 · 大道                                             │
│ DaoStatusPanel：本命道 · 道值 · Lv · 经验条 · 周目锁定态    │
├────────────────────────────┬───────────────────────────────┤
│ mode=open：                │ DaoRestraintHint              │
│  DaoOpenPanel              │  · 样本克制说明（显性设计）   │
│  · 三卡片（中文名/大类/    │ DaoUsageToggle                │
│    稀有度说明）            │  · 战斗默认运用 / 工坊默认运用│
│  · [选定为本命道]          │                               │
│  · 再开时：道池自选 Tab    │                               │
├────────────────────────────┴───────────────────────────────┤
│ mode=pool：DaoPoolGallery 网格（已收藏高亮；未获灰置）      │
│ 空态未真仙：「抵达真仙后方可开道；可先浏览图鉴样本」        │
└────────────────────────────────────────────────────────────┘
```

### 4.3 道主页布局

```text
┌────────────────────────────────────────────────────────────┐
│ ← 回大厅 · 道主之争                                         │
│ DaoLordWindowBanner / ContestScheduleBanner：               │
│   报名中 / 距开打 / 赛会进行中 / 冷却提示                     │
├────────────────────────────────────────────────────────────┤
│ mode=board：DaoLordBoardPanel                               │
│  · 每道：道名 · 道主 · 就任 · [有主可看赛程]；空位自动就任   │
│ mode=contest：（M6-D06）报名按钮 · 分道人数 · 对阵树入口     │
│ mode=challenge：（过渡）单挑确认 / 房间 / 弃权               │
│ mode=spectate：（M6-D06）                                  │
│  · 半决/决赛/道主战：直播 Feed（不可跳过）                   │
│  · EarlyRounds / 赛后：战报日志 + 可跳过动画回放             │
│  · 单直播槽：同时只能看一场                                 │
│ mode=events：WorldEventSkeletonPanel                        │
└────────────────────────────────────────────────────────────┘
```

### 4.4 战斗 / 工坊增量

**Battle**

```text
开战区追加 BattleDaoUsageLine：
  [ ] 运用本命道（耗道值 N · 预计伤害/减伤说明来自 preview）
  无本命道：禁用 +「请先开道」
```

**Workshop**

```text
开工区追加 CraftDaoUsageLine：
  [ ] 耗道值运用（失败率/词条说明来自服务端 preview）
```

### 4.5 组件职责

| 组件 | 职责 | 不负责 |
| --- | --- | --- |
| `HallDaoGate` | 入口跳转 + 角标 | 执行开道事务 |
| `DaoStatusPanel` | 展示权威道资源 | 本地改道等级 |
| `DaoOpenPanel` | 展示三选项；提交 choose | 客户端抽道 |
| `DaoPoolGallery` | 图鉴与收藏态 | 伪造收藏 |
| `DaoUsageToggle` | 记住偏好并带到 battle/craft | 扣道值权威 |
| `DaoRestraintHint` | 只读说明 | 重算矩阵 |
| `DaoLordBoardPanel` | 榜单；空位说明；入口到赛程 | 任命道主 |
| `DaoLordWindowBanner` | 开窗/赛会日程展示；到点 refresh | 本地发明赛程 |
| `DaoLordClaimPanel` | 空位就任说明（无夺位按钮） | — |
| `DaoLordContestPanel` | **M6-D06**：报名/取消/对阵树 | 客户端配对 |
| `DaoLordChallengePanel` | （过渡）发起/弃权 | 离线伪造成功 |
| `DaoLordBattleFeed` | 选手房间事件流 | 本地演算战斗 |
| `DaoLordLiveSpectate` | **M6-D06**：直播不可跳过 | 加速直播 |
| `DaoLordMatchReplay` | 战报日志+可跳过回放 | 篡改结果 |
| `WorldEventSkeletonPanel` | 骨架在场 | 完整 Boss 数值 |
| `WsStatusBadge` | 连接态与手动重连 | 业务结算 |
| `DaoView` / `DaoLordView` | 编排拉数、日志 | 大段公式 |

### 4.6 关键时序

**开道**

```text
/dao?mode=open → POST /dao/open/roll
  → 展示 3 张卡片（label 中文）
  → POST /dao/open/choose { dao_id }
  → applyCharacter；GameLog「开道成功：炎道」
  → 重复 roll → 展示 40096 文案
```

**工坊耗道值**

```text
勾选 use_dao →（可选）GET/POST preview
  → POST /craft/start { use_dao: true }
  → 40084：道值不足
  → 成功：刷新道值/经验；日志中文
```

**道主挑战（过渡 · W1～W6）**

```text
/dao-lord → POST /dao-lord/challenges → ws + room.join → feed → finished
```

**道主之争赛会（M6-D06）**

```text
/dao-lord?mode=contest
  → GET contests/current → 报名窗内 POST register
  → 开打后 GET bracket；本人比赛进房间
  → EarlyRounds：GET report + MatchReplay（可跳过）
  → Semis/Finals/Lord：POST spectate（单槽）→ live.tick（不可跳过）
  → 直播结束后仅结果+回溯；下场开赛清日志
```

**环境广播（可选）**

```text
ws 收到 world.env → worldStore.calibrate(payload)
  → 顶栏更新；与轮询二选一节流
```

**轮回后**

```text
预览 keep 含「道池收藏」；lose 含「本命道/道主身份」
  → 完成后 /dao 显示 can_open（若已真仙）或引导修炼
```

---

## 5. API / Store / WS 对应

| 文件 | 函数（示意） |
| --- | --- |
| `api/dao.ts` | `fetchDaoCatalog` · `fetchDaoMe` · `fetchDaoPool` · `rollDaoOpen` · `chooseDaoOpen` · `previewDaoUsage` |
| `api/daoLord.ts` | board/windows/claim；（过渡）challenges；**赛会** contests register/bracket/matches/spectate |
| `api/worldEvents.ts` | `fetchCurrentEvents` · `registerEvent` |
| `ws/client.ts` | `connect` · `disconnect` · `send` · `on` |

**`useWsStore`**

| state / action | 说明 |
| --- | --- |
| `status` | `idle` \| `connecting` \| `open` \| `reconnecting` \| `closed` |
| `lastError` | 可展示中文 |
| `seq` | 最近收包 |
| `connect` / `reconnect` / `subscribe` | |

**`useDaoStore`**

| state / action | 说明 |
| --- | --- |
| `me` / `pool` / `catalog` / `opening` | |
| `roll` / `choose` / `refresh` | |

**`useDaoLordStore`**

| state / action | 说明 |
| --- | --- |
| `board` / `window` / `session` / `feed[]` | |
| `claim` / `startChallenge` / `abort` / `applyRoomState` | |

**`useCharacterStore`**：`applyCharacter` 识别 `dao`、`dao_lord`；供门闸与守卫。

**`useWorldStore`**：增加 `applyEnvPush`；与 poll 去重。

---

## 6. 交互与文案约定

### 6.1 开道

- 标题：「开辟本命大道」。  
- 明确：「抽出的三道将全部录入道池，跨轮回保留；本周目本命道选定后不可更改」。  
- 禁止文案暗示可花钱买指定道。  
- 未真仙空态用中文境界名，不写裸 `true_immortal`。

### 6.2 运用道

- 开关旁写清消耗与效果来源（preview 字段）。  
- 战报克制行：「炎极道 克制 炎道（伤害×1.15）」类中文。

### 6.3 道主 / 赛会

- 空位：「虚位以待；本命道达标者自动就任，无需夺位」。  
- 有主（赛会定案）：「须在报名窗内报名；开打后同道淘汰；半决赛/决赛可看直播（不可跳过）；道主在线则实时对战，离线则打防守快照」。  
- 单直播槽：`40097` → 「你正在观看其它直播，结束后可回看本场战报」。  
- 过渡单挑文案保留至赛会 P1 隐藏入口。

### 6.4 WS

- 掉线 toast 节流；重连成功轻提示。  
- 挑战中掉线：强提示「重连宽限中，超时将判负」。

### 6.5 事件骨架

- 标注「骨架 / 后续开放完整奖励」；避免玩家以为 Boss 已成型。

### 6.6 UI 基调

- 继承 M0～M5：功能优先；Element Plus；不为 M6 做终局特效。  
- 至少 2～3 处轻量动效：开道三卡翻入、挑战 Feed 新行滑入、WS 重连脉冲。

### 6.7 GM（DEV）

- 抬真仙、灌池、任命道主、开窗、清冷却、推 env。  
- 仅 `import.meta.env.DEV`。

---

## 7. 类型对齐（要点）

```ts
export interface DaoPublic {
  fate_dao_id: string | null
  fate_dao_label: string | null
  qi: number
  level: number
  exp: number
  exp_to_next?: number
  pool_count: number
  can_open: boolean
  locked: boolean
}

export interface DaoCatalogEntry {
  dao_id: string
  label: string
  category_label: string
  rarity_label: string
  owned: boolean
  description?: string
}

export interface DaoOpenOffer {
  session_id: string
  options: DaoCatalogEntry[] // length 3
  allow_pool_pick: boolean
}

export interface DaoLordSeatPublic {
  dao_id: string
  dao_label: string
  lord_character_id?: number
  lord_name?: string
  claimed_at?: string
  can_claim: boolean
  can_challenge: boolean
}

export interface DaoLordWindowPublic {
  open: boolean
  next_open_at?: string
  closes_at?: string
  label: string
}

export interface DaoChallengePublic {
  id: number
  dao_id: string
  room_id: string
  phase: 'pending' | 'running' | 'finished'
  result?: 'challenger_win' | 'lord_win' | 'abort' | 'disconnect_loss'
}

export type WsEnvelope<T = unknown> = {
  type: string
  seq: number
  ts: string
  payload: T
}
```

---

## 8. 与后续里程碑

| 里程碑 | 对本设计的影响 |
| --- | --- |
| **M6** | 本文范围；按上表建齐双路由与 WS 客户端 |
| **M7** | 复用 `ws/` 做聊天/传承推送；**勿在 M6 预做聊天坞 UI** |
| **M11** | `WorldEventSkeletonPanel` 进化为 Boss/秘境成型 |
| **M13** | 图鉴扩至 37 道；权重与克制满表 |
| 延后项 | 一律先查 [`后续待完成.md`](./后续待完成.md) |

---

## 9. 开发进度与步骤（对照 M6）

> 本节随实现同步更新；接口以 `M6大道与道主设计.md` 为准。

### 9.1 总览（对齐大道设计 §10 W1～W6）

| 步 | 内容 | 状态 | 说明 |
| --- | --- | --- | --- |
| 0 | 设计文档（本文 + 大道骨架） | **已完成** | 2026-08-10 v1.0 |
| **W1** | 类型 + `api/dao` + DaoView 开道/道池壳 | **已完成** | 前端骨架 2026-08-10 |
| **W2** | 战斗/工坊运用道 UI + 克制展示 | **已完成** | BattleDaoUsageLine / CraftDaoUsageLine |
| **W3** | `ws/` 客户端 + 徽标 + 可选 env 推送 | **已完成** | WsStatusBadge + applyEnvPush |
| **W4** | 道主榜/claim/HTTP 挑战结果页 | **已完成** | DaoLordView board/challenge |
| **W5** | 挑战 Feed 接 WS 房间 | **已完成** | DaoLordBattleFeed + room.join |
| **W6** | 守卫 + HallDaoGate + 事件骨架 + GM + 总验收 | **已完成** | 前后端齐；GM 真仙/道池/道主/开窗 |
| 收尾 | GameLog / smoke 对照 | **已完成** | `scripts/smoke_m6.py` |

### 9.2 本地联调口令（实现后）

```powershell
# 后端
cd backend
.\.venv\Scripts\Activate.ps1
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000

# 前端
cd frontend
npm run dev
```

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/v1/dao/catalog` | GET | 图鉴 |
| `/api/v1/dao/me` | GET | 本命与资源 |
| `/api/v1/dao/pool` | GET | 道池 |
| `/api/v1/dao/open/roll` | POST | 三选项 |
| `/api/v1/dao/open/choose` | POST | 锁定本命 |
| `/api/v1/dao-lord/board` | GET | 道主榜 |
| `/api/v1/dao-lord/windows` | GET | 开窗 |
| `/api/v1/dao-lord/claim` | POST | 空位自动就任（兼容） |
| `/api/v1/dao-lord/challenges` | POST | （过渡）创建挑战 |
| `/api/v1/dao-lord/contests/current` | GET | （M6-D06）赛会状态 |
| `/api/v1/dao-lord/contests/current/register` | POST | （M6-D06）报名 |
| `/api/v1/ws` | WS | 强交互通道 |
| `/api/v1/world-events/current` | GET | 事件骨架 |

---

## 10. 修订记录

| 日期 | 说明 |
| --- | --- |
| 2026-08-10 | **v1.1**：道主页增 `contest` / `spectate`；对齐赛会定案（M6-D06）；直播不可跳过与单直播槽文案 |
| 2026-08-10 | 前端骨架落地：W1～W5 完成；W6 守卫/门闸/事件骨架完成 |
| 2026-08-10 | 初稿 **v1.0**：相对 M5 增 `/dao` `/dao-lord`；`src/ws` 横切；大厅 `HallDaoGate` |

---

*玩法与接口以 `M6大道与道主设计.md` 为准；排期以 `开发计划.md` §8 为准；延后以 `后续待完成.md` 为准。*
